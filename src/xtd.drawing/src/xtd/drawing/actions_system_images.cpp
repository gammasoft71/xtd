#include "../../../include/xtd/drawing/actions_system_images.h"

using namespace std;
using namespace xtd;
using namespace xtd::drawing;

drawing::size actions_system_images::default_size() noexcept {
  return system_images_base::default_size();
}

image actions_system_images::address_book_new() noexcept {
  return address_book_new(default_size());
}

image actions_system_images::address_book_new(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("address-book-new", size);
}

image actions_system_images::application_exit() noexcept {
  return application_exit(default_size());
}

image actions_system_images::application_exit(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("application-exit", size);
}

image actions_system_images::appointment_new() noexcept {
  return appointment_new(default_size());
}

image actions_system_images::appointment_new(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("appointment-new", size);
}

image actions_system_images::call_start() noexcept {
  return call_start(default_size());
}

image actions_system_images::call_start(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("call-start", size);
}

image actions_system_images::call_stop() noexcept {
  return call_stop(default_size());
}

image actions_system_images::call_stop(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("call-stop", size);
}

image actions_system_images::contact_new() noexcept {
  return contact_new(default_size());
}

image actions_system_images::contact_new(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("contact-new", size);
}

image actions_system_images::dialog_cancel() noexcept {
  return dialog_cancel(default_size());
}

image actions_system_images::dialog_cancel(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("dialog-cancel", size);
}

image actions_system_images::dialog_ok() noexcept {
  return dialog_ok(default_size());
}

image actions_system_images::dialog_ok(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("dialog-ok", size);
}

image actions_system_images::dialog_ok_apply() noexcept {
  return dialog_ok_apply(default_size());
}

image actions_system_images::dialog_ok_apply(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("dialog-ok-apply", size);
}

image actions_system_images::document_edit() noexcept {
  return document_edit(default_size());
}

image actions_system_images::document_edit(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-edit", size);
}

image actions_system_images::document_new() noexcept {
  return document_new(default_size());
}

image actions_system_images::document_new(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-new", size);
}

image actions_system_images::document_open() noexcept {
  return document_open(default_size());
}

image actions_system_images::document_open(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-open", size);
}

image actions_system_images::document_open_recent() noexcept {
  return document_open_recent(default_size());
}

image actions_system_images::document_open_recent(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-open-recent", size);
}

image actions_system_images::document_page_setup() noexcept {
  return document_page_setup(default_size());
}

image actions_system_images::document_page_setup(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-page-setup", size);
}

image actions_system_images::document_print() noexcept {
  return document_print(default_size());
}

image actions_system_images::document_print(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-print", size);
}

image actions_system_images::document_print_preview() noexcept {
  return document_print_preview(default_size());
}

image actions_system_images::document_print_preview(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-print-preview", size);
}

image actions_system_images::document_properties() noexcept {
  return document_properties(default_size());
}

image actions_system_images::document_properties(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-properties", size);
}

image actions_system_images::document_revert() noexcept {
  return document_revert(default_size());
}

image actions_system_images::document_revert(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-revert", size);
}

image actions_system_images::document_save() noexcept {
  return document_save(default_size());
}

image actions_system_images::document_save(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-save", size);
}

image actions_system_images::document_save_as() noexcept {
  return document_save_as(default_size());
}

image actions_system_images::document_save_as(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-save-as", size);
}

image actions_system_images::document_send() noexcept {
  return document_send(default_size());
}

image actions_system_images::document_send(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("document-send", size);
}

image actions_system_images::edit_clear() noexcept {
  return edit_clear(default_size());
}

image actions_system_images::edit_clear(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("edit-clear", size);
}

image actions_system_images::edit_copy() noexcept {
  return edit_copy(default_size());
}

image actions_system_images::edit_copy(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("edit-copy", size);
}

image actions_system_images::edit_cut() noexcept {
  return edit_cut(default_size());
}

image actions_system_images::edit_cut(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("edit-cut", size);
}

image actions_system_images::edit_delete() noexcept {
  return edit_delete(default_size());
}

image actions_system_images::edit_delete(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("edit-delete", size);
}

image actions_system_images::edit_find() noexcept {
  return edit_find(default_size());
}

image actions_system_images::edit_find(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("edit-find", size);
}

image actions_system_images::edit_find_replace() noexcept {
  return edit_find_replace(default_size());
}

image actions_system_images::edit_find_replace(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("edit-find-replace", size);
}

image actions_system_images::edit_paste() noexcept {
  return edit_paste(default_size());
}

image actions_system_images::edit_paste(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("edit-paste", size);
}

image actions_system_images::edit_redo() noexcept {
  return edit_redo(default_size());
}

image actions_system_images::edit_redo(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("edit-redo", size);
}

image actions_system_images::edit_rename() noexcept {
  return edit_rename(default_size());
}

image actions_system_images::edit_rename(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("edit-rename", size);
}

image actions_system_images::edit_select_all() noexcept {
  return edit_select_all(default_size());
}

image actions_system_images::edit_select_all(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("edit-select-all", size);
}

image actions_system_images::edit_undo() noexcept {
  return edit_undo(default_size());
}

image actions_system_images::edit_undo(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("edit-undo", size);
}

image actions_system_images::folder_new() noexcept {
  return folder_new(default_size());
}

image actions_system_images::folder_new(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-new", size);
}

image actions_system_images::format_indent_less() noexcept {
  return format_indent_less(default_size());
}

image actions_system_images::format_indent_less(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-indent-less", size);
}

image actions_system_images::format_indent_more() noexcept {
  return format_indent_more(default_size());
}

image actions_system_images::format_indent_more(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-indent-more", size);
}

image actions_system_images::format_justify_center() noexcept {
  return format_justify_center(default_size());
}

image actions_system_images::format_justify_center(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-justify-center", size);
}

image actions_system_images::format_justify_fill() noexcept {
  return format_justify_fill(default_size());
}

image actions_system_images::format_justify_fill(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-justify-fill", size);
}

image actions_system_images::format_justify_left() noexcept {
  return format_justify_left(default_size());
}

image actions_system_images::format_justify_left(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-justify-left", size);
}

image actions_system_images::format_justify_right() noexcept {
  return format_justify_right(default_size());
}

image actions_system_images::format_justify_right(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-justify-right", size);
}

image actions_system_images::format_text_direction_ltr() noexcept {
  return format_text_direction_ltr(default_size());
}

image actions_system_images::format_text_direction_ltr(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-text-direction-ltr", size);
}

image actions_system_images::format_text_direction_rtl() noexcept {
  return format_text_direction_rtl(default_size());
}

image actions_system_images::format_text_direction_rtl(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-text-direction-rtl", size);
}

image actions_system_images::format_text_bold() noexcept {
  return format_text_bold(default_size());
}

image actions_system_images::format_text_bold(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-text-bold", size);
}

image actions_system_images::format_text_italic() noexcept {
  return format_text_italic(default_size());
}

image actions_system_images::format_text_italic(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-text-italic", size);
}

image actions_system_images::format_text_underline() noexcept {
  return format_text_underline(default_size());
}

image actions_system_images::format_text_underline(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-text-underline", size);
}

image actions_system_images::format_text_strikethrough() noexcept {
  return format_text_strikethrough(default_size());
}

image actions_system_images::format_text_strikethrough(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("format-text-strikethrough", size);
}

image actions_system_images::go_bottom() noexcept {
  return go_bottom(default_size());
}

image actions_system_images::go_bottom(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("go-bottom", size);
}

image actions_system_images::go_down() noexcept {
  return go_down(default_size());
}

image actions_system_images::go_down(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("go-down", size);
}

image actions_system_images::go_first() noexcept {
  return go_first(default_size());
}

image actions_system_images::go_first(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("go-first", size);
}

image actions_system_images::go_home() noexcept {
  return go_home(default_size());
}

image actions_system_images::go_home(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("go-home", size);
}

image actions_system_images::go_jump() noexcept {
  return go_jump(default_size());
}

image actions_system_images::go_jump(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("go-jump", size);
}

image actions_system_images::go_last() noexcept {
  return go_last(default_size());
}

image actions_system_images::go_last(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("go-last", size);
}

image actions_system_images::go_next() noexcept {
  return go_next(default_size());
}

image actions_system_images::go_next(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("go-next", size);
}

image actions_system_images::go_previous() noexcept {
  return go_previous(default_size());
}

image actions_system_images::go_previous(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("go-previous", size);
}

image actions_system_images::go_top() noexcept {
  return go_top(default_size());
}

image actions_system_images::go_top(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("go-top", size);
}

image actions_system_images::go_up() noexcept {
  return go_up(default_size());
}

image actions_system_images::go_up(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("go-up", size);
}

image actions_system_images::help_about() noexcept {
  return help_about(default_size());
}

image actions_system_images::help_about(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("help-about", size);
}

image actions_system_images::help_contents() noexcept {
  return help_contents(default_size());
}

image actions_system_images::help_contents(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("help-contents", size);
}

image actions_system_images::help_faq() noexcept {
  return help_faq(default_size());
}

image actions_system_images::help_faq(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("help-faq", size);
}

image actions_system_images::insert_image() noexcept {
  return insert_image(default_size());
}

image actions_system_images::insert_image(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("insert-image", size);
}

image actions_system_images::insert_link() noexcept {
  return insert_link(default_size());
}

image actions_system_images::insert_link(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("insert-link", size);
}

image actions_system_images::insert_object() noexcept {
  return insert_object(default_size());
}

image actions_system_images::insert_object(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("insert-object", size);
}

image actions_system_images::insert_table() noexcept {
  return insert_table(default_size());
}

image actions_system_images::insert_table(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("insert-table", size);
}

image actions_system_images::insert_text() noexcept {
  return insert_text(default_size());
}

image actions_system_images::insert_text(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("insert-text", size);
}

image actions_system_images::list_add() noexcept {
  return list_add(default_size());
}

image actions_system_images::list_add(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("list-add", size);
}

image actions_system_images::list_remove() noexcept {
  return list_remove(default_size());
}

image actions_system_images::list_remove(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("list-remove", size);
}

image actions_system_images::mail_forward() noexcept {
  return mail_forward(default_size());
}

image actions_system_images::mail_forward(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("mail-forward", size);
}

image actions_system_images::mail_mark_important() noexcept {
  return mail_mark_important(default_size());
}

image actions_system_images::mail_mark_important(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("mail-mark-important", size);
}

image actions_system_images::mail_mark_junk() noexcept {
  return mail_mark_junk(default_size());
}

image actions_system_images::mail_mark_junk(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("mail-mark-junk", size);
}

image actions_system_images::mail_mark_notjunk() noexcept {
  return mail_mark_notjunk(default_size());
}

image actions_system_images::mail_mark_notjunk(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("mail-mark-notjunk", size);
}

image actions_system_images::mail_mark_read() noexcept {
  return mail_mark_read(default_size());
}

image actions_system_images::mail_mark_read(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("mail-mark-read", size);
}

image actions_system_images::mail_mark_unread() noexcept {
  return mail_mark_unread(default_size());
}

image actions_system_images::mail_mark_unread(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("mail-mark-unread", size);
}

image actions_system_images::mail_message_new() noexcept {
  return mail_message_new(default_size());
}

image actions_system_images::mail_message_new(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("mail-message-new", size);
}

image actions_system_images::mail_reply_all() noexcept {
  return mail_reply_all(default_size());
}

image actions_system_images::mail_reply_all(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("mail-reply-all", size);
}

image actions_system_images::mail_reply_sender() noexcept {
  return mail_reply_sender(default_size());
}

image actions_system_images::mail_reply_sender(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("mail-reply-sender", size);
}

image actions_system_images::mail_send() noexcept {
  return mail_send(default_size());
}

image actions_system_images::mail_send(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("mail-send", size);
}

image actions_system_images::mail_send_receive() noexcept {
  return mail_send_receive(default_size());
}

image actions_system_images::mail_send_receive(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("mail-send-receive", size);
}

image actions_system_images::media_eject() noexcept {
  return media_eject(default_size());
}

image actions_system_images::media_eject(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("media-eject", size);
}

image actions_system_images::media_optical_burn() noexcept {
  return media_optical_burn(default_size());
}

image actions_system_images::media_optical_burn(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("media-optical-burn", size);
}

image actions_system_images::media_playback_pause() noexcept {
  return media_playback_pause(default_size());
}

image actions_system_images::media_playback_pause(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("media-playback-pause", size);
}

image actions_system_images::media_playback_start() noexcept {
  return media_playback_start(default_size());
}

image actions_system_images::media_playback_start(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("media-playback-start", size);
}

image actions_system_images::media_playback_stop() noexcept {
  return media_playback_stop(default_size());
}

image actions_system_images::media_playback_stop(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("media-playback-stop", size);
}

image actions_system_images::media_record() noexcept {
  return media_record(default_size());
}

image actions_system_images::media_record(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("media-record", size);
}

image actions_system_images::media_seek_backward() noexcept {
  return media_seek_backward(default_size());
}

image actions_system_images::media_seek_backward(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("media-seek-backward", size);
}

image actions_system_images::media_seek_forward() noexcept {
  return media_seek_forward(default_size());
}

image actions_system_images::media_seek_forward(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("media-seek-forward", size);
}

image actions_system_images::media_skip_backward() noexcept {
  return media_skip_backward(default_size());
}

image actions_system_images::media_skip_backward(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("media-skip-backward", size);
}

image actions_system_images::media_skip_forward() noexcept {
  return media_skip_forward(default_size());
}

image actions_system_images::media_skip_forward(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("media-skip-forward", size);
}

image actions_system_images::object_flip_horizontal() noexcept {
  return object_flip_horizontal(default_size());
}

image actions_system_images::object_flip_horizontal(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("object-flip-horizontal", size);
}

image actions_system_images::object_flip_vertical() noexcept {
  return object_flip_vertical(default_size());
}

image actions_system_images::object_flip_vertical(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("object-flip-vertical", size);
}

image actions_system_images::object_rotate_left() noexcept {
  return object_rotate_left(default_size());
}

image actions_system_images::object_rotate_left(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("object-rotate-left", size);
}

image actions_system_images::object_rotate_right() noexcept {
  return object_rotate_right(default_size());
}

image actions_system_images::object_rotate_right(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("object-rotate-right", size);
}

image actions_system_images::process_stop() noexcept {
  return process_stop(default_size());
}

image actions_system_images::process_stop(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("process-stop", size);
}

image actions_system_images::system_lock_screen() noexcept {
  return system_lock_screen(default_size());
}

image actions_system_images::system_lock_screen(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("system-lock-screen", size);
}

image actions_system_images::system_log_out() noexcept {
  return system_log_out(default_size());
}

image actions_system_images::system_log_out(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("system-log-out", size);
}

image actions_system_images::system_run() noexcept {
  return system_run(default_size());
}

image actions_system_images::system_run(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("system-run", size);
}

image actions_system_images::system_search() noexcept {
  return system_search(default_size());
}

image actions_system_images::system_search(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("system-search", size);
}

image actions_system_images::system_reboot() noexcept {
  return system_reboot(default_size());
}

image actions_system_images::system_reboot(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("system-reboot", size);
}

image actions_system_images::system_shutdown() noexcept {
  return system_shutdown(default_size());
}

image actions_system_images::system_shutdown(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("system-shutdown", size);
}

image actions_system_images::tools_check_spelling() noexcept {
  return tools_check_spelling(default_size());
}

image actions_system_images::tools_check_spelling(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("tools-check-spelling", size);
}

image actions_system_images::view_fullscreen() noexcept {
  return view_fullscreen(default_size());
}

image actions_system_images::view_fullscreen(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("view-fullscreen", size);
}

image actions_system_images::view_media_equalizer() noexcept {
  return view_media_equalizer(default_size());
}

image actions_system_images::view_media_equalizer(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("view-media-equalizer", size);
}

image actions_system_images::view_media_lyrics() noexcept {
  return view_media_lyrics(default_size());
}

image actions_system_images::view_media_lyrics(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("view-media-lyrics", size);
}

image actions_system_images::view_refresh() noexcept {
  return view_refresh(default_size());
}

image actions_system_images::view_refresh(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("view-refresh", size);
}

image actions_system_images::view_restore() noexcept {
  return view_restore(default_size());
}

image actions_system_images::view_restore(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("view-restore", size);
}

image actions_system_images::view_sort_ascending() noexcept {
  return view_sort_ascending(default_size());
}

image actions_system_images::view_sort_ascending(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("view-sort-ascending", size);
}

image actions_system_images::view_sort_descending() noexcept {
  return view_sort_descending(default_size());
}

image actions_system_images::view_sort_descending(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("view-sort-descending", size);
}

image actions_system_images::window_close() noexcept {
  return window_close(default_size());
}

image actions_system_images::window_close(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("window-close", size);
}

image actions_system_images::window_close_hovered() noexcept {
  return window_close_hovered(default_size());
}

image actions_system_images::window_close_hovered(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("window-close-hovered", size);
}

image actions_system_images::window_fullscreen() noexcept {
  return window_fullscreen(default_size());
}

image actions_system_images::window_fullscreen(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("window-fullscreen", size);
}

image actions_system_images::window_fullscreen_hovered() noexcept {
  return window_fullscreen_hovered(default_size());
}

image actions_system_images::window_fullscreen_hovered(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("window-fullscreen-hovered", size);
}

image actions_system_images::window_maximize() noexcept {
  return window_maximize(default_size());
}

image actions_system_images::window_maximize(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("window-maximize", size);
}

image actions_system_images::window_maximize_hovered() noexcept {
  return window_maximize_hovered(default_size());
}

image actions_system_images::window_maximize_hovered(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("window-maximize-hovered", size);
}

image actions_system_images::window_minimize() noexcept {
  return window_minimize(default_size());
}

image actions_system_images::window_minimize(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("window-minimize", size);
}

image actions_system_images::window_minimize_hovered() noexcept {
  return window_minimize_hovered(default_size());
}

image actions_system_images::window_minimize_hovered(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("window-minimize-hovered", size);
}

image actions_system_images::window_new() noexcept {
  return window_new(default_size());
}

image actions_system_images::window_new(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("window-new", size);
}

image actions_system_images::window_restore() noexcept {
  return window_restore(default_size());
}

image actions_system_images::window_restore(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("window-restore", size);
}

image actions_system_images::window_restore_hovered() noexcept {
  return window_restore_hovered(default_size());
}

image actions_system_images::window_restore_hovered(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("window-restore-hovered", size);
}

image actions_system_images::zoom_fit_best() noexcept {
  return zoom_fit_best(default_size());
}

image actions_system_images::zoom_fit_best(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("zoom-fit-best", size);
}

image actions_system_images::zoom_in() noexcept {
  return zoom_in(default_size());
}

image actions_system_images::zoom_in(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("zoom-in", size);
}

image actions_system_images::zoom_original() noexcept {
  return zoom_original(default_size());
}

image actions_system_images::zoom_original(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("zoom-original", size);
}

image actions_system_images::zoom_out() noexcept {
  return zoom_out(default_size());
}

image actions_system_images::zoom_out(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("zoom-out", size);
}

const vector<image>& actions_system_images::get_images() noexcept {
  static auto images = get_images(default_size());
  return images;
}

vector<image> actions_system_images::get_images(const xtd::drawing::size& size) noexcept {
  return vector {address_book_new(size), application_exit(size), appointment_new(size), call_start(size), call_stop(size), contact_new(size), dialog_cancel(size), dialog_ok(size), dialog_ok_apply(size), document_edit(size), document_new(size), document_open(size), document_open_recent(size), document_page_setup(size), document_print(size), document_print_preview(size), document_properties(size), document_revert(size), document_save(size), document_save_as(size), document_send(size), edit_clear(size), edit_copy(size), edit_cut(size), edit_delete(size), edit_find(size), edit_find_replace(size), edit_paste(size), edit_redo(size), edit_rename(size), edit_select_all(size), edit_undo(size), folder_new(size), format_indent_less(size), format_indent_more(size), format_justify_center(size), format_justify_fill(size), format_justify_left(size), format_justify_right(size), format_text_direction_ltr(size), format_text_direction_rtl(size), format_text_bold(size), format_text_italic(size), format_text_underline(size), format_text_strikethrough(size), go_bottom(size), go_down(size), go_first(size), go_home(size), go_jump(size), go_last(size), go_next(size), go_previous(size), go_top(size), go_up(size), help_about(size), help_contents(size), help_faq(size), insert_image(size), insert_link(size), insert_object(size), insert_table(size), insert_text(size), list_add(size), list_remove(size), mail_forward(size), mail_mark_important(size), mail_mark_junk(size), mail_mark_notjunk(size), mail_mark_read(size), mail_mark_unread(size), mail_message_new(size), mail_reply_all(size), mail_reply_sender(size), mail_send(size), mail_send_receive(size), media_eject(size), media_optical_burn(size), media_playback_pause(size), media_playback_start(size), media_playback_stop(size), media_record(size), media_seek_backward(size), media_seek_forward(size), media_skip_backward(size), media_skip_forward(size), object_flip_horizontal(size), object_flip_vertical(size), object_rotate_left(size), object_rotate_right(size), process_stop(size), system_lock_screen(size), system_log_out(size), system_run(size), system_search(size), system_reboot(size), system_shutdown(size), tools_check_spelling(size), view_fullscreen(size), view_media_equalizer(size), view_media_lyrics(size), view_refresh(size), view_restore(size), view_sort_ascending(size), view_sort_descending(size), window_close(size), window_close_hovered(size), window_fullscreen(size), window_fullscreen_hovered(size), window_maximize(size), window_maximize_hovered(size), window_minimize(size), window_minimize_hovered(size), window_new(size), window_restore(size), window_restore_hovered(size), zoom_fit_best(size), zoom_in(size), zoom_original(size), zoom_out(size), };
}

const vector<ustring>& actions_system_images::get_image_names() noexcept {
  static auto names = vector {"address-book-new"_s, "application-exit"_s, "appointment-new"_s, "call-start"_s, "call-stop"_s, "contact-new"_s, "dialog-cancel"_s, "dialog-ok"_s, "dialog-ok-apply"_s, "document-edit"_s, "document-new"_s, "document-open"_s, "document-open-recent"_s, "document-page-setup"_s, "document-print"_s, "document-print-preview"_s, "document-properties"_s, "document-revert"_s, "document-save"_s, "document-save-as"_s, "document-send"_s, "edit-clear"_s, "edit-copy"_s, "edit-cut"_s, "edit-delete"_s, "edit-find"_s, "edit-find-replace"_s, "edit-paste"_s, "edit-redo"_s, "edit-rename"_s, "edit-select-all"_s, "edit-undo"_s, "folder-new"_s, "format-indent-less"_s, "format-indent-more"_s, "format-justify-center"_s, "format-justify-fill"_s, "format-justify-left"_s, "format-justify-right"_s, "format-text-direction-ltr"_s, "format-text-direction-rtl"_s, "format-text-bold"_s, "format-text-italic"_s, "format-text-underline"_s, "format-text-strikethrough"_s, "go-bottom"_s, "go-down"_s, "go-first"_s, "go-home"_s, "go-jump"_s, "go-last"_s, "go-next"_s, "go-previous"_s, "go-top"_s, "go-up"_s, "help-about"_s, "help-contents"_s, "help-faq"_s, "insert-image"_s, "insert-link"_s, "insert-object"_s, "insert-table"_s, "insert-text"_s, "list-add"_s, "list-remove"_s, "mail-forward"_s, "mail-mark-important"_s, "mail-mark-junk"_s, "mail-mark-notjunk"_s, "mail-mark-read"_s, "mail-mark-unread"_s, "mail-message-new"_s, "mail-reply-all"_s, "mail-reply-sender"_s, "mail-send"_s, "mail-send-receive"_s, "media-eject"_s, "media-optical-burn"_s, "media-playback-pause"_s, "media-playback-start"_s, "media-playback-stop"_s, "media-record"_s, "media-seek-backward"_s, "media-seek-forward"_s, "media-skip-backward"_s, "media-skip-forward"_s, "object-flip-horizontal"_s, "object-flip-vertical"_s, "object-rotate-left"_s, "object-rotate-right"_s, "process-stop"_s, "system-lock-screen"_s, "system-log-out"_s, "system-run"_s, "system-search"_s, "system-reboot"_s, "system-shutdown"_s, "tools-check-spelling"_s, "view-fullscreen"_s, "view-media-equalizer"_s, "view-media-lyrics"_s, "view-refresh"_s, "view-restore"_s, "view-sort-ascending"_s, "view-sort-descending"_s, "window-close"_s, "window-close-hovered"_s, "window-fullscreen"_s, "window-fullscreen-hovered"_s, "window-maximize"_s, "window-maximize-hovered"_s, "window-minimize"_s, "window-minimize-hovered"_s, "window-new"_s, "window-restore"_s, "window-restore-hovered"_s, "zoom-fit-best"_s, "zoom-in"_s, "zoom-original"_s, "zoom-out"_s, };
  return names;
}
