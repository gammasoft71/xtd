#include "../../../include/xtd/drawing/places_system_images.h"

using namespace std;
using namespace xtd;
using namespace xtd::drawing;

drawing::size places_system_images::default_size() noexcept {
  return system_images_base::default_size();
}

image places_system_images::folder() noexcept {
  return folder(default_size());
}

image places_system_images::folder(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder", size);
}

image places_system_images::folder_activities() noexcept {
  return folder_activities(default_size());
}

image places_system_images::folder_activities(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-activities", size);
}

image places_system_images::folder_applications() noexcept {
  return folder_applications(default_size());
}

image places_system_images::folder_applications(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-applications", size);
}

image places_system_images::folder_bookmark() noexcept {
  return folder_bookmark(default_size());
}

image places_system_images::folder_bookmark(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-bookmark", size);
}

image places_system_images::folder_cloud() noexcept {
  return folder_cloud(default_size());
}

image places_system_images::folder_cloud(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-cloud", size);
}

image places_system_images::folder_desktop() noexcept {
  return folder_desktop(default_size());
}

image places_system_images::folder_desktop(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-desktop", size);
}

image places_system_images::folder_development() noexcept {
  return folder_development(default_size());
}

image places_system_images::folder_development(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-development", size);
}

image places_system_images::folder_documents() noexcept {
  return folder_documents(default_size());
}

image places_system_images::folder_documents(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-documents", size);
}

image places_system_images::folder_download() noexcept {
  return folder_download(default_size());
}

image places_system_images::folder_download(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-download", size);
}

image places_system_images::folder_favorites() noexcept {
  return folder_favorites(default_size());
}

image places_system_images::folder_favorites(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-favorites", size);
}

image places_system_images::folder_games() noexcept {
  return folder_games(default_size());
}

image places_system_images::folder_games(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-games", size);
}

image places_system_images::folder_github() noexcept {
  return folder_github(default_size());
}

image places_system_images::folder_github(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-github", size);
}

image places_system_images::folder_home() noexcept {
  return folder_home(default_size());
}

image places_system_images::folder_home(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-home", size);
}

image places_system_images::folder_html() noexcept {
  return folder_html(default_size());
}

image places_system_images::folder_html(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-html", size);
}

image places_system_images::folder_images() noexcept {
  return folder_images(default_size());
}

image places_system_images::folder_images(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-images", size);
}

image places_system_images::folder_important() noexcept {
  return folder_important(default_size());
}

image places_system_images::folder_important(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-important", size);
}

image places_system_images::folder_locked() noexcept {
  return folder_locked(default_size());
}

image places_system_images::folder_locked(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-locked", size);
}

image places_system_images::folder_music() noexcept {
  return folder_music(default_size());
}

image places_system_images::folder_music(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-music", size);
}

image places_system_images::folder_network() noexcept {
  return folder_network(default_size());
}

image places_system_images::folder_network(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-network", size);
}

image places_system_images::folder_print() noexcept {
  return folder_print(default_size());
}

image places_system_images::folder_print(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-print", size);
}

image places_system_images::folder_public() noexcept {
  return folder_public(default_size());
}

image places_system_images::folder_public(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-public", size);
}

image places_system_images::folder_remote() noexcept {
  return folder_remote(default_size());
}

image places_system_images::folder_remote(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-remote", size);
}

image places_system_images::folder_system() noexcept {
  return folder_system(default_size());
}

image places_system_images::folder_system(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-system", size);
}

image places_system_images::folder_tar() noexcept {
  return folder_tar(default_size());
}

image places_system_images::folder_tar(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-tar", size);
}

image places_system_images::folder_temp() noexcept {
  return folder_temp(default_size());
}

image places_system_images::folder_temp(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-temp", size);
}

image places_system_images::folder_templates() noexcept {
  return folder_templates(default_size());
}

image places_system_images::folder_templates(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-templates", size);
}

image places_system_images::folder_text() noexcept {
  return folder_text(default_size());
}

image places_system_images::folder_text(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-text", size);
}

image places_system_images::folder_videos() noexcept {
  return folder_videos(default_size());
}

image places_system_images::folder_videos(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("folder-videos", size);
}

image places_system_images::network_server() noexcept {
  return network_server(default_size());
}

image places_system_images::network_server(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("network-server", size);
}

image places_system_images::network_workgroup() noexcept {
  return network_workgroup(default_size());
}

image places_system_images::network_workgroup(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("network-workgroup", size);
}

image places_system_images::start_here() noexcept {
  return start_here(default_size());
}

image places_system_images::start_here(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("start-here", size);
}

image places_system_images::user_bookmarks() noexcept {
  return user_bookmarks(default_size());
}

image places_system_images::user_bookmarks(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("user-bookmarks", size);
}

image places_system_images::user_desktop() noexcept {
  return user_desktop(default_size());
}

image places_system_images::user_desktop(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("user-desktop", size);
}

image places_system_images::user_home() noexcept {
  return user_home(default_size());
}

image places_system_images::user_home(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("user-home", size);
}

image places_system_images::user_trash() noexcept {
  return user_trash(default_size());
}

image places_system_images::user_trash(const xtd::drawing::size& size) noexcept {
  return system_images_base::from_name("user-trash", size);
}

const vector<image>& places_system_images::get_images() noexcept {
  static auto images = get_images(default_size());
  return images;
}

vector<image> places_system_images::get_images(const xtd::drawing::size& size) noexcept {
  return vector {folder(size), folder_activities(size), folder_applications(size), folder_bookmark(size), folder_cloud(size), folder_desktop(size), folder_development(size), folder_documents(size), folder_download(size), folder_favorites(size), folder_games(size), folder_github(size), folder_home(size), folder_html(size), folder_images(size), folder_important(size), folder_locked(size), folder_music(size), folder_network(size), folder_print(size), folder_public(size), folder_remote(size), folder_system(size), folder_tar(size), folder_temp(size), folder_templates(size), folder_text(size), folder_videos(size), network_server(size), network_workgroup(size), start_here(size), user_bookmarks(size), user_desktop(size), user_home(size), user_trash(size), };
}

const vector<ustring>& places_system_images::get_image_names() noexcept {
  static auto names = vector {"folder"_s, "folder-activities"_s, "folder-applications"_s, "folder-bookmark"_s, "folder-cloud"_s, "folder-desktop"_s, "folder-development"_s, "folder-documents"_s, "folder-download"_s, "folder-favorites"_s, "folder-games"_s, "folder-github"_s, "folder-home"_s, "folder-html"_s, "folder-images"_s, "folder-important"_s, "folder-locked"_s, "folder-music"_s, "folder-network"_s, "folder-print"_s, "folder-public"_s, "folder-remote"_s, "folder-system"_s, "folder-tar"_s, "folder-temp"_s, "folder-templates"_s, "folder-text"_s, "folder-videos"_s, "network-server"_s, "network-workgroup"_s, "start-here"_s, "user-bookmarks"_s, "user-desktop"_s, "user-home"_s, "user-trash"_s, };
  return names;
}
