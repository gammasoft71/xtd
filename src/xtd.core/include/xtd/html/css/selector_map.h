#pragma once
#include "selector.h"
#include <map>

namespace xtd {
  namespace html {
    namespace css {
      using selector_map = std::map<std::string, xtd::html::css::selector>;
    }
  }
}
