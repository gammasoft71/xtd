#include "../../../include/xtd/forms/toggle_buttons.h"
#include "../../../include/xtd/forms/system_texts.h"

using namespace xtd;
using namespace xtd::forms;

toggle_button toggle_buttons::abort() {
  return toggle_button::create(system_texts::abort());
}

toggle_button toggle_buttons::abort(const drawing::point& location) {
  return toggle_button::create(system_texts::abort(), location);
}

toggle_button toggle_buttons::abort(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::abort(), location, size);
}

toggle_button toggle_buttons::abort(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::abort(), location, size, name);
}

toggle_button toggle_buttons::abort(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::abort(), check_state);
}

toggle_button toggle_buttons::abort(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::abort(), check_state, location);
}

toggle_button toggle_buttons::abort(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::abort(), check_state, location, size);
}

toggle_button toggle_buttons::abort(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::abort(), check_state, location, size, name);
}

toggle_button toggle_buttons::abort(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::abort(), three_state, check_state);
}

toggle_button toggle_buttons::abort(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::abort(), three_state, check_state, location);
}

toggle_button toggle_buttons::abort(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::abort(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::abort(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::abort(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::abort(const control& parent) {
  return toggle_button::create(parent, system_texts::abort());
}

toggle_button toggle_buttons::abort(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::abort(), location);
}

toggle_button toggle_buttons::abort(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::abort(), location, size);
}

toggle_button toggle_buttons::abort(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::abort(), location, size, name);
}

toggle_button toggle_buttons::abort(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::abort(), check_state);
}

toggle_button toggle_buttons::abort(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::abort(), check_state, location);
}

toggle_button toggle_buttons::abort(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::abort(), check_state, location, size);
}

toggle_button toggle_buttons::abort(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::abort(), check_state, location, size, name);
}

toggle_button toggle_buttons::abort(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::abort(), three_state, check_state);
}

toggle_button toggle_buttons::abort(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::abort(), three_state, check_state, location);
}

toggle_button toggle_buttons::abort(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::abort(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::abort(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::abort(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::about() {
  return toggle_button::create(system_texts::about());
}

toggle_button toggle_buttons::about(const drawing::point& location) {
  return toggle_button::create(system_texts::about(), location);
}

toggle_button toggle_buttons::about(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::about(), location, size);
}

toggle_button toggle_buttons::about(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::about(), location, size, name);
}

toggle_button toggle_buttons::about(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::about(), check_state);
}

toggle_button toggle_buttons::about(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::about(), check_state, location);
}

toggle_button toggle_buttons::about(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::about(), check_state, location, size);
}

toggle_button toggle_buttons::about(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::about(), check_state, location, size, name);
}

toggle_button toggle_buttons::about(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::about(), three_state, check_state);
}

toggle_button toggle_buttons::about(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::about(), three_state, check_state, location);
}

toggle_button toggle_buttons::about(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::about(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::about(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::about(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::about(const control& parent) {
  return toggle_button::create(parent, system_texts::about());
}

toggle_button toggle_buttons::about(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::about(), location);
}

toggle_button toggle_buttons::about(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::about(), location, size);
}

toggle_button toggle_buttons::about(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::about(), location, size, name);
}

toggle_button toggle_buttons::about(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::about(), check_state);
}

toggle_button toggle_buttons::about(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::about(), check_state, location);
}

toggle_button toggle_buttons::about(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::about(), check_state, location, size);
}

toggle_button toggle_buttons::about(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::about(), check_state, location, size, name);
}

toggle_button toggle_buttons::about(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::about(), three_state, check_state);
}

toggle_button toggle_buttons::about(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::about(), three_state, check_state, location);
}

toggle_button toggle_buttons::about(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::about(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::about(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::about(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::actual_size() {
  return toggle_button::create(system_texts::actual_size());
}

toggle_button toggle_buttons::actual_size(const drawing::point& location) {
  return toggle_button::create(system_texts::actual_size(), location);
}

toggle_button toggle_buttons::actual_size(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::actual_size(), location, size);
}

toggle_button toggle_buttons::actual_size(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::actual_size(), location, size, name);
}

toggle_button toggle_buttons::actual_size(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::actual_size(), check_state);
}

toggle_button toggle_buttons::actual_size(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::actual_size(), check_state, location);
}

toggle_button toggle_buttons::actual_size(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::actual_size(), check_state, location, size);
}

toggle_button toggle_buttons::actual_size(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::actual_size(), check_state, location, size, name);
}

toggle_button toggle_buttons::actual_size(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::actual_size(), three_state, check_state);
}

toggle_button toggle_buttons::actual_size(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::actual_size(), three_state, check_state, location);
}

toggle_button toggle_buttons::actual_size(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::actual_size(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::actual_size(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::actual_size(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::actual_size(const control& parent) {
  return toggle_button::create(parent, system_texts::actual_size());
}

toggle_button toggle_buttons::actual_size(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::actual_size(), location);
}

toggle_button toggle_buttons::actual_size(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::actual_size(), location, size);
}

toggle_button toggle_buttons::actual_size(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::actual_size(), location, size, name);
}

toggle_button toggle_buttons::actual_size(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::actual_size(), check_state);
}

toggle_button toggle_buttons::actual_size(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::actual_size(), check_state, location);
}

toggle_button toggle_buttons::actual_size(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::actual_size(), check_state, location, size);
}

toggle_button toggle_buttons::actual_size(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::actual_size(), check_state, location, size, name);
}

toggle_button toggle_buttons::actual_size(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::actual_size(), three_state, check_state);
}

toggle_button toggle_buttons::actual_size(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::actual_size(), three_state, check_state, location);
}

toggle_button toggle_buttons::actual_size(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::actual_size(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::actual_size(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::actual_size(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::add() {
  return toggle_button::create(system_texts::add());
}

toggle_button toggle_buttons::add(const drawing::point& location) {
  return toggle_button::create(system_texts::add(), location);
}

toggle_button toggle_buttons::add(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::add(), location, size);
}

toggle_button toggle_buttons::add(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::add(), location, size, name);
}

toggle_button toggle_buttons::add(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::add(), check_state);
}

toggle_button toggle_buttons::add(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::add(), check_state, location);
}

toggle_button toggle_buttons::add(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::add(), check_state, location, size);
}

toggle_button toggle_buttons::add(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::add(), check_state, location, size, name);
}

toggle_button toggle_buttons::add(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::add(), three_state, check_state);
}

toggle_button toggle_buttons::add(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::add(), three_state, check_state, location);
}

toggle_button toggle_buttons::add(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::add(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::add(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::add(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::add(const control& parent) {
  return toggle_button::create(parent, system_texts::add());
}

toggle_button toggle_buttons::add(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::add(), location);
}

toggle_button toggle_buttons::add(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::add(), location, size);
}

toggle_button toggle_buttons::add(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::add(), location, size, name);
}

toggle_button toggle_buttons::add(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::add(), check_state);
}

toggle_button toggle_buttons::add(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::add(), check_state, location);
}

toggle_button toggle_buttons::add(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::add(), check_state, location, size);
}

toggle_button toggle_buttons::add(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::add(), check_state, location, size, name);
}

toggle_button toggle_buttons::add(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::add(), three_state, check_state);
}

toggle_button toggle_buttons::add(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::add(), three_state, check_state, location);
}

toggle_button toggle_buttons::add(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::add(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::add(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::add(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::alignment() {
  return toggle_button::create(system_texts::alignment());
}

toggle_button toggle_buttons::alignment(const drawing::point& location) {
  return toggle_button::create(system_texts::alignment(), location);
}

toggle_button toggle_buttons::alignment(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::alignment(), location, size);
}

toggle_button toggle_buttons::alignment(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::alignment(), location, size, name);
}

toggle_button toggle_buttons::alignment(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::alignment(), check_state);
}

toggle_button toggle_buttons::alignment(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::alignment(), check_state, location);
}

toggle_button toggle_buttons::alignment(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::alignment(), check_state, location, size);
}

toggle_button toggle_buttons::alignment(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::alignment(), check_state, location, size, name);
}

toggle_button toggle_buttons::alignment(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::alignment(), three_state, check_state);
}

toggle_button toggle_buttons::alignment(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::alignment(), three_state, check_state, location);
}

toggle_button toggle_buttons::alignment(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::alignment(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::alignment(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::alignment(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::alignment(const control& parent) {
  return toggle_button::create(parent, system_texts::alignment());
}

toggle_button toggle_buttons::alignment(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::alignment(), location);
}

toggle_button toggle_buttons::alignment(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::alignment(), location, size);
}

toggle_button toggle_buttons::alignment(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::alignment(), location, size, name);
}

toggle_button toggle_buttons::alignment(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::alignment(), check_state);
}

toggle_button toggle_buttons::alignment(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::alignment(), check_state, location);
}

toggle_button toggle_buttons::alignment(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::alignment(), check_state, location, size);
}

toggle_button toggle_buttons::alignment(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::alignment(), check_state, location, size, name);
}

toggle_button toggle_buttons::alignment(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::alignment(), three_state, check_state);
}

toggle_button toggle_buttons::alignment(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::alignment(), three_state, check_state, location);
}

toggle_button toggle_buttons::alignment(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::alignment(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::alignment(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::alignment(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::align_left() {
  return toggle_button::create(system_texts::align_left());
}

toggle_button toggle_buttons::align_left(const drawing::point& location) {
  return toggle_button::create(system_texts::align_left(), location);
}

toggle_button toggle_buttons::align_left(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::align_left(), location, size);
}

toggle_button toggle_buttons::align_left(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::align_left(), location, size, name);
}

toggle_button toggle_buttons::align_left(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::align_left(), check_state);
}

toggle_button toggle_buttons::align_left(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::align_left(), check_state, location);
}

toggle_button toggle_buttons::align_left(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::align_left(), check_state, location, size);
}

toggle_button toggle_buttons::align_left(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::align_left(), check_state, location, size, name);
}

toggle_button toggle_buttons::align_left(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::align_left(), three_state, check_state);
}

toggle_button toggle_buttons::align_left(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::align_left(), three_state, check_state, location);
}

toggle_button toggle_buttons::align_left(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::align_left(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::align_left(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::align_left(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::align_left(const control& parent) {
  return toggle_button::create(parent, system_texts::align_left());
}

toggle_button toggle_buttons::align_left(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::align_left(), location);
}

toggle_button toggle_buttons::align_left(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::align_left(), location, size);
}

toggle_button toggle_buttons::align_left(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::align_left(), location, size, name);
}

toggle_button toggle_buttons::align_left(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::align_left(), check_state);
}

toggle_button toggle_buttons::align_left(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::align_left(), check_state, location);
}

toggle_button toggle_buttons::align_left(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::align_left(), check_state, location, size);
}

toggle_button toggle_buttons::align_left(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::align_left(), check_state, location, size, name);
}

toggle_button toggle_buttons::align_left(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::align_left(), three_state, check_state);
}

toggle_button toggle_buttons::align_left(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::align_left(), three_state, check_state, location);
}

toggle_button toggle_buttons::align_left(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::align_left(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::align_left(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::align_left(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::align_right() {
  return toggle_button::create(system_texts::align_right());
}

toggle_button toggle_buttons::align_right(const drawing::point& location) {
  return toggle_button::create(system_texts::align_right(), location);
}

toggle_button toggle_buttons::align_right(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::align_right(), location, size);
}

toggle_button toggle_buttons::align_right(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::align_right(), location, size, name);
}

toggle_button toggle_buttons::align_right(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::align_right(), check_state);
}

toggle_button toggle_buttons::align_right(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::align_right(), check_state, location);
}

toggle_button toggle_buttons::align_right(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::align_right(), check_state, location, size);
}

toggle_button toggle_buttons::align_right(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::align_right(), check_state, location, size, name);
}

toggle_button toggle_buttons::align_right(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::align_right(), three_state, check_state);
}

toggle_button toggle_buttons::align_right(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::align_right(), three_state, check_state, location);
}

toggle_button toggle_buttons::align_right(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::align_right(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::align_right(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::align_right(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::align_right(const control& parent) {
  return toggle_button::create(parent, system_texts::align_right());
}

toggle_button toggle_buttons::align_right(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::align_right(), location);
}

toggle_button toggle_buttons::align_right(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::align_right(), location, size);
}

toggle_button toggle_buttons::align_right(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::align_right(), location, size, name);
}

toggle_button toggle_buttons::align_right(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::align_right(), check_state);
}

toggle_button toggle_buttons::align_right(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::align_right(), check_state, location);
}

toggle_button toggle_buttons::align_right(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::align_right(), check_state, location, size);
}

toggle_button toggle_buttons::align_right(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::align_right(), check_state, location, size, name);
}

toggle_button toggle_buttons::align_right(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::align_right(), three_state, check_state);
}

toggle_button toggle_buttons::align_right(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::align_right(), three_state, check_state, location);
}

toggle_button toggle_buttons::align_right(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::align_right(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::align_right(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::align_right(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::apply() {
  return toggle_button::create(system_texts::apply());
}

toggle_button toggle_buttons::apply(const drawing::point& location) {
  return toggle_button::create(system_texts::apply(), location);
}

toggle_button toggle_buttons::apply(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::apply(), location, size);
}

toggle_button toggle_buttons::apply(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::apply(), location, size, name);
}

toggle_button toggle_buttons::apply(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::apply(), check_state);
}

toggle_button toggle_buttons::apply(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::apply(), check_state, location);
}

toggle_button toggle_buttons::apply(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::apply(), check_state, location, size);
}

toggle_button toggle_buttons::apply(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::apply(), check_state, location, size, name);
}

toggle_button toggle_buttons::apply(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::apply(), three_state, check_state);
}

toggle_button toggle_buttons::apply(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::apply(), three_state, check_state, location);
}

toggle_button toggle_buttons::apply(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::apply(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::apply(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::apply(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::apply(const control& parent) {
  return toggle_button::create(parent, system_texts::apply());
}

toggle_button toggle_buttons::apply(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::apply(), location);
}

toggle_button toggle_buttons::apply(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::apply(), location, size);
}

toggle_button toggle_buttons::apply(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::apply(), location, size, name);
}

toggle_button toggle_buttons::apply(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::apply(), check_state);
}

toggle_button toggle_buttons::apply(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::apply(), check_state, location);
}

toggle_button toggle_buttons::apply(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::apply(), check_state, location, size);
}

toggle_button toggle_buttons::apply(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::apply(), check_state, location, size, name);
}

toggle_button toggle_buttons::apply(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::apply(), three_state, check_state);
}

toggle_button toggle_buttons::apply(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::apply(), three_state, check_state, location);
}

toggle_button toggle_buttons::apply(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::apply(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::apply(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::apply(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::ascending() {
  return toggle_button::create(system_texts::ascending());
}

toggle_button toggle_buttons::ascending(const drawing::point& location) {
  return toggle_button::create(system_texts::ascending(), location);
}

toggle_button toggle_buttons::ascending(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::ascending(), location, size);
}

toggle_button toggle_buttons::ascending(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::ascending(), location, size, name);
}

toggle_button toggle_buttons::ascending(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::ascending(), check_state);
}

toggle_button toggle_buttons::ascending(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::ascending(), check_state, location);
}

toggle_button toggle_buttons::ascending(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::ascending(), check_state, location, size);
}

toggle_button toggle_buttons::ascending(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::ascending(), check_state, location, size, name);
}

toggle_button toggle_buttons::ascending(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::ascending(), three_state, check_state);
}

toggle_button toggle_buttons::ascending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::ascending(), three_state, check_state, location);
}

toggle_button toggle_buttons::ascending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::ascending(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::ascending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::ascending(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::ascending(const control& parent) {
  return toggle_button::create(parent, system_texts::ascending());
}

toggle_button toggle_buttons::ascending(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::ascending(), location);
}

toggle_button toggle_buttons::ascending(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::ascending(), location, size);
}

toggle_button toggle_buttons::ascending(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::ascending(), location, size, name);
}

toggle_button toggle_buttons::ascending(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::ascending(), check_state);
}

toggle_button toggle_buttons::ascending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::ascending(), check_state, location);
}

toggle_button toggle_buttons::ascending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::ascending(), check_state, location, size);
}

toggle_button toggle_buttons::ascending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::ascending(), check_state, location, size, name);
}

toggle_button toggle_buttons::ascending(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::ascending(), three_state, check_state);
}

toggle_button toggle_buttons::ascending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::ascending(), three_state, check_state, location);
}

toggle_button toggle_buttons::ascending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::ascending(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::ascending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::ascending(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::back() {
  return toggle_button::create(system_texts::back());
}

toggle_button toggle_buttons::back(const drawing::point& location) {
  return toggle_button::create(system_texts::back(), location);
}

toggle_button toggle_buttons::back(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::back(), location, size);
}

toggle_button toggle_buttons::back(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::back(), location, size, name);
}

toggle_button toggle_buttons::back(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::back(), check_state);
}

toggle_button toggle_buttons::back(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::back(), check_state, location);
}

toggle_button toggle_buttons::back(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::back(), check_state, location, size);
}

toggle_button toggle_buttons::back(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::back(), check_state, location, size, name);
}

toggle_button toggle_buttons::back(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::back(), three_state, check_state);
}

toggle_button toggle_buttons::back(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::back(), three_state, check_state, location);
}

toggle_button toggle_buttons::back(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::back(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::back(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::back(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::back(const control& parent) {
  return toggle_button::create(parent, system_texts::back());
}

toggle_button toggle_buttons::back(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::back(), location);
}

toggle_button toggle_buttons::back(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::back(), location, size);
}

toggle_button toggle_buttons::back(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::back(), location, size, name);
}

toggle_button toggle_buttons::back(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::back(), check_state);
}

toggle_button toggle_buttons::back(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::back(), check_state, location);
}

toggle_button toggle_buttons::back(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::back(), check_state, location, size);
}

toggle_button toggle_buttons::back(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::back(), check_state, location, size, name);
}

toggle_button toggle_buttons::back(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::back(), three_state, check_state);
}

toggle_button toggle_buttons::back(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::back(), three_state, check_state, location);
}

toggle_button toggle_buttons::back(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::back(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::back(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::back(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::bold() {
  return toggle_button::create(system_texts::bold());
}

toggle_button toggle_buttons::bold(const drawing::point& location) {
  return toggle_button::create(system_texts::bold(), location);
}

toggle_button toggle_buttons::bold(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::bold(), location, size);
}

toggle_button toggle_buttons::bold(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::bold(), location, size, name);
}

toggle_button toggle_buttons::bold(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::bold(), check_state);
}

toggle_button toggle_buttons::bold(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::bold(), check_state, location);
}

toggle_button toggle_buttons::bold(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::bold(), check_state, location, size);
}

toggle_button toggle_buttons::bold(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::bold(), check_state, location, size, name);
}

toggle_button toggle_buttons::bold(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::bold(), three_state, check_state);
}

toggle_button toggle_buttons::bold(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::bold(), three_state, check_state, location);
}

toggle_button toggle_buttons::bold(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::bold(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::bold(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::bold(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::bold(const control& parent) {
  return toggle_button::create(parent, system_texts::bold());
}

toggle_button toggle_buttons::bold(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::bold(), location);
}

toggle_button toggle_buttons::bold(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::bold(), location, size);
}

toggle_button toggle_buttons::bold(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::bold(), location, size, name);
}

toggle_button toggle_buttons::bold(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::bold(), check_state);
}

toggle_button toggle_buttons::bold(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::bold(), check_state, location);
}

toggle_button toggle_buttons::bold(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::bold(), check_state, location, size);
}

toggle_button toggle_buttons::bold(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::bold(), check_state, location, size, name);
}

toggle_button toggle_buttons::bold(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::bold(), three_state, check_state);
}

toggle_button toggle_buttons::bold(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::bold(), three_state, check_state, location);
}

toggle_button toggle_buttons::bold(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::bold(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::bold(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::bold(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::bottom() {
  return toggle_button::create(system_texts::bottom());
}

toggle_button toggle_buttons::bottom(const drawing::point& location) {
  return toggle_button::create(system_texts::bottom(), location);
}

toggle_button toggle_buttons::bottom(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::bottom(), location, size);
}

toggle_button toggle_buttons::bottom(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::bottom(), location, size, name);
}

toggle_button toggle_buttons::bottom(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::bottom(), check_state);
}

toggle_button toggle_buttons::bottom(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::bottom(), check_state, location);
}

toggle_button toggle_buttons::bottom(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::bottom(), check_state, location, size);
}

toggle_button toggle_buttons::bottom(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::bottom(), check_state, location, size, name);
}

toggle_button toggle_buttons::bottom(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::bottom(), three_state, check_state);
}

toggle_button toggle_buttons::bottom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::bottom(), three_state, check_state, location);
}

toggle_button toggle_buttons::bottom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::bottom(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::bottom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::bottom(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::bottom(const control& parent) {
  return toggle_button::create(parent, system_texts::bottom());
}

toggle_button toggle_buttons::bottom(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::bottom(), location);
}

toggle_button toggle_buttons::bottom(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::bottom(), location, size);
}

toggle_button toggle_buttons::bottom(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::bottom(), location, size, name);
}

toggle_button toggle_buttons::bottom(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::bottom(), check_state);
}

toggle_button toggle_buttons::bottom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::bottom(), check_state, location);
}

toggle_button toggle_buttons::bottom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::bottom(), check_state, location, size);
}

toggle_button toggle_buttons::bottom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::bottom(), check_state, location, size, name);
}

toggle_button toggle_buttons::bottom(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::bottom(), three_state, check_state);
}

toggle_button toggle_buttons::bottom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::bottom(), three_state, check_state, location);
}

toggle_button toggle_buttons::bottom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::bottom(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::bottom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::bottom(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::cancel() {
  return toggle_button::create(system_texts::cancel());
}

toggle_button toggle_buttons::cancel(const drawing::point& location) {
  return toggle_button::create(system_texts::cancel(), location);
}

toggle_button toggle_buttons::cancel(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::cancel(), location, size);
}

toggle_button toggle_buttons::cancel(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::cancel(), location, size, name);
}

toggle_button toggle_buttons::cancel(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::cancel(), check_state);
}

toggle_button toggle_buttons::cancel(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::cancel(), check_state, location);
}

toggle_button toggle_buttons::cancel(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::cancel(), check_state, location, size);
}

toggle_button toggle_buttons::cancel(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::cancel(), check_state, location, size, name);
}

toggle_button toggle_buttons::cancel(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::cancel(), three_state, check_state);
}

toggle_button toggle_buttons::cancel(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::cancel(), three_state, check_state, location);
}

toggle_button toggle_buttons::cancel(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::cancel(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::cancel(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::cancel(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::cancel(const control& parent) {
  return toggle_button::create(parent, system_texts::cancel());
}

toggle_button toggle_buttons::cancel(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::cancel(), location);
}

toggle_button toggle_buttons::cancel(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::cancel(), location, size);
}

toggle_button toggle_buttons::cancel(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::cancel(), location, size, name);
}

toggle_button toggle_buttons::cancel(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::cancel(), check_state);
}

toggle_button toggle_buttons::cancel(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::cancel(), check_state, location);
}

toggle_button toggle_buttons::cancel(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::cancel(), check_state, location, size);
}

toggle_button toggle_buttons::cancel(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::cancel(), check_state, location, size, name);
}

toggle_button toggle_buttons::cancel(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::cancel(), three_state, check_state);
}

toggle_button toggle_buttons::cancel(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::cancel(), three_state, check_state, location);
}

toggle_button toggle_buttons::cancel(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::cancel(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::cancel(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::cancel(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::cdrom() {
  return toggle_button::create(system_texts::cdrom());
}

toggle_button toggle_buttons::cdrom(const drawing::point& location) {
  return toggle_button::create(system_texts::cdrom(), location);
}

toggle_button toggle_buttons::cdrom(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::cdrom(), location, size);
}

toggle_button toggle_buttons::cdrom(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::cdrom(), location, size, name);
}

toggle_button toggle_buttons::cdrom(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::cdrom(), check_state);
}

toggle_button toggle_buttons::cdrom(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::cdrom(), check_state, location);
}

toggle_button toggle_buttons::cdrom(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::cdrom(), check_state, location, size);
}

toggle_button toggle_buttons::cdrom(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::cdrom(), check_state, location, size, name);
}

toggle_button toggle_buttons::cdrom(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::cdrom(), three_state, check_state);
}

toggle_button toggle_buttons::cdrom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::cdrom(), three_state, check_state, location);
}

toggle_button toggle_buttons::cdrom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::cdrom(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::cdrom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::cdrom(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::cdrom(const control& parent) {
  return toggle_button::create(parent, system_texts::cdrom());
}

toggle_button toggle_buttons::cdrom(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::cdrom(), location);
}

toggle_button toggle_buttons::cdrom(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::cdrom(), location, size);
}

toggle_button toggle_buttons::cdrom(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::cdrom(), location, size, name);
}

toggle_button toggle_buttons::cdrom(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::cdrom(), check_state);
}

toggle_button toggle_buttons::cdrom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::cdrom(), check_state, location);
}

toggle_button toggle_buttons::cdrom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::cdrom(), check_state, location, size);
}

toggle_button toggle_buttons::cdrom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::cdrom(), check_state, location, size, name);
}

toggle_button toggle_buttons::cdrom(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::cdrom(), three_state, check_state);
}

toggle_button toggle_buttons::cdrom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::cdrom(), three_state, check_state, location);
}

toggle_button toggle_buttons::cdrom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::cdrom(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::cdrom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::cdrom(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::centered() {
  return toggle_button::create(system_texts::centered());
}

toggle_button toggle_buttons::centered(const drawing::point& location) {
  return toggle_button::create(system_texts::centered(), location);
}

toggle_button toggle_buttons::centered(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::centered(), location, size);
}

toggle_button toggle_buttons::centered(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::centered(), location, size, name);
}

toggle_button toggle_buttons::centered(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::centered(), check_state);
}

toggle_button toggle_buttons::centered(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::centered(), check_state, location);
}

toggle_button toggle_buttons::centered(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::centered(), check_state, location, size);
}

toggle_button toggle_buttons::centered(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::centered(), check_state, location, size, name);
}

toggle_button toggle_buttons::centered(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::centered(), three_state, check_state);
}

toggle_button toggle_buttons::centered(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::centered(), three_state, check_state, location);
}

toggle_button toggle_buttons::centered(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::centered(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::centered(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::centered(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::centered(const control& parent) {
  return toggle_button::create(parent, system_texts::centered());
}

toggle_button toggle_buttons::centered(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::centered(), location);
}

toggle_button toggle_buttons::centered(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::centered(), location, size);
}

toggle_button toggle_buttons::centered(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::centered(), location, size, name);
}

toggle_button toggle_buttons::centered(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::centered(), check_state);
}

toggle_button toggle_buttons::centered(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::centered(), check_state, location);
}

toggle_button toggle_buttons::centered(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::centered(), check_state, location, size);
}

toggle_button toggle_buttons::centered(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::centered(), check_state, location, size, name);
}

toggle_button toggle_buttons::centered(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::centered(), three_state, check_state);
}

toggle_button toggle_buttons::centered(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::centered(), three_state, check_state, location);
}

toggle_button toggle_buttons::centered(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::centered(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::centered(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::centered(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::clear() {
  return toggle_button::create(system_texts::clear());
}

toggle_button toggle_buttons::clear(const drawing::point& location) {
  return toggle_button::create(system_texts::clear(), location);
}

toggle_button toggle_buttons::clear(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::clear(), location, size);
}

toggle_button toggle_buttons::clear(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::clear(), location, size, name);
}

toggle_button toggle_buttons::clear(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::clear(), check_state);
}

toggle_button toggle_buttons::clear(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::clear(), check_state, location);
}

toggle_button toggle_buttons::clear(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::clear(), check_state, location, size);
}

toggle_button toggle_buttons::clear(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::clear(), check_state, location, size, name);
}

toggle_button toggle_buttons::clear(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::clear(), three_state, check_state);
}

toggle_button toggle_buttons::clear(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::clear(), three_state, check_state, location);
}

toggle_button toggle_buttons::clear(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::clear(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::clear(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::clear(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::clear(const control& parent) {
  return toggle_button::create(parent, system_texts::clear());
}

toggle_button toggle_buttons::clear(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::clear(), location);
}

toggle_button toggle_buttons::clear(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::clear(), location, size);
}

toggle_button toggle_buttons::clear(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::clear(), location, size, name);
}

toggle_button toggle_buttons::clear(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::clear(), check_state);
}

toggle_button toggle_buttons::clear(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::clear(), check_state, location);
}

toggle_button toggle_buttons::clear(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::clear(), check_state, location, size);
}

toggle_button toggle_buttons::clear(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::clear(), check_state, location, size, name);
}

toggle_button toggle_buttons::clear(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::clear(), three_state, check_state);
}

toggle_button toggle_buttons::clear(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::clear(), three_state, check_state, location);
}

toggle_button toggle_buttons::clear(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::clear(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::clear(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::clear(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::close() {
  return toggle_button::create(system_texts::close());
}

toggle_button toggle_buttons::close(const drawing::point& location) {
  return toggle_button::create(system_texts::close(), location);
}

toggle_button toggle_buttons::close(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::close(), location, size);
}

toggle_button toggle_buttons::close(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::close(), location, size, name);
}

toggle_button toggle_buttons::close(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::close(), check_state);
}

toggle_button toggle_buttons::close(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::close(), check_state, location);
}

toggle_button toggle_buttons::close(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::close(), check_state, location, size);
}

toggle_button toggle_buttons::close(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::close(), check_state, location, size, name);
}

toggle_button toggle_buttons::close(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::close(), three_state, check_state);
}

toggle_button toggle_buttons::close(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::close(), three_state, check_state, location);
}

toggle_button toggle_buttons::close(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::close(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::close(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::close(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::close(const control& parent) {
  return toggle_button::create(parent, system_texts::close());
}

toggle_button toggle_buttons::close(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::close(), location);
}

toggle_button toggle_buttons::close(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::close(), location, size);
}

toggle_button toggle_buttons::close(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::close(), location, size, name);
}

toggle_button toggle_buttons::close(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::close(), check_state);
}

toggle_button toggle_buttons::close(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::close(), check_state, location);
}

toggle_button toggle_buttons::close(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::close(), check_state, location, size);
}

toggle_button toggle_buttons::close(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::close(), check_state, location, size, name);
}

toggle_button toggle_buttons::close(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::close(), three_state, check_state);
}

toggle_button toggle_buttons::close(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::close(), three_state, check_state, location);
}

toggle_button toggle_buttons::close(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::close(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::close(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::close(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::color() {
  return toggle_button::create(system_texts::color());
}

toggle_button toggle_buttons::color(const drawing::point& location) {
  return toggle_button::create(system_texts::color(), location);
}

toggle_button toggle_buttons::color(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::color(), location, size);
}

toggle_button toggle_buttons::color(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::color(), location, size, name);
}

toggle_button toggle_buttons::color(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::color(), check_state);
}

toggle_button toggle_buttons::color(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::color(), check_state, location);
}

toggle_button toggle_buttons::color(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::color(), check_state, location, size);
}

toggle_button toggle_buttons::color(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::color(), check_state, location, size, name);
}

toggle_button toggle_buttons::color(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::color(), three_state, check_state);
}

toggle_button toggle_buttons::color(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::color(), three_state, check_state, location);
}

toggle_button toggle_buttons::color(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::color(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::color(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::color(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::color(const control& parent) {
  return toggle_button::create(parent, system_texts::color());
}

toggle_button toggle_buttons::color(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::color(), location);
}

toggle_button toggle_buttons::color(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::color(), location, size);
}

toggle_button toggle_buttons::color(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::color(), location, size, name);
}

toggle_button toggle_buttons::color(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::color(), check_state);
}

toggle_button toggle_buttons::color(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::color(), check_state, location);
}

toggle_button toggle_buttons::color(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::color(), check_state, location, size);
}

toggle_button toggle_buttons::color(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::color(), check_state, location, size, name);
}

toggle_button toggle_buttons::color(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::color(), three_state, check_state);
}

toggle_button toggle_buttons::color(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::color(), three_state, check_state, location);
}

toggle_button toggle_buttons::color(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::color(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::color(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::color(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::contents() {
  return toggle_button::create(system_texts::contents());
}

toggle_button toggle_buttons::contents(const drawing::point& location) {
  return toggle_button::create(system_texts::contents(), location);
}

toggle_button toggle_buttons::contents(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::contents(), location, size);
}

toggle_button toggle_buttons::contents(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::contents(), location, size, name);
}

toggle_button toggle_buttons::contents(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::contents(), check_state);
}

toggle_button toggle_buttons::contents(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::contents(), check_state, location);
}

toggle_button toggle_buttons::contents(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::contents(), check_state, location, size);
}

toggle_button toggle_buttons::contents(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::contents(), check_state, location, size, name);
}

toggle_button toggle_buttons::contents(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::contents(), three_state, check_state);
}

toggle_button toggle_buttons::contents(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::contents(), three_state, check_state, location);
}

toggle_button toggle_buttons::contents(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::contents(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::contents(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::contents(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::contents(const control& parent) {
  return toggle_button::create(parent, system_texts::contents());
}

toggle_button toggle_buttons::contents(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::contents(), location);
}

toggle_button toggle_buttons::contents(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::contents(), location, size);
}

toggle_button toggle_buttons::contents(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::contents(), location, size, name);
}

toggle_button toggle_buttons::contents(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::contents(), check_state);
}

toggle_button toggle_buttons::contents(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::contents(), check_state, location);
}

toggle_button toggle_buttons::contents(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::contents(), check_state, location, size);
}

toggle_button toggle_buttons::contents(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::contents(), check_state, location, size, name);
}

toggle_button toggle_buttons::contents(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::contents(), three_state, check_state);
}

toggle_button toggle_buttons::contents(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::contents(), three_state, check_state, location);
}

toggle_button toggle_buttons::contents(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::contents(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::contents(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::contents(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::convert() {
  return toggle_button::create(system_texts::convert());
}

toggle_button toggle_buttons::convert(const drawing::point& location) {
  return toggle_button::create(system_texts::convert(), location);
}

toggle_button toggle_buttons::convert(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::convert(), location, size);
}

toggle_button toggle_buttons::convert(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::convert(), location, size, name);
}

toggle_button toggle_buttons::convert(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::convert(), check_state);
}

toggle_button toggle_buttons::convert(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::convert(), check_state, location);
}

toggle_button toggle_buttons::convert(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::convert(), check_state, location, size);
}

toggle_button toggle_buttons::convert(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::convert(), check_state, location, size, name);
}

toggle_button toggle_buttons::convert(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::convert(), three_state, check_state);
}

toggle_button toggle_buttons::convert(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::convert(), three_state, check_state, location);
}

toggle_button toggle_buttons::convert(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::convert(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::convert(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::convert(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::convert(const control& parent) {
  return toggle_button::create(parent, system_texts::convert());
}

toggle_button toggle_buttons::convert(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::convert(), location);
}

toggle_button toggle_buttons::convert(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::convert(), location, size);
}

toggle_button toggle_buttons::convert(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::convert(), location, size, name);
}

toggle_button toggle_buttons::convert(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::convert(), check_state);
}

toggle_button toggle_buttons::convert(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::convert(), check_state, location);
}

toggle_button toggle_buttons::convert(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::convert(), check_state, location, size);
}

toggle_button toggle_buttons::convert(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::convert(), check_state, location, size, name);
}

toggle_button toggle_buttons::convert(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::convert(), three_state, check_state);
}

toggle_button toggle_buttons::convert(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::convert(), three_state, check_state, location);
}

toggle_button toggle_buttons::convert(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::convert(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::convert(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::convert(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::copy() {
  return toggle_button::create(system_texts::copy());
}

toggle_button toggle_buttons::copy(const drawing::point& location) {
  return toggle_button::create(system_texts::copy(), location);
}

toggle_button toggle_buttons::copy(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::copy(), location, size);
}

toggle_button toggle_buttons::copy(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::copy(), location, size, name);
}

toggle_button toggle_buttons::copy(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::copy(), check_state);
}

toggle_button toggle_buttons::copy(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::copy(), check_state, location);
}

toggle_button toggle_buttons::copy(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::copy(), check_state, location, size);
}

toggle_button toggle_buttons::copy(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::copy(), check_state, location, size, name);
}

toggle_button toggle_buttons::copy(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::copy(), three_state, check_state);
}

toggle_button toggle_buttons::copy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::copy(), three_state, check_state, location);
}

toggle_button toggle_buttons::copy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::copy(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::copy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::copy(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::copy(const control& parent) {
  return toggle_button::create(parent, system_texts::copy());
}

toggle_button toggle_buttons::copy(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::copy(), location);
}

toggle_button toggle_buttons::copy(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::copy(), location, size);
}

toggle_button toggle_buttons::copy(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::copy(), location, size, name);
}

toggle_button toggle_buttons::copy(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::copy(), check_state);
}

toggle_button toggle_buttons::copy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::copy(), check_state, location);
}

toggle_button toggle_buttons::copy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::copy(), check_state, location, size);
}

toggle_button toggle_buttons::copy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::copy(), check_state, location, size, name);
}

toggle_button toggle_buttons::copy(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::copy(), three_state, check_state);
}

toggle_button toggle_buttons::copy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::copy(), three_state, check_state, location);
}

toggle_button toggle_buttons::copy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::copy(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::copy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::copy(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::customize() {
  return toggle_button::create(system_texts::customize());
}

toggle_button toggle_buttons::customize(const drawing::point& location) {
  return toggle_button::create(system_texts::customize(), location);
}

toggle_button toggle_buttons::customize(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::customize(), location, size);
}

toggle_button toggle_buttons::customize(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::customize(), location, size, name);
}

toggle_button toggle_buttons::customize(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::customize(), check_state);
}

toggle_button toggle_buttons::customize(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::customize(), check_state, location);
}

toggle_button toggle_buttons::customize(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::customize(), check_state, location, size);
}

toggle_button toggle_buttons::customize(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::customize(), check_state, location, size, name);
}

toggle_button toggle_buttons::customize(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::customize(), three_state, check_state);
}

toggle_button toggle_buttons::customize(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::customize(), three_state, check_state, location);
}

toggle_button toggle_buttons::customize(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::customize(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::customize(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::customize(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::customize(const control& parent) {
  return toggle_button::create(parent, system_texts::customize());
}

toggle_button toggle_buttons::customize(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::customize(), location);
}

toggle_button toggle_buttons::customize(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::customize(), location, size);
}

toggle_button toggle_buttons::customize(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::customize(), location, size, name);
}

toggle_button toggle_buttons::customize(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::customize(), check_state);
}

toggle_button toggle_buttons::customize(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::customize(), check_state, location);
}

toggle_button toggle_buttons::customize(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::customize(), check_state, location, size);
}

toggle_button toggle_buttons::customize(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::customize(), check_state, location, size, name);
}

toggle_button toggle_buttons::customize(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::customize(), three_state, check_state);
}

toggle_button toggle_buttons::customize(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::customize(), three_state, check_state, location);
}

toggle_button toggle_buttons::customize(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::customize(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::customize(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::customize(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::cut() {
  return toggle_button::create(system_texts::cut());
}

toggle_button toggle_buttons::cut(const drawing::point& location) {
  return toggle_button::create(system_texts::cut(), location);
}

toggle_button toggle_buttons::cut(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::cut(), location, size);
}

toggle_button toggle_buttons::cut(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::cut(), location, size, name);
}

toggle_button toggle_buttons::cut(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::cut(), check_state);
}

toggle_button toggle_buttons::cut(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::cut(), check_state, location);
}

toggle_button toggle_buttons::cut(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::cut(), check_state, location, size);
}

toggle_button toggle_buttons::cut(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::cut(), check_state, location, size, name);
}

toggle_button toggle_buttons::cut(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::cut(), three_state, check_state);
}

toggle_button toggle_buttons::cut(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::cut(), three_state, check_state, location);
}

toggle_button toggle_buttons::cut(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::cut(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::cut(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::cut(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::cut(const control& parent) {
  return toggle_button::create(parent, system_texts::cut());
}

toggle_button toggle_buttons::cut(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::cut(), location);
}

toggle_button toggle_buttons::cut(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::cut(), location, size);
}

toggle_button toggle_buttons::cut(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::cut(), location, size, name);
}

toggle_button toggle_buttons::cut(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::cut(), check_state);
}

toggle_button toggle_buttons::cut(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::cut(), check_state, location);
}

toggle_button toggle_buttons::cut(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::cut(), check_state, location, size);
}

toggle_button toggle_buttons::cut(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::cut(), check_state, location, size, name);
}

toggle_button toggle_buttons::cut(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::cut(), three_state, check_state);
}

toggle_button toggle_buttons::cut(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::cut(), three_state, check_state, location);
}

toggle_button toggle_buttons::cut(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::cut(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::cut(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::cut(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::del() {
  return toggle_button::create(system_texts::del());
}

toggle_button toggle_buttons::del(const drawing::point& location) {
  return toggle_button::create(system_texts::del(), location);
}

toggle_button toggle_buttons::del(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::del(), location, size);
}

toggle_button toggle_buttons::del(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::del(), location, size, name);
}

toggle_button toggle_buttons::del(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::del(), check_state);
}

toggle_button toggle_buttons::del(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::del(), check_state, location);
}

toggle_button toggle_buttons::del(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::del(), check_state, location, size);
}

toggle_button toggle_buttons::del(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::del(), check_state, location, size, name);
}

toggle_button toggle_buttons::del(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::del(), three_state, check_state);
}

toggle_button toggle_buttons::del(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::del(), three_state, check_state, location);
}

toggle_button toggle_buttons::del(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::del(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::del(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::del(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::del(const control& parent) {
  return toggle_button::create(parent, system_texts::del());
}

toggle_button toggle_buttons::del(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::del(), location);
}

toggle_button toggle_buttons::del(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::del(), location, size);
}

toggle_button toggle_buttons::del(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::del(), location, size, name);
}

toggle_button toggle_buttons::del(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::del(), check_state);
}

toggle_button toggle_buttons::del(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::del(), check_state, location);
}

toggle_button toggle_buttons::del(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::del(), check_state, location, size);
}

toggle_button toggle_buttons::del(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::del(), check_state, location, size, name);
}

toggle_button toggle_buttons::del(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::del(), three_state, check_state);
}

toggle_button toggle_buttons::del(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::del(), three_state, check_state, location);
}

toggle_button toggle_buttons::del(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::del(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::del(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::del(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::descending() {
  return toggle_button::create(system_texts::descending());
}

toggle_button toggle_buttons::descending(const drawing::point& location) {
  return toggle_button::create(system_texts::descending(), location);
}

toggle_button toggle_buttons::descending(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::descending(), location, size);
}

toggle_button toggle_buttons::descending(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::descending(), location, size, name);
}

toggle_button toggle_buttons::descending(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::descending(), check_state);
}

toggle_button toggle_buttons::descending(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::descending(), check_state, location);
}

toggle_button toggle_buttons::descending(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::descending(), check_state, location, size);
}

toggle_button toggle_buttons::descending(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::descending(), check_state, location, size, name);
}

toggle_button toggle_buttons::descending(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::descending(), three_state, check_state);
}

toggle_button toggle_buttons::descending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::descending(), three_state, check_state, location);
}

toggle_button toggle_buttons::descending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::descending(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::descending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::descending(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::descending(const control& parent) {
  return toggle_button::create(parent, system_texts::descending());
}

toggle_button toggle_buttons::descending(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::descending(), location);
}

toggle_button toggle_buttons::descending(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::descending(), location, size);
}

toggle_button toggle_buttons::descending(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::descending(), location, size, name);
}

toggle_button toggle_buttons::descending(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::descending(), check_state);
}

toggle_button toggle_buttons::descending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::descending(), check_state, location);
}

toggle_button toggle_buttons::descending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::descending(), check_state, location, size);
}

toggle_button toggle_buttons::descending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::descending(), check_state, location, size, name);
}

toggle_button toggle_buttons::descending(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::descending(), three_state, check_state);
}

toggle_button toggle_buttons::descending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::descending(), three_state, check_state, location);
}

toggle_button toggle_buttons::descending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::descending(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::descending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::descending(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::down() {
  return toggle_button::create(system_texts::down());
}

toggle_button toggle_buttons::down(const drawing::point& location) {
  return toggle_button::create(system_texts::down(), location);
}

toggle_button toggle_buttons::down(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::down(), location, size);
}

toggle_button toggle_buttons::down(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::down(), location, size, name);
}

toggle_button toggle_buttons::down(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::down(), check_state);
}

toggle_button toggle_buttons::down(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::down(), check_state, location);
}

toggle_button toggle_buttons::down(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::down(), check_state, location, size);
}

toggle_button toggle_buttons::down(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::down(), check_state, location, size, name);
}

toggle_button toggle_buttons::down(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::down(), three_state, check_state);
}

toggle_button toggle_buttons::down(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::down(), three_state, check_state, location);
}

toggle_button toggle_buttons::down(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::down(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::down(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::down(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::down(const control& parent) {
  return toggle_button::create(parent, system_texts::down());
}

toggle_button toggle_buttons::down(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::down(), location);
}

toggle_button toggle_buttons::down(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::down(), location, size);
}

toggle_button toggle_buttons::down(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::down(), location, size, name);
}

toggle_button toggle_buttons::down(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::down(), check_state);
}

toggle_button toggle_buttons::down(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::down(), check_state, location);
}

toggle_button toggle_buttons::down(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::down(), check_state, location, size);
}

toggle_button toggle_buttons::down(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::down(), check_state, location, size, name);
}

toggle_button toggle_buttons::down(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::down(), three_state, check_state);
}

toggle_button toggle_buttons::down(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::down(), three_state, check_state, location);
}

toggle_button toggle_buttons::down(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::down(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::down(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::down(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::edit() {
  return toggle_button::create(system_texts::edit());
}

toggle_button toggle_buttons::edit(const drawing::point& location) {
  return toggle_button::create(system_texts::edit(), location);
}

toggle_button toggle_buttons::edit(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::edit(), location, size);
}

toggle_button toggle_buttons::edit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::edit(), location, size, name);
}

toggle_button toggle_buttons::edit(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::edit(), check_state);
}

toggle_button toggle_buttons::edit(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::edit(), check_state, location);
}

toggle_button toggle_buttons::edit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::edit(), check_state, location, size);
}

toggle_button toggle_buttons::edit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::edit(), check_state, location, size, name);
}

toggle_button toggle_buttons::edit(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::edit(), three_state, check_state);
}

toggle_button toggle_buttons::edit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::edit(), three_state, check_state, location);
}

toggle_button toggle_buttons::edit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::edit(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::edit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::edit(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::edit(const control& parent) {
  return toggle_button::create(parent, system_texts::edit());
}

toggle_button toggle_buttons::edit(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::edit(), location);
}

toggle_button toggle_buttons::edit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::edit(), location, size);
}

toggle_button toggle_buttons::edit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::edit(), location, size, name);
}

toggle_button toggle_buttons::edit(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::edit(), check_state);
}

toggle_button toggle_buttons::edit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::edit(), check_state, location);
}

toggle_button toggle_buttons::edit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::edit(), check_state, location, size);
}

toggle_button toggle_buttons::edit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::edit(), check_state, location, size, name);
}

toggle_button toggle_buttons::edit(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::edit(), three_state, check_state);
}

toggle_button toggle_buttons::edit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::edit(), three_state, check_state, location);
}

toggle_button toggle_buttons::edit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::edit(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::edit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::edit(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::execute() {
  return toggle_button::create(system_texts::execute());
}

toggle_button toggle_buttons::execute(const drawing::point& location) {
  return toggle_button::create(system_texts::execute(), location);
}

toggle_button toggle_buttons::execute(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::execute(), location, size);
}

toggle_button toggle_buttons::execute(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::execute(), location, size, name);
}

toggle_button toggle_buttons::execute(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::execute(), check_state);
}

toggle_button toggle_buttons::execute(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::execute(), check_state, location);
}

toggle_button toggle_buttons::execute(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::execute(), check_state, location, size);
}

toggle_button toggle_buttons::execute(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::execute(), check_state, location, size, name);
}

toggle_button toggle_buttons::execute(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::execute(), three_state, check_state);
}

toggle_button toggle_buttons::execute(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::execute(), three_state, check_state, location);
}

toggle_button toggle_buttons::execute(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::execute(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::execute(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::execute(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::execute(const control& parent) {
  return toggle_button::create(parent, system_texts::execute());
}

toggle_button toggle_buttons::execute(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::execute(), location);
}

toggle_button toggle_buttons::execute(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::execute(), location, size);
}

toggle_button toggle_buttons::execute(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::execute(), location, size, name);
}

toggle_button toggle_buttons::execute(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::execute(), check_state);
}

toggle_button toggle_buttons::execute(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::execute(), check_state, location);
}

toggle_button toggle_buttons::execute(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::execute(), check_state, location, size);
}

toggle_button toggle_buttons::execute(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::execute(), check_state, location, size, name);
}

toggle_button toggle_buttons::execute(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::execute(), three_state, check_state);
}

toggle_button toggle_buttons::execute(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::execute(), three_state, check_state, location);
}

toggle_button toggle_buttons::execute(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::execute(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::execute(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::execute(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::exit() {
  return toggle_button::create(system_texts::exit());
}

toggle_button toggle_buttons::exit(const drawing::point& location) {
  return toggle_button::create(system_texts::exit(), location);
}

toggle_button toggle_buttons::exit(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::exit(), location, size);
}

toggle_button toggle_buttons::exit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::exit(), location, size, name);
}

toggle_button toggle_buttons::exit(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::exit(), check_state);
}

toggle_button toggle_buttons::exit(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::exit(), check_state, location);
}

toggle_button toggle_buttons::exit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::exit(), check_state, location, size);
}

toggle_button toggle_buttons::exit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::exit(), check_state, location, size, name);
}

toggle_button toggle_buttons::exit(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::exit(), three_state, check_state);
}

toggle_button toggle_buttons::exit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::exit(), three_state, check_state, location);
}

toggle_button toggle_buttons::exit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::exit(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::exit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::exit(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::exit(const control& parent) {
  return toggle_button::create(parent, system_texts::exit());
}

toggle_button toggle_buttons::exit(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::exit(), location);
}

toggle_button toggle_buttons::exit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::exit(), location, size);
}

toggle_button toggle_buttons::exit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::exit(), location, size, name);
}

toggle_button toggle_buttons::exit(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::exit(), check_state);
}

toggle_button toggle_buttons::exit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::exit(), check_state, location);
}

toggle_button toggle_buttons::exit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::exit(), check_state, location, size);
}

toggle_button toggle_buttons::exit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::exit(), check_state, location, size, name);
}

toggle_button toggle_buttons::exit(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::exit(), three_state, check_state);
}

toggle_button toggle_buttons::exit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::exit(), three_state, check_state, location);
}

toggle_button toggle_buttons::exit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::exit(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::exit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::exit(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::file() {
  return toggle_button::create(system_texts::file());
}

toggle_button toggle_buttons::file(const drawing::point& location) {
  return toggle_button::create(system_texts::file(), location);
}

toggle_button toggle_buttons::file(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::file(), location, size);
}

toggle_button toggle_buttons::file(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::file(), location, size, name);
}

toggle_button toggle_buttons::file(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::file(), check_state);
}

toggle_button toggle_buttons::file(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::file(), check_state, location);
}

toggle_button toggle_buttons::file(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::file(), check_state, location, size);
}

toggle_button toggle_buttons::file(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::file(), check_state, location, size, name);
}

toggle_button toggle_buttons::file(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::file(), three_state, check_state);
}

toggle_button toggle_buttons::file(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::file(), three_state, check_state, location);
}

toggle_button toggle_buttons::file(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::file(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::file(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::file(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::file(const control& parent) {
  return toggle_button::create(parent, system_texts::file());
}

toggle_button toggle_buttons::file(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::file(), location);
}

toggle_button toggle_buttons::file(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::file(), location, size);
}

toggle_button toggle_buttons::file(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::file(), location, size, name);
}

toggle_button toggle_buttons::file(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::file(), check_state);
}

toggle_button toggle_buttons::file(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::file(), check_state, location);
}

toggle_button toggle_buttons::file(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::file(), check_state, location, size);
}

toggle_button toggle_buttons::file(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::file(), check_state, location, size, name);
}

toggle_button toggle_buttons::file(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::file(), three_state, check_state);
}

toggle_button toggle_buttons::file(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::file(), three_state, check_state, location);
}

toggle_button toggle_buttons::file(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::file(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::file(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::file(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::find() {
  return toggle_button::create(system_texts::find());
}

toggle_button toggle_buttons::find(const drawing::point& location) {
  return toggle_button::create(system_texts::find(), location);
}

toggle_button toggle_buttons::find(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::find(), location, size);
}

toggle_button toggle_buttons::find(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::find(), location, size, name);
}

toggle_button toggle_buttons::find(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::find(), check_state);
}

toggle_button toggle_buttons::find(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::find(), check_state, location);
}

toggle_button toggle_buttons::find(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::find(), check_state, location, size);
}

toggle_button toggle_buttons::find(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::find(), check_state, location, size, name);
}

toggle_button toggle_buttons::find(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::find(), three_state, check_state);
}

toggle_button toggle_buttons::find(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::find(), three_state, check_state, location);
}

toggle_button toggle_buttons::find(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::find(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::find(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::find(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::find(const control& parent) {
  return toggle_button::create(parent, system_texts::find());
}

toggle_button toggle_buttons::find(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::find(), location);
}

toggle_button toggle_buttons::find(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::find(), location, size);
}

toggle_button toggle_buttons::find(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::find(), location, size, name);
}

toggle_button toggle_buttons::find(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::find(), check_state);
}

toggle_button toggle_buttons::find(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::find(), check_state, location);
}

toggle_button toggle_buttons::find(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::find(), check_state, location, size);
}

toggle_button toggle_buttons::find(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::find(), check_state, location, size, name);
}

toggle_button toggle_buttons::find(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::find(), three_state, check_state);
}

toggle_button toggle_buttons::find(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::find(), three_state, check_state, location);
}

toggle_button toggle_buttons::find(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::find(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::find(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::find(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::first() {
  return toggle_button::create(system_texts::first());
}

toggle_button toggle_buttons::first(const drawing::point& location) {
  return toggle_button::create(system_texts::first(), location);
}

toggle_button toggle_buttons::first(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::first(), location, size);
}

toggle_button toggle_buttons::first(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::first(), location, size, name);
}

toggle_button toggle_buttons::first(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::first(), check_state);
}

toggle_button toggle_buttons::first(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::first(), check_state, location);
}

toggle_button toggle_buttons::first(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::first(), check_state, location, size);
}

toggle_button toggle_buttons::first(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::first(), check_state, location, size, name);
}

toggle_button toggle_buttons::first(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::first(), three_state, check_state);
}

toggle_button toggle_buttons::first(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::first(), three_state, check_state, location);
}

toggle_button toggle_buttons::first(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::first(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::first(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::first(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::first(const control& parent) {
  return toggle_button::create(parent, system_texts::first());
}

toggle_button toggle_buttons::first(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::first(), location);
}

toggle_button toggle_buttons::first(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::first(), location, size);
}

toggle_button toggle_buttons::first(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::first(), location, size, name);
}

toggle_button toggle_buttons::first(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::first(), check_state);
}

toggle_button toggle_buttons::first(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::first(), check_state, location);
}

toggle_button toggle_buttons::first(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::first(), check_state, location, size);
}

toggle_button toggle_buttons::first(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::first(), check_state, location, size, name);
}

toggle_button toggle_buttons::first(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::first(), three_state, check_state);
}

toggle_button toggle_buttons::first(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::first(), three_state, check_state, location);
}

toggle_button toggle_buttons::first(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::first(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::first(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::first(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::floppy() {
  return toggle_button::create(system_texts::floppy());
}

toggle_button toggle_buttons::floppy(const drawing::point& location) {
  return toggle_button::create(system_texts::floppy(), location);
}

toggle_button toggle_buttons::floppy(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::floppy(), location, size);
}

toggle_button toggle_buttons::floppy(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::floppy(), location, size, name);
}

toggle_button toggle_buttons::floppy(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::floppy(), check_state);
}

toggle_button toggle_buttons::floppy(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::floppy(), check_state, location);
}

toggle_button toggle_buttons::floppy(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::floppy(), check_state, location, size);
}

toggle_button toggle_buttons::floppy(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::floppy(), check_state, location, size, name);
}

toggle_button toggle_buttons::floppy(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::floppy(), three_state, check_state);
}

toggle_button toggle_buttons::floppy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::floppy(), three_state, check_state, location);
}

toggle_button toggle_buttons::floppy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::floppy(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::floppy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::floppy(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::floppy(const control& parent) {
  return toggle_button::create(parent, system_texts::floppy());
}

toggle_button toggle_buttons::floppy(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::floppy(), location);
}

toggle_button toggle_buttons::floppy(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::floppy(), location, size);
}

toggle_button toggle_buttons::floppy(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::floppy(), location, size, name);
}

toggle_button toggle_buttons::floppy(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::floppy(), check_state);
}

toggle_button toggle_buttons::floppy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::floppy(), check_state, location);
}

toggle_button toggle_buttons::floppy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::floppy(), check_state, location, size);
}

toggle_button toggle_buttons::floppy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::floppy(), check_state, location, size, name);
}

toggle_button toggle_buttons::floppy(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::floppy(), three_state, check_state);
}

toggle_button toggle_buttons::floppy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::floppy(), three_state, check_state, location);
}

toggle_button toggle_buttons::floppy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::floppy(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::floppy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::floppy(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::font() {
  return toggle_button::create(system_texts::font());
}

toggle_button toggle_buttons::font(const drawing::point& location) {
  return toggle_button::create(system_texts::font(), location);
}

toggle_button toggle_buttons::font(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::font(), location, size);
}

toggle_button toggle_buttons::font(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::font(), location, size, name);
}

toggle_button toggle_buttons::font(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::font(), check_state);
}

toggle_button toggle_buttons::font(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::font(), check_state, location);
}

toggle_button toggle_buttons::font(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::font(), check_state, location, size);
}

toggle_button toggle_buttons::font(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::font(), check_state, location, size, name);
}

toggle_button toggle_buttons::font(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::font(), three_state, check_state);
}

toggle_button toggle_buttons::font(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::font(), three_state, check_state, location);
}

toggle_button toggle_buttons::font(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::font(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::font(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::font(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::font(const control& parent) {
  return toggle_button::create(parent, system_texts::font());
}

toggle_button toggle_buttons::font(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::font(), location);
}

toggle_button toggle_buttons::font(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::font(), location, size);
}

toggle_button toggle_buttons::font(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::font(), location, size, name);
}

toggle_button toggle_buttons::font(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::font(), check_state);
}

toggle_button toggle_buttons::font(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::font(), check_state, location);
}

toggle_button toggle_buttons::font(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::font(), check_state, location, size);
}

toggle_button toggle_buttons::font(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::font(), check_state, location, size, name);
}

toggle_button toggle_buttons::font(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::font(), three_state, check_state);
}

toggle_button toggle_buttons::font(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::font(), three_state, check_state, location);
}

toggle_button toggle_buttons::font(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::font(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::font(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::font(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::forward() {
  return toggle_button::create(system_texts::forward());
}

toggle_button toggle_buttons::forward(const drawing::point& location) {
  return toggle_button::create(system_texts::forward(), location);
}

toggle_button toggle_buttons::forward(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::forward(), location, size);
}

toggle_button toggle_buttons::forward(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::forward(), location, size, name);
}

toggle_button toggle_buttons::forward(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::forward(), check_state);
}

toggle_button toggle_buttons::forward(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::forward(), check_state, location);
}

toggle_button toggle_buttons::forward(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::forward(), check_state, location, size);
}

toggle_button toggle_buttons::forward(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::forward(), check_state, location, size, name);
}

toggle_button toggle_buttons::forward(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::forward(), three_state, check_state);
}

toggle_button toggle_buttons::forward(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::forward(), three_state, check_state, location);
}

toggle_button toggle_buttons::forward(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::forward(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::forward(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::forward(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::forward(const control& parent) {
  return toggle_button::create(parent, system_texts::forward());
}

toggle_button toggle_buttons::forward(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::forward(), location);
}

toggle_button toggle_buttons::forward(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::forward(), location, size);
}

toggle_button toggle_buttons::forward(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::forward(), location, size, name);
}

toggle_button toggle_buttons::forward(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::forward(), check_state);
}

toggle_button toggle_buttons::forward(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::forward(), check_state, location);
}

toggle_button toggle_buttons::forward(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::forward(), check_state, location, size);
}

toggle_button toggle_buttons::forward(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::forward(), check_state, location, size, name);
}

toggle_button toggle_buttons::forward(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::forward(), three_state, check_state);
}

toggle_button toggle_buttons::forward(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::forward(), three_state, check_state, location);
}

toggle_button toggle_buttons::forward(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::forward(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::forward(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::forward(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::harddisk() {
  return toggle_button::create(system_texts::harddisk());
}

toggle_button toggle_buttons::harddisk(const drawing::point& location) {
  return toggle_button::create(system_texts::harddisk(), location);
}

toggle_button toggle_buttons::harddisk(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::harddisk(), location, size);
}

toggle_button toggle_buttons::harddisk(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::harddisk(), location, size, name);
}

toggle_button toggle_buttons::harddisk(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::harddisk(), check_state);
}

toggle_button toggle_buttons::harddisk(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::harddisk(), check_state, location);
}

toggle_button toggle_buttons::harddisk(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::harddisk(), check_state, location, size);
}

toggle_button toggle_buttons::harddisk(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::harddisk(), check_state, location, size, name);
}

toggle_button toggle_buttons::harddisk(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::harddisk(), three_state, check_state);
}

toggle_button toggle_buttons::harddisk(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::harddisk(), three_state, check_state, location);
}

toggle_button toggle_buttons::harddisk(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::harddisk(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::harddisk(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::harddisk(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::harddisk(const control& parent) {
  return toggle_button::create(parent, system_texts::harddisk());
}

toggle_button toggle_buttons::harddisk(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::harddisk(), location);
}

toggle_button toggle_buttons::harddisk(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::harddisk(), location, size);
}

toggle_button toggle_buttons::harddisk(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::harddisk(), location, size, name);
}

toggle_button toggle_buttons::harddisk(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::harddisk(), check_state);
}

toggle_button toggle_buttons::harddisk(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::harddisk(), check_state, location);
}

toggle_button toggle_buttons::harddisk(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::harddisk(), check_state, location, size);
}

toggle_button toggle_buttons::harddisk(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::harddisk(), check_state, location, size, name);
}

toggle_button toggle_buttons::harddisk(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::harddisk(), three_state, check_state);
}

toggle_button toggle_buttons::harddisk(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::harddisk(), three_state, check_state, location);
}

toggle_button toggle_buttons::harddisk(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::harddisk(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::harddisk(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::harddisk(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::help() {
  return toggle_button::create(system_texts::help());
}

toggle_button toggle_buttons::help(const drawing::point& location) {
  return toggle_button::create(system_texts::help(), location);
}

toggle_button toggle_buttons::help(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::help(), location, size);
}

toggle_button toggle_buttons::help(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::help(), location, size, name);
}

toggle_button toggle_buttons::help(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::help(), check_state);
}

toggle_button toggle_buttons::help(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::help(), check_state, location);
}

toggle_button toggle_buttons::help(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::help(), check_state, location, size);
}

toggle_button toggle_buttons::help(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::help(), check_state, location, size, name);
}

toggle_button toggle_buttons::help(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::help(), three_state, check_state);
}

toggle_button toggle_buttons::help(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::help(), three_state, check_state, location);
}

toggle_button toggle_buttons::help(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::help(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::help(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::help(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::help(const control& parent) {
  return toggle_button::create(parent, system_texts::help());
}

toggle_button toggle_buttons::help(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::help(), location);
}

toggle_button toggle_buttons::help(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::help(), location, size);
}

toggle_button toggle_buttons::help(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::help(), location, size, name);
}

toggle_button toggle_buttons::help(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::help(), check_state);
}

toggle_button toggle_buttons::help(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::help(), check_state, location);
}

toggle_button toggle_buttons::help(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::help(), check_state, location, size);
}

toggle_button toggle_buttons::help(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::help(), check_state, location, size, name);
}

toggle_button toggle_buttons::help(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::help(), three_state, check_state);
}

toggle_button toggle_buttons::help(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::help(), three_state, check_state, location);
}

toggle_button toggle_buttons::help(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::help(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::help(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::help(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::home() {
  return toggle_button::create(system_texts::home());
}

toggle_button toggle_buttons::home(const drawing::point& location) {
  return toggle_button::create(system_texts::home(), location);
}

toggle_button toggle_buttons::home(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::home(), location, size);
}

toggle_button toggle_buttons::home(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::home(), location, size, name);
}

toggle_button toggle_buttons::home(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::home(), check_state);
}

toggle_button toggle_buttons::home(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::home(), check_state, location);
}

toggle_button toggle_buttons::home(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::home(), check_state, location, size);
}

toggle_button toggle_buttons::home(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::home(), check_state, location, size, name);
}

toggle_button toggle_buttons::home(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::home(), three_state, check_state);
}

toggle_button toggle_buttons::home(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::home(), three_state, check_state, location);
}

toggle_button toggle_buttons::home(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::home(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::home(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::home(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::home(const control& parent) {
  return toggle_button::create(parent, system_texts::home());
}

toggle_button toggle_buttons::home(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::home(), location);
}

toggle_button toggle_buttons::home(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::home(), location, size);
}

toggle_button toggle_buttons::home(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::home(), location, size, name);
}

toggle_button toggle_buttons::home(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::home(), check_state);
}

toggle_button toggle_buttons::home(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::home(), check_state, location);
}

toggle_button toggle_buttons::home(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::home(), check_state, location, size);
}

toggle_button toggle_buttons::home(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::home(), check_state, location, size, name);
}

toggle_button toggle_buttons::home(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::home(), three_state, check_state);
}

toggle_button toggle_buttons::home(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::home(), three_state, check_state, location);
}

toggle_button toggle_buttons::home(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::home(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::home(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::home(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::ignore() {
  return toggle_button::create(system_texts::ignore());
}

toggle_button toggle_buttons::ignore(const drawing::point& location) {
  return toggle_button::create(system_texts::ignore(), location);
}

toggle_button toggle_buttons::ignore(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::ignore(), location, size);
}

toggle_button toggle_buttons::ignore(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::ignore(), location, size, name);
}

toggle_button toggle_buttons::ignore(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::ignore(), check_state);
}

toggle_button toggle_buttons::ignore(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::ignore(), check_state, location);
}

toggle_button toggle_buttons::ignore(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::ignore(), check_state, location, size);
}

toggle_button toggle_buttons::ignore(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::ignore(), check_state, location, size, name);
}

toggle_button toggle_buttons::ignore(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::ignore(), three_state, check_state);
}

toggle_button toggle_buttons::ignore(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::ignore(), three_state, check_state, location);
}

toggle_button toggle_buttons::ignore(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::ignore(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::ignore(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::ignore(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::ignore(const control& parent) {
  return toggle_button::create(parent, system_texts::ignore());
}

toggle_button toggle_buttons::ignore(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::ignore(), location);
}

toggle_button toggle_buttons::ignore(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::ignore(), location, size);
}

toggle_button toggle_buttons::ignore(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::ignore(), location, size, name);
}

toggle_button toggle_buttons::ignore(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::ignore(), check_state);
}

toggle_button toggle_buttons::ignore(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::ignore(), check_state, location);
}

toggle_button toggle_buttons::ignore(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::ignore(), check_state, location, size);
}

toggle_button toggle_buttons::ignore(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::ignore(), check_state, location, size, name);
}

toggle_button toggle_buttons::ignore(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::ignore(), three_state, check_state);
}

toggle_button toggle_buttons::ignore(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::ignore(), three_state, check_state, location);
}

toggle_button toggle_buttons::ignore(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::ignore(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::ignore(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::ignore(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::indent() {
  return toggle_button::create(system_texts::indent());
}

toggle_button toggle_buttons::indent(const drawing::point& location) {
  return toggle_button::create(system_texts::indent(), location);
}

toggle_button toggle_buttons::indent(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::indent(), location, size);
}

toggle_button toggle_buttons::indent(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::indent(), location, size, name);
}

toggle_button toggle_buttons::indent(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::indent(), check_state);
}

toggle_button toggle_buttons::indent(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::indent(), check_state, location);
}

toggle_button toggle_buttons::indent(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::indent(), check_state, location, size);
}

toggle_button toggle_buttons::indent(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::indent(), check_state, location, size, name);
}

toggle_button toggle_buttons::indent(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::indent(), three_state, check_state);
}

toggle_button toggle_buttons::indent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::indent(), three_state, check_state, location);
}

toggle_button toggle_buttons::indent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::indent(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::indent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::indent(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::indent(const control& parent) {
  return toggle_button::create(parent, system_texts::indent());
}

toggle_button toggle_buttons::indent(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::indent(), location);
}

toggle_button toggle_buttons::indent(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::indent(), location, size);
}

toggle_button toggle_buttons::indent(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::indent(), location, size, name);
}

toggle_button toggle_buttons::indent(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::indent(), check_state);
}

toggle_button toggle_buttons::indent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::indent(), check_state, location);
}

toggle_button toggle_buttons::indent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::indent(), check_state, location, size);
}

toggle_button toggle_buttons::indent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::indent(), check_state, location, size, name);
}

toggle_button toggle_buttons::indent(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::indent(), three_state, check_state);
}

toggle_button toggle_buttons::indent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::indent(), three_state, check_state, location);
}

toggle_button toggle_buttons::indent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::indent(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::indent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::indent(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::index() {
  return toggle_button::create(system_texts::index());
}

toggle_button toggle_buttons::index(const drawing::point& location) {
  return toggle_button::create(system_texts::index(), location);
}

toggle_button toggle_buttons::index(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::index(), location, size);
}

toggle_button toggle_buttons::index(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::index(), location, size, name);
}

toggle_button toggle_buttons::index(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::index(), check_state);
}

toggle_button toggle_buttons::index(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::index(), check_state, location);
}

toggle_button toggle_buttons::index(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::index(), check_state, location, size);
}

toggle_button toggle_buttons::index(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::index(), check_state, location, size, name);
}

toggle_button toggle_buttons::index(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::index(), three_state, check_state);
}

toggle_button toggle_buttons::index(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::index(), three_state, check_state, location);
}

toggle_button toggle_buttons::index(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::index(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::index(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::index(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::index(const control& parent) {
  return toggle_button::create(parent, system_texts::index());
}

toggle_button toggle_buttons::index(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::index(), location);
}

toggle_button toggle_buttons::index(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::index(), location, size);
}

toggle_button toggle_buttons::index(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::index(), location, size, name);
}

toggle_button toggle_buttons::index(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::index(), check_state);
}

toggle_button toggle_buttons::index(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::index(), check_state, location);
}

toggle_button toggle_buttons::index(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::index(), check_state, location, size);
}

toggle_button toggle_buttons::index(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::index(), check_state, location, size, name);
}

toggle_button toggle_buttons::index(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::index(), three_state, check_state);
}

toggle_button toggle_buttons::index(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::index(), three_state, check_state, location);
}

toggle_button toggle_buttons::index(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::index(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::index(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::index(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::info() {
  return toggle_button::create(system_texts::info());
}

toggle_button toggle_buttons::info(const drawing::point& location) {
  return toggle_button::create(system_texts::info(), location);
}

toggle_button toggle_buttons::info(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::info(), location, size);
}

toggle_button toggle_buttons::info(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::info(), location, size, name);
}

toggle_button toggle_buttons::info(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::info(), check_state);
}

toggle_button toggle_buttons::info(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::info(), check_state, location);
}

toggle_button toggle_buttons::info(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::info(), check_state, location, size);
}

toggle_button toggle_buttons::info(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::info(), check_state, location, size, name);
}

toggle_button toggle_buttons::info(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::info(), three_state, check_state);
}

toggle_button toggle_buttons::info(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::info(), three_state, check_state, location);
}

toggle_button toggle_buttons::info(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::info(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::info(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::info(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::info(const control& parent) {
  return toggle_button::create(parent, system_texts::info());
}

toggle_button toggle_buttons::info(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::info(), location);
}

toggle_button toggle_buttons::info(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::info(), location, size);
}

toggle_button toggle_buttons::info(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::info(), location, size, name);
}

toggle_button toggle_buttons::info(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::info(), check_state);
}

toggle_button toggle_buttons::info(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::info(), check_state, location);
}

toggle_button toggle_buttons::info(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::info(), check_state, location, size);
}

toggle_button toggle_buttons::info(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::info(), check_state, location, size, name);
}

toggle_button toggle_buttons::info(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::info(), three_state, check_state);
}

toggle_button toggle_buttons::info(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::info(), three_state, check_state, location);
}

toggle_button toggle_buttons::info(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::info(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::info(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::info(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::italic() {
  return toggle_button::create(system_texts::italic());
}

toggle_button toggle_buttons::italic(const drawing::point& location) {
  return toggle_button::create(system_texts::italic(), location);
}

toggle_button toggle_buttons::italic(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::italic(), location, size);
}

toggle_button toggle_buttons::italic(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::italic(), location, size, name);
}

toggle_button toggle_buttons::italic(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::italic(), check_state);
}

toggle_button toggle_buttons::italic(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::italic(), check_state, location);
}

toggle_button toggle_buttons::italic(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::italic(), check_state, location, size);
}

toggle_button toggle_buttons::italic(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::italic(), check_state, location, size, name);
}

toggle_button toggle_buttons::italic(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::italic(), three_state, check_state);
}

toggle_button toggle_buttons::italic(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::italic(), three_state, check_state, location);
}

toggle_button toggle_buttons::italic(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::italic(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::italic(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::italic(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::italic(const control& parent) {
  return toggle_button::create(parent, system_texts::italic());
}

toggle_button toggle_buttons::italic(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::italic(), location);
}

toggle_button toggle_buttons::italic(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::italic(), location, size);
}

toggle_button toggle_buttons::italic(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::italic(), location, size, name);
}

toggle_button toggle_buttons::italic(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::italic(), check_state);
}

toggle_button toggle_buttons::italic(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::italic(), check_state, location);
}

toggle_button toggle_buttons::italic(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::italic(), check_state, location, size);
}

toggle_button toggle_buttons::italic(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::italic(), check_state, location, size, name);
}

toggle_button toggle_buttons::italic(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::italic(), three_state, check_state);
}

toggle_button toggle_buttons::italic(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::italic(), three_state, check_state, location);
}

toggle_button toggle_buttons::italic(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::italic(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::italic(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::italic(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::justified() {
  return toggle_button::create(system_texts::justified());
}

toggle_button toggle_buttons::justified(const drawing::point& location) {
  return toggle_button::create(system_texts::justified(), location);
}

toggle_button toggle_buttons::justified(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::justified(), location, size);
}

toggle_button toggle_buttons::justified(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::justified(), location, size, name);
}

toggle_button toggle_buttons::justified(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::justified(), check_state);
}

toggle_button toggle_buttons::justified(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::justified(), check_state, location);
}

toggle_button toggle_buttons::justified(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::justified(), check_state, location, size);
}

toggle_button toggle_buttons::justified(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::justified(), check_state, location, size, name);
}

toggle_button toggle_buttons::justified(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::justified(), three_state, check_state);
}

toggle_button toggle_buttons::justified(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::justified(), three_state, check_state, location);
}

toggle_button toggle_buttons::justified(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::justified(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::justified(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::justified(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::justified(const control& parent) {
  return toggle_button::create(parent, system_texts::justified());
}

toggle_button toggle_buttons::justified(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::justified(), location);
}

toggle_button toggle_buttons::justified(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::justified(), location, size);
}

toggle_button toggle_buttons::justified(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::justified(), location, size, name);
}

toggle_button toggle_buttons::justified(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::justified(), check_state);
}

toggle_button toggle_buttons::justified(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::justified(), check_state, location);
}

toggle_button toggle_buttons::justified(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::justified(), check_state, location, size);
}

toggle_button toggle_buttons::justified(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::justified(), check_state, location, size, name);
}

toggle_button toggle_buttons::justified(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::justified(), three_state, check_state);
}

toggle_button toggle_buttons::justified(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::justified(), three_state, check_state, location);
}

toggle_button toggle_buttons::justified(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::justified(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::justified(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::justified(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::jump_to() {
  return toggle_button::create(system_texts::jump_to());
}

toggle_button toggle_buttons::jump_to(const drawing::point& location) {
  return toggle_button::create(system_texts::jump_to(), location);
}

toggle_button toggle_buttons::jump_to(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::jump_to(), location, size);
}

toggle_button toggle_buttons::jump_to(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::jump_to(), location, size, name);
}

toggle_button toggle_buttons::jump_to(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::jump_to(), check_state);
}

toggle_button toggle_buttons::jump_to(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::jump_to(), check_state, location);
}

toggle_button toggle_buttons::jump_to(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::jump_to(), check_state, location, size);
}

toggle_button toggle_buttons::jump_to(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::jump_to(), check_state, location, size, name);
}

toggle_button toggle_buttons::jump_to(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::jump_to(), three_state, check_state);
}

toggle_button toggle_buttons::jump_to(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::jump_to(), three_state, check_state, location);
}

toggle_button toggle_buttons::jump_to(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::jump_to(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::jump_to(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::jump_to(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::jump_to(const control& parent) {
  return toggle_button::create(parent, system_texts::jump_to());
}

toggle_button toggle_buttons::jump_to(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::jump_to(), location);
}

toggle_button toggle_buttons::jump_to(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::jump_to(), location, size);
}

toggle_button toggle_buttons::jump_to(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::jump_to(), location, size, name);
}

toggle_button toggle_buttons::jump_to(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::jump_to(), check_state);
}

toggle_button toggle_buttons::jump_to(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::jump_to(), check_state, location);
}

toggle_button toggle_buttons::jump_to(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::jump_to(), check_state, location, size);
}

toggle_button toggle_buttons::jump_to(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::jump_to(), check_state, location, size, name);
}

toggle_button toggle_buttons::jump_to(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::jump_to(), three_state, check_state);
}

toggle_button toggle_buttons::jump_to(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::jump_to(), three_state, check_state, location);
}

toggle_button toggle_buttons::jump_to(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::jump_to(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::jump_to(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::jump_to(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::last() {
  return toggle_button::create(system_texts::last());
}

toggle_button toggle_buttons::last(const drawing::point& location) {
  return toggle_button::create(system_texts::last(), location);
}

toggle_button toggle_buttons::last(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::last(), location, size);
}

toggle_button toggle_buttons::last(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::last(), location, size, name);
}

toggle_button toggle_buttons::last(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::last(), check_state);
}

toggle_button toggle_buttons::last(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::last(), check_state, location);
}

toggle_button toggle_buttons::last(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::last(), check_state, location, size);
}

toggle_button toggle_buttons::last(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::last(), check_state, location, size, name);
}

toggle_button toggle_buttons::last(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::last(), three_state, check_state);
}

toggle_button toggle_buttons::last(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::last(), three_state, check_state, location);
}

toggle_button toggle_buttons::last(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::last(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::last(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::last(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::last(const control& parent) {
  return toggle_button::create(parent, system_texts::last());
}

toggle_button toggle_buttons::last(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::last(), location);
}

toggle_button toggle_buttons::last(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::last(), location, size);
}

toggle_button toggle_buttons::last(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::last(), location, size, name);
}

toggle_button toggle_buttons::last(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::last(), check_state);
}

toggle_button toggle_buttons::last(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::last(), check_state, location);
}

toggle_button toggle_buttons::last(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::last(), check_state, location, size);
}

toggle_button toggle_buttons::last(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::last(), check_state, location, size, name);
}

toggle_button toggle_buttons::last(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::last(), three_state, check_state);
}

toggle_button toggle_buttons::last(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::last(), three_state, check_state, location);
}

toggle_button toggle_buttons::last(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::last(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::last(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::last(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::network() {
  return toggle_button::create(system_texts::network());
}

toggle_button toggle_buttons::network(const drawing::point& location) {
  return toggle_button::create(system_texts::network(), location);
}

toggle_button toggle_buttons::network(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::network(), location, size);
}

toggle_button toggle_buttons::network(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::network(), location, size, name);
}

toggle_button toggle_buttons::network(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::network(), check_state);
}

toggle_button toggle_buttons::network(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::network(), check_state, location);
}

toggle_button toggle_buttons::network(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::network(), check_state, location, size);
}

toggle_button toggle_buttons::network(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::network(), check_state, location, size, name);
}

toggle_button toggle_buttons::network(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::network(), three_state, check_state);
}

toggle_button toggle_buttons::network(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::network(), three_state, check_state, location);
}

toggle_button toggle_buttons::network(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::network(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::network(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::network(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::network(const control& parent) {
  return toggle_button::create(parent, system_texts::network());
}

toggle_button toggle_buttons::network(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::network(), location);
}

toggle_button toggle_buttons::network(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::network(), location, size);
}

toggle_button toggle_buttons::network(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::network(), location, size, name);
}

toggle_button toggle_buttons::network(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::network(), check_state);
}

toggle_button toggle_buttons::network(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::network(), check_state, location);
}

toggle_button toggle_buttons::network(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::network(), check_state, location, size);
}

toggle_button toggle_buttons::network(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::network(), check_state, location, size, name);
}

toggle_button toggle_buttons::network(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::network(), three_state, check_state);
}

toggle_button toggle_buttons::network(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::network(), three_state, check_state, location);
}

toggle_button toggle_buttons::network(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::network(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::network(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::network(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::new_() {
  return toggle_button::create(system_texts::new_());
}

toggle_button toggle_buttons::new_(const drawing::point& location) {
  return toggle_button::create(system_texts::new_(), location);
}

toggle_button toggle_buttons::new_(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::new_(), location, size);
}

toggle_button toggle_buttons::new_(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::new_(), location, size, name);
}

toggle_button toggle_buttons::new_(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::new_(), check_state);
}

toggle_button toggle_buttons::new_(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::new_(), check_state, location);
}

toggle_button toggle_buttons::new_(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::new_(), check_state, location, size);
}

toggle_button toggle_buttons::new_(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::new_(), check_state, location, size, name);
}

toggle_button toggle_buttons::new_(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::new_(), three_state, check_state);
}

toggle_button toggle_buttons::new_(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::new_(), three_state, check_state, location);
}

toggle_button toggle_buttons::new_(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::new_(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::new_(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::new_(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::new_(const control& parent) {
  return toggle_button::create(parent, system_texts::new_());
}

toggle_button toggle_buttons::new_(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::new_(), location);
}

toggle_button toggle_buttons::new_(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::new_(), location, size);
}

toggle_button toggle_buttons::new_(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::new_(), location, size, name);
}

toggle_button toggle_buttons::new_(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::new_(), check_state);
}

toggle_button toggle_buttons::new_(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::new_(), check_state, location);
}

toggle_button toggle_buttons::new_(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::new_(), check_state, location, size);
}

toggle_button toggle_buttons::new_(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::new_(), check_state, location, size, name);
}

toggle_button toggle_buttons::new_(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::new_(), three_state, check_state);
}

toggle_button toggle_buttons::new_(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::new_(), three_state, check_state, location);
}

toggle_button toggle_buttons::new_(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::new_(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::new_(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::new_(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::next() {
  return toggle_button::create(system_texts::next());
}

toggle_button toggle_buttons::next(const drawing::point& location) {
  return toggle_button::create(system_texts::next(), location);
}

toggle_button toggle_buttons::next(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::next(), location, size);
}

toggle_button toggle_buttons::next(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::next(), location, size, name);
}

toggle_button toggle_buttons::next(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::next(), check_state);
}

toggle_button toggle_buttons::next(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::next(), check_state, location);
}

toggle_button toggle_buttons::next(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::next(), check_state, location, size);
}

toggle_button toggle_buttons::next(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::next(), check_state, location, size, name);
}

toggle_button toggle_buttons::next(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::next(), three_state, check_state);
}

toggle_button toggle_buttons::next(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::next(), three_state, check_state, location);
}

toggle_button toggle_buttons::next(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::next(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::next(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::next(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::next(const control& parent) {
  return toggle_button::create(parent, system_texts::next());
}

toggle_button toggle_buttons::next(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::next(), location);
}

toggle_button toggle_buttons::next(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::next(), location, size);
}

toggle_button toggle_buttons::next(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::next(), location, size, name);
}

toggle_button toggle_buttons::next(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::next(), check_state);
}

toggle_button toggle_buttons::next(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::next(), check_state, location);
}

toggle_button toggle_buttons::next(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::next(), check_state, location, size);
}

toggle_button toggle_buttons::next(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::next(), check_state, location, size, name);
}

toggle_button toggle_buttons::next(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::next(), three_state, check_state);
}

toggle_button toggle_buttons::next(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::next(), three_state, check_state, location);
}

toggle_button toggle_buttons::next(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::next(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::next(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::next(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::no() {
  return toggle_button::create(system_texts::no());
}

toggle_button toggle_buttons::no(const drawing::point& location) {
  return toggle_button::create(system_texts::no(), location);
}

toggle_button toggle_buttons::no(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::no(), location, size);
}

toggle_button toggle_buttons::no(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::no(), location, size, name);
}

toggle_button toggle_buttons::no(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::no(), check_state);
}

toggle_button toggle_buttons::no(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::no(), check_state, location);
}

toggle_button toggle_buttons::no(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::no(), check_state, location, size);
}

toggle_button toggle_buttons::no(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::no(), check_state, location, size, name);
}

toggle_button toggle_buttons::no(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::no(), three_state, check_state);
}

toggle_button toggle_buttons::no(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::no(), three_state, check_state, location);
}

toggle_button toggle_buttons::no(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::no(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::no(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::no(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::no(const control& parent) {
  return toggle_button::create(parent, system_texts::no());
}

toggle_button toggle_buttons::no(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::no(), location);
}

toggle_button toggle_buttons::no(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::no(), location, size);
}

toggle_button toggle_buttons::no(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::no(), location, size, name);
}

toggle_button toggle_buttons::no(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::no(), check_state);
}

toggle_button toggle_buttons::no(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::no(), check_state, location);
}

toggle_button toggle_buttons::no(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::no(), check_state, location, size);
}

toggle_button toggle_buttons::no(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::no(), check_state, location, size, name);
}

toggle_button toggle_buttons::no(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::no(), three_state, check_state);
}

toggle_button toggle_buttons::no(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::no(), three_state, check_state, location);
}

toggle_button toggle_buttons::no(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::no(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::no(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::no(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::ok() {
  return toggle_button::create(system_texts::ok());
}

toggle_button toggle_buttons::ok(const drawing::point& location) {
  return toggle_button::create(system_texts::ok(), location);
}

toggle_button toggle_buttons::ok(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::ok(), location, size);
}

toggle_button toggle_buttons::ok(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::ok(), location, size, name);
}

toggle_button toggle_buttons::ok(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::ok(), check_state);
}

toggle_button toggle_buttons::ok(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::ok(), check_state, location);
}

toggle_button toggle_buttons::ok(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::ok(), check_state, location, size);
}

toggle_button toggle_buttons::ok(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::ok(), check_state, location, size, name);
}

toggle_button toggle_buttons::ok(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::ok(), three_state, check_state);
}

toggle_button toggle_buttons::ok(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::ok(), three_state, check_state, location);
}

toggle_button toggle_buttons::ok(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::ok(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::ok(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::ok(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::ok(const control& parent) {
  return toggle_button::create(parent, system_texts::ok());
}

toggle_button toggle_buttons::ok(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::ok(), location);
}

toggle_button toggle_buttons::ok(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::ok(), location, size);
}

toggle_button toggle_buttons::ok(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::ok(), location, size, name);
}

toggle_button toggle_buttons::ok(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::ok(), check_state);
}

toggle_button toggle_buttons::ok(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::ok(), check_state, location);
}

toggle_button toggle_buttons::ok(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::ok(), check_state, location, size);
}

toggle_button toggle_buttons::ok(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::ok(), check_state, location, size, name);
}

toggle_button toggle_buttons::ok(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::ok(), three_state, check_state);
}

toggle_button toggle_buttons::ok(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::ok(), three_state, check_state, location);
}

toggle_button toggle_buttons::ok(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::ok(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::ok(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::ok(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::open() {
  return toggle_button::create(system_texts::open());
}

toggle_button toggle_buttons::open(const drawing::point& location) {
  return toggle_button::create(system_texts::open(), location);
}

toggle_button toggle_buttons::open(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::open(), location, size);
}

toggle_button toggle_buttons::open(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::open(), location, size, name);
}

toggle_button toggle_buttons::open(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::open(), check_state);
}

toggle_button toggle_buttons::open(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::open(), check_state, location);
}

toggle_button toggle_buttons::open(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::open(), check_state, location, size);
}

toggle_button toggle_buttons::open(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::open(), check_state, location, size, name);
}

toggle_button toggle_buttons::open(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::open(), three_state, check_state);
}

toggle_button toggle_buttons::open(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::open(), three_state, check_state, location);
}

toggle_button toggle_buttons::open(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::open(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::open(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::open(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::open(const control& parent) {
  return toggle_button::create(parent, system_texts::open());
}

toggle_button toggle_buttons::open(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::open(), location);
}

toggle_button toggle_buttons::open(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::open(), location, size);
}

toggle_button toggle_buttons::open(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::open(), location, size, name);
}

toggle_button toggle_buttons::open(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::open(), check_state);
}

toggle_button toggle_buttons::open(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::open(), check_state, location);
}

toggle_button toggle_buttons::open(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::open(), check_state, location, size);
}

toggle_button toggle_buttons::open(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::open(), check_state, location, size, name);
}

toggle_button toggle_buttons::open(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::open(), three_state, check_state);
}

toggle_button toggle_buttons::open(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::open(), three_state, check_state, location);
}

toggle_button toggle_buttons::open(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::open(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::open(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::open(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::options() {
  return toggle_button::create(system_texts::options());
}

toggle_button toggle_buttons::options(const drawing::point& location) {
  return toggle_button::create(system_texts::options(), location);
}

toggle_button toggle_buttons::options(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::options(), location, size);
}

toggle_button toggle_buttons::options(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::options(), location, size, name);
}

toggle_button toggle_buttons::options(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::options(), check_state);
}

toggle_button toggle_buttons::options(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::options(), check_state, location);
}

toggle_button toggle_buttons::options(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::options(), check_state, location, size);
}

toggle_button toggle_buttons::options(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::options(), check_state, location, size, name);
}

toggle_button toggle_buttons::options(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::options(), three_state, check_state);
}

toggle_button toggle_buttons::options(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::options(), three_state, check_state, location);
}

toggle_button toggle_buttons::options(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::options(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::options(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::options(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::options(const control& parent) {
  return toggle_button::create(parent, system_texts::options());
}

toggle_button toggle_buttons::options(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::options(), location);
}

toggle_button toggle_buttons::options(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::options(), location, size);
}

toggle_button toggle_buttons::options(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::options(), location, size, name);
}

toggle_button toggle_buttons::options(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::options(), check_state);
}

toggle_button toggle_buttons::options(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::options(), check_state, location);
}

toggle_button toggle_buttons::options(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::options(), check_state, location, size);
}

toggle_button toggle_buttons::options(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::options(), check_state, location, size, name);
}

toggle_button toggle_buttons::options(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::options(), three_state, check_state);
}

toggle_button toggle_buttons::options(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::options(), three_state, check_state, location);
}

toggle_button toggle_buttons::options(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::options(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::options(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::options(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::paste() {
  return toggle_button::create(system_texts::paste());
}

toggle_button toggle_buttons::paste(const drawing::point& location) {
  return toggle_button::create(system_texts::paste(), location);
}

toggle_button toggle_buttons::paste(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::paste(), location, size);
}

toggle_button toggle_buttons::paste(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::paste(), location, size, name);
}

toggle_button toggle_buttons::paste(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::paste(), check_state);
}

toggle_button toggle_buttons::paste(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::paste(), check_state, location);
}

toggle_button toggle_buttons::paste(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::paste(), check_state, location, size);
}

toggle_button toggle_buttons::paste(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::paste(), check_state, location, size, name);
}

toggle_button toggle_buttons::paste(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::paste(), three_state, check_state);
}

toggle_button toggle_buttons::paste(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::paste(), three_state, check_state, location);
}

toggle_button toggle_buttons::paste(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::paste(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::paste(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::paste(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::paste(const control& parent) {
  return toggle_button::create(parent, system_texts::paste());
}

toggle_button toggle_buttons::paste(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::paste(), location);
}

toggle_button toggle_buttons::paste(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::paste(), location, size);
}

toggle_button toggle_buttons::paste(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::paste(), location, size, name);
}

toggle_button toggle_buttons::paste(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::paste(), check_state);
}

toggle_button toggle_buttons::paste(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::paste(), check_state, location);
}

toggle_button toggle_buttons::paste(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::paste(), check_state, location, size);
}

toggle_button toggle_buttons::paste(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::paste(), check_state, location, size, name);
}

toggle_button toggle_buttons::paste(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::paste(), three_state, check_state);
}

toggle_button toggle_buttons::paste(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::paste(), three_state, check_state, location);
}

toggle_button toggle_buttons::paste(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::paste(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::paste(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::paste(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::preferences() {
  return toggle_button::create(system_texts::preferences());
}

toggle_button toggle_buttons::preferences(const drawing::point& location) {
  return toggle_button::create(system_texts::preferences(), location);
}

toggle_button toggle_buttons::preferences(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::preferences(), location, size);
}

toggle_button toggle_buttons::preferences(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::preferences(), location, size, name);
}

toggle_button toggle_buttons::preferences(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::preferences(), check_state);
}

toggle_button toggle_buttons::preferences(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::preferences(), check_state, location);
}

toggle_button toggle_buttons::preferences(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::preferences(), check_state, location, size);
}

toggle_button toggle_buttons::preferences(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::preferences(), check_state, location, size, name);
}

toggle_button toggle_buttons::preferences(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::preferences(), three_state, check_state);
}

toggle_button toggle_buttons::preferences(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::preferences(), three_state, check_state, location);
}

toggle_button toggle_buttons::preferences(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::preferences(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::preferences(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::preferences(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::preferences(const control& parent) {
  return toggle_button::create(parent, system_texts::preferences());
}

toggle_button toggle_buttons::preferences(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::preferences(), location);
}

toggle_button toggle_buttons::preferences(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::preferences(), location, size);
}

toggle_button toggle_buttons::preferences(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::preferences(), location, size, name);
}

toggle_button toggle_buttons::preferences(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::preferences(), check_state);
}

toggle_button toggle_buttons::preferences(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::preferences(), check_state, location);
}

toggle_button toggle_buttons::preferences(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::preferences(), check_state, location, size);
}

toggle_button toggle_buttons::preferences(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::preferences(), check_state, location, size, name);
}

toggle_button toggle_buttons::preferences(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::preferences(), three_state, check_state);
}

toggle_button toggle_buttons::preferences(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::preferences(), three_state, check_state, location);
}

toggle_button toggle_buttons::preferences(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::preferences(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::preferences(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::preferences(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::previous() {
  return toggle_button::create(system_texts::previous());
}

toggle_button toggle_buttons::previous(const drawing::point& location) {
  return toggle_button::create(system_texts::previous(), location);
}

toggle_button toggle_buttons::previous(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::previous(), location, size);
}

toggle_button toggle_buttons::previous(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::previous(), location, size, name);
}

toggle_button toggle_buttons::previous(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::previous(), check_state);
}

toggle_button toggle_buttons::previous(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::previous(), check_state, location);
}

toggle_button toggle_buttons::previous(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::previous(), check_state, location, size);
}

toggle_button toggle_buttons::previous(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::previous(), check_state, location, size, name);
}

toggle_button toggle_buttons::previous(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::previous(), three_state, check_state);
}

toggle_button toggle_buttons::previous(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::previous(), three_state, check_state, location);
}

toggle_button toggle_buttons::previous(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::previous(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::previous(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::previous(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::previous(const control& parent) {
  return toggle_button::create(parent, system_texts::previous());
}

toggle_button toggle_buttons::previous(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::previous(), location);
}

toggle_button toggle_buttons::previous(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::previous(), location, size);
}

toggle_button toggle_buttons::previous(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::previous(), location, size, name);
}

toggle_button toggle_buttons::previous(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::previous(), check_state);
}

toggle_button toggle_buttons::previous(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::previous(), check_state, location);
}

toggle_button toggle_buttons::previous(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::previous(), check_state, location, size);
}

toggle_button toggle_buttons::previous(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::previous(), check_state, location, size, name);
}

toggle_button toggle_buttons::previous(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::previous(), three_state, check_state);
}

toggle_button toggle_buttons::previous(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::previous(), three_state, check_state, location);
}

toggle_button toggle_buttons::previous(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::previous(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::previous(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::previous(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::print() {
  return toggle_button::create(system_texts::print());
}

toggle_button toggle_buttons::print(const drawing::point& location) {
  return toggle_button::create(system_texts::print(), location);
}

toggle_button toggle_buttons::print(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::print(), location, size);
}

toggle_button toggle_buttons::print(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::print(), location, size, name);
}

toggle_button toggle_buttons::print(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::print(), check_state);
}

toggle_button toggle_buttons::print(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::print(), check_state, location);
}

toggle_button toggle_buttons::print(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::print(), check_state, location, size);
}

toggle_button toggle_buttons::print(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::print(), check_state, location, size, name);
}

toggle_button toggle_buttons::print(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::print(), three_state, check_state);
}

toggle_button toggle_buttons::print(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::print(), three_state, check_state, location);
}

toggle_button toggle_buttons::print(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::print(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::print(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::print(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::print(const control& parent) {
  return toggle_button::create(parent, system_texts::print());
}

toggle_button toggle_buttons::print(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::print(), location);
}

toggle_button toggle_buttons::print(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::print(), location, size);
}

toggle_button toggle_buttons::print(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::print(), location, size, name);
}

toggle_button toggle_buttons::print(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::print(), check_state);
}

toggle_button toggle_buttons::print(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::print(), check_state, location);
}

toggle_button toggle_buttons::print(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::print(), check_state, location, size);
}

toggle_button toggle_buttons::print(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::print(), check_state, location, size, name);
}

toggle_button toggle_buttons::print(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::print(), three_state, check_state);
}

toggle_button toggle_buttons::print(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::print(), three_state, check_state, location);
}

toggle_button toggle_buttons::print(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::print(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::print(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::print(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::print_preview() {
  return toggle_button::create(system_texts::print_preview());
}

toggle_button toggle_buttons::print_preview(const drawing::point& location) {
  return toggle_button::create(system_texts::print_preview(), location);
}

toggle_button toggle_buttons::print_preview(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::print_preview(), location, size);
}

toggle_button toggle_buttons::print_preview(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::print_preview(), location, size, name);
}

toggle_button toggle_buttons::print_preview(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::print_preview(), check_state);
}

toggle_button toggle_buttons::print_preview(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::print_preview(), check_state, location);
}

toggle_button toggle_buttons::print_preview(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::print_preview(), check_state, location, size);
}

toggle_button toggle_buttons::print_preview(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::print_preview(), check_state, location, size, name);
}

toggle_button toggle_buttons::print_preview(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::print_preview(), three_state, check_state);
}

toggle_button toggle_buttons::print_preview(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::print_preview(), three_state, check_state, location);
}

toggle_button toggle_buttons::print_preview(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::print_preview(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::print_preview(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::print_preview(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::print_preview(const control& parent) {
  return toggle_button::create(parent, system_texts::print_preview());
}

toggle_button toggle_buttons::print_preview(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::print_preview(), location);
}

toggle_button toggle_buttons::print_preview(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::print_preview(), location, size);
}

toggle_button toggle_buttons::print_preview(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::print_preview(), location, size, name);
}

toggle_button toggle_buttons::print_preview(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::print_preview(), check_state);
}

toggle_button toggle_buttons::print_preview(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::print_preview(), check_state, location);
}

toggle_button toggle_buttons::print_preview(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::print_preview(), check_state, location, size);
}

toggle_button toggle_buttons::print_preview(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::print_preview(), check_state, location, size, name);
}

toggle_button toggle_buttons::print_preview(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::print_preview(), three_state, check_state);
}

toggle_button toggle_buttons::print_preview(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::print_preview(), three_state, check_state, location);
}

toggle_button toggle_buttons::print_preview(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::print_preview(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::print_preview(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::print_preview(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::properties() {
  return toggle_button::create(system_texts::properties());
}

toggle_button toggle_buttons::properties(const drawing::point& location) {
  return toggle_button::create(system_texts::properties(), location);
}

toggle_button toggle_buttons::properties(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::properties(), location, size);
}

toggle_button toggle_buttons::properties(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::properties(), location, size, name);
}

toggle_button toggle_buttons::properties(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::properties(), check_state);
}

toggle_button toggle_buttons::properties(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::properties(), check_state, location);
}

toggle_button toggle_buttons::properties(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::properties(), check_state, location, size);
}

toggle_button toggle_buttons::properties(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::properties(), check_state, location, size, name);
}

toggle_button toggle_buttons::properties(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::properties(), three_state, check_state);
}

toggle_button toggle_buttons::properties(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::properties(), three_state, check_state, location);
}

toggle_button toggle_buttons::properties(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::properties(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::properties(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::properties(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::properties(const control& parent) {
  return toggle_button::create(parent, system_texts::properties());
}

toggle_button toggle_buttons::properties(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::properties(), location);
}

toggle_button toggle_buttons::properties(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::properties(), location, size);
}

toggle_button toggle_buttons::properties(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::properties(), location, size, name);
}

toggle_button toggle_buttons::properties(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::properties(), check_state);
}

toggle_button toggle_buttons::properties(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::properties(), check_state, location);
}

toggle_button toggle_buttons::properties(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::properties(), check_state, location, size);
}

toggle_button toggle_buttons::properties(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::properties(), check_state, location, size, name);
}

toggle_button toggle_buttons::properties(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::properties(), three_state, check_state);
}

toggle_button toggle_buttons::properties(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::properties(), three_state, check_state, location);
}

toggle_button toggle_buttons::properties(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::properties(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::properties(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::properties(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::quit() {
  return toggle_button::create(system_texts::quit());
}

toggle_button toggle_buttons::quit(const drawing::point& location) {
  return toggle_button::create(system_texts::quit(), location);
}

toggle_button toggle_buttons::quit(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::quit(), location, size);
}

toggle_button toggle_buttons::quit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::quit(), location, size, name);
}

toggle_button toggle_buttons::quit(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::quit(), check_state);
}

toggle_button toggle_buttons::quit(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::quit(), check_state, location);
}

toggle_button toggle_buttons::quit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::quit(), check_state, location, size);
}

toggle_button toggle_buttons::quit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::quit(), check_state, location, size, name);
}

toggle_button toggle_buttons::quit(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::quit(), three_state, check_state);
}

toggle_button toggle_buttons::quit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::quit(), three_state, check_state, location);
}

toggle_button toggle_buttons::quit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::quit(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::quit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::quit(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::quit(const control& parent) {
  return toggle_button::create(parent, system_texts::quit());
}

toggle_button toggle_buttons::quit(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::quit(), location);
}

toggle_button toggle_buttons::quit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::quit(), location, size);
}

toggle_button toggle_buttons::quit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::quit(), location, size, name);
}

toggle_button toggle_buttons::quit(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::quit(), check_state);
}

toggle_button toggle_buttons::quit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::quit(), check_state, location);
}

toggle_button toggle_buttons::quit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::quit(), check_state, location, size);
}

toggle_button toggle_buttons::quit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::quit(), check_state, location, size, name);
}

toggle_button toggle_buttons::quit(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::quit(), three_state, check_state);
}

toggle_button toggle_buttons::quit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::quit(), three_state, check_state, location);
}

toggle_button toggle_buttons::quit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::quit(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::quit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::quit(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::redo() {
  return toggle_button::create(system_texts::redo());
}

toggle_button toggle_buttons::redo(const drawing::point& location) {
  return toggle_button::create(system_texts::redo(), location);
}

toggle_button toggle_buttons::redo(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::redo(), location, size);
}

toggle_button toggle_buttons::redo(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::redo(), location, size, name);
}

toggle_button toggle_buttons::redo(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::redo(), check_state);
}

toggle_button toggle_buttons::redo(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::redo(), check_state, location);
}

toggle_button toggle_buttons::redo(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::redo(), check_state, location, size);
}

toggle_button toggle_buttons::redo(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::redo(), check_state, location, size, name);
}

toggle_button toggle_buttons::redo(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::redo(), three_state, check_state);
}

toggle_button toggle_buttons::redo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::redo(), three_state, check_state, location);
}

toggle_button toggle_buttons::redo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::redo(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::redo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::redo(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::redo(const control& parent) {
  return toggle_button::create(parent, system_texts::redo());
}

toggle_button toggle_buttons::redo(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::redo(), location);
}

toggle_button toggle_buttons::redo(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::redo(), location, size);
}

toggle_button toggle_buttons::redo(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::redo(), location, size, name);
}

toggle_button toggle_buttons::redo(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::redo(), check_state);
}

toggle_button toggle_buttons::redo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::redo(), check_state, location);
}

toggle_button toggle_buttons::redo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::redo(), check_state, location, size);
}

toggle_button toggle_buttons::redo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::redo(), check_state, location, size, name);
}

toggle_button toggle_buttons::redo(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::redo(), three_state, check_state);
}

toggle_button toggle_buttons::redo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::redo(), three_state, check_state, location);
}

toggle_button toggle_buttons::redo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::redo(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::redo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::redo(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::refresh() {
  return toggle_button::create(system_texts::refresh());
}

toggle_button toggle_buttons::refresh(const drawing::point& location) {
  return toggle_button::create(system_texts::refresh(), location);
}

toggle_button toggle_buttons::refresh(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::refresh(), location, size);
}

toggle_button toggle_buttons::refresh(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::refresh(), location, size, name);
}

toggle_button toggle_buttons::refresh(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::refresh(), check_state);
}

toggle_button toggle_buttons::refresh(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::refresh(), check_state, location);
}

toggle_button toggle_buttons::refresh(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::refresh(), check_state, location, size);
}

toggle_button toggle_buttons::refresh(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::refresh(), check_state, location, size, name);
}

toggle_button toggle_buttons::refresh(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::refresh(), three_state, check_state);
}

toggle_button toggle_buttons::refresh(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::refresh(), three_state, check_state, location);
}

toggle_button toggle_buttons::refresh(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::refresh(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::refresh(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::refresh(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::refresh(const control& parent) {
  return toggle_button::create(parent, system_texts::refresh());
}

toggle_button toggle_buttons::refresh(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::refresh(), location);
}

toggle_button toggle_buttons::refresh(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::refresh(), location, size);
}

toggle_button toggle_buttons::refresh(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::refresh(), location, size, name);
}

toggle_button toggle_buttons::refresh(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::refresh(), check_state);
}

toggle_button toggle_buttons::refresh(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::refresh(), check_state, location);
}

toggle_button toggle_buttons::refresh(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::refresh(), check_state, location, size);
}

toggle_button toggle_buttons::refresh(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::refresh(), check_state, location, size, name);
}

toggle_button toggle_buttons::refresh(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::refresh(), three_state, check_state);
}

toggle_button toggle_buttons::refresh(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::refresh(), three_state, check_state, location);
}

toggle_button toggle_buttons::refresh(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::refresh(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::refresh(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::refresh(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::remove() {
  return toggle_button::create(system_texts::remove());
}

toggle_button toggle_buttons::remove(const drawing::point& location) {
  return toggle_button::create(system_texts::remove(), location);
}

toggle_button toggle_buttons::remove(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::remove(), location, size);
}

toggle_button toggle_buttons::remove(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::remove(), location, size, name);
}

toggle_button toggle_buttons::remove(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::remove(), check_state);
}

toggle_button toggle_buttons::remove(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::remove(), check_state, location);
}

toggle_button toggle_buttons::remove(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::remove(), check_state, location, size);
}

toggle_button toggle_buttons::remove(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::remove(), check_state, location, size, name);
}

toggle_button toggle_buttons::remove(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::remove(), three_state, check_state);
}

toggle_button toggle_buttons::remove(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::remove(), three_state, check_state, location);
}

toggle_button toggle_buttons::remove(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::remove(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::remove(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::remove(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::remove(const control& parent) {
  return toggle_button::create(parent, system_texts::remove());
}

toggle_button toggle_buttons::remove(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::remove(), location);
}

toggle_button toggle_buttons::remove(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::remove(), location, size);
}

toggle_button toggle_buttons::remove(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::remove(), location, size, name);
}

toggle_button toggle_buttons::remove(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::remove(), check_state);
}

toggle_button toggle_buttons::remove(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::remove(), check_state, location);
}

toggle_button toggle_buttons::remove(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::remove(), check_state, location, size);
}

toggle_button toggle_buttons::remove(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::remove(), check_state, location, size, name);
}

toggle_button toggle_buttons::remove(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::remove(), three_state, check_state);
}

toggle_button toggle_buttons::remove(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::remove(), three_state, check_state, location);
}

toggle_button toggle_buttons::remove(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::remove(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::remove(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::remove(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::replace() {
  return toggle_button::create(system_texts::replace());
}

toggle_button toggle_buttons::replace(const drawing::point& location) {
  return toggle_button::create(system_texts::replace(), location);
}

toggle_button toggle_buttons::replace(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::replace(), location, size);
}

toggle_button toggle_buttons::replace(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::replace(), location, size, name);
}

toggle_button toggle_buttons::replace(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::replace(), check_state);
}

toggle_button toggle_buttons::replace(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::replace(), check_state, location);
}

toggle_button toggle_buttons::replace(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::replace(), check_state, location, size);
}

toggle_button toggle_buttons::replace(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::replace(), check_state, location, size, name);
}

toggle_button toggle_buttons::replace(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::replace(), three_state, check_state);
}

toggle_button toggle_buttons::replace(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::replace(), three_state, check_state, location);
}

toggle_button toggle_buttons::replace(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::replace(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::replace(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::replace(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::replace(const control& parent) {
  return toggle_button::create(parent, system_texts::replace());
}

toggle_button toggle_buttons::replace(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::replace(), location);
}

toggle_button toggle_buttons::replace(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::replace(), location, size);
}

toggle_button toggle_buttons::replace(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::replace(), location, size, name);
}

toggle_button toggle_buttons::replace(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::replace(), check_state);
}

toggle_button toggle_buttons::replace(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::replace(), check_state, location);
}

toggle_button toggle_buttons::replace(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::replace(), check_state, location, size);
}

toggle_button toggle_buttons::replace(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::replace(), check_state, location, size, name);
}

toggle_button toggle_buttons::replace(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::replace(), three_state, check_state);
}

toggle_button toggle_buttons::replace(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::replace(), three_state, check_state, location);
}

toggle_button toggle_buttons::replace(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::replace(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::replace(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::replace(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::retry() {
  return toggle_button::create(system_texts::retry());
}

toggle_button toggle_buttons::retry(const drawing::point& location) {
  return toggle_button::create(system_texts::retry(), location);
}

toggle_button toggle_buttons::retry(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::retry(), location, size);
}

toggle_button toggle_buttons::retry(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::retry(), location, size, name);
}

toggle_button toggle_buttons::retry(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::retry(), check_state);
}

toggle_button toggle_buttons::retry(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::retry(), check_state, location);
}

toggle_button toggle_buttons::retry(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::retry(), check_state, location, size);
}

toggle_button toggle_buttons::retry(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::retry(), check_state, location, size, name);
}

toggle_button toggle_buttons::retry(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::retry(), three_state, check_state);
}

toggle_button toggle_buttons::retry(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::retry(), three_state, check_state, location);
}

toggle_button toggle_buttons::retry(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::retry(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::retry(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::retry(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::retry(const control& parent) {
  return toggle_button::create(parent, system_texts::retry());
}

toggle_button toggle_buttons::retry(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::retry(), location);
}

toggle_button toggle_buttons::retry(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::retry(), location, size);
}

toggle_button toggle_buttons::retry(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::retry(), location, size, name);
}

toggle_button toggle_buttons::retry(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::retry(), check_state);
}

toggle_button toggle_buttons::retry(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::retry(), check_state, location);
}

toggle_button toggle_buttons::retry(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::retry(), check_state, location, size);
}

toggle_button toggle_buttons::retry(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::retry(), check_state, location, size, name);
}

toggle_button toggle_buttons::retry(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::retry(), three_state, check_state);
}

toggle_button toggle_buttons::retry(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::retry(), three_state, check_state, location);
}

toggle_button toggle_buttons::retry(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::retry(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::retry(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::retry(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::revert_to_saved() {
  return toggle_button::create(system_texts::revert_to_saved());
}

toggle_button toggle_buttons::revert_to_saved(const drawing::point& location) {
  return toggle_button::create(system_texts::revert_to_saved(), location);
}

toggle_button toggle_buttons::revert_to_saved(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::revert_to_saved(), location, size);
}

toggle_button toggle_buttons::revert_to_saved(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::revert_to_saved(), location, size, name);
}

toggle_button toggle_buttons::revert_to_saved(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::revert_to_saved(), check_state);
}

toggle_button toggle_buttons::revert_to_saved(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::revert_to_saved(), check_state, location);
}

toggle_button toggle_buttons::revert_to_saved(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::revert_to_saved(), check_state, location, size);
}

toggle_button toggle_buttons::revert_to_saved(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::revert_to_saved(), check_state, location, size, name);
}

toggle_button toggle_buttons::revert_to_saved(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::revert_to_saved(), three_state, check_state);
}

toggle_button toggle_buttons::revert_to_saved(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::revert_to_saved(), three_state, check_state, location);
}

toggle_button toggle_buttons::revert_to_saved(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::revert_to_saved(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::revert_to_saved(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::revert_to_saved(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::revert_to_saved(const control& parent) {
  return toggle_button::create(parent, system_texts::revert_to_saved());
}

toggle_button toggle_buttons::revert_to_saved(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::revert_to_saved(), location);
}

toggle_button toggle_buttons::revert_to_saved(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::revert_to_saved(), location, size);
}

toggle_button toggle_buttons::revert_to_saved(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::revert_to_saved(), location, size, name);
}

toggle_button toggle_buttons::revert_to_saved(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::revert_to_saved(), check_state);
}

toggle_button toggle_buttons::revert_to_saved(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::revert_to_saved(), check_state, location);
}

toggle_button toggle_buttons::revert_to_saved(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::revert_to_saved(), check_state, location, size);
}

toggle_button toggle_buttons::revert_to_saved(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::revert_to_saved(), check_state, location, size, name);
}

toggle_button toggle_buttons::revert_to_saved(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::revert_to_saved(), three_state, check_state);
}

toggle_button toggle_buttons::revert_to_saved(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::revert_to_saved(), three_state, check_state, location);
}

toggle_button toggle_buttons::revert_to_saved(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::revert_to_saved(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::revert_to_saved(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::revert_to_saved(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::save() {
  return toggle_button::create(system_texts::save());
}

toggle_button toggle_buttons::save(const drawing::point& location) {
  return toggle_button::create(system_texts::save(), location);
}

toggle_button toggle_buttons::save(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::save(), location, size);
}

toggle_button toggle_buttons::save(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::save(), location, size, name);
}

toggle_button toggle_buttons::save(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::save(), check_state);
}

toggle_button toggle_buttons::save(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::save(), check_state, location);
}

toggle_button toggle_buttons::save(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::save(), check_state, location, size);
}

toggle_button toggle_buttons::save(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::save(), check_state, location, size, name);
}

toggle_button toggle_buttons::save(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::save(), three_state, check_state);
}

toggle_button toggle_buttons::save(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::save(), three_state, check_state, location);
}

toggle_button toggle_buttons::save(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::save(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::save(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::save(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::save(const control& parent) {
  return toggle_button::create(parent, system_texts::save());
}

toggle_button toggle_buttons::save(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::save(), location);
}

toggle_button toggle_buttons::save(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::save(), location, size);
}

toggle_button toggle_buttons::save(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::save(), location, size, name);
}

toggle_button toggle_buttons::save(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::save(), check_state);
}

toggle_button toggle_buttons::save(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::save(), check_state, location);
}

toggle_button toggle_buttons::save(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::save(), check_state, location, size);
}

toggle_button toggle_buttons::save(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::save(), check_state, location, size, name);
}

toggle_button toggle_buttons::save(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::save(), three_state, check_state);
}

toggle_button toggle_buttons::save(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::save(), three_state, check_state, location);
}

toggle_button toggle_buttons::save(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::save(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::save(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::save(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::save_as() {
  return toggle_button::create(system_texts::save_as());
}

toggle_button toggle_buttons::save_as(const drawing::point& location) {
  return toggle_button::create(system_texts::save_as(), location);
}

toggle_button toggle_buttons::save_as(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::save_as(), location, size);
}

toggle_button toggle_buttons::save_as(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::save_as(), location, size, name);
}

toggle_button toggle_buttons::save_as(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::save_as(), check_state);
}

toggle_button toggle_buttons::save_as(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::save_as(), check_state, location);
}

toggle_button toggle_buttons::save_as(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::save_as(), check_state, location, size);
}

toggle_button toggle_buttons::save_as(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::save_as(), check_state, location, size, name);
}

toggle_button toggle_buttons::save_as(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::save_as(), three_state, check_state);
}

toggle_button toggle_buttons::save_as(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::save_as(), three_state, check_state, location);
}

toggle_button toggle_buttons::save_as(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::save_as(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::save_as(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::save_as(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::save_as(const control& parent) {
  return toggle_button::create(parent, system_texts::save_as());
}

toggle_button toggle_buttons::save_as(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::save_as(), location);
}

toggle_button toggle_buttons::save_as(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::save_as(), location, size);
}

toggle_button toggle_buttons::save_as(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::save_as(), location, size, name);
}

toggle_button toggle_buttons::save_as(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::save_as(), check_state);
}

toggle_button toggle_buttons::save_as(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::save_as(), check_state, location);
}

toggle_button toggle_buttons::save_as(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::save_as(), check_state, location, size);
}

toggle_button toggle_buttons::save_as(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::save_as(), check_state, location, size, name);
}

toggle_button toggle_buttons::save_as(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::save_as(), three_state, check_state);
}

toggle_button toggle_buttons::save_as(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::save_as(), three_state, check_state, location);
}

toggle_button toggle_buttons::save_as(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::save_as(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::save_as(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::save_as(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::search() {
  return toggle_button::create(system_texts::search());
}

toggle_button toggle_buttons::search(const drawing::point& location) {
  return toggle_button::create(system_texts::search(), location);
}

toggle_button toggle_buttons::search(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::search(), location, size);
}

toggle_button toggle_buttons::search(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::search(), location, size, name);
}

toggle_button toggle_buttons::search(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::search(), check_state);
}

toggle_button toggle_buttons::search(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::search(), check_state, location);
}

toggle_button toggle_buttons::search(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::search(), check_state, location, size);
}

toggle_button toggle_buttons::search(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::search(), check_state, location, size, name);
}

toggle_button toggle_buttons::search(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::search(), three_state, check_state);
}

toggle_button toggle_buttons::search(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::search(), three_state, check_state, location);
}

toggle_button toggle_buttons::search(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::search(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::search(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::search(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::search(const control& parent) {
  return toggle_button::create(parent, system_texts::search());
}

toggle_button toggle_buttons::search(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::search(), location);
}

toggle_button toggle_buttons::search(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::search(), location, size);
}

toggle_button toggle_buttons::search(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::search(), location, size, name);
}

toggle_button toggle_buttons::search(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::search(), check_state);
}

toggle_button toggle_buttons::search(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::search(), check_state, location);
}

toggle_button toggle_buttons::search(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::search(), check_state, location, size);
}

toggle_button toggle_buttons::search(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::search(), check_state, location, size, name);
}

toggle_button toggle_buttons::search(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::search(), three_state, check_state);
}

toggle_button toggle_buttons::search(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::search(), three_state, check_state, location);
}

toggle_button toggle_buttons::search(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::search(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::search(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::search(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::select_all() {
  return toggle_button::create(system_texts::select_all());
}

toggle_button toggle_buttons::select_all(const drawing::point& location) {
  return toggle_button::create(system_texts::select_all(), location);
}

toggle_button toggle_buttons::select_all(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::select_all(), location, size);
}

toggle_button toggle_buttons::select_all(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::select_all(), location, size, name);
}

toggle_button toggle_buttons::select_all(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::select_all(), check_state);
}

toggle_button toggle_buttons::select_all(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::select_all(), check_state, location);
}

toggle_button toggle_buttons::select_all(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::select_all(), check_state, location, size);
}

toggle_button toggle_buttons::select_all(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::select_all(), check_state, location, size, name);
}

toggle_button toggle_buttons::select_all(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::select_all(), three_state, check_state);
}

toggle_button toggle_buttons::select_all(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::select_all(), three_state, check_state, location);
}

toggle_button toggle_buttons::select_all(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::select_all(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::select_all(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::select_all(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::select_all(const control& parent) {
  return toggle_button::create(parent, system_texts::select_all());
}

toggle_button toggle_buttons::select_all(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::select_all(), location);
}

toggle_button toggle_buttons::select_all(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::select_all(), location, size);
}

toggle_button toggle_buttons::select_all(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::select_all(), location, size, name);
}

toggle_button toggle_buttons::select_all(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::select_all(), check_state);
}

toggle_button toggle_buttons::select_all(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::select_all(), check_state, location);
}

toggle_button toggle_buttons::select_all(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::select_all(), check_state, location, size);
}

toggle_button toggle_buttons::select_all(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::select_all(), check_state, location, size, name);
}

toggle_button toggle_buttons::select_all(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::select_all(), three_state, check_state);
}

toggle_button toggle_buttons::select_all(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::select_all(), three_state, check_state, location);
}

toggle_button toggle_buttons::select_all(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::select_all(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::select_all(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::select_all(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::settings() {
  return toggle_button::create(system_texts::settings());
}

toggle_button toggle_buttons::settings(const drawing::point& location) {
  return toggle_button::create(system_texts::settings(), location);
}

toggle_button toggle_buttons::settings(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::settings(), location, size);
}

toggle_button toggle_buttons::settings(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::settings(), location, size, name);
}

toggle_button toggle_buttons::settings(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::settings(), check_state);
}

toggle_button toggle_buttons::settings(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::settings(), check_state, location);
}

toggle_button toggle_buttons::settings(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::settings(), check_state, location, size);
}

toggle_button toggle_buttons::settings(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::settings(), check_state, location, size, name);
}

toggle_button toggle_buttons::settings(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::settings(), three_state, check_state);
}

toggle_button toggle_buttons::settings(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::settings(), three_state, check_state, location);
}

toggle_button toggle_buttons::settings(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::settings(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::settings(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::settings(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::settings(const control& parent) {
  return toggle_button::create(parent, system_texts::settings());
}

toggle_button toggle_buttons::settings(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::settings(), location);
}

toggle_button toggle_buttons::settings(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::settings(), location, size);
}

toggle_button toggle_buttons::settings(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::settings(), location, size, name);
}

toggle_button toggle_buttons::settings(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::settings(), check_state);
}

toggle_button toggle_buttons::settings(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::settings(), check_state, location);
}

toggle_button toggle_buttons::settings(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::settings(), check_state, location, size);
}

toggle_button toggle_buttons::settings(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::settings(), check_state, location, size, name);
}

toggle_button toggle_buttons::settings(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::settings(), three_state, check_state);
}

toggle_button toggle_buttons::settings(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::settings(), three_state, check_state, location);
}

toggle_button toggle_buttons::settings(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::settings(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::settings(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::settings(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::spell_check() {
  return toggle_button::create(system_texts::spell_check());
}

toggle_button toggle_buttons::spell_check(const drawing::point& location) {
  return toggle_button::create(system_texts::spell_check(), location);
}

toggle_button toggle_buttons::spell_check(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::spell_check(), location, size);
}

toggle_button toggle_buttons::spell_check(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::spell_check(), location, size, name);
}

toggle_button toggle_buttons::spell_check(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::spell_check(), check_state);
}

toggle_button toggle_buttons::spell_check(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::spell_check(), check_state, location);
}

toggle_button toggle_buttons::spell_check(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::spell_check(), check_state, location, size);
}

toggle_button toggle_buttons::spell_check(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::spell_check(), check_state, location, size, name);
}

toggle_button toggle_buttons::spell_check(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::spell_check(), three_state, check_state);
}

toggle_button toggle_buttons::spell_check(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::spell_check(), three_state, check_state, location);
}

toggle_button toggle_buttons::spell_check(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::spell_check(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::spell_check(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::spell_check(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::spell_check(const control& parent) {
  return toggle_button::create(parent, system_texts::spell_check());
}

toggle_button toggle_buttons::spell_check(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::spell_check(), location);
}

toggle_button toggle_buttons::spell_check(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::spell_check(), location, size);
}

toggle_button toggle_buttons::spell_check(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::spell_check(), location, size, name);
}

toggle_button toggle_buttons::spell_check(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::spell_check(), check_state);
}

toggle_button toggle_buttons::spell_check(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::spell_check(), check_state, location);
}

toggle_button toggle_buttons::spell_check(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::spell_check(), check_state, location, size);
}

toggle_button toggle_buttons::spell_check(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::spell_check(), check_state, location, size, name);
}

toggle_button toggle_buttons::spell_check(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::spell_check(), three_state, check_state);
}

toggle_button toggle_buttons::spell_check(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::spell_check(), three_state, check_state, location);
}

toggle_button toggle_buttons::spell_check(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::spell_check(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::spell_check(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::spell_check(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::stop() {
  return toggle_button::create(system_texts::stop());
}

toggle_button toggle_buttons::stop(const drawing::point& location) {
  return toggle_button::create(system_texts::stop(), location);
}

toggle_button toggle_buttons::stop(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::stop(), location, size);
}

toggle_button toggle_buttons::stop(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::stop(), location, size, name);
}

toggle_button toggle_buttons::stop(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::stop(), check_state);
}

toggle_button toggle_buttons::stop(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::stop(), check_state, location);
}

toggle_button toggle_buttons::stop(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::stop(), check_state, location, size);
}

toggle_button toggle_buttons::stop(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::stop(), check_state, location, size, name);
}

toggle_button toggle_buttons::stop(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::stop(), three_state, check_state);
}

toggle_button toggle_buttons::stop(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::stop(), three_state, check_state, location);
}

toggle_button toggle_buttons::stop(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::stop(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::stop(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::stop(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::stop(const control& parent) {
  return toggle_button::create(parent, system_texts::stop());
}

toggle_button toggle_buttons::stop(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::stop(), location);
}

toggle_button toggle_buttons::stop(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::stop(), location, size);
}

toggle_button toggle_buttons::stop(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::stop(), location, size, name);
}

toggle_button toggle_buttons::stop(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::stop(), check_state);
}

toggle_button toggle_buttons::stop(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::stop(), check_state, location);
}

toggle_button toggle_buttons::stop(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::stop(), check_state, location, size);
}

toggle_button toggle_buttons::stop(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::stop(), check_state, location, size, name);
}

toggle_button toggle_buttons::stop(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::stop(), three_state, check_state);
}

toggle_button toggle_buttons::stop(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::stop(), three_state, check_state, location);
}

toggle_button toggle_buttons::stop(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::stop(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::stop(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::stop(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::strikeout() {
  return toggle_button::create(system_texts::strikeout());
}

toggle_button toggle_buttons::strikeout(const drawing::point& location) {
  return toggle_button::create(system_texts::strikeout(), location);
}

toggle_button toggle_buttons::strikeout(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::strikeout(), location, size);
}

toggle_button toggle_buttons::strikeout(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::strikeout(), location, size, name);
}

toggle_button toggle_buttons::strikeout(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::strikeout(), check_state);
}

toggle_button toggle_buttons::strikeout(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::strikeout(), check_state, location);
}

toggle_button toggle_buttons::strikeout(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::strikeout(), check_state, location, size);
}

toggle_button toggle_buttons::strikeout(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::strikeout(), check_state, location, size, name);
}

toggle_button toggle_buttons::strikeout(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::strikeout(), three_state, check_state);
}

toggle_button toggle_buttons::strikeout(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::strikeout(), three_state, check_state, location);
}

toggle_button toggle_buttons::strikeout(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::strikeout(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::strikeout(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::strikeout(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::strikeout(const control& parent) {
  return toggle_button::create(parent, system_texts::strikeout());
}

toggle_button toggle_buttons::strikeout(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::strikeout(), location);
}

toggle_button toggle_buttons::strikeout(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::strikeout(), location, size);
}

toggle_button toggle_buttons::strikeout(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::strikeout(), location, size, name);
}

toggle_button toggle_buttons::strikeout(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::strikeout(), check_state);
}

toggle_button toggle_buttons::strikeout(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::strikeout(), check_state, location);
}

toggle_button toggle_buttons::strikeout(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::strikeout(), check_state, location, size);
}

toggle_button toggle_buttons::strikeout(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::strikeout(), check_state, location, size, name);
}

toggle_button toggle_buttons::strikeout(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::strikeout(), three_state, check_state);
}

toggle_button toggle_buttons::strikeout(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::strikeout(), three_state, check_state, location);
}

toggle_button toggle_buttons::strikeout(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::strikeout(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::strikeout(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::strikeout(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::tools() {
  return toggle_button::create(system_texts::tools());
}

toggle_button toggle_buttons::tools(const drawing::point& location) {
  return toggle_button::create(system_texts::tools(), location);
}

toggle_button toggle_buttons::tools(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::tools(), location, size);
}

toggle_button toggle_buttons::tools(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::tools(), location, size, name);
}

toggle_button toggle_buttons::tools(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::tools(), check_state);
}

toggle_button toggle_buttons::tools(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::tools(), check_state, location);
}

toggle_button toggle_buttons::tools(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::tools(), check_state, location, size);
}

toggle_button toggle_buttons::tools(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::tools(), check_state, location, size, name);
}

toggle_button toggle_buttons::tools(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::tools(), three_state, check_state);
}

toggle_button toggle_buttons::tools(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::tools(), three_state, check_state, location);
}

toggle_button toggle_buttons::tools(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::tools(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::tools(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::tools(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::tools(const control& parent) {
  return toggle_button::create(parent, system_texts::tools());
}

toggle_button toggle_buttons::tools(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::tools(), location);
}

toggle_button toggle_buttons::tools(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::tools(), location, size);
}

toggle_button toggle_buttons::tools(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::tools(), location, size, name);
}

toggle_button toggle_buttons::tools(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::tools(), check_state);
}

toggle_button toggle_buttons::tools(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::tools(), check_state, location);
}

toggle_button toggle_buttons::tools(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::tools(), check_state, location, size);
}

toggle_button toggle_buttons::tools(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::tools(), check_state, location, size, name);
}

toggle_button toggle_buttons::tools(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::tools(), three_state, check_state);
}

toggle_button toggle_buttons::tools(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::tools(), three_state, check_state, location);
}

toggle_button toggle_buttons::tools(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::tools(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::tools(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::tools(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::top() {
  return toggle_button::create(system_texts::top());
}

toggle_button toggle_buttons::top(const drawing::point& location) {
  return toggle_button::create(system_texts::top(), location);
}

toggle_button toggle_buttons::top(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::top(), location, size);
}

toggle_button toggle_buttons::top(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::top(), location, size, name);
}

toggle_button toggle_buttons::top(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::top(), check_state);
}

toggle_button toggle_buttons::top(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::top(), check_state, location);
}

toggle_button toggle_buttons::top(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::top(), check_state, location, size);
}

toggle_button toggle_buttons::top(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::top(), check_state, location, size, name);
}

toggle_button toggle_buttons::top(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::top(), three_state, check_state);
}

toggle_button toggle_buttons::top(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::top(), three_state, check_state, location);
}

toggle_button toggle_buttons::top(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::top(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::top(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::top(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::top(const control& parent) {
  return toggle_button::create(parent, system_texts::top());
}

toggle_button toggle_buttons::top(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::top(), location);
}

toggle_button toggle_buttons::top(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::top(), location, size);
}

toggle_button toggle_buttons::top(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::top(), location, size, name);
}

toggle_button toggle_buttons::top(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::top(), check_state);
}

toggle_button toggle_buttons::top(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::top(), check_state, location);
}

toggle_button toggle_buttons::top(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::top(), check_state, location, size);
}

toggle_button toggle_buttons::top(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::top(), check_state, location, size, name);
}

toggle_button toggle_buttons::top(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::top(), three_state, check_state);
}

toggle_button toggle_buttons::top(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::top(), three_state, check_state, location);
}

toggle_button toggle_buttons::top(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::top(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::top(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::top(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::undelete() {
  return toggle_button::create(system_texts::undelete());
}

toggle_button toggle_buttons::undelete(const drawing::point& location) {
  return toggle_button::create(system_texts::undelete(), location);
}

toggle_button toggle_buttons::undelete(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::undelete(), location, size);
}

toggle_button toggle_buttons::undelete(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::undelete(), location, size, name);
}

toggle_button toggle_buttons::undelete(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::undelete(), check_state);
}

toggle_button toggle_buttons::undelete(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::undelete(), check_state, location);
}

toggle_button toggle_buttons::undelete(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::undelete(), check_state, location, size);
}

toggle_button toggle_buttons::undelete(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::undelete(), check_state, location, size, name);
}

toggle_button toggle_buttons::undelete(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::undelete(), three_state, check_state);
}

toggle_button toggle_buttons::undelete(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::undelete(), three_state, check_state, location);
}

toggle_button toggle_buttons::undelete(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::undelete(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::undelete(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::undelete(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::undelete(const control& parent) {
  return toggle_button::create(parent, system_texts::undelete());
}

toggle_button toggle_buttons::undelete(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::undelete(), location);
}

toggle_button toggle_buttons::undelete(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::undelete(), location, size);
}

toggle_button toggle_buttons::undelete(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::undelete(), location, size, name);
}

toggle_button toggle_buttons::undelete(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::undelete(), check_state);
}

toggle_button toggle_buttons::undelete(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::undelete(), check_state, location);
}

toggle_button toggle_buttons::undelete(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::undelete(), check_state, location, size);
}

toggle_button toggle_buttons::undelete(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::undelete(), check_state, location, size, name);
}

toggle_button toggle_buttons::undelete(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::undelete(), three_state, check_state);
}

toggle_button toggle_buttons::undelete(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::undelete(), three_state, check_state, location);
}

toggle_button toggle_buttons::undelete(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::undelete(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::undelete(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::undelete(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::underline() {
  return toggle_button::create(system_texts::underline());
}

toggle_button toggle_buttons::underline(const drawing::point& location) {
  return toggle_button::create(system_texts::underline(), location);
}

toggle_button toggle_buttons::underline(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::underline(), location, size);
}

toggle_button toggle_buttons::underline(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::underline(), location, size, name);
}

toggle_button toggle_buttons::underline(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::underline(), check_state);
}

toggle_button toggle_buttons::underline(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::underline(), check_state, location);
}

toggle_button toggle_buttons::underline(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::underline(), check_state, location, size);
}

toggle_button toggle_buttons::underline(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::underline(), check_state, location, size, name);
}

toggle_button toggle_buttons::underline(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::underline(), three_state, check_state);
}

toggle_button toggle_buttons::underline(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::underline(), three_state, check_state, location);
}

toggle_button toggle_buttons::underline(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::underline(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::underline(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::underline(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::underline(const control& parent) {
  return toggle_button::create(parent, system_texts::underline());
}

toggle_button toggle_buttons::underline(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::underline(), location);
}

toggle_button toggle_buttons::underline(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::underline(), location, size);
}

toggle_button toggle_buttons::underline(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::underline(), location, size, name);
}

toggle_button toggle_buttons::underline(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::underline(), check_state);
}

toggle_button toggle_buttons::underline(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::underline(), check_state, location);
}

toggle_button toggle_buttons::underline(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::underline(), check_state, location, size);
}

toggle_button toggle_buttons::underline(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::underline(), check_state, location, size, name);
}

toggle_button toggle_buttons::underline(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::underline(), three_state, check_state);
}

toggle_button toggle_buttons::underline(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::underline(), three_state, check_state, location);
}

toggle_button toggle_buttons::underline(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::underline(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::underline(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::underline(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::undo() {
  return toggle_button::create(system_texts::undo());
}

toggle_button toggle_buttons::undo(const drawing::point& location) {
  return toggle_button::create(system_texts::undo(), location);
}

toggle_button toggle_buttons::undo(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::undo(), location, size);
}

toggle_button toggle_buttons::undo(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::undo(), location, size, name);
}

toggle_button toggle_buttons::undo(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::undo(), check_state);
}

toggle_button toggle_buttons::undo(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::undo(), check_state, location);
}

toggle_button toggle_buttons::undo(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::undo(), check_state, location, size);
}

toggle_button toggle_buttons::undo(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::undo(), check_state, location, size, name);
}

toggle_button toggle_buttons::undo(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::undo(), three_state, check_state);
}

toggle_button toggle_buttons::undo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::undo(), three_state, check_state, location);
}

toggle_button toggle_buttons::undo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::undo(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::undo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::undo(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::undo(const control& parent) {
  return toggle_button::create(parent, system_texts::undo());
}

toggle_button toggle_buttons::undo(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::undo(), location);
}

toggle_button toggle_buttons::undo(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::undo(), location, size);
}

toggle_button toggle_buttons::undo(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::undo(), location, size, name);
}

toggle_button toggle_buttons::undo(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::undo(), check_state);
}

toggle_button toggle_buttons::undo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::undo(), check_state, location);
}

toggle_button toggle_buttons::undo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::undo(), check_state, location, size);
}

toggle_button toggle_buttons::undo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::undo(), check_state, location, size, name);
}

toggle_button toggle_buttons::undo(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::undo(), three_state, check_state);
}

toggle_button toggle_buttons::undo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::undo(), three_state, check_state, location);
}

toggle_button toggle_buttons::undo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::undo(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::undo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::undo(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::unindent() {
  return toggle_button::create(system_texts::unindent());
}

toggle_button toggle_buttons::unindent(const drawing::point& location) {
  return toggle_button::create(system_texts::unindent(), location);
}

toggle_button toggle_buttons::unindent(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::unindent(), location, size);
}

toggle_button toggle_buttons::unindent(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::unindent(), location, size, name);
}

toggle_button toggle_buttons::unindent(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::unindent(), check_state);
}

toggle_button toggle_buttons::unindent(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::unindent(), check_state, location);
}

toggle_button toggle_buttons::unindent(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::unindent(), check_state, location, size);
}

toggle_button toggle_buttons::unindent(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::unindent(), check_state, location, size, name);
}

toggle_button toggle_buttons::unindent(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::unindent(), three_state, check_state);
}

toggle_button toggle_buttons::unindent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::unindent(), three_state, check_state, location);
}

toggle_button toggle_buttons::unindent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::unindent(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::unindent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::unindent(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::unindent(const control& parent) {
  return toggle_button::create(parent, system_texts::unindent());
}

toggle_button toggle_buttons::unindent(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::unindent(), location);
}

toggle_button toggle_buttons::unindent(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::unindent(), location, size);
}

toggle_button toggle_buttons::unindent(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::unindent(), location, size, name);
}

toggle_button toggle_buttons::unindent(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::unindent(), check_state);
}

toggle_button toggle_buttons::unindent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::unindent(), check_state, location);
}

toggle_button toggle_buttons::unindent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::unindent(), check_state, location, size);
}

toggle_button toggle_buttons::unindent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::unindent(), check_state, location, size, name);
}

toggle_button toggle_buttons::unindent(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::unindent(), three_state, check_state);
}

toggle_button toggle_buttons::unindent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::unindent(), three_state, check_state, location);
}

toggle_button toggle_buttons::unindent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::unindent(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::unindent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::unindent(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::up() {
  return toggle_button::create(system_texts::up());
}

toggle_button toggle_buttons::up(const drawing::point& location) {
  return toggle_button::create(system_texts::up(), location);
}

toggle_button toggle_buttons::up(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::up(), location, size);
}

toggle_button toggle_buttons::up(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::up(), location, size, name);
}

toggle_button toggle_buttons::up(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::up(), check_state);
}

toggle_button toggle_buttons::up(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::up(), check_state, location);
}

toggle_button toggle_buttons::up(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::up(), check_state, location, size);
}

toggle_button toggle_buttons::up(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::up(), check_state, location, size, name);
}

toggle_button toggle_buttons::up(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::up(), three_state, check_state);
}

toggle_button toggle_buttons::up(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::up(), three_state, check_state, location);
}

toggle_button toggle_buttons::up(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::up(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::up(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::up(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::up(const control& parent) {
  return toggle_button::create(parent, system_texts::up());
}

toggle_button toggle_buttons::up(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::up(), location);
}

toggle_button toggle_buttons::up(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::up(), location, size);
}

toggle_button toggle_buttons::up(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::up(), location, size, name);
}

toggle_button toggle_buttons::up(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::up(), check_state);
}

toggle_button toggle_buttons::up(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::up(), check_state, location);
}

toggle_button toggle_buttons::up(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::up(), check_state, location, size);
}

toggle_button toggle_buttons::up(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::up(), check_state, location, size, name);
}

toggle_button toggle_buttons::up(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::up(), three_state, check_state);
}

toggle_button toggle_buttons::up(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::up(), three_state, check_state, location);
}

toggle_button toggle_buttons::up(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::up(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::up(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::up(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::view() {
  return toggle_button::create(system_texts::view());
}

toggle_button toggle_buttons::view(const drawing::point& location) {
  return toggle_button::create(system_texts::view(), location);
}

toggle_button toggle_buttons::view(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::view(), location, size);
}

toggle_button toggle_buttons::view(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::view(), location, size, name);
}

toggle_button toggle_buttons::view(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::view(), check_state);
}

toggle_button toggle_buttons::view(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::view(), check_state, location);
}

toggle_button toggle_buttons::view(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::view(), check_state, location, size);
}

toggle_button toggle_buttons::view(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::view(), check_state, location, size, name);
}

toggle_button toggle_buttons::view(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::view(), three_state, check_state);
}

toggle_button toggle_buttons::view(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::view(), three_state, check_state, location);
}

toggle_button toggle_buttons::view(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::view(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::view(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::view(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::view(const control& parent) {
  return toggle_button::create(parent, system_texts::view());
}

toggle_button toggle_buttons::view(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::view(), location);
}

toggle_button toggle_buttons::view(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::view(), location, size);
}

toggle_button toggle_buttons::view(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::view(), location, size, name);
}

toggle_button toggle_buttons::view(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::view(), check_state);
}

toggle_button toggle_buttons::view(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::view(), check_state, location);
}

toggle_button toggle_buttons::view(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::view(), check_state, location, size);
}

toggle_button toggle_buttons::view(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::view(), check_state, location, size, name);
}

toggle_button toggle_buttons::view(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::view(), three_state, check_state);
}

toggle_button toggle_buttons::view(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::view(), three_state, check_state, location);
}

toggle_button toggle_buttons::view(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::view(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::view(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::view(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::window() {
  return toggle_button::create(system_texts::window());
}

toggle_button toggle_buttons::window(const drawing::point& location) {
  return toggle_button::create(system_texts::window(), location);
}

toggle_button toggle_buttons::window(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::window(), location, size);
}

toggle_button toggle_buttons::window(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::window(), location, size, name);
}

toggle_button toggle_buttons::window(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::window(), check_state);
}

toggle_button toggle_buttons::window(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::window(), check_state, location);
}

toggle_button toggle_buttons::window(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::window(), check_state, location, size);
}

toggle_button toggle_buttons::window(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::window(), check_state, location, size, name);
}

toggle_button toggle_buttons::window(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::window(), three_state, check_state);
}

toggle_button toggle_buttons::window(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::window(), three_state, check_state, location);
}

toggle_button toggle_buttons::window(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::window(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::window(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::window(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::window(const control& parent) {
  return toggle_button::create(parent, system_texts::window());
}

toggle_button toggle_buttons::window(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::window(), location);
}

toggle_button toggle_buttons::window(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::window(), location, size);
}

toggle_button toggle_buttons::window(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::window(), location, size, name);
}

toggle_button toggle_buttons::window(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::window(), check_state);
}

toggle_button toggle_buttons::window(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::window(), check_state, location);
}

toggle_button toggle_buttons::window(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::window(), check_state, location, size);
}

toggle_button toggle_buttons::window(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::window(), check_state, location, size, name);
}

toggle_button toggle_buttons::window(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::window(), three_state, check_state);
}

toggle_button toggle_buttons::window(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::window(), three_state, check_state, location);
}

toggle_button toggle_buttons::window(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::window(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::window(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::window(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::yes() {
  return toggle_button::create(system_texts::yes());
}

toggle_button toggle_buttons::yes(const drawing::point& location) {
  return toggle_button::create(system_texts::yes(), location);
}

toggle_button toggle_buttons::yes(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::yes(), location, size);
}

toggle_button toggle_buttons::yes(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::yes(), location, size, name);
}

toggle_button toggle_buttons::yes(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::yes(), check_state);
}

toggle_button toggle_buttons::yes(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::yes(), check_state, location);
}

toggle_button toggle_buttons::yes(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::yes(), check_state, location, size);
}

toggle_button toggle_buttons::yes(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::yes(), check_state, location, size, name);
}

toggle_button toggle_buttons::yes(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::yes(), three_state, check_state);
}

toggle_button toggle_buttons::yes(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::yes(), three_state, check_state, location);
}

toggle_button toggle_buttons::yes(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::yes(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::yes(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::yes(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::yes(const control& parent) {
  return toggle_button::create(parent, system_texts::yes());
}

toggle_button toggle_buttons::yes(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::yes(), location);
}

toggle_button toggle_buttons::yes(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::yes(), location, size);
}

toggle_button toggle_buttons::yes(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::yes(), location, size, name);
}

toggle_button toggle_buttons::yes(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::yes(), check_state);
}

toggle_button toggle_buttons::yes(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::yes(), check_state, location);
}

toggle_button toggle_buttons::yes(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::yes(), check_state, location, size);
}

toggle_button toggle_buttons::yes(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::yes(), check_state, location, size, name);
}

toggle_button toggle_buttons::yes(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::yes(), three_state, check_state);
}

toggle_button toggle_buttons::yes(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::yes(), three_state, check_state, location);
}

toggle_button toggle_buttons::yes(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::yes(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::yes(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::yes(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_in() {
  return toggle_button::create(system_texts::zoom_in());
}

toggle_button toggle_buttons::zoom_in(const drawing::point& location) {
  return toggle_button::create(system_texts::zoom_in(), location);
}

toggle_button toggle_buttons::zoom_in(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::zoom_in(), location, size);
}

toggle_button toggle_buttons::zoom_in(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::zoom_in(), location, size, name);
}

toggle_button toggle_buttons::zoom_in(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::zoom_in(), check_state);
}

toggle_button toggle_buttons::zoom_in(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::zoom_in(), check_state, location);
}

toggle_button toggle_buttons::zoom_in(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::zoom_in(), check_state, location, size);
}

toggle_button toggle_buttons::zoom_in(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::zoom_in(), check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_in(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::zoom_in(), three_state, check_state);
}

toggle_button toggle_buttons::zoom_in(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::zoom_in(), three_state, check_state, location);
}

toggle_button toggle_buttons::zoom_in(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::zoom_in(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::zoom_in(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::zoom_in(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_in(const control& parent) {
  return toggle_button::create(parent, system_texts::zoom_in());
}

toggle_button toggle_buttons::zoom_in(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::zoom_in(), location);
}

toggle_button toggle_buttons::zoom_in(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::zoom_in(), location, size);
}

toggle_button toggle_buttons::zoom_in(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::zoom_in(), location, size, name);
}

toggle_button toggle_buttons::zoom_in(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::zoom_in(), check_state);
}

toggle_button toggle_buttons::zoom_in(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::zoom_in(), check_state, location);
}

toggle_button toggle_buttons::zoom_in(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::zoom_in(), check_state, location, size);
}

toggle_button toggle_buttons::zoom_in(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::zoom_in(), check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_in(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::zoom_in(), three_state, check_state);
}

toggle_button toggle_buttons::zoom_in(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::zoom_in(), three_state, check_state, location);
}

toggle_button toggle_buttons::zoom_in(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::zoom_in(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::zoom_in(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::zoom_in(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_out() {
  return toggle_button::create(system_texts::zoom_out());
}

toggle_button toggle_buttons::zoom_out(const drawing::point& location) {
  return toggle_button::create(system_texts::zoom_out(), location);
}

toggle_button toggle_buttons::zoom_out(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::zoom_out(), location, size);
}

toggle_button toggle_buttons::zoom_out(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::zoom_out(), location, size, name);
}

toggle_button toggle_buttons::zoom_out(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::zoom_out(), check_state);
}

toggle_button toggle_buttons::zoom_out(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::zoom_out(), check_state, location);
}

toggle_button toggle_buttons::zoom_out(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::zoom_out(), check_state, location, size);
}

toggle_button toggle_buttons::zoom_out(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::zoom_out(), check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_out(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::zoom_out(), three_state, check_state);
}

toggle_button toggle_buttons::zoom_out(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::zoom_out(), three_state, check_state, location);
}

toggle_button toggle_buttons::zoom_out(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::zoom_out(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::zoom_out(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::zoom_out(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_out(const control& parent) {
  return toggle_button::create(parent, system_texts::zoom_out());
}

toggle_button toggle_buttons::zoom_out(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::zoom_out(), location);
}

toggle_button toggle_buttons::zoom_out(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::zoom_out(), location, size);
}

toggle_button toggle_buttons::zoom_out(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::zoom_out(), location, size, name);
}

toggle_button toggle_buttons::zoom_out(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::zoom_out(), check_state);
}

toggle_button toggle_buttons::zoom_out(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::zoom_out(), check_state, location);
}

toggle_button toggle_buttons::zoom_out(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::zoom_out(), check_state, location, size);
}

toggle_button toggle_buttons::zoom_out(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::zoom_out(), check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_out(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::zoom_out(), three_state, check_state);
}

toggle_button toggle_buttons::zoom_out(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::zoom_out(), three_state, check_state, location);
}

toggle_button toggle_buttons::zoom_out(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::zoom_out(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::zoom_out(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::zoom_out(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_to_fit() {
  return toggle_button::create(system_texts::zoom_to_fit());
}

toggle_button toggle_buttons::zoom_to_fit(const drawing::point& location) {
  return toggle_button::create(system_texts::zoom_to_fit(), location);
}

toggle_button toggle_buttons::zoom_to_fit(const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::zoom_to_fit(), location, size);
}

toggle_button toggle_buttons::zoom_to_fit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::zoom_to_fit(), location, size, name);
}

toggle_button toggle_buttons::zoom_to_fit(xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::zoom_to_fit(), check_state);
}

toggle_button toggle_buttons::zoom_to_fit(xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::zoom_to_fit(), check_state, location);
}

toggle_button toggle_buttons::zoom_to_fit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::zoom_to_fit(), check_state, location, size);
}

toggle_button toggle_buttons::zoom_to_fit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::zoom_to_fit(), check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_to_fit(bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(system_texts::zoom_to_fit(), three_state, check_state);
}

toggle_button toggle_buttons::zoom_to_fit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(system_texts::zoom_to_fit(), three_state, check_state, location);
}

toggle_button toggle_buttons::zoom_to_fit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(system_texts::zoom_to_fit(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::zoom_to_fit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(system_texts::zoom_to_fit(), three_state, check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent) {
  return toggle_button::create(parent, system_texts::zoom_to_fit());
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::zoom_to_fit(), location);
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::zoom_to_fit(), location, size);
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::zoom_to_fit(), location, size, name);
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::zoom_to_fit(), check_state);
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::zoom_to_fit(), check_state, location);
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::zoom_to_fit(), check_state, location, size);
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::zoom_to_fit(), check_state, location, size, name);
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return toggle_button::create(parent, system_texts::zoom_to_fit(), three_state, check_state);
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return toggle_button::create(parent, system_texts::zoom_to_fit(), three_state, check_state, location);
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return toggle_button::create(parent, system_texts::zoom_to_fit(), three_state, check_state, location, size);
}

toggle_button toggle_buttons::zoom_to_fit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return toggle_button::create(parent, system_texts::zoom_to_fit(), three_state, check_state, location, size, name);
}
