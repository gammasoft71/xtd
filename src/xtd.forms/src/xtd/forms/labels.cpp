#include "../../../include/xtd/forms/labels.h"
#include "../../../include/xtd/forms/system_texts.h"

using namespace xtd;
using namespace xtd::forms;

label labels::abort() {
  return label::create(system_texts::abort());
}

label labels::abort(const drawing::point& location) {
  return label::create(system_texts::abort(), location);
}

label labels::abort(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::abort(), location, size);
}

label labels::abort(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::abort(), location, size, name);
}

label labels::abort(const control& parent) {
  return label::create(parent, system_texts::abort());
}

label labels::abort(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::abort(), location);
}

label labels::abort(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::abort(), location, size);
}

label labels::abort(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::abort(), location, size, name);
}

label labels::about() {
  return label::create(system_texts::about());
}

label labels::about(const drawing::point& location) {
  return label::create(system_texts::about(), location);
}

label labels::about(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::about(), location, size);
}

label labels::about(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::about(), location, size, name);
}

label labels::about(const control& parent) {
  return label::create(parent, system_texts::about());
}

label labels::about(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::about(), location);
}

label labels::about(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::about(), location, size);
}

label labels::about(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::about(), location, size, name);
}

label labels::actual_size() {
  return label::create(system_texts::actual_size());
}

label labels::actual_size(const drawing::point& location) {
  return label::create(system_texts::actual_size(), location);
}

label labels::actual_size(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::actual_size(), location, size);
}

label labels::actual_size(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::actual_size(), location, size, name);
}

label labels::actual_size(const control& parent) {
  return label::create(parent, system_texts::actual_size());
}

label labels::actual_size(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::actual_size(), location);
}

label labels::actual_size(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::actual_size(), location, size);
}

label labels::actual_size(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::actual_size(), location, size, name);
}

label labels::add() {
  return label::create(system_texts::add());
}

label labels::add(const drawing::point& location) {
  return label::create(system_texts::add(), location);
}

label labels::add(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::add(), location, size);
}

label labels::add(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::add(), location, size, name);
}

label labels::add(const control& parent) {
  return label::create(parent, system_texts::add());
}

label labels::add(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::add(), location);
}

label labels::add(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::add(), location, size);
}

label labels::add(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::add(), location, size, name);
}

label labels::alignment() {
  return label::create(system_texts::alignment());
}

label labels::alignment(const drawing::point& location) {
  return label::create(system_texts::alignment(), location);
}

label labels::alignment(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::alignment(), location, size);
}

label labels::alignment(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::alignment(), location, size, name);
}

label labels::alignment(const control& parent) {
  return label::create(parent, system_texts::alignment());
}

label labels::alignment(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::alignment(), location);
}

label labels::alignment(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::alignment(), location, size);
}

label labels::alignment(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::alignment(), location, size, name);
}

label labels::align_left() {
  return label::create(system_texts::align_left());
}

label labels::align_left(const drawing::point& location) {
  return label::create(system_texts::align_left(), location);
}

label labels::align_left(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::align_left(), location, size);
}

label labels::align_left(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::align_left(), location, size, name);
}

label labels::align_left(const control& parent) {
  return label::create(parent, system_texts::align_left());
}

label labels::align_left(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::align_left(), location);
}

label labels::align_left(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::align_left(), location, size);
}

label labels::align_left(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::align_left(), location, size, name);
}

label labels::align_right() {
  return label::create(system_texts::align_right());
}

label labels::align_right(const drawing::point& location) {
  return label::create(system_texts::align_right(), location);
}

label labels::align_right(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::align_right(), location, size);
}

label labels::align_right(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::align_right(), location, size, name);
}

label labels::align_right(const control& parent) {
  return label::create(parent, system_texts::align_right());
}

label labels::align_right(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::align_right(), location);
}

label labels::align_right(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::align_right(), location, size);
}

label labels::align_right(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::align_right(), location, size, name);
}

label labels::apply() {
  return label::create(system_texts::apply());
}

label labels::apply(const drawing::point& location) {
  return label::create(system_texts::apply(), location);
}

label labels::apply(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::apply(), location, size);
}

label labels::apply(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::apply(), location, size, name);
}

label labels::apply(const control& parent) {
  return label::create(parent, system_texts::apply());
}

label labels::apply(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::apply(), location);
}

label labels::apply(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::apply(), location, size);
}

label labels::apply(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::apply(), location, size, name);
}

label labels::ascending() {
  return label::create(system_texts::ascending());
}

label labels::ascending(const drawing::point& location) {
  return label::create(system_texts::ascending(), location);
}

label labels::ascending(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::ascending(), location, size);
}

label labels::ascending(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::ascending(), location, size, name);
}

label labels::ascending(const control& parent) {
  return label::create(parent, system_texts::ascending());
}

label labels::ascending(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::ascending(), location);
}

label labels::ascending(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::ascending(), location, size);
}

label labels::ascending(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::ascending(), location, size, name);
}

label labels::back() {
  return label::create(system_texts::back());
}

label labels::back(const drawing::point& location) {
  return label::create(system_texts::back(), location);
}

label labels::back(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::back(), location, size);
}

label labels::back(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::back(), location, size, name);
}

label labels::back(const control& parent) {
  return label::create(parent, system_texts::back());
}

label labels::back(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::back(), location);
}

label labels::back(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::back(), location, size);
}

label labels::back(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::back(), location, size, name);
}

label labels::bold() {
  return label::create(system_texts::bold());
}

label labels::bold(const drawing::point& location) {
  return label::create(system_texts::bold(), location);
}

label labels::bold(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::bold(), location, size);
}

label labels::bold(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::bold(), location, size, name);
}

label labels::bold(const control& parent) {
  return label::create(parent, system_texts::bold());
}

label labels::bold(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::bold(), location);
}

label labels::bold(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::bold(), location, size);
}

label labels::bold(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::bold(), location, size, name);
}

label labels::bottom() {
  return label::create(system_texts::bottom());
}

label labels::bottom(const drawing::point& location) {
  return label::create(system_texts::bottom(), location);
}

label labels::bottom(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::bottom(), location, size);
}

label labels::bottom(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::bottom(), location, size, name);
}

label labels::bottom(const control& parent) {
  return label::create(parent, system_texts::bottom());
}

label labels::bottom(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::bottom(), location);
}

label labels::bottom(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::bottom(), location, size);
}

label labels::bottom(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::bottom(), location, size, name);
}

label labels::cancel() {
  return label::create(system_texts::cancel());
}

label labels::cancel(const drawing::point& location) {
  return label::create(system_texts::cancel(), location);
}

label labels::cancel(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::cancel(), location, size);
}

label labels::cancel(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::cancel(), location, size, name);
}

label labels::cancel(const control& parent) {
  return label::create(parent, system_texts::cancel());
}

label labels::cancel(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::cancel(), location);
}

label labels::cancel(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::cancel(), location, size);
}

label labels::cancel(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::cancel(), location, size, name);
}

label labels::cdrom() {
  return label::create(system_texts::cdrom());
}

label labels::cdrom(const drawing::point& location) {
  return label::create(system_texts::cdrom(), location);
}

label labels::cdrom(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::cdrom(), location, size);
}

label labels::cdrom(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::cdrom(), location, size, name);
}

label labels::cdrom(const control& parent) {
  return label::create(parent, system_texts::cdrom());
}

label labels::cdrom(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::cdrom(), location);
}

label labels::cdrom(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::cdrom(), location, size);
}

label labels::cdrom(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::cdrom(), location, size, name);
}

label labels::centered() {
  return label::create(system_texts::centered());
}

label labels::centered(const drawing::point& location) {
  return label::create(system_texts::centered(), location);
}

label labels::centered(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::centered(), location, size);
}

label labels::centered(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::centered(), location, size, name);
}

label labels::centered(const control& parent) {
  return label::create(parent, system_texts::centered());
}

label labels::centered(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::centered(), location);
}

label labels::centered(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::centered(), location, size);
}

label labels::centered(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::centered(), location, size, name);
}

label labels::clear() {
  return label::create(system_texts::clear());
}

label labels::clear(const drawing::point& location) {
  return label::create(system_texts::clear(), location);
}

label labels::clear(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::clear(), location, size);
}

label labels::clear(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::clear(), location, size, name);
}

label labels::clear(const control& parent) {
  return label::create(parent, system_texts::clear());
}

label labels::clear(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::clear(), location);
}

label labels::clear(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::clear(), location, size);
}

label labels::clear(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::clear(), location, size, name);
}

label labels::close() {
  return label::create(system_texts::close());
}

label labels::close(const drawing::point& location) {
  return label::create(system_texts::close(), location);
}

label labels::close(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::close(), location, size);
}

label labels::close(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::close(), location, size, name);
}

label labels::close(const control& parent) {
  return label::create(parent, system_texts::close());
}

label labels::close(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::close(), location);
}

label labels::close(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::close(), location, size);
}

label labels::close(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::close(), location, size, name);
}

label labels::color() {
  return label::create(system_texts::color());
}

label labels::color(const drawing::point& location) {
  return label::create(system_texts::color(), location);
}

label labels::color(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::color(), location, size);
}

label labels::color(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::color(), location, size, name);
}

label labels::color(const control& parent) {
  return label::create(parent, system_texts::color());
}

label labels::color(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::color(), location);
}

label labels::color(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::color(), location, size);
}

label labels::color(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::color(), location, size, name);
}

label labels::contents() {
  return label::create(system_texts::contents());
}

label labels::contents(const drawing::point& location) {
  return label::create(system_texts::contents(), location);
}

label labels::contents(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::contents(), location, size);
}

label labels::contents(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::contents(), location, size, name);
}

label labels::contents(const control& parent) {
  return label::create(parent, system_texts::contents());
}

label labels::contents(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::contents(), location);
}

label labels::contents(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::contents(), location, size);
}

label labels::contents(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::contents(), location, size, name);
}

label labels::convert() {
  return label::create(system_texts::convert());
}

label labels::convert(const drawing::point& location) {
  return label::create(system_texts::convert(), location);
}

label labels::convert(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::convert(), location, size);
}

label labels::convert(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::convert(), location, size, name);
}

label labels::convert(const control& parent) {
  return label::create(parent, system_texts::convert());
}

label labels::convert(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::convert(), location);
}

label labels::convert(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::convert(), location, size);
}

label labels::convert(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::convert(), location, size, name);
}

label labels::copy() {
  return label::create(system_texts::copy());
}

label labels::copy(const drawing::point& location) {
  return label::create(system_texts::copy(), location);
}

label labels::copy(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::copy(), location, size);
}

label labels::copy(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::copy(), location, size, name);
}

label labels::copy(const control& parent) {
  return label::create(parent, system_texts::copy());
}

label labels::copy(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::copy(), location);
}

label labels::copy(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::copy(), location, size);
}

label labels::copy(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::copy(), location, size, name);
}

label labels::customize() {
  return label::create(system_texts::customize());
}

label labels::customize(const drawing::point& location) {
  return label::create(system_texts::customize(), location);
}

label labels::customize(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::customize(), location, size);
}

label labels::customize(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::customize(), location, size, name);
}

label labels::customize(const control& parent) {
  return label::create(parent, system_texts::customize());
}

label labels::customize(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::customize(), location);
}

label labels::customize(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::customize(), location, size);
}

label labels::customize(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::customize(), location, size, name);
}

label labels::cut() {
  return label::create(system_texts::cut());
}

label labels::cut(const drawing::point& location) {
  return label::create(system_texts::cut(), location);
}

label labels::cut(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::cut(), location, size);
}

label labels::cut(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::cut(), location, size, name);
}

label labels::cut(const control& parent) {
  return label::create(parent, system_texts::cut());
}

label labels::cut(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::cut(), location);
}

label labels::cut(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::cut(), location, size);
}

label labels::cut(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::cut(), location, size, name);
}

label labels::del() {
  return label::create(system_texts::del());
}

label labels::del(const drawing::point& location) {
  return label::create(system_texts::del(), location);
}

label labels::del(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::del(), location, size);
}

label labels::del(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::del(), location, size, name);
}

label labels::del(const control& parent) {
  return label::create(parent, system_texts::del());
}

label labels::del(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::del(), location);
}

label labels::del(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::del(), location, size);
}

label labels::del(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::del(), location, size, name);
}

label labels::descending() {
  return label::create(system_texts::descending());
}

label labels::descending(const drawing::point& location) {
  return label::create(system_texts::descending(), location);
}

label labels::descending(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::descending(), location, size);
}

label labels::descending(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::descending(), location, size, name);
}

label labels::descending(const control& parent) {
  return label::create(parent, system_texts::descending());
}

label labels::descending(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::descending(), location);
}

label labels::descending(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::descending(), location, size);
}

label labels::descending(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::descending(), location, size, name);
}

label labels::down() {
  return label::create(system_texts::down());
}

label labels::down(const drawing::point& location) {
  return label::create(system_texts::down(), location);
}

label labels::down(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::down(), location, size);
}

label labels::down(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::down(), location, size, name);
}

label labels::down(const control& parent) {
  return label::create(parent, system_texts::down());
}

label labels::down(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::down(), location);
}

label labels::down(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::down(), location, size);
}

label labels::down(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::down(), location, size, name);
}

label labels::edit() {
  return label::create(system_texts::edit());
}

label labels::edit(const drawing::point& location) {
  return label::create(system_texts::edit(), location);
}

label labels::edit(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::edit(), location, size);
}

label labels::edit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::edit(), location, size, name);
}

label labels::edit(const control& parent) {
  return label::create(parent, system_texts::edit());
}

label labels::edit(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::edit(), location);
}

label labels::edit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::edit(), location, size);
}

label labels::edit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::edit(), location, size, name);
}

label labels::execute() {
  return label::create(system_texts::execute());
}

label labels::execute(const drawing::point& location) {
  return label::create(system_texts::execute(), location);
}

label labels::execute(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::execute(), location, size);
}

label labels::execute(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::execute(), location, size, name);
}

label labels::execute(const control& parent) {
  return label::create(parent, system_texts::execute());
}

label labels::execute(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::execute(), location);
}

label labels::execute(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::execute(), location, size);
}

label labels::execute(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::execute(), location, size, name);
}

label labels::exit() {
  return label::create(system_texts::exit());
}

label labels::exit(const drawing::point& location) {
  return label::create(system_texts::exit(), location);
}

label labels::exit(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::exit(), location, size);
}

label labels::exit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::exit(), location, size, name);
}

label labels::exit(const control& parent) {
  return label::create(parent, system_texts::exit());
}

label labels::exit(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::exit(), location);
}

label labels::exit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::exit(), location, size);
}

label labels::exit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::exit(), location, size, name);
}

label labels::file() {
  return label::create(system_texts::file());
}

label labels::file(const drawing::point& location) {
  return label::create(system_texts::file(), location);
}

label labels::file(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::file(), location, size);
}

label labels::file(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::file(), location, size, name);
}

label labels::file(const control& parent) {
  return label::create(parent, system_texts::file());
}

label labels::file(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::file(), location);
}

label labels::file(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::file(), location, size);
}

label labels::file(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::file(), location, size, name);
}

label labels::find() {
  return label::create(system_texts::find());
}

label labels::find(const drawing::point& location) {
  return label::create(system_texts::find(), location);
}

label labels::find(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::find(), location, size);
}

label labels::find(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::find(), location, size, name);
}

label labels::find(const control& parent) {
  return label::create(parent, system_texts::find());
}

label labels::find(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::find(), location);
}

label labels::find(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::find(), location, size);
}

label labels::find(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::find(), location, size, name);
}

label labels::first() {
  return label::create(system_texts::first());
}

label labels::first(const drawing::point& location) {
  return label::create(system_texts::first(), location);
}

label labels::first(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::first(), location, size);
}

label labels::first(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::first(), location, size, name);
}

label labels::first(const control& parent) {
  return label::create(parent, system_texts::first());
}

label labels::first(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::first(), location);
}

label labels::first(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::first(), location, size);
}

label labels::first(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::first(), location, size, name);
}

label labels::floppy() {
  return label::create(system_texts::floppy());
}

label labels::floppy(const drawing::point& location) {
  return label::create(system_texts::floppy(), location);
}

label labels::floppy(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::floppy(), location, size);
}

label labels::floppy(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::floppy(), location, size, name);
}

label labels::floppy(const control& parent) {
  return label::create(parent, system_texts::floppy());
}

label labels::floppy(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::floppy(), location);
}

label labels::floppy(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::floppy(), location, size);
}

label labels::floppy(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::floppy(), location, size, name);
}

label labels::font() {
  return label::create(system_texts::font());
}

label labels::font(const drawing::point& location) {
  return label::create(system_texts::font(), location);
}

label labels::font(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::font(), location, size);
}

label labels::font(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::font(), location, size, name);
}

label labels::font(const control& parent) {
  return label::create(parent, system_texts::font());
}

label labels::font(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::font(), location);
}

label labels::font(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::font(), location, size);
}

label labels::font(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::font(), location, size, name);
}

label labels::forward() {
  return label::create(system_texts::forward());
}

label labels::forward(const drawing::point& location) {
  return label::create(system_texts::forward(), location);
}

label labels::forward(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::forward(), location, size);
}

label labels::forward(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::forward(), location, size, name);
}

label labels::forward(const control& parent) {
  return label::create(parent, system_texts::forward());
}

label labels::forward(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::forward(), location);
}

label labels::forward(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::forward(), location, size);
}

label labels::forward(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::forward(), location, size, name);
}

label labels::harddisk() {
  return label::create(system_texts::harddisk());
}

label labels::harddisk(const drawing::point& location) {
  return label::create(system_texts::harddisk(), location);
}

label labels::harddisk(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::harddisk(), location, size);
}

label labels::harddisk(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::harddisk(), location, size, name);
}

label labels::harddisk(const control& parent) {
  return label::create(parent, system_texts::harddisk());
}

label labels::harddisk(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::harddisk(), location);
}

label labels::harddisk(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::harddisk(), location, size);
}

label labels::harddisk(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::harddisk(), location, size, name);
}

label labels::help() {
  return label::create(system_texts::help());
}

label labels::help(const drawing::point& location) {
  return label::create(system_texts::help(), location);
}

label labels::help(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::help(), location, size);
}

label labels::help(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::help(), location, size, name);
}

label labels::help(const control& parent) {
  return label::create(parent, system_texts::help());
}

label labels::help(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::help(), location);
}

label labels::help(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::help(), location, size);
}

label labels::help(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::help(), location, size, name);
}

label labels::home() {
  return label::create(system_texts::home());
}

label labels::home(const drawing::point& location) {
  return label::create(system_texts::home(), location);
}

label labels::home(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::home(), location, size);
}

label labels::home(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::home(), location, size, name);
}

label labels::home(const control& parent) {
  return label::create(parent, system_texts::home());
}

label labels::home(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::home(), location);
}

label labels::home(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::home(), location, size);
}

label labels::home(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::home(), location, size, name);
}

label labels::ignore() {
  return label::create(system_texts::ignore());
}

label labels::ignore(const drawing::point& location) {
  return label::create(system_texts::ignore(), location);
}

label labels::ignore(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::ignore(), location, size);
}

label labels::ignore(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::ignore(), location, size, name);
}

label labels::ignore(const control& parent) {
  return label::create(parent, system_texts::ignore());
}

label labels::ignore(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::ignore(), location);
}

label labels::ignore(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::ignore(), location, size);
}

label labels::ignore(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::ignore(), location, size, name);
}

label labels::indent() {
  return label::create(system_texts::indent());
}

label labels::indent(const drawing::point& location) {
  return label::create(system_texts::indent(), location);
}

label labels::indent(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::indent(), location, size);
}

label labels::indent(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::indent(), location, size, name);
}

label labels::indent(const control& parent) {
  return label::create(parent, system_texts::indent());
}

label labels::indent(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::indent(), location);
}

label labels::indent(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::indent(), location, size);
}

label labels::indent(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::indent(), location, size, name);
}

label labels::index() {
  return label::create(system_texts::index());
}

label labels::index(const drawing::point& location) {
  return label::create(system_texts::index(), location);
}

label labels::index(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::index(), location, size);
}

label labels::index(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::index(), location, size, name);
}

label labels::index(const control& parent) {
  return label::create(parent, system_texts::index());
}

label labels::index(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::index(), location);
}

label labels::index(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::index(), location, size);
}

label labels::index(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::index(), location, size, name);
}

label labels::info() {
  return label::create(system_texts::info());
}

label labels::info(const drawing::point& location) {
  return label::create(system_texts::info(), location);
}

label labels::info(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::info(), location, size);
}

label labels::info(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::info(), location, size, name);
}

label labels::info(const control& parent) {
  return label::create(parent, system_texts::info());
}

label labels::info(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::info(), location);
}

label labels::info(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::info(), location, size);
}

label labels::info(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::info(), location, size, name);
}

label labels::italic() {
  return label::create(system_texts::italic());
}

label labels::italic(const drawing::point& location) {
  return label::create(system_texts::italic(), location);
}

label labels::italic(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::italic(), location, size);
}

label labels::italic(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::italic(), location, size, name);
}

label labels::italic(const control& parent) {
  return label::create(parent, system_texts::italic());
}

label labels::italic(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::italic(), location);
}

label labels::italic(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::italic(), location, size);
}

label labels::italic(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::italic(), location, size, name);
}

label labels::justified() {
  return label::create(system_texts::justified());
}

label labels::justified(const drawing::point& location) {
  return label::create(system_texts::justified(), location);
}

label labels::justified(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::justified(), location, size);
}

label labels::justified(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::justified(), location, size, name);
}

label labels::justified(const control& parent) {
  return label::create(parent, system_texts::justified());
}

label labels::justified(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::justified(), location);
}

label labels::justified(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::justified(), location, size);
}

label labels::justified(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::justified(), location, size, name);
}

label labels::jump_to() {
  return label::create(system_texts::jump_to());
}

label labels::jump_to(const drawing::point& location) {
  return label::create(system_texts::jump_to(), location);
}

label labels::jump_to(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::jump_to(), location, size);
}

label labels::jump_to(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::jump_to(), location, size, name);
}

label labels::jump_to(const control& parent) {
  return label::create(parent, system_texts::jump_to());
}

label labels::jump_to(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::jump_to(), location);
}

label labels::jump_to(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::jump_to(), location, size);
}

label labels::jump_to(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::jump_to(), location, size, name);
}

label labels::last() {
  return label::create(system_texts::last());
}

label labels::last(const drawing::point& location) {
  return label::create(system_texts::last(), location);
}

label labels::last(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::last(), location, size);
}

label labels::last(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::last(), location, size, name);
}

label labels::last(const control& parent) {
  return label::create(parent, system_texts::last());
}

label labels::last(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::last(), location);
}

label labels::last(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::last(), location, size);
}

label labels::last(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::last(), location, size, name);
}

label labels::network() {
  return label::create(system_texts::network());
}

label labels::network(const drawing::point& location) {
  return label::create(system_texts::network(), location);
}

label labels::network(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::network(), location, size);
}

label labels::network(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::network(), location, size, name);
}

label labels::network(const control& parent) {
  return label::create(parent, system_texts::network());
}

label labels::network(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::network(), location);
}

label labels::network(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::network(), location, size);
}

label labels::network(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::network(), location, size, name);
}

label labels::new_() {
  return label::create(system_texts::new_());
}

label labels::new_(const drawing::point& location) {
  return label::create(system_texts::new_(), location);
}

label labels::new_(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::new_(), location, size);
}

label labels::new_(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::new_(), location, size, name);
}

label labels::new_(const control& parent) {
  return label::create(parent, system_texts::new_());
}

label labels::new_(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::new_(), location);
}

label labels::new_(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::new_(), location, size);
}

label labels::new_(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::new_(), location, size, name);
}

label labels::next() {
  return label::create(system_texts::next());
}

label labels::next(const drawing::point& location) {
  return label::create(system_texts::next(), location);
}

label labels::next(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::next(), location, size);
}

label labels::next(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::next(), location, size, name);
}

label labels::next(const control& parent) {
  return label::create(parent, system_texts::next());
}

label labels::next(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::next(), location);
}

label labels::next(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::next(), location, size);
}

label labels::next(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::next(), location, size, name);
}

label labels::no() {
  return label::create(system_texts::no());
}

label labels::no(const drawing::point& location) {
  return label::create(system_texts::no(), location);
}

label labels::no(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::no(), location, size);
}

label labels::no(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::no(), location, size, name);
}

label labels::no(const control& parent) {
  return label::create(parent, system_texts::no());
}

label labels::no(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::no(), location);
}

label labels::no(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::no(), location, size);
}

label labels::no(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::no(), location, size, name);
}

label labels::ok() {
  return label::create(system_texts::ok());
}

label labels::ok(const drawing::point& location) {
  return label::create(system_texts::ok(), location);
}

label labels::ok(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::ok(), location, size);
}

label labels::ok(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::ok(), location, size, name);
}

label labels::ok(const control& parent) {
  return label::create(parent, system_texts::ok());
}

label labels::ok(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::ok(), location);
}

label labels::ok(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::ok(), location, size);
}

label labels::ok(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::ok(), location, size, name);
}

label labels::open() {
  return label::create(system_texts::open());
}

label labels::open(const drawing::point& location) {
  return label::create(system_texts::open(), location);
}

label labels::open(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::open(), location, size);
}

label labels::open(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::open(), location, size, name);
}

label labels::open(const control& parent) {
  return label::create(parent, system_texts::open());
}

label labels::open(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::open(), location);
}

label labels::open(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::open(), location, size);
}

label labels::open(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::open(), location, size, name);
}

label labels::options() {
  return label::create(system_texts::options());
}

label labels::options(const drawing::point& location) {
  return label::create(system_texts::options(), location);
}

label labels::options(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::options(), location, size);
}

label labels::options(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::options(), location, size, name);
}

label labels::options(const control& parent) {
  return label::create(parent, system_texts::options());
}

label labels::options(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::options(), location);
}

label labels::options(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::options(), location, size);
}

label labels::options(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::options(), location, size, name);
}

label labels::paste() {
  return label::create(system_texts::paste());
}

label labels::paste(const drawing::point& location) {
  return label::create(system_texts::paste(), location);
}

label labels::paste(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::paste(), location, size);
}

label labels::paste(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::paste(), location, size, name);
}

label labels::paste(const control& parent) {
  return label::create(parent, system_texts::paste());
}

label labels::paste(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::paste(), location);
}

label labels::paste(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::paste(), location, size);
}

label labels::paste(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::paste(), location, size, name);
}

label labels::preferences() {
  return label::create(system_texts::preferences());
}

label labels::preferences(const drawing::point& location) {
  return label::create(system_texts::preferences(), location);
}

label labels::preferences(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::preferences(), location, size);
}

label labels::preferences(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::preferences(), location, size, name);
}

label labels::preferences(const control& parent) {
  return label::create(parent, system_texts::preferences());
}

label labels::preferences(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::preferences(), location);
}

label labels::preferences(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::preferences(), location, size);
}

label labels::preferences(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::preferences(), location, size, name);
}

label labels::previous() {
  return label::create(system_texts::previous());
}

label labels::previous(const drawing::point& location) {
  return label::create(system_texts::previous(), location);
}

label labels::previous(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::previous(), location, size);
}

label labels::previous(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::previous(), location, size, name);
}

label labels::previous(const control& parent) {
  return label::create(parent, system_texts::previous());
}

label labels::previous(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::previous(), location);
}

label labels::previous(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::previous(), location, size);
}

label labels::previous(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::previous(), location, size, name);
}

label labels::print() {
  return label::create(system_texts::print());
}

label labels::print(const drawing::point& location) {
  return label::create(system_texts::print(), location);
}

label labels::print(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::print(), location, size);
}

label labels::print(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::print(), location, size, name);
}

label labels::print(const control& parent) {
  return label::create(parent, system_texts::print());
}

label labels::print(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::print(), location);
}

label labels::print(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::print(), location, size);
}

label labels::print(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::print(), location, size, name);
}

label labels::print_preview() {
  return label::create(system_texts::print_preview());
}

label labels::print_preview(const drawing::point& location) {
  return label::create(system_texts::print_preview(), location);
}

label labels::print_preview(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::print_preview(), location, size);
}

label labels::print_preview(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::print_preview(), location, size, name);
}

label labels::print_preview(const control& parent) {
  return label::create(parent, system_texts::print_preview());
}

label labels::print_preview(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::print_preview(), location);
}

label labels::print_preview(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::print_preview(), location, size);
}

label labels::print_preview(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::print_preview(), location, size, name);
}

label labels::properties() {
  return label::create(system_texts::properties());
}

label labels::properties(const drawing::point& location) {
  return label::create(system_texts::properties(), location);
}

label labels::properties(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::properties(), location, size);
}

label labels::properties(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::properties(), location, size, name);
}

label labels::properties(const control& parent) {
  return label::create(parent, system_texts::properties());
}

label labels::properties(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::properties(), location);
}

label labels::properties(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::properties(), location, size);
}

label labels::properties(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::properties(), location, size, name);
}

label labels::quit() {
  return label::create(system_texts::quit());
}

label labels::quit(const drawing::point& location) {
  return label::create(system_texts::quit(), location);
}

label labels::quit(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::quit(), location, size);
}

label labels::quit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::quit(), location, size, name);
}

label labels::quit(const control& parent) {
  return label::create(parent, system_texts::quit());
}

label labels::quit(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::quit(), location);
}

label labels::quit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::quit(), location, size);
}

label labels::quit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::quit(), location, size, name);
}

label labels::redo() {
  return label::create(system_texts::redo());
}

label labels::redo(const drawing::point& location) {
  return label::create(system_texts::redo(), location);
}

label labels::redo(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::redo(), location, size);
}

label labels::redo(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::redo(), location, size, name);
}

label labels::redo(const control& parent) {
  return label::create(parent, system_texts::redo());
}

label labels::redo(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::redo(), location);
}

label labels::redo(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::redo(), location, size);
}

label labels::redo(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::redo(), location, size, name);
}

label labels::refresh() {
  return label::create(system_texts::refresh());
}

label labels::refresh(const drawing::point& location) {
  return label::create(system_texts::refresh(), location);
}

label labels::refresh(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::refresh(), location, size);
}

label labels::refresh(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::refresh(), location, size, name);
}

label labels::refresh(const control& parent) {
  return label::create(parent, system_texts::refresh());
}

label labels::refresh(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::refresh(), location);
}

label labels::refresh(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::refresh(), location, size);
}

label labels::refresh(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::refresh(), location, size, name);
}

label labels::remove() {
  return label::create(system_texts::remove());
}

label labels::remove(const drawing::point& location) {
  return label::create(system_texts::remove(), location);
}

label labels::remove(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::remove(), location, size);
}

label labels::remove(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::remove(), location, size, name);
}

label labels::remove(const control& parent) {
  return label::create(parent, system_texts::remove());
}

label labels::remove(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::remove(), location);
}

label labels::remove(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::remove(), location, size);
}

label labels::remove(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::remove(), location, size, name);
}

label labels::replace() {
  return label::create(system_texts::replace());
}

label labels::replace(const drawing::point& location) {
  return label::create(system_texts::replace(), location);
}

label labels::replace(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::replace(), location, size);
}

label labels::replace(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::replace(), location, size, name);
}

label labels::replace(const control& parent) {
  return label::create(parent, system_texts::replace());
}

label labels::replace(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::replace(), location);
}

label labels::replace(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::replace(), location, size);
}

label labels::replace(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::replace(), location, size, name);
}

label labels::retry() {
  return label::create(system_texts::retry());
}

label labels::retry(const drawing::point& location) {
  return label::create(system_texts::retry(), location);
}

label labels::retry(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::retry(), location, size);
}

label labels::retry(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::retry(), location, size, name);
}

label labels::retry(const control& parent) {
  return label::create(parent, system_texts::retry());
}

label labels::retry(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::retry(), location);
}

label labels::retry(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::retry(), location, size);
}

label labels::retry(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::retry(), location, size, name);
}

label labels::revert_to_saved() {
  return label::create(system_texts::revert_to_saved());
}

label labels::revert_to_saved(const drawing::point& location) {
  return label::create(system_texts::revert_to_saved(), location);
}

label labels::revert_to_saved(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::revert_to_saved(), location, size);
}

label labels::revert_to_saved(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::revert_to_saved(), location, size, name);
}

label labels::revert_to_saved(const control& parent) {
  return label::create(parent, system_texts::revert_to_saved());
}

label labels::revert_to_saved(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::revert_to_saved(), location);
}

label labels::revert_to_saved(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::revert_to_saved(), location, size);
}

label labels::revert_to_saved(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::revert_to_saved(), location, size, name);
}

label labels::save() {
  return label::create(system_texts::save());
}

label labels::save(const drawing::point& location) {
  return label::create(system_texts::save(), location);
}

label labels::save(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::save(), location, size);
}

label labels::save(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::save(), location, size, name);
}

label labels::save(const control& parent) {
  return label::create(parent, system_texts::save());
}

label labels::save(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::save(), location);
}

label labels::save(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::save(), location, size);
}

label labels::save(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::save(), location, size, name);
}

label labels::save_as() {
  return label::create(system_texts::save_as());
}

label labels::save_as(const drawing::point& location) {
  return label::create(system_texts::save_as(), location);
}

label labels::save_as(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::save_as(), location, size);
}

label labels::save_as(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::save_as(), location, size, name);
}

label labels::save_as(const control& parent) {
  return label::create(parent, system_texts::save_as());
}

label labels::save_as(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::save_as(), location);
}

label labels::save_as(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::save_as(), location, size);
}

label labels::save_as(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::save_as(), location, size, name);
}

label labels::search() {
  return label::create(system_texts::search());
}

label labels::search(const drawing::point& location) {
  return label::create(system_texts::search(), location);
}

label labels::search(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::search(), location, size);
}

label labels::search(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::search(), location, size, name);
}

label labels::search(const control& parent) {
  return label::create(parent, system_texts::search());
}

label labels::search(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::search(), location);
}

label labels::search(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::search(), location, size);
}

label labels::search(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::search(), location, size, name);
}

label labels::select_all() {
  return label::create(system_texts::select_all());
}

label labels::select_all(const drawing::point& location) {
  return label::create(system_texts::select_all(), location);
}

label labels::select_all(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::select_all(), location, size);
}

label labels::select_all(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::select_all(), location, size, name);
}

label labels::select_all(const control& parent) {
  return label::create(parent, system_texts::select_all());
}

label labels::select_all(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::select_all(), location);
}

label labels::select_all(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::select_all(), location, size);
}

label labels::select_all(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::select_all(), location, size, name);
}

label labels::settings() {
  return label::create(system_texts::settings());
}

label labels::settings(const drawing::point& location) {
  return label::create(system_texts::settings(), location);
}

label labels::settings(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::settings(), location, size);
}

label labels::settings(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::settings(), location, size, name);
}

label labels::settings(const control& parent) {
  return label::create(parent, system_texts::settings());
}

label labels::settings(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::settings(), location);
}

label labels::settings(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::settings(), location, size);
}

label labels::settings(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::settings(), location, size, name);
}

label labels::spell_check() {
  return label::create(system_texts::spell_check());
}

label labels::spell_check(const drawing::point& location) {
  return label::create(system_texts::spell_check(), location);
}

label labels::spell_check(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::spell_check(), location, size);
}

label labels::spell_check(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::spell_check(), location, size, name);
}

label labels::spell_check(const control& parent) {
  return label::create(parent, system_texts::spell_check());
}

label labels::spell_check(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::spell_check(), location);
}

label labels::spell_check(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::spell_check(), location, size);
}

label labels::spell_check(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::spell_check(), location, size, name);
}

label labels::stop() {
  return label::create(system_texts::stop());
}

label labels::stop(const drawing::point& location) {
  return label::create(system_texts::stop(), location);
}

label labels::stop(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::stop(), location, size);
}

label labels::stop(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::stop(), location, size, name);
}

label labels::stop(const control& parent) {
  return label::create(parent, system_texts::stop());
}

label labels::stop(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::stop(), location);
}

label labels::stop(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::stop(), location, size);
}

label labels::stop(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::stop(), location, size, name);
}

label labels::strikeout() {
  return label::create(system_texts::strikeout());
}

label labels::strikeout(const drawing::point& location) {
  return label::create(system_texts::strikeout(), location);
}

label labels::strikeout(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::strikeout(), location, size);
}

label labels::strikeout(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::strikeout(), location, size, name);
}

label labels::strikeout(const control& parent) {
  return label::create(parent, system_texts::strikeout());
}

label labels::strikeout(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::strikeout(), location);
}

label labels::strikeout(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::strikeout(), location, size);
}

label labels::strikeout(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::strikeout(), location, size, name);
}

label labels::tools() {
  return label::create(system_texts::tools());
}

label labels::tools(const drawing::point& location) {
  return label::create(system_texts::tools(), location);
}

label labels::tools(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::tools(), location, size);
}

label labels::tools(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::tools(), location, size, name);
}

label labels::tools(const control& parent) {
  return label::create(parent, system_texts::tools());
}

label labels::tools(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::tools(), location);
}

label labels::tools(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::tools(), location, size);
}

label labels::tools(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::tools(), location, size, name);
}

label labels::top() {
  return label::create(system_texts::top());
}

label labels::top(const drawing::point& location) {
  return label::create(system_texts::top(), location);
}

label labels::top(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::top(), location, size);
}

label labels::top(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::top(), location, size, name);
}

label labels::top(const control& parent) {
  return label::create(parent, system_texts::top());
}

label labels::top(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::top(), location);
}

label labels::top(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::top(), location, size);
}

label labels::top(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::top(), location, size, name);
}

label labels::undelete() {
  return label::create(system_texts::undelete());
}

label labels::undelete(const drawing::point& location) {
  return label::create(system_texts::undelete(), location);
}

label labels::undelete(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::undelete(), location, size);
}

label labels::undelete(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::undelete(), location, size, name);
}

label labels::undelete(const control& parent) {
  return label::create(parent, system_texts::undelete());
}

label labels::undelete(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::undelete(), location);
}

label labels::undelete(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::undelete(), location, size);
}

label labels::undelete(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::undelete(), location, size, name);
}

label labels::underline() {
  return label::create(system_texts::underline());
}

label labels::underline(const drawing::point& location) {
  return label::create(system_texts::underline(), location);
}

label labels::underline(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::underline(), location, size);
}

label labels::underline(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::underline(), location, size, name);
}

label labels::underline(const control& parent) {
  return label::create(parent, system_texts::underline());
}

label labels::underline(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::underline(), location);
}

label labels::underline(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::underline(), location, size);
}

label labels::underline(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::underline(), location, size, name);
}

label labels::undo() {
  return label::create(system_texts::undo());
}

label labels::undo(const drawing::point& location) {
  return label::create(system_texts::undo(), location);
}

label labels::undo(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::undo(), location, size);
}

label labels::undo(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::undo(), location, size, name);
}

label labels::undo(const control& parent) {
  return label::create(parent, system_texts::undo());
}

label labels::undo(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::undo(), location);
}

label labels::undo(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::undo(), location, size);
}

label labels::undo(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::undo(), location, size, name);
}

label labels::unindent() {
  return label::create(system_texts::unindent());
}

label labels::unindent(const drawing::point& location) {
  return label::create(system_texts::unindent(), location);
}

label labels::unindent(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::unindent(), location, size);
}

label labels::unindent(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::unindent(), location, size, name);
}

label labels::unindent(const control& parent) {
  return label::create(parent, system_texts::unindent());
}

label labels::unindent(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::unindent(), location);
}

label labels::unindent(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::unindent(), location, size);
}

label labels::unindent(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::unindent(), location, size, name);
}

label labels::up() {
  return label::create(system_texts::up());
}

label labels::up(const drawing::point& location) {
  return label::create(system_texts::up(), location);
}

label labels::up(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::up(), location, size);
}

label labels::up(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::up(), location, size, name);
}

label labels::up(const control& parent) {
  return label::create(parent, system_texts::up());
}

label labels::up(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::up(), location);
}

label labels::up(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::up(), location, size);
}

label labels::up(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::up(), location, size, name);
}

label labels::view() {
  return label::create(system_texts::view());
}

label labels::view(const drawing::point& location) {
  return label::create(system_texts::view(), location);
}

label labels::view(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::view(), location, size);
}

label labels::view(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::view(), location, size, name);
}

label labels::view(const control& parent) {
  return label::create(parent, system_texts::view());
}

label labels::view(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::view(), location);
}

label labels::view(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::view(), location, size);
}

label labels::view(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::view(), location, size, name);
}

label labels::window() {
  return label::create(system_texts::window());
}

label labels::window(const drawing::point& location) {
  return label::create(system_texts::window(), location);
}

label labels::window(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::window(), location, size);
}

label labels::window(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::window(), location, size, name);
}

label labels::window(const control& parent) {
  return label::create(parent, system_texts::window());
}

label labels::window(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::window(), location);
}

label labels::window(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::window(), location, size);
}

label labels::window(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::window(), location, size, name);
}

label labels::yes() {
  return label::create(system_texts::yes());
}

label labels::yes(const drawing::point& location) {
  return label::create(system_texts::yes(), location);
}

label labels::yes(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::yes(), location, size);
}

label labels::yes(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::yes(), location, size, name);
}

label labels::yes(const control& parent) {
  return label::create(parent, system_texts::yes());
}

label labels::yes(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::yes(), location);
}

label labels::yes(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::yes(), location, size);
}

label labels::yes(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::yes(), location, size, name);
}

label labels::zoom_in() {
  return label::create(system_texts::zoom_in());
}

label labels::zoom_in(const drawing::point& location) {
  return label::create(system_texts::zoom_in(), location);
}

label labels::zoom_in(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::zoom_in(), location, size);
}

label labels::zoom_in(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::zoom_in(), location, size, name);
}

label labels::zoom_in(const control& parent) {
  return label::create(parent, system_texts::zoom_in());
}

label labels::zoom_in(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::zoom_in(), location);
}

label labels::zoom_in(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::zoom_in(), location, size);
}

label labels::zoom_in(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::zoom_in(), location, size, name);
}

label labels::zoom_out() {
  return label::create(system_texts::zoom_out());
}

label labels::zoom_out(const drawing::point& location) {
  return label::create(system_texts::zoom_out(), location);
}

label labels::zoom_out(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::zoom_out(), location, size);
}

label labels::zoom_out(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::zoom_out(), location, size, name);
}

label labels::zoom_out(const control& parent) {
  return label::create(parent, system_texts::zoom_out());
}

label labels::zoom_out(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::zoom_out(), location);
}

label labels::zoom_out(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::zoom_out(), location, size);
}

label labels::zoom_out(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::zoom_out(), location, size, name);
}

label labels::zoom_to_fit() {
  return label::create(system_texts::zoom_to_fit());
}

label labels::zoom_to_fit(const drawing::point& location) {
  return label::create(system_texts::zoom_to_fit(), location);
}

label labels::zoom_to_fit(const drawing::point& location, const drawing::size& size) {
  return label::create(system_texts::zoom_to_fit(), location, size);
}

label labels::zoom_to_fit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(system_texts::zoom_to_fit(), location, size, name);
}

label labels::zoom_to_fit(const control& parent) {
  return label::create(parent, system_texts::zoom_to_fit());
}

label labels::zoom_to_fit(const control& parent, const drawing::point& location) {
  return label::create(parent, system_texts::zoom_to_fit(), location);
}

label labels::zoom_to_fit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return label::create(parent, system_texts::zoom_to_fit(), location, size);
}

label labels::zoom_to_fit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return label::create(parent, system_texts::zoom_to_fit(), location, size, name);
}
