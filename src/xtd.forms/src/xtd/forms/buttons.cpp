#include "../../../include/xtd/forms/buttons.h"
#include "../../../include/xtd/forms/system_texts.h"

using namespace xtd;
using namespace xtd::forms;

button buttons::abort() {
  return button::create(system_texts::abort());
}

button buttons::abort(const drawing::point& location) {
  return button::create(system_texts::abort(), location);
}

button buttons::abort(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::abort(), location, size);
}

button buttons::abort(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::abort(), location, size, name);
}

button buttons::abort(const control& parent) {
  return button::create(parent, system_texts::abort());
}

button buttons::abort(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::abort(), location);
}

button buttons::abort(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::abort(), location, size);
}

button buttons::abort(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::abort(), location, size, name);
}

button buttons::about() {
  return button::create(system_texts::about());
}

button buttons::about(const drawing::point& location) {
  return button::create(system_texts::about(), location);
}

button buttons::about(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::about(), location, size);
}

button buttons::about(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::about(), location, size, name);
}

button buttons::about(const control& parent) {
  return button::create(parent, system_texts::about());
}

button buttons::about(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::about(), location);
}

button buttons::about(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::about(), location, size);
}

button buttons::about(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::about(), location, size, name);
}

button buttons::actual_size() {
  return button::create(system_texts::actual_size());
}

button buttons::actual_size(const drawing::point& location) {
  return button::create(system_texts::actual_size(), location);
}

button buttons::actual_size(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::actual_size(), location, size);
}

button buttons::actual_size(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::actual_size(), location, size, name);
}

button buttons::actual_size(const control& parent) {
  return button::create(parent, system_texts::actual_size());
}

button buttons::actual_size(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::actual_size(), location);
}

button buttons::actual_size(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::actual_size(), location, size);
}

button buttons::actual_size(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::actual_size(), location, size, name);
}

button buttons::add() {
  return button::create(system_texts::add());
}

button buttons::add(const drawing::point& location) {
  return button::create(system_texts::add(), location);
}

button buttons::add(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::add(), location, size);
}

button buttons::add(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::add(), location, size, name);
}

button buttons::add(const control& parent) {
  return button::create(parent, system_texts::add());
}

button buttons::add(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::add(), location);
}

button buttons::add(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::add(), location, size);
}

button buttons::add(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::add(), location, size, name);
}

button buttons::alignment() {
  return button::create(system_texts::alignment());
}

button buttons::alignment(const drawing::point& location) {
  return button::create(system_texts::alignment(), location);
}

button buttons::alignment(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::alignment(), location, size);
}

button buttons::alignment(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::alignment(), location, size, name);
}

button buttons::alignment(const control& parent) {
  return button::create(parent, system_texts::alignment());
}

button buttons::alignment(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::alignment(), location);
}

button buttons::alignment(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::alignment(), location, size);
}

button buttons::alignment(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::alignment(), location, size, name);
}

button buttons::align_left() {
  return button::create(system_texts::align_left());
}

button buttons::align_left(const drawing::point& location) {
  return button::create(system_texts::align_left(), location);
}

button buttons::align_left(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::align_left(), location, size);
}

button buttons::align_left(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::align_left(), location, size, name);
}

button buttons::align_left(const control& parent) {
  return button::create(parent, system_texts::align_left());
}

button buttons::align_left(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::align_left(), location);
}

button buttons::align_left(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::align_left(), location, size);
}

button buttons::align_left(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::align_left(), location, size, name);
}

button buttons::align_right() {
  return button::create(system_texts::align_right());
}

button buttons::align_right(const drawing::point& location) {
  return button::create(system_texts::align_right(), location);
}

button buttons::align_right(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::align_right(), location, size);
}

button buttons::align_right(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::align_right(), location, size, name);
}

button buttons::align_right(const control& parent) {
  return button::create(parent, system_texts::align_right());
}

button buttons::align_right(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::align_right(), location);
}

button buttons::align_right(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::align_right(), location, size);
}

button buttons::align_right(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::align_right(), location, size, name);
}

button buttons::apply() {
  return button::create(system_texts::apply());
}

button buttons::apply(const drawing::point& location) {
  return button::create(system_texts::apply(), location);
}

button buttons::apply(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::apply(), location, size);
}

button buttons::apply(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::apply(), location, size, name);
}

button buttons::apply(const control& parent) {
  return button::create(parent, system_texts::apply());
}

button buttons::apply(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::apply(), location);
}

button buttons::apply(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::apply(), location, size);
}

button buttons::apply(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::apply(), location, size, name);
}

button buttons::ascending() {
  return button::create(system_texts::ascending());
}

button buttons::ascending(const drawing::point& location) {
  return button::create(system_texts::ascending(), location);
}

button buttons::ascending(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::ascending(), location, size);
}

button buttons::ascending(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::ascending(), location, size, name);
}

button buttons::ascending(const control& parent) {
  return button::create(parent, system_texts::ascending());
}

button buttons::ascending(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::ascending(), location);
}

button buttons::ascending(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::ascending(), location, size);
}

button buttons::ascending(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::ascending(), location, size, name);
}

button buttons::back() {
  return button::create(system_texts::back());
}

button buttons::back(const drawing::point& location) {
  return button::create(system_texts::back(), location);
}

button buttons::back(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::back(), location, size);
}

button buttons::back(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::back(), location, size, name);
}

button buttons::back(const control& parent) {
  return button::create(parent, system_texts::back());
}

button buttons::back(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::back(), location);
}

button buttons::back(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::back(), location, size);
}

button buttons::back(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::back(), location, size, name);
}

button buttons::bold() {
  return button::create(system_texts::bold());
}

button buttons::bold(const drawing::point& location) {
  return button::create(system_texts::bold(), location);
}

button buttons::bold(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::bold(), location, size);
}

button buttons::bold(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::bold(), location, size, name);
}

button buttons::bold(const control& parent) {
  return button::create(parent, system_texts::bold());
}

button buttons::bold(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::bold(), location);
}

button buttons::bold(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::bold(), location, size);
}

button buttons::bold(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::bold(), location, size, name);
}

button buttons::bottom() {
  return button::create(system_texts::bottom());
}

button buttons::bottom(const drawing::point& location) {
  return button::create(system_texts::bottom(), location);
}

button buttons::bottom(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::bottom(), location, size);
}

button buttons::bottom(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::bottom(), location, size, name);
}

button buttons::bottom(const control& parent) {
  return button::create(parent, system_texts::bottom());
}

button buttons::bottom(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::bottom(), location);
}

button buttons::bottom(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::bottom(), location, size);
}

button buttons::bottom(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::bottom(), location, size, name);
}

button buttons::cancel() {
  return button::create(system_texts::cancel());
}

button buttons::cancel(const drawing::point& location) {
  return button::create(system_texts::cancel(), location);
}

button buttons::cancel(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::cancel(), location, size);
}

button buttons::cancel(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::cancel(), location, size, name);
}

button buttons::cancel(const control& parent) {
  return button::create(parent, system_texts::cancel());
}

button buttons::cancel(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::cancel(), location);
}

button buttons::cancel(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::cancel(), location, size);
}

button buttons::cancel(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::cancel(), location, size, name);
}

button buttons::cdrom() {
  return button::create(system_texts::cdrom());
}

button buttons::cdrom(const drawing::point& location) {
  return button::create(system_texts::cdrom(), location);
}

button buttons::cdrom(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::cdrom(), location, size);
}

button buttons::cdrom(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::cdrom(), location, size, name);
}

button buttons::cdrom(const control& parent) {
  return button::create(parent, system_texts::cdrom());
}

button buttons::cdrom(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::cdrom(), location);
}

button buttons::cdrom(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::cdrom(), location, size);
}

button buttons::cdrom(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::cdrom(), location, size, name);
}

button buttons::centered() {
  return button::create(system_texts::centered());
}

button buttons::centered(const drawing::point& location) {
  return button::create(system_texts::centered(), location);
}

button buttons::centered(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::centered(), location, size);
}

button buttons::centered(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::centered(), location, size, name);
}

button buttons::centered(const control& parent) {
  return button::create(parent, system_texts::centered());
}

button buttons::centered(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::centered(), location);
}

button buttons::centered(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::centered(), location, size);
}

button buttons::centered(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::centered(), location, size, name);
}

button buttons::clear() {
  return button::create(system_texts::clear());
}

button buttons::clear(const drawing::point& location) {
  return button::create(system_texts::clear(), location);
}

button buttons::clear(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::clear(), location, size);
}

button buttons::clear(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::clear(), location, size, name);
}

button buttons::clear(const control& parent) {
  return button::create(parent, system_texts::clear());
}

button buttons::clear(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::clear(), location);
}

button buttons::clear(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::clear(), location, size);
}

button buttons::clear(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::clear(), location, size, name);
}

button buttons::close() {
  return button::create(system_texts::close());
}

button buttons::close(const drawing::point& location) {
  return button::create(system_texts::close(), location);
}

button buttons::close(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::close(), location, size);
}

button buttons::close(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::close(), location, size, name);
}

button buttons::close(const control& parent) {
  return button::create(parent, system_texts::close());
}

button buttons::close(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::close(), location);
}

button buttons::close(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::close(), location, size);
}

button buttons::close(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::close(), location, size, name);
}

button buttons::color() {
  return button::create(system_texts::color());
}

button buttons::color(const drawing::point& location) {
  return button::create(system_texts::color(), location);
}

button buttons::color(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::color(), location, size);
}

button buttons::color(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::color(), location, size, name);
}

button buttons::color(const control& parent) {
  return button::create(parent, system_texts::color());
}

button buttons::color(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::color(), location);
}

button buttons::color(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::color(), location, size);
}

button buttons::color(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::color(), location, size, name);
}

button buttons::contents() {
  return button::create(system_texts::contents());
}

button buttons::contents(const drawing::point& location) {
  return button::create(system_texts::contents(), location);
}

button buttons::contents(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::contents(), location, size);
}

button buttons::contents(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::contents(), location, size, name);
}

button buttons::contents(const control& parent) {
  return button::create(parent, system_texts::contents());
}

button buttons::contents(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::contents(), location);
}

button buttons::contents(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::contents(), location, size);
}

button buttons::contents(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::contents(), location, size, name);
}

button buttons::convert() {
  return button::create(system_texts::convert());
}

button buttons::convert(const drawing::point& location) {
  return button::create(system_texts::convert(), location);
}

button buttons::convert(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::convert(), location, size);
}

button buttons::convert(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::convert(), location, size, name);
}

button buttons::convert(const control& parent) {
  return button::create(parent, system_texts::convert());
}

button buttons::convert(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::convert(), location);
}

button buttons::convert(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::convert(), location, size);
}

button buttons::convert(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::convert(), location, size, name);
}

button buttons::copy() {
  return button::create(system_texts::copy());
}

button buttons::copy(const drawing::point& location) {
  return button::create(system_texts::copy(), location);
}

button buttons::copy(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::copy(), location, size);
}

button buttons::copy(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::copy(), location, size, name);
}

button buttons::copy(const control& parent) {
  return button::create(parent, system_texts::copy());
}

button buttons::copy(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::copy(), location);
}

button buttons::copy(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::copy(), location, size);
}

button buttons::copy(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::copy(), location, size, name);
}

button buttons::customize() {
  return button::create(system_texts::customize());
}

button buttons::customize(const drawing::point& location) {
  return button::create(system_texts::customize(), location);
}

button buttons::customize(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::customize(), location, size);
}

button buttons::customize(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::customize(), location, size, name);
}

button buttons::customize(const control& parent) {
  return button::create(parent, system_texts::customize());
}

button buttons::customize(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::customize(), location);
}

button buttons::customize(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::customize(), location, size);
}

button buttons::customize(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::customize(), location, size, name);
}

button buttons::cut() {
  return button::create(system_texts::cut());
}

button buttons::cut(const drawing::point& location) {
  return button::create(system_texts::cut(), location);
}

button buttons::cut(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::cut(), location, size);
}

button buttons::cut(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::cut(), location, size, name);
}

button buttons::cut(const control& parent) {
  return button::create(parent, system_texts::cut());
}

button buttons::cut(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::cut(), location);
}

button buttons::cut(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::cut(), location, size);
}

button buttons::cut(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::cut(), location, size, name);
}

button buttons::del() {
  return button::create(system_texts::del());
}

button buttons::del(const drawing::point& location) {
  return button::create(system_texts::del(), location);
}

button buttons::del(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::del(), location, size);
}

button buttons::del(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::del(), location, size, name);
}

button buttons::del(const control& parent) {
  return button::create(parent, system_texts::del());
}

button buttons::del(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::del(), location);
}

button buttons::del(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::del(), location, size);
}

button buttons::del(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::del(), location, size, name);
}

button buttons::descending() {
  return button::create(system_texts::descending());
}

button buttons::descending(const drawing::point& location) {
  return button::create(system_texts::descending(), location);
}

button buttons::descending(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::descending(), location, size);
}

button buttons::descending(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::descending(), location, size, name);
}

button buttons::descending(const control& parent) {
  return button::create(parent, system_texts::descending());
}

button buttons::descending(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::descending(), location);
}

button buttons::descending(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::descending(), location, size);
}

button buttons::descending(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::descending(), location, size, name);
}

button buttons::down() {
  return button::create(system_texts::down());
}

button buttons::down(const drawing::point& location) {
  return button::create(system_texts::down(), location);
}

button buttons::down(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::down(), location, size);
}

button buttons::down(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::down(), location, size, name);
}

button buttons::down(const control& parent) {
  return button::create(parent, system_texts::down());
}

button buttons::down(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::down(), location);
}

button buttons::down(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::down(), location, size);
}

button buttons::down(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::down(), location, size, name);
}

button buttons::edit() {
  return button::create(system_texts::edit());
}

button buttons::edit(const drawing::point& location) {
  return button::create(system_texts::edit(), location);
}

button buttons::edit(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::edit(), location, size);
}

button buttons::edit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::edit(), location, size, name);
}

button buttons::edit(const control& parent) {
  return button::create(parent, system_texts::edit());
}

button buttons::edit(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::edit(), location);
}

button buttons::edit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::edit(), location, size);
}

button buttons::edit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::edit(), location, size, name);
}

button buttons::execute() {
  return button::create(system_texts::execute());
}

button buttons::execute(const drawing::point& location) {
  return button::create(system_texts::execute(), location);
}

button buttons::execute(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::execute(), location, size);
}

button buttons::execute(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::execute(), location, size, name);
}

button buttons::execute(const control& parent) {
  return button::create(parent, system_texts::execute());
}

button buttons::execute(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::execute(), location);
}

button buttons::execute(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::execute(), location, size);
}

button buttons::execute(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::execute(), location, size, name);
}

button buttons::exit() {
  return button::create(system_texts::exit());
}

button buttons::exit(const drawing::point& location) {
  return button::create(system_texts::exit(), location);
}

button buttons::exit(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::exit(), location, size);
}

button buttons::exit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::exit(), location, size, name);
}

button buttons::exit(const control& parent) {
  return button::create(parent, system_texts::exit());
}

button buttons::exit(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::exit(), location);
}

button buttons::exit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::exit(), location, size);
}

button buttons::exit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::exit(), location, size, name);
}

button buttons::file() {
  return button::create(system_texts::file());
}

button buttons::file(const drawing::point& location) {
  return button::create(system_texts::file(), location);
}

button buttons::file(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::file(), location, size);
}

button buttons::file(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::file(), location, size, name);
}

button buttons::file(const control& parent) {
  return button::create(parent, system_texts::file());
}

button buttons::file(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::file(), location);
}

button buttons::file(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::file(), location, size);
}

button buttons::file(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::file(), location, size, name);
}

button buttons::find() {
  return button::create(system_texts::find());
}

button buttons::find(const drawing::point& location) {
  return button::create(system_texts::find(), location);
}

button buttons::find(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::find(), location, size);
}

button buttons::find(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::find(), location, size, name);
}

button buttons::find(const control& parent) {
  return button::create(parent, system_texts::find());
}

button buttons::find(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::find(), location);
}

button buttons::find(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::find(), location, size);
}

button buttons::find(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::find(), location, size, name);
}

button buttons::first() {
  return button::create(system_texts::first());
}

button buttons::first(const drawing::point& location) {
  return button::create(system_texts::first(), location);
}

button buttons::first(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::first(), location, size);
}

button buttons::first(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::first(), location, size, name);
}

button buttons::first(const control& parent) {
  return button::create(parent, system_texts::first());
}

button buttons::first(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::first(), location);
}

button buttons::first(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::first(), location, size);
}

button buttons::first(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::first(), location, size, name);
}

button buttons::floppy() {
  return button::create(system_texts::floppy());
}

button buttons::floppy(const drawing::point& location) {
  return button::create(system_texts::floppy(), location);
}

button buttons::floppy(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::floppy(), location, size);
}

button buttons::floppy(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::floppy(), location, size, name);
}

button buttons::floppy(const control& parent) {
  return button::create(parent, system_texts::floppy());
}

button buttons::floppy(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::floppy(), location);
}

button buttons::floppy(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::floppy(), location, size);
}

button buttons::floppy(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::floppy(), location, size, name);
}

button buttons::font() {
  return button::create(system_texts::font());
}

button buttons::font(const drawing::point& location) {
  return button::create(system_texts::font(), location);
}

button buttons::font(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::font(), location, size);
}

button buttons::font(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::font(), location, size, name);
}

button buttons::font(const control& parent) {
  return button::create(parent, system_texts::font());
}

button buttons::font(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::font(), location);
}

button buttons::font(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::font(), location, size);
}

button buttons::font(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::font(), location, size, name);
}

button buttons::forward() {
  return button::create(system_texts::forward());
}

button buttons::forward(const drawing::point& location) {
  return button::create(system_texts::forward(), location);
}

button buttons::forward(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::forward(), location, size);
}

button buttons::forward(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::forward(), location, size, name);
}

button buttons::forward(const control& parent) {
  return button::create(parent, system_texts::forward());
}

button buttons::forward(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::forward(), location);
}

button buttons::forward(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::forward(), location, size);
}

button buttons::forward(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::forward(), location, size, name);
}

button buttons::harddisk() {
  return button::create(system_texts::harddisk());
}

button buttons::harddisk(const drawing::point& location) {
  return button::create(system_texts::harddisk(), location);
}

button buttons::harddisk(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::harddisk(), location, size);
}

button buttons::harddisk(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::harddisk(), location, size, name);
}

button buttons::harddisk(const control& parent) {
  return button::create(parent, system_texts::harddisk());
}

button buttons::harddisk(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::harddisk(), location);
}

button buttons::harddisk(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::harddisk(), location, size);
}

button buttons::harddisk(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::harddisk(), location, size, name);
}

button buttons::help() {
  return button::create(system_texts::help());
}

button buttons::help(const drawing::point& location) {
  return button::create(system_texts::help(), location);
}

button buttons::help(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::help(), location, size);
}

button buttons::help(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::help(), location, size, name);
}

button buttons::help(const control& parent) {
  return button::create(parent, system_texts::help());
}

button buttons::help(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::help(), location);
}

button buttons::help(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::help(), location, size);
}

button buttons::help(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::help(), location, size, name);
}

button buttons::home() {
  return button::create(system_texts::home());
}

button buttons::home(const drawing::point& location) {
  return button::create(system_texts::home(), location);
}

button buttons::home(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::home(), location, size);
}

button buttons::home(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::home(), location, size, name);
}

button buttons::home(const control& parent) {
  return button::create(parent, system_texts::home());
}

button buttons::home(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::home(), location);
}

button buttons::home(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::home(), location, size);
}

button buttons::home(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::home(), location, size, name);
}

button buttons::ignore() {
  return button::create(system_texts::ignore());
}

button buttons::ignore(const drawing::point& location) {
  return button::create(system_texts::ignore(), location);
}

button buttons::ignore(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::ignore(), location, size);
}

button buttons::ignore(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::ignore(), location, size, name);
}

button buttons::ignore(const control& parent) {
  return button::create(parent, system_texts::ignore());
}

button buttons::ignore(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::ignore(), location);
}

button buttons::ignore(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::ignore(), location, size);
}

button buttons::ignore(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::ignore(), location, size, name);
}

button buttons::indent() {
  return button::create(system_texts::indent());
}

button buttons::indent(const drawing::point& location) {
  return button::create(system_texts::indent(), location);
}

button buttons::indent(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::indent(), location, size);
}

button buttons::indent(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::indent(), location, size, name);
}

button buttons::indent(const control& parent) {
  return button::create(parent, system_texts::indent());
}

button buttons::indent(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::indent(), location);
}

button buttons::indent(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::indent(), location, size);
}

button buttons::indent(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::indent(), location, size, name);
}

button buttons::index() {
  return button::create(system_texts::index());
}

button buttons::index(const drawing::point& location) {
  return button::create(system_texts::index(), location);
}

button buttons::index(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::index(), location, size);
}

button buttons::index(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::index(), location, size, name);
}

button buttons::index(const control& parent) {
  return button::create(parent, system_texts::index());
}

button buttons::index(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::index(), location);
}

button buttons::index(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::index(), location, size);
}

button buttons::index(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::index(), location, size, name);
}

button buttons::info() {
  return button::create(system_texts::info());
}

button buttons::info(const drawing::point& location) {
  return button::create(system_texts::info(), location);
}

button buttons::info(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::info(), location, size);
}

button buttons::info(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::info(), location, size, name);
}

button buttons::info(const control& parent) {
  return button::create(parent, system_texts::info());
}

button buttons::info(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::info(), location);
}

button buttons::info(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::info(), location, size);
}

button buttons::info(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::info(), location, size, name);
}

button buttons::italic() {
  return button::create(system_texts::italic());
}

button buttons::italic(const drawing::point& location) {
  return button::create(system_texts::italic(), location);
}

button buttons::italic(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::italic(), location, size);
}

button buttons::italic(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::italic(), location, size, name);
}

button buttons::italic(const control& parent) {
  return button::create(parent, system_texts::italic());
}

button buttons::italic(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::italic(), location);
}

button buttons::italic(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::italic(), location, size);
}

button buttons::italic(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::italic(), location, size, name);
}

button buttons::justified() {
  return button::create(system_texts::justified());
}

button buttons::justified(const drawing::point& location) {
  return button::create(system_texts::justified(), location);
}

button buttons::justified(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::justified(), location, size);
}

button buttons::justified(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::justified(), location, size, name);
}

button buttons::justified(const control& parent) {
  return button::create(parent, system_texts::justified());
}

button buttons::justified(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::justified(), location);
}

button buttons::justified(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::justified(), location, size);
}

button buttons::justified(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::justified(), location, size, name);
}

button buttons::jump_to() {
  return button::create(system_texts::jump_to());
}

button buttons::jump_to(const drawing::point& location) {
  return button::create(system_texts::jump_to(), location);
}

button buttons::jump_to(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::jump_to(), location, size);
}

button buttons::jump_to(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::jump_to(), location, size, name);
}

button buttons::jump_to(const control& parent) {
  return button::create(parent, system_texts::jump_to());
}

button buttons::jump_to(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::jump_to(), location);
}

button buttons::jump_to(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::jump_to(), location, size);
}

button buttons::jump_to(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::jump_to(), location, size, name);
}

button buttons::last() {
  return button::create(system_texts::last());
}

button buttons::last(const drawing::point& location) {
  return button::create(system_texts::last(), location);
}

button buttons::last(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::last(), location, size);
}

button buttons::last(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::last(), location, size, name);
}

button buttons::last(const control& parent) {
  return button::create(parent, system_texts::last());
}

button buttons::last(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::last(), location);
}

button buttons::last(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::last(), location, size);
}

button buttons::last(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::last(), location, size, name);
}

button buttons::network() {
  return button::create(system_texts::network());
}

button buttons::network(const drawing::point& location) {
  return button::create(system_texts::network(), location);
}

button buttons::network(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::network(), location, size);
}

button buttons::network(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::network(), location, size, name);
}

button buttons::network(const control& parent) {
  return button::create(parent, system_texts::network());
}

button buttons::network(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::network(), location);
}

button buttons::network(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::network(), location, size);
}

button buttons::network(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::network(), location, size, name);
}

button buttons::new_() {
  return button::create(system_texts::new_());
}

button buttons::new_(const drawing::point& location) {
  return button::create(system_texts::new_(), location);
}

button buttons::new_(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::new_(), location, size);
}

button buttons::new_(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::new_(), location, size, name);
}

button buttons::new_(const control& parent) {
  return button::create(parent, system_texts::new_());
}

button buttons::new_(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::new_(), location);
}

button buttons::new_(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::new_(), location, size);
}

button buttons::new_(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::new_(), location, size, name);
}

button buttons::next() {
  return button::create(system_texts::next());
}

button buttons::next(const drawing::point& location) {
  return button::create(system_texts::next(), location);
}

button buttons::next(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::next(), location, size);
}

button buttons::next(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::next(), location, size, name);
}

button buttons::next(const control& parent) {
  return button::create(parent, system_texts::next());
}

button buttons::next(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::next(), location);
}

button buttons::next(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::next(), location, size);
}

button buttons::next(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::next(), location, size, name);
}

button buttons::no() {
  return button::create(system_texts::no());
}

button buttons::no(const drawing::point& location) {
  return button::create(system_texts::no(), location);
}

button buttons::no(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::no(), location, size);
}

button buttons::no(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::no(), location, size, name);
}

button buttons::no(const control& parent) {
  return button::create(parent, system_texts::no());
}

button buttons::no(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::no(), location);
}

button buttons::no(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::no(), location, size);
}

button buttons::no(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::no(), location, size, name);
}

button buttons::ok() {
  return button::create(system_texts::ok());
}

button buttons::ok(const drawing::point& location) {
  return button::create(system_texts::ok(), location);
}

button buttons::ok(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::ok(), location, size);
}

button buttons::ok(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::ok(), location, size, name);
}

button buttons::ok(const control& parent) {
  return button::create(parent, system_texts::ok());
}

button buttons::ok(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::ok(), location);
}

button buttons::ok(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::ok(), location, size);
}

button buttons::ok(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::ok(), location, size, name);
}

button buttons::open() {
  return button::create(system_texts::open());
}

button buttons::open(const drawing::point& location) {
  return button::create(system_texts::open(), location);
}

button buttons::open(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::open(), location, size);
}

button buttons::open(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::open(), location, size, name);
}

button buttons::open(const control& parent) {
  return button::create(parent, system_texts::open());
}

button buttons::open(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::open(), location);
}

button buttons::open(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::open(), location, size);
}

button buttons::open(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::open(), location, size, name);
}

button buttons::options() {
  return button::create(system_texts::options());
}

button buttons::options(const drawing::point& location) {
  return button::create(system_texts::options(), location);
}

button buttons::options(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::options(), location, size);
}

button buttons::options(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::options(), location, size, name);
}

button buttons::options(const control& parent) {
  return button::create(parent, system_texts::options());
}

button buttons::options(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::options(), location);
}

button buttons::options(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::options(), location, size);
}

button buttons::options(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::options(), location, size, name);
}

button buttons::paste() {
  return button::create(system_texts::paste());
}

button buttons::paste(const drawing::point& location) {
  return button::create(system_texts::paste(), location);
}

button buttons::paste(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::paste(), location, size);
}

button buttons::paste(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::paste(), location, size, name);
}

button buttons::paste(const control& parent) {
  return button::create(parent, system_texts::paste());
}

button buttons::paste(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::paste(), location);
}

button buttons::paste(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::paste(), location, size);
}

button buttons::paste(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::paste(), location, size, name);
}

button buttons::preferences() {
  return button::create(system_texts::preferences());
}

button buttons::preferences(const drawing::point& location) {
  return button::create(system_texts::preferences(), location);
}

button buttons::preferences(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::preferences(), location, size);
}

button buttons::preferences(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::preferences(), location, size, name);
}

button buttons::preferences(const control& parent) {
  return button::create(parent, system_texts::preferences());
}

button buttons::preferences(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::preferences(), location);
}

button buttons::preferences(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::preferences(), location, size);
}

button buttons::preferences(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::preferences(), location, size, name);
}

button buttons::previous() {
  return button::create(system_texts::previous());
}

button buttons::previous(const drawing::point& location) {
  return button::create(system_texts::previous(), location);
}

button buttons::previous(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::previous(), location, size);
}

button buttons::previous(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::previous(), location, size, name);
}

button buttons::previous(const control& parent) {
  return button::create(parent, system_texts::previous());
}

button buttons::previous(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::previous(), location);
}

button buttons::previous(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::previous(), location, size);
}

button buttons::previous(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::previous(), location, size, name);
}

button buttons::print() {
  return button::create(system_texts::print());
}

button buttons::print(const drawing::point& location) {
  return button::create(system_texts::print(), location);
}

button buttons::print(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::print(), location, size);
}

button buttons::print(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::print(), location, size, name);
}

button buttons::print(const control& parent) {
  return button::create(parent, system_texts::print());
}

button buttons::print(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::print(), location);
}

button buttons::print(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::print(), location, size);
}

button buttons::print(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::print(), location, size, name);
}

button buttons::print_preview() {
  return button::create(system_texts::print_preview());
}

button buttons::print_preview(const drawing::point& location) {
  return button::create(system_texts::print_preview(), location);
}

button buttons::print_preview(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::print_preview(), location, size);
}

button buttons::print_preview(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::print_preview(), location, size, name);
}

button buttons::print_preview(const control& parent) {
  return button::create(parent, system_texts::print_preview());
}

button buttons::print_preview(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::print_preview(), location);
}

button buttons::print_preview(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::print_preview(), location, size);
}

button buttons::print_preview(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::print_preview(), location, size, name);
}

button buttons::properties() {
  return button::create(system_texts::properties());
}

button buttons::properties(const drawing::point& location) {
  return button::create(system_texts::properties(), location);
}

button buttons::properties(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::properties(), location, size);
}

button buttons::properties(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::properties(), location, size, name);
}

button buttons::properties(const control& parent) {
  return button::create(parent, system_texts::properties());
}

button buttons::properties(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::properties(), location);
}

button buttons::properties(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::properties(), location, size);
}

button buttons::properties(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::properties(), location, size, name);
}

button buttons::quit() {
  return button::create(system_texts::quit());
}

button buttons::quit(const drawing::point& location) {
  return button::create(system_texts::quit(), location);
}

button buttons::quit(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::quit(), location, size);
}

button buttons::quit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::quit(), location, size, name);
}

button buttons::quit(const control& parent) {
  return button::create(parent, system_texts::quit());
}

button buttons::quit(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::quit(), location);
}

button buttons::quit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::quit(), location, size);
}

button buttons::quit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::quit(), location, size, name);
}

button buttons::redo() {
  return button::create(system_texts::redo());
}

button buttons::redo(const drawing::point& location) {
  return button::create(system_texts::redo(), location);
}

button buttons::redo(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::redo(), location, size);
}

button buttons::redo(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::redo(), location, size, name);
}

button buttons::redo(const control& parent) {
  return button::create(parent, system_texts::redo());
}

button buttons::redo(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::redo(), location);
}

button buttons::redo(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::redo(), location, size);
}

button buttons::redo(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::redo(), location, size, name);
}

button buttons::refresh() {
  return button::create(system_texts::refresh());
}

button buttons::refresh(const drawing::point& location) {
  return button::create(system_texts::refresh(), location);
}

button buttons::refresh(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::refresh(), location, size);
}

button buttons::refresh(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::refresh(), location, size, name);
}

button buttons::refresh(const control& parent) {
  return button::create(parent, system_texts::refresh());
}

button buttons::refresh(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::refresh(), location);
}

button buttons::refresh(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::refresh(), location, size);
}

button buttons::refresh(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::refresh(), location, size, name);
}

button buttons::remove() {
  return button::create(system_texts::remove());
}

button buttons::remove(const drawing::point& location) {
  return button::create(system_texts::remove(), location);
}

button buttons::remove(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::remove(), location, size);
}

button buttons::remove(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::remove(), location, size, name);
}

button buttons::remove(const control& parent) {
  return button::create(parent, system_texts::remove());
}

button buttons::remove(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::remove(), location);
}

button buttons::remove(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::remove(), location, size);
}

button buttons::remove(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::remove(), location, size, name);
}

button buttons::replace() {
  return button::create(system_texts::replace());
}

button buttons::replace(const drawing::point& location) {
  return button::create(system_texts::replace(), location);
}

button buttons::replace(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::replace(), location, size);
}

button buttons::replace(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::replace(), location, size, name);
}

button buttons::replace(const control& parent) {
  return button::create(parent, system_texts::replace());
}

button buttons::replace(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::replace(), location);
}

button buttons::replace(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::replace(), location, size);
}

button buttons::replace(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::replace(), location, size, name);
}

button buttons::retry() {
  return button::create(system_texts::retry());
}

button buttons::retry(const drawing::point& location) {
  return button::create(system_texts::retry(), location);
}

button buttons::retry(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::retry(), location, size);
}

button buttons::retry(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::retry(), location, size, name);
}

button buttons::retry(const control& parent) {
  return button::create(parent, system_texts::retry());
}

button buttons::retry(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::retry(), location);
}

button buttons::retry(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::retry(), location, size);
}

button buttons::retry(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::retry(), location, size, name);
}

button buttons::revert_to_saved() {
  return button::create(system_texts::revert_to_saved());
}

button buttons::revert_to_saved(const drawing::point& location) {
  return button::create(system_texts::revert_to_saved(), location);
}

button buttons::revert_to_saved(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::revert_to_saved(), location, size);
}

button buttons::revert_to_saved(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::revert_to_saved(), location, size, name);
}

button buttons::revert_to_saved(const control& parent) {
  return button::create(parent, system_texts::revert_to_saved());
}

button buttons::revert_to_saved(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::revert_to_saved(), location);
}

button buttons::revert_to_saved(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::revert_to_saved(), location, size);
}

button buttons::revert_to_saved(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::revert_to_saved(), location, size, name);
}

button buttons::save() {
  return button::create(system_texts::save());
}

button buttons::save(const drawing::point& location) {
  return button::create(system_texts::save(), location);
}

button buttons::save(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::save(), location, size);
}

button buttons::save(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::save(), location, size, name);
}

button buttons::save(const control& parent) {
  return button::create(parent, system_texts::save());
}

button buttons::save(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::save(), location);
}

button buttons::save(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::save(), location, size);
}

button buttons::save(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::save(), location, size, name);
}

button buttons::save_as() {
  return button::create(system_texts::save_as());
}

button buttons::save_as(const drawing::point& location) {
  return button::create(system_texts::save_as(), location);
}

button buttons::save_as(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::save_as(), location, size);
}

button buttons::save_as(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::save_as(), location, size, name);
}

button buttons::save_as(const control& parent) {
  return button::create(parent, system_texts::save_as());
}

button buttons::save_as(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::save_as(), location);
}

button buttons::save_as(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::save_as(), location, size);
}

button buttons::save_as(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::save_as(), location, size, name);
}

button buttons::search() {
  return button::create(system_texts::search());
}

button buttons::search(const drawing::point& location) {
  return button::create(system_texts::search(), location);
}

button buttons::search(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::search(), location, size);
}

button buttons::search(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::search(), location, size, name);
}

button buttons::search(const control& parent) {
  return button::create(parent, system_texts::search());
}

button buttons::search(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::search(), location);
}

button buttons::search(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::search(), location, size);
}

button buttons::search(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::search(), location, size, name);
}

button buttons::select_all() {
  return button::create(system_texts::select_all());
}

button buttons::select_all(const drawing::point& location) {
  return button::create(system_texts::select_all(), location);
}

button buttons::select_all(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::select_all(), location, size);
}

button buttons::select_all(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::select_all(), location, size, name);
}

button buttons::select_all(const control& parent) {
  return button::create(parent, system_texts::select_all());
}

button buttons::select_all(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::select_all(), location);
}

button buttons::select_all(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::select_all(), location, size);
}

button buttons::select_all(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::select_all(), location, size, name);
}

button buttons::settings() {
  return button::create(system_texts::settings());
}

button buttons::settings(const drawing::point& location) {
  return button::create(system_texts::settings(), location);
}

button buttons::settings(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::settings(), location, size);
}

button buttons::settings(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::settings(), location, size, name);
}

button buttons::settings(const control& parent) {
  return button::create(parent, system_texts::settings());
}

button buttons::settings(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::settings(), location);
}

button buttons::settings(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::settings(), location, size);
}

button buttons::settings(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::settings(), location, size, name);
}

button buttons::spell_check() {
  return button::create(system_texts::spell_check());
}

button buttons::spell_check(const drawing::point& location) {
  return button::create(system_texts::spell_check(), location);
}

button buttons::spell_check(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::spell_check(), location, size);
}

button buttons::spell_check(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::spell_check(), location, size, name);
}

button buttons::spell_check(const control& parent) {
  return button::create(parent, system_texts::spell_check());
}

button buttons::spell_check(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::spell_check(), location);
}

button buttons::spell_check(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::spell_check(), location, size);
}

button buttons::spell_check(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::spell_check(), location, size, name);
}

button buttons::stop() {
  return button::create(system_texts::stop());
}

button buttons::stop(const drawing::point& location) {
  return button::create(system_texts::stop(), location);
}

button buttons::stop(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::stop(), location, size);
}

button buttons::stop(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::stop(), location, size, name);
}

button buttons::stop(const control& parent) {
  return button::create(parent, system_texts::stop());
}

button buttons::stop(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::stop(), location);
}

button buttons::stop(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::stop(), location, size);
}

button buttons::stop(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::stop(), location, size, name);
}

button buttons::strikeout() {
  return button::create(system_texts::strikeout());
}

button buttons::strikeout(const drawing::point& location) {
  return button::create(system_texts::strikeout(), location);
}

button buttons::strikeout(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::strikeout(), location, size);
}

button buttons::strikeout(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::strikeout(), location, size, name);
}

button buttons::strikeout(const control& parent) {
  return button::create(parent, system_texts::strikeout());
}

button buttons::strikeout(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::strikeout(), location);
}

button buttons::strikeout(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::strikeout(), location, size);
}

button buttons::strikeout(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::strikeout(), location, size, name);
}

button buttons::tools() {
  return button::create(system_texts::tools());
}

button buttons::tools(const drawing::point& location) {
  return button::create(system_texts::tools(), location);
}

button buttons::tools(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::tools(), location, size);
}

button buttons::tools(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::tools(), location, size, name);
}

button buttons::tools(const control& parent) {
  return button::create(parent, system_texts::tools());
}

button buttons::tools(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::tools(), location);
}

button buttons::tools(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::tools(), location, size);
}

button buttons::tools(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::tools(), location, size, name);
}

button buttons::top() {
  return button::create(system_texts::top());
}

button buttons::top(const drawing::point& location) {
  return button::create(system_texts::top(), location);
}

button buttons::top(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::top(), location, size);
}

button buttons::top(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::top(), location, size, name);
}

button buttons::top(const control& parent) {
  return button::create(parent, system_texts::top());
}

button buttons::top(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::top(), location);
}

button buttons::top(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::top(), location, size);
}

button buttons::top(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::top(), location, size, name);
}

button buttons::undelete() {
  return button::create(system_texts::undelete());
}

button buttons::undelete(const drawing::point& location) {
  return button::create(system_texts::undelete(), location);
}

button buttons::undelete(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::undelete(), location, size);
}

button buttons::undelete(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::undelete(), location, size, name);
}

button buttons::undelete(const control& parent) {
  return button::create(parent, system_texts::undelete());
}

button buttons::undelete(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::undelete(), location);
}

button buttons::undelete(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::undelete(), location, size);
}

button buttons::undelete(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::undelete(), location, size, name);
}

button buttons::underline() {
  return button::create(system_texts::underline());
}

button buttons::underline(const drawing::point& location) {
  return button::create(system_texts::underline(), location);
}

button buttons::underline(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::underline(), location, size);
}

button buttons::underline(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::underline(), location, size, name);
}

button buttons::underline(const control& parent) {
  return button::create(parent, system_texts::underline());
}

button buttons::underline(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::underline(), location);
}

button buttons::underline(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::underline(), location, size);
}

button buttons::underline(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::underline(), location, size, name);
}

button buttons::undo() {
  return button::create(system_texts::undo());
}

button buttons::undo(const drawing::point& location) {
  return button::create(system_texts::undo(), location);
}

button buttons::undo(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::undo(), location, size);
}

button buttons::undo(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::undo(), location, size, name);
}

button buttons::undo(const control& parent) {
  return button::create(parent, system_texts::undo());
}

button buttons::undo(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::undo(), location);
}

button buttons::undo(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::undo(), location, size);
}

button buttons::undo(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::undo(), location, size, name);
}

button buttons::unindent() {
  return button::create(system_texts::unindent());
}

button buttons::unindent(const drawing::point& location) {
  return button::create(system_texts::unindent(), location);
}

button buttons::unindent(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::unindent(), location, size);
}

button buttons::unindent(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::unindent(), location, size, name);
}

button buttons::unindent(const control& parent) {
  return button::create(parent, system_texts::unindent());
}

button buttons::unindent(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::unindent(), location);
}

button buttons::unindent(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::unindent(), location, size);
}

button buttons::unindent(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::unindent(), location, size, name);
}

button buttons::up() {
  return button::create(system_texts::up());
}

button buttons::up(const drawing::point& location) {
  return button::create(system_texts::up(), location);
}

button buttons::up(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::up(), location, size);
}

button buttons::up(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::up(), location, size, name);
}

button buttons::up(const control& parent) {
  return button::create(parent, system_texts::up());
}

button buttons::up(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::up(), location);
}

button buttons::up(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::up(), location, size);
}

button buttons::up(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::up(), location, size, name);
}

button buttons::view() {
  return button::create(system_texts::view());
}

button buttons::view(const drawing::point& location) {
  return button::create(system_texts::view(), location);
}

button buttons::view(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::view(), location, size);
}

button buttons::view(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::view(), location, size, name);
}

button buttons::view(const control& parent) {
  return button::create(parent, system_texts::view());
}

button buttons::view(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::view(), location);
}

button buttons::view(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::view(), location, size);
}

button buttons::view(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::view(), location, size, name);
}

button buttons::window() {
  return button::create(system_texts::window());
}

button buttons::window(const drawing::point& location) {
  return button::create(system_texts::window(), location);
}

button buttons::window(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::window(), location, size);
}

button buttons::window(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::window(), location, size, name);
}

button buttons::window(const control& parent) {
  return button::create(parent, system_texts::window());
}

button buttons::window(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::window(), location);
}

button buttons::window(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::window(), location, size);
}

button buttons::window(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::window(), location, size, name);
}

button buttons::yes() {
  return button::create(system_texts::yes());
}

button buttons::yes(const drawing::point& location) {
  return button::create(system_texts::yes(), location);
}

button buttons::yes(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::yes(), location, size);
}

button buttons::yes(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::yes(), location, size, name);
}

button buttons::yes(const control& parent) {
  return button::create(parent, system_texts::yes());
}

button buttons::yes(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::yes(), location);
}

button buttons::yes(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::yes(), location, size);
}

button buttons::yes(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::yes(), location, size, name);
}

button buttons::zoom_in() {
  return button::create(system_texts::zoom_in());
}

button buttons::zoom_in(const drawing::point& location) {
  return button::create(system_texts::zoom_in(), location);
}

button buttons::zoom_in(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::zoom_in(), location, size);
}

button buttons::zoom_in(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::zoom_in(), location, size, name);
}

button buttons::zoom_in(const control& parent) {
  return button::create(parent, system_texts::zoom_in());
}

button buttons::zoom_in(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::zoom_in(), location);
}

button buttons::zoom_in(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::zoom_in(), location, size);
}

button buttons::zoom_in(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::zoom_in(), location, size, name);
}

button buttons::zoom_out() {
  return button::create(system_texts::zoom_out());
}

button buttons::zoom_out(const drawing::point& location) {
  return button::create(system_texts::zoom_out(), location);
}

button buttons::zoom_out(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::zoom_out(), location, size);
}

button buttons::zoom_out(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::zoom_out(), location, size, name);
}

button buttons::zoom_out(const control& parent) {
  return button::create(parent, system_texts::zoom_out());
}

button buttons::zoom_out(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::zoom_out(), location);
}

button buttons::zoom_out(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::zoom_out(), location, size);
}

button buttons::zoom_out(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::zoom_out(), location, size, name);
}

button buttons::zoom_to_fit() {
  return button::create(system_texts::zoom_to_fit());
}

button buttons::zoom_to_fit(const drawing::point& location) {
  return button::create(system_texts::zoom_to_fit(), location);
}

button buttons::zoom_to_fit(const drawing::point& location, const drawing::size& size) {
  return button::create(system_texts::zoom_to_fit(), location, size);
}

button buttons::zoom_to_fit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(system_texts::zoom_to_fit(), location, size, name);
}

button buttons::zoom_to_fit(const control& parent) {
  return button::create(parent, system_texts::zoom_to_fit());
}

button buttons::zoom_to_fit(const control& parent, const drawing::point& location) {
  return button::create(parent, system_texts::zoom_to_fit(), location);
}

button buttons::zoom_to_fit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return button::create(parent, system_texts::zoom_to_fit(), location, size);
}

button buttons::zoom_to_fit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return button::create(parent, system_texts::zoom_to_fit(), location, size, name);
}
