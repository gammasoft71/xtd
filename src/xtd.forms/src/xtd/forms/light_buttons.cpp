#include "../../../include/xtd/forms/light_buttons.h"
#include "../../../include/xtd/forms/system_texts.h"

using namespace xtd;
using namespace xtd::forms;

light_button light_buttons::abort() {
  return light_button::create(system_texts::abort());
}

light_button light_buttons::abort(const drawing::point& location) {
  return light_button::create(system_texts::abort(), location);
}

light_button light_buttons::abort(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::abort(), location, size);
}

light_button light_buttons::abort(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::abort(), location, size, name);
}

light_button light_buttons::abort(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::abort(), check_state);
}

light_button light_buttons::abort(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::abort(), check_state, location);
}

light_button light_buttons::abort(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::abort(), check_state, location, size);
}

light_button light_buttons::abort(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::abort(), check_state, location, size, name);
}

light_button light_buttons::abort(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::abort(), three_state, check_state);
}

light_button light_buttons::abort(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::abort(), three_state, check_state, location);
}

light_button light_buttons::abort(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::abort(), three_state, check_state, location, size);
}

light_button light_buttons::abort(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::abort(), three_state, check_state, location, size, name);
}

light_button light_buttons::abort(const control& parent) {
  return light_button::create(parent, system_texts::abort());
}

light_button light_buttons::abort(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::abort(), location);
}

light_button light_buttons::abort(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::abort(), location, size);
}

light_button light_buttons::abort(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::abort(), location, size, name);
}

light_button light_buttons::abort(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::abort(), check_state);
}

light_button light_buttons::abort(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::abort(), check_state, location);
}

light_button light_buttons::abort(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::abort(), check_state, location, size);
}

light_button light_buttons::abort(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::abort(), check_state, location, size, name);
}

light_button light_buttons::abort(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::abort(), three_state, check_state);
}

light_button light_buttons::abort(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::abort(), three_state, check_state, location);
}

light_button light_buttons::abort(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::abort(), three_state, check_state, location, size);
}

light_button light_buttons::abort(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::abort(), three_state, check_state, location, size, name);
}

light_button light_buttons::about() {
  return light_button::create(system_texts::about());
}

light_button light_buttons::about(const drawing::point& location) {
  return light_button::create(system_texts::about(), location);
}

light_button light_buttons::about(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::about(), location, size);
}

light_button light_buttons::about(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::about(), location, size, name);
}

light_button light_buttons::about(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::about(), check_state);
}

light_button light_buttons::about(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::about(), check_state, location);
}

light_button light_buttons::about(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::about(), check_state, location, size);
}

light_button light_buttons::about(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::about(), check_state, location, size, name);
}

light_button light_buttons::about(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::about(), three_state, check_state);
}

light_button light_buttons::about(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::about(), three_state, check_state, location);
}

light_button light_buttons::about(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::about(), three_state, check_state, location, size);
}

light_button light_buttons::about(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::about(), three_state, check_state, location, size, name);
}

light_button light_buttons::about(const control& parent) {
  return light_button::create(parent, system_texts::about());
}

light_button light_buttons::about(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::about(), location);
}

light_button light_buttons::about(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::about(), location, size);
}

light_button light_buttons::about(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::about(), location, size, name);
}

light_button light_buttons::about(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::about(), check_state);
}

light_button light_buttons::about(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::about(), check_state, location);
}

light_button light_buttons::about(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::about(), check_state, location, size);
}

light_button light_buttons::about(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::about(), check_state, location, size, name);
}

light_button light_buttons::about(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::about(), three_state, check_state);
}

light_button light_buttons::about(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::about(), three_state, check_state, location);
}

light_button light_buttons::about(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::about(), three_state, check_state, location, size);
}

light_button light_buttons::about(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::about(), three_state, check_state, location, size, name);
}

light_button light_buttons::actual_size() {
  return light_button::create(system_texts::actual_size());
}

light_button light_buttons::actual_size(const drawing::point& location) {
  return light_button::create(system_texts::actual_size(), location);
}

light_button light_buttons::actual_size(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::actual_size(), location, size);
}

light_button light_buttons::actual_size(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::actual_size(), location, size, name);
}

light_button light_buttons::actual_size(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::actual_size(), check_state);
}

light_button light_buttons::actual_size(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::actual_size(), check_state, location);
}

light_button light_buttons::actual_size(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::actual_size(), check_state, location, size);
}

light_button light_buttons::actual_size(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::actual_size(), check_state, location, size, name);
}

light_button light_buttons::actual_size(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::actual_size(), three_state, check_state);
}

light_button light_buttons::actual_size(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::actual_size(), three_state, check_state, location);
}

light_button light_buttons::actual_size(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::actual_size(), three_state, check_state, location, size);
}

light_button light_buttons::actual_size(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::actual_size(), three_state, check_state, location, size, name);
}

light_button light_buttons::actual_size(const control& parent) {
  return light_button::create(parent, system_texts::actual_size());
}

light_button light_buttons::actual_size(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::actual_size(), location);
}

light_button light_buttons::actual_size(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::actual_size(), location, size);
}

light_button light_buttons::actual_size(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::actual_size(), location, size, name);
}

light_button light_buttons::actual_size(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::actual_size(), check_state);
}

light_button light_buttons::actual_size(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::actual_size(), check_state, location);
}

light_button light_buttons::actual_size(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::actual_size(), check_state, location, size);
}

light_button light_buttons::actual_size(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::actual_size(), check_state, location, size, name);
}

light_button light_buttons::actual_size(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::actual_size(), three_state, check_state);
}

light_button light_buttons::actual_size(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::actual_size(), three_state, check_state, location);
}

light_button light_buttons::actual_size(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::actual_size(), three_state, check_state, location, size);
}

light_button light_buttons::actual_size(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::actual_size(), three_state, check_state, location, size, name);
}

light_button light_buttons::add() {
  return light_button::create(system_texts::add());
}

light_button light_buttons::add(const drawing::point& location) {
  return light_button::create(system_texts::add(), location);
}

light_button light_buttons::add(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::add(), location, size);
}

light_button light_buttons::add(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::add(), location, size, name);
}

light_button light_buttons::add(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::add(), check_state);
}

light_button light_buttons::add(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::add(), check_state, location);
}

light_button light_buttons::add(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::add(), check_state, location, size);
}

light_button light_buttons::add(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::add(), check_state, location, size, name);
}

light_button light_buttons::add(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::add(), three_state, check_state);
}

light_button light_buttons::add(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::add(), three_state, check_state, location);
}

light_button light_buttons::add(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::add(), three_state, check_state, location, size);
}

light_button light_buttons::add(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::add(), three_state, check_state, location, size, name);
}

light_button light_buttons::add(const control& parent) {
  return light_button::create(parent, system_texts::add());
}

light_button light_buttons::add(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::add(), location);
}

light_button light_buttons::add(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::add(), location, size);
}

light_button light_buttons::add(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::add(), location, size, name);
}

light_button light_buttons::add(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::add(), check_state);
}

light_button light_buttons::add(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::add(), check_state, location);
}

light_button light_buttons::add(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::add(), check_state, location, size);
}

light_button light_buttons::add(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::add(), check_state, location, size, name);
}

light_button light_buttons::add(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::add(), three_state, check_state);
}

light_button light_buttons::add(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::add(), three_state, check_state, location);
}

light_button light_buttons::add(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::add(), three_state, check_state, location, size);
}

light_button light_buttons::add(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::add(), three_state, check_state, location, size, name);
}

light_button light_buttons::alignment() {
  return light_button::create(system_texts::alignment());
}

light_button light_buttons::alignment(const drawing::point& location) {
  return light_button::create(system_texts::alignment(), location);
}

light_button light_buttons::alignment(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::alignment(), location, size);
}

light_button light_buttons::alignment(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::alignment(), location, size, name);
}

light_button light_buttons::alignment(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::alignment(), check_state);
}

light_button light_buttons::alignment(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::alignment(), check_state, location);
}

light_button light_buttons::alignment(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::alignment(), check_state, location, size);
}

light_button light_buttons::alignment(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::alignment(), check_state, location, size, name);
}

light_button light_buttons::alignment(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::alignment(), three_state, check_state);
}

light_button light_buttons::alignment(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::alignment(), three_state, check_state, location);
}

light_button light_buttons::alignment(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::alignment(), three_state, check_state, location, size);
}

light_button light_buttons::alignment(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::alignment(), three_state, check_state, location, size, name);
}

light_button light_buttons::alignment(const control& parent) {
  return light_button::create(parent, system_texts::alignment());
}

light_button light_buttons::alignment(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::alignment(), location);
}

light_button light_buttons::alignment(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::alignment(), location, size);
}

light_button light_buttons::alignment(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::alignment(), location, size, name);
}

light_button light_buttons::alignment(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::alignment(), check_state);
}

light_button light_buttons::alignment(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::alignment(), check_state, location);
}

light_button light_buttons::alignment(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::alignment(), check_state, location, size);
}

light_button light_buttons::alignment(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::alignment(), check_state, location, size, name);
}

light_button light_buttons::alignment(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::alignment(), three_state, check_state);
}

light_button light_buttons::alignment(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::alignment(), three_state, check_state, location);
}

light_button light_buttons::alignment(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::alignment(), three_state, check_state, location, size);
}

light_button light_buttons::alignment(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::alignment(), three_state, check_state, location, size, name);
}

light_button light_buttons::align_left() {
  return light_button::create(system_texts::align_left());
}

light_button light_buttons::align_left(const drawing::point& location) {
  return light_button::create(system_texts::align_left(), location);
}

light_button light_buttons::align_left(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::align_left(), location, size);
}

light_button light_buttons::align_left(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::align_left(), location, size, name);
}

light_button light_buttons::align_left(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::align_left(), check_state);
}

light_button light_buttons::align_left(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::align_left(), check_state, location);
}

light_button light_buttons::align_left(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::align_left(), check_state, location, size);
}

light_button light_buttons::align_left(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::align_left(), check_state, location, size, name);
}

light_button light_buttons::align_left(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::align_left(), three_state, check_state);
}

light_button light_buttons::align_left(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::align_left(), three_state, check_state, location);
}

light_button light_buttons::align_left(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::align_left(), three_state, check_state, location, size);
}

light_button light_buttons::align_left(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::align_left(), three_state, check_state, location, size, name);
}

light_button light_buttons::align_left(const control& parent) {
  return light_button::create(parent, system_texts::align_left());
}

light_button light_buttons::align_left(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::align_left(), location);
}

light_button light_buttons::align_left(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::align_left(), location, size);
}

light_button light_buttons::align_left(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::align_left(), location, size, name);
}

light_button light_buttons::align_left(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::align_left(), check_state);
}

light_button light_buttons::align_left(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::align_left(), check_state, location);
}

light_button light_buttons::align_left(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::align_left(), check_state, location, size);
}

light_button light_buttons::align_left(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::align_left(), check_state, location, size, name);
}

light_button light_buttons::align_left(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::align_left(), three_state, check_state);
}

light_button light_buttons::align_left(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::align_left(), three_state, check_state, location);
}

light_button light_buttons::align_left(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::align_left(), three_state, check_state, location, size);
}

light_button light_buttons::align_left(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::align_left(), three_state, check_state, location, size, name);
}

light_button light_buttons::align_right() {
  return light_button::create(system_texts::align_right());
}

light_button light_buttons::align_right(const drawing::point& location) {
  return light_button::create(system_texts::align_right(), location);
}

light_button light_buttons::align_right(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::align_right(), location, size);
}

light_button light_buttons::align_right(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::align_right(), location, size, name);
}

light_button light_buttons::align_right(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::align_right(), check_state);
}

light_button light_buttons::align_right(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::align_right(), check_state, location);
}

light_button light_buttons::align_right(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::align_right(), check_state, location, size);
}

light_button light_buttons::align_right(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::align_right(), check_state, location, size, name);
}

light_button light_buttons::align_right(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::align_right(), three_state, check_state);
}

light_button light_buttons::align_right(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::align_right(), three_state, check_state, location);
}

light_button light_buttons::align_right(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::align_right(), three_state, check_state, location, size);
}

light_button light_buttons::align_right(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::align_right(), three_state, check_state, location, size, name);
}

light_button light_buttons::align_right(const control& parent) {
  return light_button::create(parent, system_texts::align_right());
}

light_button light_buttons::align_right(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::align_right(), location);
}

light_button light_buttons::align_right(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::align_right(), location, size);
}

light_button light_buttons::align_right(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::align_right(), location, size, name);
}

light_button light_buttons::align_right(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::align_right(), check_state);
}

light_button light_buttons::align_right(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::align_right(), check_state, location);
}

light_button light_buttons::align_right(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::align_right(), check_state, location, size);
}

light_button light_buttons::align_right(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::align_right(), check_state, location, size, name);
}

light_button light_buttons::align_right(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::align_right(), three_state, check_state);
}

light_button light_buttons::align_right(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::align_right(), three_state, check_state, location);
}

light_button light_buttons::align_right(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::align_right(), three_state, check_state, location, size);
}

light_button light_buttons::align_right(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::align_right(), three_state, check_state, location, size, name);
}

light_button light_buttons::apply() {
  return light_button::create(system_texts::apply());
}

light_button light_buttons::apply(const drawing::point& location) {
  return light_button::create(system_texts::apply(), location);
}

light_button light_buttons::apply(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::apply(), location, size);
}

light_button light_buttons::apply(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::apply(), location, size, name);
}

light_button light_buttons::apply(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::apply(), check_state);
}

light_button light_buttons::apply(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::apply(), check_state, location);
}

light_button light_buttons::apply(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::apply(), check_state, location, size);
}

light_button light_buttons::apply(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::apply(), check_state, location, size, name);
}

light_button light_buttons::apply(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::apply(), three_state, check_state);
}

light_button light_buttons::apply(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::apply(), three_state, check_state, location);
}

light_button light_buttons::apply(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::apply(), three_state, check_state, location, size);
}

light_button light_buttons::apply(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::apply(), three_state, check_state, location, size, name);
}

light_button light_buttons::apply(const control& parent) {
  return light_button::create(parent, system_texts::apply());
}

light_button light_buttons::apply(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::apply(), location);
}

light_button light_buttons::apply(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::apply(), location, size);
}

light_button light_buttons::apply(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::apply(), location, size, name);
}

light_button light_buttons::apply(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::apply(), check_state);
}

light_button light_buttons::apply(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::apply(), check_state, location);
}

light_button light_buttons::apply(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::apply(), check_state, location, size);
}

light_button light_buttons::apply(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::apply(), check_state, location, size, name);
}

light_button light_buttons::apply(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::apply(), three_state, check_state);
}

light_button light_buttons::apply(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::apply(), three_state, check_state, location);
}

light_button light_buttons::apply(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::apply(), three_state, check_state, location, size);
}

light_button light_buttons::apply(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::apply(), three_state, check_state, location, size, name);
}

light_button light_buttons::ascending() {
  return light_button::create(system_texts::ascending());
}

light_button light_buttons::ascending(const drawing::point& location) {
  return light_button::create(system_texts::ascending(), location);
}

light_button light_buttons::ascending(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::ascending(), location, size);
}

light_button light_buttons::ascending(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::ascending(), location, size, name);
}

light_button light_buttons::ascending(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::ascending(), check_state);
}

light_button light_buttons::ascending(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::ascending(), check_state, location);
}

light_button light_buttons::ascending(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::ascending(), check_state, location, size);
}

light_button light_buttons::ascending(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::ascending(), check_state, location, size, name);
}

light_button light_buttons::ascending(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::ascending(), three_state, check_state);
}

light_button light_buttons::ascending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::ascending(), three_state, check_state, location);
}

light_button light_buttons::ascending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::ascending(), three_state, check_state, location, size);
}

light_button light_buttons::ascending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::ascending(), three_state, check_state, location, size, name);
}

light_button light_buttons::ascending(const control& parent) {
  return light_button::create(parent, system_texts::ascending());
}

light_button light_buttons::ascending(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::ascending(), location);
}

light_button light_buttons::ascending(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::ascending(), location, size);
}

light_button light_buttons::ascending(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::ascending(), location, size, name);
}

light_button light_buttons::ascending(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::ascending(), check_state);
}

light_button light_buttons::ascending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::ascending(), check_state, location);
}

light_button light_buttons::ascending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::ascending(), check_state, location, size);
}

light_button light_buttons::ascending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::ascending(), check_state, location, size, name);
}

light_button light_buttons::ascending(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::ascending(), three_state, check_state);
}

light_button light_buttons::ascending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::ascending(), three_state, check_state, location);
}

light_button light_buttons::ascending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::ascending(), three_state, check_state, location, size);
}

light_button light_buttons::ascending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::ascending(), three_state, check_state, location, size, name);
}

light_button light_buttons::back() {
  return light_button::create(system_texts::back());
}

light_button light_buttons::back(const drawing::point& location) {
  return light_button::create(system_texts::back(), location);
}

light_button light_buttons::back(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::back(), location, size);
}

light_button light_buttons::back(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::back(), location, size, name);
}

light_button light_buttons::back(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::back(), check_state);
}

light_button light_buttons::back(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::back(), check_state, location);
}

light_button light_buttons::back(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::back(), check_state, location, size);
}

light_button light_buttons::back(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::back(), check_state, location, size, name);
}

light_button light_buttons::back(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::back(), three_state, check_state);
}

light_button light_buttons::back(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::back(), three_state, check_state, location);
}

light_button light_buttons::back(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::back(), three_state, check_state, location, size);
}

light_button light_buttons::back(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::back(), three_state, check_state, location, size, name);
}

light_button light_buttons::back(const control& parent) {
  return light_button::create(parent, system_texts::back());
}

light_button light_buttons::back(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::back(), location);
}

light_button light_buttons::back(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::back(), location, size);
}

light_button light_buttons::back(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::back(), location, size, name);
}

light_button light_buttons::back(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::back(), check_state);
}

light_button light_buttons::back(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::back(), check_state, location);
}

light_button light_buttons::back(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::back(), check_state, location, size);
}

light_button light_buttons::back(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::back(), check_state, location, size, name);
}

light_button light_buttons::back(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::back(), three_state, check_state);
}

light_button light_buttons::back(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::back(), three_state, check_state, location);
}

light_button light_buttons::back(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::back(), three_state, check_state, location, size);
}

light_button light_buttons::back(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::back(), three_state, check_state, location, size, name);
}

light_button light_buttons::bold() {
  return light_button::create(system_texts::bold());
}

light_button light_buttons::bold(const drawing::point& location) {
  return light_button::create(system_texts::bold(), location);
}

light_button light_buttons::bold(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::bold(), location, size);
}

light_button light_buttons::bold(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::bold(), location, size, name);
}

light_button light_buttons::bold(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::bold(), check_state);
}

light_button light_buttons::bold(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::bold(), check_state, location);
}

light_button light_buttons::bold(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::bold(), check_state, location, size);
}

light_button light_buttons::bold(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::bold(), check_state, location, size, name);
}

light_button light_buttons::bold(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::bold(), three_state, check_state);
}

light_button light_buttons::bold(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::bold(), three_state, check_state, location);
}

light_button light_buttons::bold(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::bold(), three_state, check_state, location, size);
}

light_button light_buttons::bold(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::bold(), three_state, check_state, location, size, name);
}

light_button light_buttons::bold(const control& parent) {
  return light_button::create(parent, system_texts::bold());
}

light_button light_buttons::bold(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::bold(), location);
}

light_button light_buttons::bold(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::bold(), location, size);
}

light_button light_buttons::bold(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::bold(), location, size, name);
}

light_button light_buttons::bold(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::bold(), check_state);
}

light_button light_buttons::bold(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::bold(), check_state, location);
}

light_button light_buttons::bold(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::bold(), check_state, location, size);
}

light_button light_buttons::bold(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::bold(), check_state, location, size, name);
}

light_button light_buttons::bold(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::bold(), three_state, check_state);
}

light_button light_buttons::bold(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::bold(), three_state, check_state, location);
}

light_button light_buttons::bold(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::bold(), three_state, check_state, location, size);
}

light_button light_buttons::bold(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::bold(), three_state, check_state, location, size, name);
}

light_button light_buttons::bottom() {
  return light_button::create(system_texts::bottom());
}

light_button light_buttons::bottom(const drawing::point& location) {
  return light_button::create(system_texts::bottom(), location);
}

light_button light_buttons::bottom(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::bottom(), location, size);
}

light_button light_buttons::bottom(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::bottom(), location, size, name);
}

light_button light_buttons::bottom(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::bottom(), check_state);
}

light_button light_buttons::bottom(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::bottom(), check_state, location);
}

light_button light_buttons::bottom(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::bottom(), check_state, location, size);
}

light_button light_buttons::bottom(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::bottom(), check_state, location, size, name);
}

light_button light_buttons::bottom(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::bottom(), three_state, check_state);
}

light_button light_buttons::bottom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::bottom(), three_state, check_state, location);
}

light_button light_buttons::bottom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::bottom(), three_state, check_state, location, size);
}

light_button light_buttons::bottom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::bottom(), three_state, check_state, location, size, name);
}

light_button light_buttons::bottom(const control& parent) {
  return light_button::create(parent, system_texts::bottom());
}

light_button light_buttons::bottom(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::bottom(), location);
}

light_button light_buttons::bottom(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::bottom(), location, size);
}

light_button light_buttons::bottom(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::bottom(), location, size, name);
}

light_button light_buttons::bottom(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::bottom(), check_state);
}

light_button light_buttons::bottom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::bottom(), check_state, location);
}

light_button light_buttons::bottom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::bottom(), check_state, location, size);
}

light_button light_buttons::bottom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::bottom(), check_state, location, size, name);
}

light_button light_buttons::bottom(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::bottom(), three_state, check_state);
}

light_button light_buttons::bottom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::bottom(), three_state, check_state, location);
}

light_button light_buttons::bottom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::bottom(), three_state, check_state, location, size);
}

light_button light_buttons::bottom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::bottom(), three_state, check_state, location, size, name);
}

light_button light_buttons::cancel() {
  return light_button::create(system_texts::cancel());
}

light_button light_buttons::cancel(const drawing::point& location) {
  return light_button::create(system_texts::cancel(), location);
}

light_button light_buttons::cancel(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::cancel(), location, size);
}

light_button light_buttons::cancel(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::cancel(), location, size, name);
}

light_button light_buttons::cancel(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::cancel(), check_state);
}

light_button light_buttons::cancel(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::cancel(), check_state, location);
}

light_button light_buttons::cancel(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::cancel(), check_state, location, size);
}

light_button light_buttons::cancel(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::cancel(), check_state, location, size, name);
}

light_button light_buttons::cancel(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::cancel(), three_state, check_state);
}

light_button light_buttons::cancel(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::cancel(), three_state, check_state, location);
}

light_button light_buttons::cancel(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::cancel(), three_state, check_state, location, size);
}

light_button light_buttons::cancel(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::cancel(), three_state, check_state, location, size, name);
}

light_button light_buttons::cancel(const control& parent) {
  return light_button::create(parent, system_texts::cancel());
}

light_button light_buttons::cancel(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::cancel(), location);
}

light_button light_buttons::cancel(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::cancel(), location, size);
}

light_button light_buttons::cancel(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::cancel(), location, size, name);
}

light_button light_buttons::cancel(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::cancel(), check_state);
}

light_button light_buttons::cancel(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::cancel(), check_state, location);
}

light_button light_buttons::cancel(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::cancel(), check_state, location, size);
}

light_button light_buttons::cancel(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::cancel(), check_state, location, size, name);
}

light_button light_buttons::cancel(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::cancel(), three_state, check_state);
}

light_button light_buttons::cancel(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::cancel(), three_state, check_state, location);
}

light_button light_buttons::cancel(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::cancel(), three_state, check_state, location, size);
}

light_button light_buttons::cancel(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::cancel(), three_state, check_state, location, size, name);
}

light_button light_buttons::cdrom() {
  return light_button::create(system_texts::cdrom());
}

light_button light_buttons::cdrom(const drawing::point& location) {
  return light_button::create(system_texts::cdrom(), location);
}

light_button light_buttons::cdrom(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::cdrom(), location, size);
}

light_button light_buttons::cdrom(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::cdrom(), location, size, name);
}

light_button light_buttons::cdrom(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::cdrom(), check_state);
}

light_button light_buttons::cdrom(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::cdrom(), check_state, location);
}

light_button light_buttons::cdrom(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::cdrom(), check_state, location, size);
}

light_button light_buttons::cdrom(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::cdrom(), check_state, location, size, name);
}

light_button light_buttons::cdrom(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::cdrom(), three_state, check_state);
}

light_button light_buttons::cdrom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::cdrom(), three_state, check_state, location);
}

light_button light_buttons::cdrom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::cdrom(), three_state, check_state, location, size);
}

light_button light_buttons::cdrom(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::cdrom(), three_state, check_state, location, size, name);
}

light_button light_buttons::cdrom(const control& parent) {
  return light_button::create(parent, system_texts::cdrom());
}

light_button light_buttons::cdrom(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::cdrom(), location);
}

light_button light_buttons::cdrom(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::cdrom(), location, size);
}

light_button light_buttons::cdrom(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::cdrom(), location, size, name);
}

light_button light_buttons::cdrom(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::cdrom(), check_state);
}

light_button light_buttons::cdrom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::cdrom(), check_state, location);
}

light_button light_buttons::cdrom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::cdrom(), check_state, location, size);
}

light_button light_buttons::cdrom(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::cdrom(), check_state, location, size, name);
}

light_button light_buttons::cdrom(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::cdrom(), three_state, check_state);
}

light_button light_buttons::cdrom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::cdrom(), three_state, check_state, location);
}

light_button light_buttons::cdrom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::cdrom(), three_state, check_state, location, size);
}

light_button light_buttons::cdrom(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::cdrom(), three_state, check_state, location, size, name);
}

light_button light_buttons::centered() {
  return light_button::create(system_texts::centered());
}

light_button light_buttons::centered(const drawing::point& location) {
  return light_button::create(system_texts::centered(), location);
}

light_button light_buttons::centered(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::centered(), location, size);
}

light_button light_buttons::centered(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::centered(), location, size, name);
}

light_button light_buttons::centered(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::centered(), check_state);
}

light_button light_buttons::centered(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::centered(), check_state, location);
}

light_button light_buttons::centered(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::centered(), check_state, location, size);
}

light_button light_buttons::centered(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::centered(), check_state, location, size, name);
}

light_button light_buttons::centered(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::centered(), three_state, check_state);
}

light_button light_buttons::centered(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::centered(), three_state, check_state, location);
}

light_button light_buttons::centered(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::centered(), three_state, check_state, location, size);
}

light_button light_buttons::centered(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::centered(), three_state, check_state, location, size, name);
}

light_button light_buttons::centered(const control& parent) {
  return light_button::create(parent, system_texts::centered());
}

light_button light_buttons::centered(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::centered(), location);
}

light_button light_buttons::centered(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::centered(), location, size);
}

light_button light_buttons::centered(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::centered(), location, size, name);
}

light_button light_buttons::centered(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::centered(), check_state);
}

light_button light_buttons::centered(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::centered(), check_state, location);
}

light_button light_buttons::centered(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::centered(), check_state, location, size);
}

light_button light_buttons::centered(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::centered(), check_state, location, size, name);
}

light_button light_buttons::centered(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::centered(), three_state, check_state);
}

light_button light_buttons::centered(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::centered(), three_state, check_state, location);
}

light_button light_buttons::centered(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::centered(), three_state, check_state, location, size);
}

light_button light_buttons::centered(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::centered(), three_state, check_state, location, size, name);
}

light_button light_buttons::clear() {
  return light_button::create(system_texts::clear());
}

light_button light_buttons::clear(const drawing::point& location) {
  return light_button::create(system_texts::clear(), location);
}

light_button light_buttons::clear(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::clear(), location, size);
}

light_button light_buttons::clear(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::clear(), location, size, name);
}

light_button light_buttons::clear(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::clear(), check_state);
}

light_button light_buttons::clear(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::clear(), check_state, location);
}

light_button light_buttons::clear(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::clear(), check_state, location, size);
}

light_button light_buttons::clear(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::clear(), check_state, location, size, name);
}

light_button light_buttons::clear(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::clear(), three_state, check_state);
}

light_button light_buttons::clear(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::clear(), three_state, check_state, location);
}

light_button light_buttons::clear(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::clear(), three_state, check_state, location, size);
}

light_button light_buttons::clear(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::clear(), three_state, check_state, location, size, name);
}

light_button light_buttons::clear(const control& parent) {
  return light_button::create(parent, system_texts::clear());
}

light_button light_buttons::clear(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::clear(), location);
}

light_button light_buttons::clear(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::clear(), location, size);
}

light_button light_buttons::clear(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::clear(), location, size, name);
}

light_button light_buttons::clear(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::clear(), check_state);
}

light_button light_buttons::clear(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::clear(), check_state, location);
}

light_button light_buttons::clear(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::clear(), check_state, location, size);
}

light_button light_buttons::clear(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::clear(), check_state, location, size, name);
}

light_button light_buttons::clear(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::clear(), three_state, check_state);
}

light_button light_buttons::clear(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::clear(), three_state, check_state, location);
}

light_button light_buttons::clear(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::clear(), three_state, check_state, location, size);
}

light_button light_buttons::clear(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::clear(), three_state, check_state, location, size, name);
}

light_button light_buttons::close() {
  return light_button::create(system_texts::close());
}

light_button light_buttons::close(const drawing::point& location) {
  return light_button::create(system_texts::close(), location);
}

light_button light_buttons::close(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::close(), location, size);
}

light_button light_buttons::close(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::close(), location, size, name);
}

light_button light_buttons::close(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::close(), check_state);
}

light_button light_buttons::close(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::close(), check_state, location);
}

light_button light_buttons::close(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::close(), check_state, location, size);
}

light_button light_buttons::close(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::close(), check_state, location, size, name);
}

light_button light_buttons::close(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::close(), three_state, check_state);
}

light_button light_buttons::close(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::close(), three_state, check_state, location);
}

light_button light_buttons::close(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::close(), three_state, check_state, location, size);
}

light_button light_buttons::close(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::close(), three_state, check_state, location, size, name);
}

light_button light_buttons::close(const control& parent) {
  return light_button::create(parent, system_texts::close());
}

light_button light_buttons::close(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::close(), location);
}

light_button light_buttons::close(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::close(), location, size);
}

light_button light_buttons::close(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::close(), location, size, name);
}

light_button light_buttons::close(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::close(), check_state);
}

light_button light_buttons::close(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::close(), check_state, location);
}

light_button light_buttons::close(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::close(), check_state, location, size);
}

light_button light_buttons::close(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::close(), check_state, location, size, name);
}

light_button light_buttons::close(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::close(), three_state, check_state);
}

light_button light_buttons::close(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::close(), three_state, check_state, location);
}

light_button light_buttons::close(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::close(), three_state, check_state, location, size);
}

light_button light_buttons::close(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::close(), three_state, check_state, location, size, name);
}

light_button light_buttons::color() {
  return light_button::create(system_texts::color());
}

light_button light_buttons::color(const drawing::point& location) {
  return light_button::create(system_texts::color(), location);
}

light_button light_buttons::color(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::color(), location, size);
}

light_button light_buttons::color(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::color(), location, size, name);
}

light_button light_buttons::color(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::color(), check_state);
}

light_button light_buttons::color(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::color(), check_state, location);
}

light_button light_buttons::color(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::color(), check_state, location, size);
}

light_button light_buttons::color(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::color(), check_state, location, size, name);
}

light_button light_buttons::color(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::color(), three_state, check_state);
}

light_button light_buttons::color(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::color(), three_state, check_state, location);
}

light_button light_buttons::color(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::color(), three_state, check_state, location, size);
}

light_button light_buttons::color(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::color(), three_state, check_state, location, size, name);
}

light_button light_buttons::color(const control& parent) {
  return light_button::create(parent, system_texts::color());
}

light_button light_buttons::color(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::color(), location);
}

light_button light_buttons::color(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::color(), location, size);
}

light_button light_buttons::color(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::color(), location, size, name);
}

light_button light_buttons::color(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::color(), check_state);
}

light_button light_buttons::color(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::color(), check_state, location);
}

light_button light_buttons::color(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::color(), check_state, location, size);
}

light_button light_buttons::color(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::color(), check_state, location, size, name);
}

light_button light_buttons::color(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::color(), three_state, check_state);
}

light_button light_buttons::color(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::color(), three_state, check_state, location);
}

light_button light_buttons::color(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::color(), three_state, check_state, location, size);
}

light_button light_buttons::color(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::color(), three_state, check_state, location, size, name);
}

light_button light_buttons::contents() {
  return light_button::create(system_texts::contents());
}

light_button light_buttons::contents(const drawing::point& location) {
  return light_button::create(system_texts::contents(), location);
}

light_button light_buttons::contents(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::contents(), location, size);
}

light_button light_buttons::contents(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::contents(), location, size, name);
}

light_button light_buttons::contents(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::contents(), check_state);
}

light_button light_buttons::contents(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::contents(), check_state, location);
}

light_button light_buttons::contents(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::contents(), check_state, location, size);
}

light_button light_buttons::contents(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::contents(), check_state, location, size, name);
}

light_button light_buttons::contents(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::contents(), three_state, check_state);
}

light_button light_buttons::contents(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::contents(), three_state, check_state, location);
}

light_button light_buttons::contents(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::contents(), three_state, check_state, location, size);
}

light_button light_buttons::contents(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::contents(), three_state, check_state, location, size, name);
}

light_button light_buttons::contents(const control& parent) {
  return light_button::create(parent, system_texts::contents());
}

light_button light_buttons::contents(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::contents(), location);
}

light_button light_buttons::contents(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::contents(), location, size);
}

light_button light_buttons::contents(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::contents(), location, size, name);
}

light_button light_buttons::contents(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::contents(), check_state);
}

light_button light_buttons::contents(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::contents(), check_state, location);
}

light_button light_buttons::contents(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::contents(), check_state, location, size);
}

light_button light_buttons::contents(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::contents(), check_state, location, size, name);
}

light_button light_buttons::contents(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::contents(), three_state, check_state);
}

light_button light_buttons::contents(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::contents(), three_state, check_state, location);
}

light_button light_buttons::contents(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::contents(), three_state, check_state, location, size);
}

light_button light_buttons::contents(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::contents(), three_state, check_state, location, size, name);
}

light_button light_buttons::convert() {
  return light_button::create(system_texts::convert());
}

light_button light_buttons::convert(const drawing::point& location) {
  return light_button::create(system_texts::convert(), location);
}

light_button light_buttons::convert(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::convert(), location, size);
}

light_button light_buttons::convert(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::convert(), location, size, name);
}

light_button light_buttons::convert(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::convert(), check_state);
}

light_button light_buttons::convert(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::convert(), check_state, location);
}

light_button light_buttons::convert(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::convert(), check_state, location, size);
}

light_button light_buttons::convert(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::convert(), check_state, location, size, name);
}

light_button light_buttons::convert(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::convert(), three_state, check_state);
}

light_button light_buttons::convert(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::convert(), three_state, check_state, location);
}

light_button light_buttons::convert(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::convert(), three_state, check_state, location, size);
}

light_button light_buttons::convert(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::convert(), three_state, check_state, location, size, name);
}

light_button light_buttons::convert(const control& parent) {
  return light_button::create(parent, system_texts::convert());
}

light_button light_buttons::convert(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::convert(), location);
}

light_button light_buttons::convert(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::convert(), location, size);
}

light_button light_buttons::convert(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::convert(), location, size, name);
}

light_button light_buttons::convert(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::convert(), check_state);
}

light_button light_buttons::convert(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::convert(), check_state, location);
}

light_button light_buttons::convert(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::convert(), check_state, location, size);
}

light_button light_buttons::convert(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::convert(), check_state, location, size, name);
}

light_button light_buttons::convert(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::convert(), three_state, check_state);
}

light_button light_buttons::convert(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::convert(), three_state, check_state, location);
}

light_button light_buttons::convert(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::convert(), three_state, check_state, location, size);
}

light_button light_buttons::convert(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::convert(), three_state, check_state, location, size, name);
}

light_button light_buttons::copy() {
  return light_button::create(system_texts::copy());
}

light_button light_buttons::copy(const drawing::point& location) {
  return light_button::create(system_texts::copy(), location);
}

light_button light_buttons::copy(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::copy(), location, size);
}

light_button light_buttons::copy(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::copy(), location, size, name);
}

light_button light_buttons::copy(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::copy(), check_state);
}

light_button light_buttons::copy(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::copy(), check_state, location);
}

light_button light_buttons::copy(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::copy(), check_state, location, size);
}

light_button light_buttons::copy(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::copy(), check_state, location, size, name);
}

light_button light_buttons::copy(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::copy(), three_state, check_state);
}

light_button light_buttons::copy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::copy(), three_state, check_state, location);
}

light_button light_buttons::copy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::copy(), three_state, check_state, location, size);
}

light_button light_buttons::copy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::copy(), three_state, check_state, location, size, name);
}

light_button light_buttons::copy(const control& parent) {
  return light_button::create(parent, system_texts::copy());
}

light_button light_buttons::copy(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::copy(), location);
}

light_button light_buttons::copy(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::copy(), location, size);
}

light_button light_buttons::copy(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::copy(), location, size, name);
}

light_button light_buttons::copy(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::copy(), check_state);
}

light_button light_buttons::copy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::copy(), check_state, location);
}

light_button light_buttons::copy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::copy(), check_state, location, size);
}

light_button light_buttons::copy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::copy(), check_state, location, size, name);
}

light_button light_buttons::copy(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::copy(), three_state, check_state);
}

light_button light_buttons::copy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::copy(), three_state, check_state, location);
}

light_button light_buttons::copy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::copy(), three_state, check_state, location, size);
}

light_button light_buttons::copy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::copy(), three_state, check_state, location, size, name);
}

light_button light_buttons::customize() {
  return light_button::create(system_texts::customize());
}

light_button light_buttons::customize(const drawing::point& location) {
  return light_button::create(system_texts::customize(), location);
}

light_button light_buttons::customize(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::customize(), location, size);
}

light_button light_buttons::customize(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::customize(), location, size, name);
}

light_button light_buttons::customize(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::customize(), check_state);
}

light_button light_buttons::customize(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::customize(), check_state, location);
}

light_button light_buttons::customize(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::customize(), check_state, location, size);
}

light_button light_buttons::customize(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::customize(), check_state, location, size, name);
}

light_button light_buttons::customize(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::customize(), three_state, check_state);
}

light_button light_buttons::customize(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::customize(), three_state, check_state, location);
}

light_button light_buttons::customize(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::customize(), three_state, check_state, location, size);
}

light_button light_buttons::customize(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::customize(), three_state, check_state, location, size, name);
}

light_button light_buttons::customize(const control& parent) {
  return light_button::create(parent, system_texts::customize());
}

light_button light_buttons::customize(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::customize(), location);
}

light_button light_buttons::customize(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::customize(), location, size);
}

light_button light_buttons::customize(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::customize(), location, size, name);
}

light_button light_buttons::customize(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::customize(), check_state);
}

light_button light_buttons::customize(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::customize(), check_state, location);
}

light_button light_buttons::customize(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::customize(), check_state, location, size);
}

light_button light_buttons::customize(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::customize(), check_state, location, size, name);
}

light_button light_buttons::customize(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::customize(), three_state, check_state);
}

light_button light_buttons::customize(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::customize(), three_state, check_state, location);
}

light_button light_buttons::customize(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::customize(), three_state, check_state, location, size);
}

light_button light_buttons::customize(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::customize(), three_state, check_state, location, size, name);
}

light_button light_buttons::cut() {
  return light_button::create(system_texts::cut());
}

light_button light_buttons::cut(const drawing::point& location) {
  return light_button::create(system_texts::cut(), location);
}

light_button light_buttons::cut(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::cut(), location, size);
}

light_button light_buttons::cut(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::cut(), location, size, name);
}

light_button light_buttons::cut(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::cut(), check_state);
}

light_button light_buttons::cut(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::cut(), check_state, location);
}

light_button light_buttons::cut(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::cut(), check_state, location, size);
}

light_button light_buttons::cut(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::cut(), check_state, location, size, name);
}

light_button light_buttons::cut(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::cut(), three_state, check_state);
}

light_button light_buttons::cut(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::cut(), three_state, check_state, location);
}

light_button light_buttons::cut(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::cut(), three_state, check_state, location, size);
}

light_button light_buttons::cut(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::cut(), three_state, check_state, location, size, name);
}

light_button light_buttons::cut(const control& parent) {
  return light_button::create(parent, system_texts::cut());
}

light_button light_buttons::cut(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::cut(), location);
}

light_button light_buttons::cut(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::cut(), location, size);
}

light_button light_buttons::cut(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::cut(), location, size, name);
}

light_button light_buttons::cut(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::cut(), check_state);
}

light_button light_buttons::cut(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::cut(), check_state, location);
}

light_button light_buttons::cut(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::cut(), check_state, location, size);
}

light_button light_buttons::cut(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::cut(), check_state, location, size, name);
}

light_button light_buttons::cut(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::cut(), three_state, check_state);
}

light_button light_buttons::cut(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::cut(), three_state, check_state, location);
}

light_button light_buttons::cut(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::cut(), three_state, check_state, location, size);
}

light_button light_buttons::cut(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::cut(), three_state, check_state, location, size, name);
}

light_button light_buttons::del() {
  return light_button::create(system_texts::del());
}

light_button light_buttons::del(const drawing::point& location) {
  return light_button::create(system_texts::del(), location);
}

light_button light_buttons::del(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::del(), location, size);
}

light_button light_buttons::del(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::del(), location, size, name);
}

light_button light_buttons::del(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::del(), check_state);
}

light_button light_buttons::del(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::del(), check_state, location);
}

light_button light_buttons::del(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::del(), check_state, location, size);
}

light_button light_buttons::del(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::del(), check_state, location, size, name);
}

light_button light_buttons::del(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::del(), three_state, check_state);
}

light_button light_buttons::del(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::del(), three_state, check_state, location);
}

light_button light_buttons::del(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::del(), three_state, check_state, location, size);
}

light_button light_buttons::del(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::del(), three_state, check_state, location, size, name);
}

light_button light_buttons::del(const control& parent) {
  return light_button::create(parent, system_texts::del());
}

light_button light_buttons::del(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::del(), location);
}

light_button light_buttons::del(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::del(), location, size);
}

light_button light_buttons::del(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::del(), location, size, name);
}

light_button light_buttons::del(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::del(), check_state);
}

light_button light_buttons::del(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::del(), check_state, location);
}

light_button light_buttons::del(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::del(), check_state, location, size);
}

light_button light_buttons::del(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::del(), check_state, location, size, name);
}

light_button light_buttons::del(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::del(), three_state, check_state);
}

light_button light_buttons::del(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::del(), three_state, check_state, location);
}

light_button light_buttons::del(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::del(), three_state, check_state, location, size);
}

light_button light_buttons::del(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::del(), three_state, check_state, location, size, name);
}

light_button light_buttons::descending() {
  return light_button::create(system_texts::descending());
}

light_button light_buttons::descending(const drawing::point& location) {
  return light_button::create(system_texts::descending(), location);
}

light_button light_buttons::descending(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::descending(), location, size);
}

light_button light_buttons::descending(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::descending(), location, size, name);
}

light_button light_buttons::descending(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::descending(), check_state);
}

light_button light_buttons::descending(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::descending(), check_state, location);
}

light_button light_buttons::descending(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::descending(), check_state, location, size);
}

light_button light_buttons::descending(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::descending(), check_state, location, size, name);
}

light_button light_buttons::descending(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::descending(), three_state, check_state);
}

light_button light_buttons::descending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::descending(), three_state, check_state, location);
}

light_button light_buttons::descending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::descending(), three_state, check_state, location, size);
}

light_button light_buttons::descending(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::descending(), three_state, check_state, location, size, name);
}

light_button light_buttons::descending(const control& parent) {
  return light_button::create(parent, system_texts::descending());
}

light_button light_buttons::descending(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::descending(), location);
}

light_button light_buttons::descending(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::descending(), location, size);
}

light_button light_buttons::descending(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::descending(), location, size, name);
}

light_button light_buttons::descending(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::descending(), check_state);
}

light_button light_buttons::descending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::descending(), check_state, location);
}

light_button light_buttons::descending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::descending(), check_state, location, size);
}

light_button light_buttons::descending(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::descending(), check_state, location, size, name);
}

light_button light_buttons::descending(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::descending(), three_state, check_state);
}

light_button light_buttons::descending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::descending(), three_state, check_state, location);
}

light_button light_buttons::descending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::descending(), three_state, check_state, location, size);
}

light_button light_buttons::descending(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::descending(), three_state, check_state, location, size, name);
}

light_button light_buttons::down() {
  return light_button::create(system_texts::down());
}

light_button light_buttons::down(const drawing::point& location) {
  return light_button::create(system_texts::down(), location);
}

light_button light_buttons::down(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::down(), location, size);
}

light_button light_buttons::down(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::down(), location, size, name);
}

light_button light_buttons::down(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::down(), check_state);
}

light_button light_buttons::down(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::down(), check_state, location);
}

light_button light_buttons::down(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::down(), check_state, location, size);
}

light_button light_buttons::down(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::down(), check_state, location, size, name);
}

light_button light_buttons::down(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::down(), three_state, check_state);
}

light_button light_buttons::down(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::down(), three_state, check_state, location);
}

light_button light_buttons::down(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::down(), three_state, check_state, location, size);
}

light_button light_buttons::down(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::down(), three_state, check_state, location, size, name);
}

light_button light_buttons::down(const control& parent) {
  return light_button::create(parent, system_texts::down());
}

light_button light_buttons::down(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::down(), location);
}

light_button light_buttons::down(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::down(), location, size);
}

light_button light_buttons::down(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::down(), location, size, name);
}

light_button light_buttons::down(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::down(), check_state);
}

light_button light_buttons::down(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::down(), check_state, location);
}

light_button light_buttons::down(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::down(), check_state, location, size);
}

light_button light_buttons::down(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::down(), check_state, location, size, name);
}

light_button light_buttons::down(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::down(), three_state, check_state);
}

light_button light_buttons::down(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::down(), three_state, check_state, location);
}

light_button light_buttons::down(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::down(), three_state, check_state, location, size);
}

light_button light_buttons::down(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::down(), three_state, check_state, location, size, name);
}

light_button light_buttons::edit() {
  return light_button::create(system_texts::edit());
}

light_button light_buttons::edit(const drawing::point& location) {
  return light_button::create(system_texts::edit(), location);
}

light_button light_buttons::edit(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::edit(), location, size);
}

light_button light_buttons::edit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::edit(), location, size, name);
}

light_button light_buttons::edit(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::edit(), check_state);
}

light_button light_buttons::edit(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::edit(), check_state, location);
}

light_button light_buttons::edit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::edit(), check_state, location, size);
}

light_button light_buttons::edit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::edit(), check_state, location, size, name);
}

light_button light_buttons::edit(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::edit(), three_state, check_state);
}

light_button light_buttons::edit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::edit(), three_state, check_state, location);
}

light_button light_buttons::edit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::edit(), three_state, check_state, location, size);
}

light_button light_buttons::edit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::edit(), three_state, check_state, location, size, name);
}

light_button light_buttons::edit(const control& parent) {
  return light_button::create(parent, system_texts::edit());
}

light_button light_buttons::edit(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::edit(), location);
}

light_button light_buttons::edit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::edit(), location, size);
}

light_button light_buttons::edit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::edit(), location, size, name);
}

light_button light_buttons::edit(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::edit(), check_state);
}

light_button light_buttons::edit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::edit(), check_state, location);
}

light_button light_buttons::edit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::edit(), check_state, location, size);
}

light_button light_buttons::edit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::edit(), check_state, location, size, name);
}

light_button light_buttons::edit(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::edit(), three_state, check_state);
}

light_button light_buttons::edit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::edit(), three_state, check_state, location);
}

light_button light_buttons::edit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::edit(), three_state, check_state, location, size);
}

light_button light_buttons::edit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::edit(), three_state, check_state, location, size, name);
}

light_button light_buttons::execute() {
  return light_button::create(system_texts::execute());
}

light_button light_buttons::execute(const drawing::point& location) {
  return light_button::create(system_texts::execute(), location);
}

light_button light_buttons::execute(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::execute(), location, size);
}

light_button light_buttons::execute(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::execute(), location, size, name);
}

light_button light_buttons::execute(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::execute(), check_state);
}

light_button light_buttons::execute(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::execute(), check_state, location);
}

light_button light_buttons::execute(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::execute(), check_state, location, size);
}

light_button light_buttons::execute(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::execute(), check_state, location, size, name);
}

light_button light_buttons::execute(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::execute(), three_state, check_state);
}

light_button light_buttons::execute(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::execute(), three_state, check_state, location);
}

light_button light_buttons::execute(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::execute(), three_state, check_state, location, size);
}

light_button light_buttons::execute(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::execute(), three_state, check_state, location, size, name);
}

light_button light_buttons::execute(const control& parent) {
  return light_button::create(parent, system_texts::execute());
}

light_button light_buttons::execute(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::execute(), location);
}

light_button light_buttons::execute(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::execute(), location, size);
}

light_button light_buttons::execute(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::execute(), location, size, name);
}

light_button light_buttons::execute(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::execute(), check_state);
}

light_button light_buttons::execute(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::execute(), check_state, location);
}

light_button light_buttons::execute(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::execute(), check_state, location, size);
}

light_button light_buttons::execute(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::execute(), check_state, location, size, name);
}

light_button light_buttons::execute(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::execute(), three_state, check_state);
}

light_button light_buttons::execute(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::execute(), three_state, check_state, location);
}

light_button light_buttons::execute(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::execute(), three_state, check_state, location, size);
}

light_button light_buttons::execute(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::execute(), three_state, check_state, location, size, name);
}

light_button light_buttons::exit() {
  return light_button::create(system_texts::exit());
}

light_button light_buttons::exit(const drawing::point& location) {
  return light_button::create(system_texts::exit(), location);
}

light_button light_buttons::exit(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::exit(), location, size);
}

light_button light_buttons::exit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::exit(), location, size, name);
}

light_button light_buttons::exit(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::exit(), check_state);
}

light_button light_buttons::exit(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::exit(), check_state, location);
}

light_button light_buttons::exit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::exit(), check_state, location, size);
}

light_button light_buttons::exit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::exit(), check_state, location, size, name);
}

light_button light_buttons::exit(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::exit(), three_state, check_state);
}

light_button light_buttons::exit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::exit(), three_state, check_state, location);
}

light_button light_buttons::exit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::exit(), three_state, check_state, location, size);
}

light_button light_buttons::exit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::exit(), three_state, check_state, location, size, name);
}

light_button light_buttons::exit(const control& parent) {
  return light_button::create(parent, system_texts::exit());
}

light_button light_buttons::exit(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::exit(), location);
}

light_button light_buttons::exit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::exit(), location, size);
}

light_button light_buttons::exit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::exit(), location, size, name);
}

light_button light_buttons::exit(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::exit(), check_state);
}

light_button light_buttons::exit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::exit(), check_state, location);
}

light_button light_buttons::exit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::exit(), check_state, location, size);
}

light_button light_buttons::exit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::exit(), check_state, location, size, name);
}

light_button light_buttons::exit(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::exit(), three_state, check_state);
}

light_button light_buttons::exit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::exit(), three_state, check_state, location);
}

light_button light_buttons::exit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::exit(), three_state, check_state, location, size);
}

light_button light_buttons::exit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::exit(), three_state, check_state, location, size, name);
}

light_button light_buttons::file() {
  return light_button::create(system_texts::file());
}

light_button light_buttons::file(const drawing::point& location) {
  return light_button::create(system_texts::file(), location);
}

light_button light_buttons::file(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::file(), location, size);
}

light_button light_buttons::file(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::file(), location, size, name);
}

light_button light_buttons::file(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::file(), check_state);
}

light_button light_buttons::file(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::file(), check_state, location);
}

light_button light_buttons::file(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::file(), check_state, location, size);
}

light_button light_buttons::file(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::file(), check_state, location, size, name);
}

light_button light_buttons::file(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::file(), three_state, check_state);
}

light_button light_buttons::file(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::file(), three_state, check_state, location);
}

light_button light_buttons::file(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::file(), three_state, check_state, location, size);
}

light_button light_buttons::file(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::file(), three_state, check_state, location, size, name);
}

light_button light_buttons::file(const control& parent) {
  return light_button::create(parent, system_texts::file());
}

light_button light_buttons::file(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::file(), location);
}

light_button light_buttons::file(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::file(), location, size);
}

light_button light_buttons::file(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::file(), location, size, name);
}

light_button light_buttons::file(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::file(), check_state);
}

light_button light_buttons::file(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::file(), check_state, location);
}

light_button light_buttons::file(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::file(), check_state, location, size);
}

light_button light_buttons::file(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::file(), check_state, location, size, name);
}

light_button light_buttons::file(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::file(), three_state, check_state);
}

light_button light_buttons::file(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::file(), three_state, check_state, location);
}

light_button light_buttons::file(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::file(), three_state, check_state, location, size);
}

light_button light_buttons::file(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::file(), three_state, check_state, location, size, name);
}

light_button light_buttons::find() {
  return light_button::create(system_texts::find());
}

light_button light_buttons::find(const drawing::point& location) {
  return light_button::create(system_texts::find(), location);
}

light_button light_buttons::find(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::find(), location, size);
}

light_button light_buttons::find(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::find(), location, size, name);
}

light_button light_buttons::find(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::find(), check_state);
}

light_button light_buttons::find(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::find(), check_state, location);
}

light_button light_buttons::find(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::find(), check_state, location, size);
}

light_button light_buttons::find(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::find(), check_state, location, size, name);
}

light_button light_buttons::find(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::find(), three_state, check_state);
}

light_button light_buttons::find(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::find(), three_state, check_state, location);
}

light_button light_buttons::find(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::find(), three_state, check_state, location, size);
}

light_button light_buttons::find(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::find(), three_state, check_state, location, size, name);
}

light_button light_buttons::find(const control& parent) {
  return light_button::create(parent, system_texts::find());
}

light_button light_buttons::find(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::find(), location);
}

light_button light_buttons::find(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::find(), location, size);
}

light_button light_buttons::find(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::find(), location, size, name);
}

light_button light_buttons::find(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::find(), check_state);
}

light_button light_buttons::find(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::find(), check_state, location);
}

light_button light_buttons::find(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::find(), check_state, location, size);
}

light_button light_buttons::find(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::find(), check_state, location, size, name);
}

light_button light_buttons::find(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::find(), three_state, check_state);
}

light_button light_buttons::find(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::find(), three_state, check_state, location);
}

light_button light_buttons::find(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::find(), three_state, check_state, location, size);
}

light_button light_buttons::find(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::find(), three_state, check_state, location, size, name);
}

light_button light_buttons::first() {
  return light_button::create(system_texts::first());
}

light_button light_buttons::first(const drawing::point& location) {
  return light_button::create(system_texts::first(), location);
}

light_button light_buttons::first(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::first(), location, size);
}

light_button light_buttons::first(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::first(), location, size, name);
}

light_button light_buttons::first(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::first(), check_state);
}

light_button light_buttons::first(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::first(), check_state, location);
}

light_button light_buttons::first(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::first(), check_state, location, size);
}

light_button light_buttons::first(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::first(), check_state, location, size, name);
}

light_button light_buttons::first(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::first(), three_state, check_state);
}

light_button light_buttons::first(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::first(), three_state, check_state, location);
}

light_button light_buttons::first(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::first(), three_state, check_state, location, size);
}

light_button light_buttons::first(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::first(), three_state, check_state, location, size, name);
}

light_button light_buttons::first(const control& parent) {
  return light_button::create(parent, system_texts::first());
}

light_button light_buttons::first(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::first(), location);
}

light_button light_buttons::first(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::first(), location, size);
}

light_button light_buttons::first(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::first(), location, size, name);
}

light_button light_buttons::first(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::first(), check_state);
}

light_button light_buttons::first(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::first(), check_state, location);
}

light_button light_buttons::first(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::first(), check_state, location, size);
}

light_button light_buttons::first(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::first(), check_state, location, size, name);
}

light_button light_buttons::first(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::first(), three_state, check_state);
}

light_button light_buttons::first(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::first(), three_state, check_state, location);
}

light_button light_buttons::first(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::first(), three_state, check_state, location, size);
}

light_button light_buttons::first(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::first(), three_state, check_state, location, size, name);
}

light_button light_buttons::floppy() {
  return light_button::create(system_texts::floppy());
}

light_button light_buttons::floppy(const drawing::point& location) {
  return light_button::create(system_texts::floppy(), location);
}

light_button light_buttons::floppy(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::floppy(), location, size);
}

light_button light_buttons::floppy(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::floppy(), location, size, name);
}

light_button light_buttons::floppy(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::floppy(), check_state);
}

light_button light_buttons::floppy(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::floppy(), check_state, location);
}

light_button light_buttons::floppy(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::floppy(), check_state, location, size);
}

light_button light_buttons::floppy(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::floppy(), check_state, location, size, name);
}

light_button light_buttons::floppy(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::floppy(), three_state, check_state);
}

light_button light_buttons::floppy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::floppy(), three_state, check_state, location);
}

light_button light_buttons::floppy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::floppy(), three_state, check_state, location, size);
}

light_button light_buttons::floppy(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::floppy(), three_state, check_state, location, size, name);
}

light_button light_buttons::floppy(const control& parent) {
  return light_button::create(parent, system_texts::floppy());
}

light_button light_buttons::floppy(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::floppy(), location);
}

light_button light_buttons::floppy(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::floppy(), location, size);
}

light_button light_buttons::floppy(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::floppy(), location, size, name);
}

light_button light_buttons::floppy(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::floppy(), check_state);
}

light_button light_buttons::floppy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::floppy(), check_state, location);
}

light_button light_buttons::floppy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::floppy(), check_state, location, size);
}

light_button light_buttons::floppy(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::floppy(), check_state, location, size, name);
}

light_button light_buttons::floppy(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::floppy(), three_state, check_state);
}

light_button light_buttons::floppy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::floppy(), three_state, check_state, location);
}

light_button light_buttons::floppy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::floppy(), three_state, check_state, location, size);
}

light_button light_buttons::floppy(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::floppy(), three_state, check_state, location, size, name);
}

light_button light_buttons::font() {
  return light_button::create(system_texts::font());
}

light_button light_buttons::font(const drawing::point& location) {
  return light_button::create(system_texts::font(), location);
}

light_button light_buttons::font(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::font(), location, size);
}

light_button light_buttons::font(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::font(), location, size, name);
}

light_button light_buttons::font(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::font(), check_state);
}

light_button light_buttons::font(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::font(), check_state, location);
}

light_button light_buttons::font(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::font(), check_state, location, size);
}

light_button light_buttons::font(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::font(), check_state, location, size, name);
}

light_button light_buttons::font(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::font(), three_state, check_state);
}

light_button light_buttons::font(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::font(), three_state, check_state, location);
}

light_button light_buttons::font(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::font(), three_state, check_state, location, size);
}

light_button light_buttons::font(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::font(), three_state, check_state, location, size, name);
}

light_button light_buttons::font(const control& parent) {
  return light_button::create(parent, system_texts::font());
}

light_button light_buttons::font(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::font(), location);
}

light_button light_buttons::font(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::font(), location, size);
}

light_button light_buttons::font(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::font(), location, size, name);
}

light_button light_buttons::font(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::font(), check_state);
}

light_button light_buttons::font(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::font(), check_state, location);
}

light_button light_buttons::font(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::font(), check_state, location, size);
}

light_button light_buttons::font(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::font(), check_state, location, size, name);
}

light_button light_buttons::font(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::font(), three_state, check_state);
}

light_button light_buttons::font(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::font(), three_state, check_state, location);
}

light_button light_buttons::font(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::font(), three_state, check_state, location, size);
}

light_button light_buttons::font(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::font(), three_state, check_state, location, size, name);
}

light_button light_buttons::forward() {
  return light_button::create(system_texts::forward());
}

light_button light_buttons::forward(const drawing::point& location) {
  return light_button::create(system_texts::forward(), location);
}

light_button light_buttons::forward(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::forward(), location, size);
}

light_button light_buttons::forward(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::forward(), location, size, name);
}

light_button light_buttons::forward(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::forward(), check_state);
}

light_button light_buttons::forward(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::forward(), check_state, location);
}

light_button light_buttons::forward(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::forward(), check_state, location, size);
}

light_button light_buttons::forward(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::forward(), check_state, location, size, name);
}

light_button light_buttons::forward(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::forward(), three_state, check_state);
}

light_button light_buttons::forward(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::forward(), three_state, check_state, location);
}

light_button light_buttons::forward(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::forward(), three_state, check_state, location, size);
}

light_button light_buttons::forward(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::forward(), three_state, check_state, location, size, name);
}

light_button light_buttons::forward(const control& parent) {
  return light_button::create(parent, system_texts::forward());
}

light_button light_buttons::forward(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::forward(), location);
}

light_button light_buttons::forward(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::forward(), location, size);
}

light_button light_buttons::forward(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::forward(), location, size, name);
}

light_button light_buttons::forward(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::forward(), check_state);
}

light_button light_buttons::forward(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::forward(), check_state, location);
}

light_button light_buttons::forward(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::forward(), check_state, location, size);
}

light_button light_buttons::forward(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::forward(), check_state, location, size, name);
}

light_button light_buttons::forward(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::forward(), three_state, check_state);
}

light_button light_buttons::forward(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::forward(), three_state, check_state, location);
}

light_button light_buttons::forward(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::forward(), three_state, check_state, location, size);
}

light_button light_buttons::forward(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::forward(), three_state, check_state, location, size, name);
}

light_button light_buttons::harddisk() {
  return light_button::create(system_texts::harddisk());
}

light_button light_buttons::harddisk(const drawing::point& location) {
  return light_button::create(system_texts::harddisk(), location);
}

light_button light_buttons::harddisk(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::harddisk(), location, size);
}

light_button light_buttons::harddisk(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::harddisk(), location, size, name);
}

light_button light_buttons::harddisk(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::harddisk(), check_state);
}

light_button light_buttons::harddisk(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::harddisk(), check_state, location);
}

light_button light_buttons::harddisk(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::harddisk(), check_state, location, size);
}

light_button light_buttons::harddisk(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::harddisk(), check_state, location, size, name);
}

light_button light_buttons::harddisk(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::harddisk(), three_state, check_state);
}

light_button light_buttons::harddisk(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::harddisk(), three_state, check_state, location);
}

light_button light_buttons::harddisk(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::harddisk(), three_state, check_state, location, size);
}

light_button light_buttons::harddisk(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::harddisk(), three_state, check_state, location, size, name);
}

light_button light_buttons::harddisk(const control& parent) {
  return light_button::create(parent, system_texts::harddisk());
}

light_button light_buttons::harddisk(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::harddisk(), location);
}

light_button light_buttons::harddisk(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::harddisk(), location, size);
}

light_button light_buttons::harddisk(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::harddisk(), location, size, name);
}

light_button light_buttons::harddisk(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::harddisk(), check_state);
}

light_button light_buttons::harddisk(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::harddisk(), check_state, location);
}

light_button light_buttons::harddisk(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::harddisk(), check_state, location, size);
}

light_button light_buttons::harddisk(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::harddisk(), check_state, location, size, name);
}

light_button light_buttons::harddisk(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::harddisk(), three_state, check_state);
}

light_button light_buttons::harddisk(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::harddisk(), three_state, check_state, location);
}

light_button light_buttons::harddisk(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::harddisk(), three_state, check_state, location, size);
}

light_button light_buttons::harddisk(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::harddisk(), three_state, check_state, location, size, name);
}

light_button light_buttons::help() {
  return light_button::create(system_texts::help());
}

light_button light_buttons::help(const drawing::point& location) {
  return light_button::create(system_texts::help(), location);
}

light_button light_buttons::help(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::help(), location, size);
}

light_button light_buttons::help(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::help(), location, size, name);
}

light_button light_buttons::help(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::help(), check_state);
}

light_button light_buttons::help(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::help(), check_state, location);
}

light_button light_buttons::help(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::help(), check_state, location, size);
}

light_button light_buttons::help(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::help(), check_state, location, size, name);
}

light_button light_buttons::help(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::help(), three_state, check_state);
}

light_button light_buttons::help(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::help(), three_state, check_state, location);
}

light_button light_buttons::help(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::help(), three_state, check_state, location, size);
}

light_button light_buttons::help(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::help(), three_state, check_state, location, size, name);
}

light_button light_buttons::help(const control& parent) {
  return light_button::create(parent, system_texts::help());
}

light_button light_buttons::help(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::help(), location);
}

light_button light_buttons::help(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::help(), location, size);
}

light_button light_buttons::help(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::help(), location, size, name);
}

light_button light_buttons::help(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::help(), check_state);
}

light_button light_buttons::help(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::help(), check_state, location);
}

light_button light_buttons::help(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::help(), check_state, location, size);
}

light_button light_buttons::help(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::help(), check_state, location, size, name);
}

light_button light_buttons::help(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::help(), three_state, check_state);
}

light_button light_buttons::help(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::help(), three_state, check_state, location);
}

light_button light_buttons::help(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::help(), three_state, check_state, location, size);
}

light_button light_buttons::help(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::help(), three_state, check_state, location, size, name);
}

light_button light_buttons::home() {
  return light_button::create(system_texts::home());
}

light_button light_buttons::home(const drawing::point& location) {
  return light_button::create(system_texts::home(), location);
}

light_button light_buttons::home(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::home(), location, size);
}

light_button light_buttons::home(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::home(), location, size, name);
}

light_button light_buttons::home(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::home(), check_state);
}

light_button light_buttons::home(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::home(), check_state, location);
}

light_button light_buttons::home(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::home(), check_state, location, size);
}

light_button light_buttons::home(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::home(), check_state, location, size, name);
}

light_button light_buttons::home(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::home(), three_state, check_state);
}

light_button light_buttons::home(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::home(), three_state, check_state, location);
}

light_button light_buttons::home(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::home(), three_state, check_state, location, size);
}

light_button light_buttons::home(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::home(), three_state, check_state, location, size, name);
}

light_button light_buttons::home(const control& parent) {
  return light_button::create(parent, system_texts::home());
}

light_button light_buttons::home(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::home(), location);
}

light_button light_buttons::home(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::home(), location, size);
}

light_button light_buttons::home(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::home(), location, size, name);
}

light_button light_buttons::home(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::home(), check_state);
}

light_button light_buttons::home(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::home(), check_state, location);
}

light_button light_buttons::home(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::home(), check_state, location, size);
}

light_button light_buttons::home(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::home(), check_state, location, size, name);
}

light_button light_buttons::home(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::home(), three_state, check_state);
}

light_button light_buttons::home(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::home(), three_state, check_state, location);
}

light_button light_buttons::home(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::home(), three_state, check_state, location, size);
}

light_button light_buttons::home(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::home(), three_state, check_state, location, size, name);
}

light_button light_buttons::ignore() {
  return light_button::create(system_texts::ignore());
}

light_button light_buttons::ignore(const drawing::point& location) {
  return light_button::create(system_texts::ignore(), location);
}

light_button light_buttons::ignore(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::ignore(), location, size);
}

light_button light_buttons::ignore(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::ignore(), location, size, name);
}

light_button light_buttons::ignore(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::ignore(), check_state);
}

light_button light_buttons::ignore(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::ignore(), check_state, location);
}

light_button light_buttons::ignore(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::ignore(), check_state, location, size);
}

light_button light_buttons::ignore(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::ignore(), check_state, location, size, name);
}

light_button light_buttons::ignore(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::ignore(), three_state, check_state);
}

light_button light_buttons::ignore(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::ignore(), three_state, check_state, location);
}

light_button light_buttons::ignore(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::ignore(), three_state, check_state, location, size);
}

light_button light_buttons::ignore(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::ignore(), three_state, check_state, location, size, name);
}

light_button light_buttons::ignore(const control& parent) {
  return light_button::create(parent, system_texts::ignore());
}

light_button light_buttons::ignore(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::ignore(), location);
}

light_button light_buttons::ignore(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::ignore(), location, size);
}

light_button light_buttons::ignore(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::ignore(), location, size, name);
}

light_button light_buttons::ignore(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::ignore(), check_state);
}

light_button light_buttons::ignore(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::ignore(), check_state, location);
}

light_button light_buttons::ignore(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::ignore(), check_state, location, size);
}

light_button light_buttons::ignore(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::ignore(), check_state, location, size, name);
}

light_button light_buttons::ignore(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::ignore(), three_state, check_state);
}

light_button light_buttons::ignore(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::ignore(), three_state, check_state, location);
}

light_button light_buttons::ignore(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::ignore(), three_state, check_state, location, size);
}

light_button light_buttons::ignore(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::ignore(), three_state, check_state, location, size, name);
}

light_button light_buttons::indent() {
  return light_button::create(system_texts::indent());
}

light_button light_buttons::indent(const drawing::point& location) {
  return light_button::create(system_texts::indent(), location);
}

light_button light_buttons::indent(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::indent(), location, size);
}

light_button light_buttons::indent(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::indent(), location, size, name);
}

light_button light_buttons::indent(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::indent(), check_state);
}

light_button light_buttons::indent(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::indent(), check_state, location);
}

light_button light_buttons::indent(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::indent(), check_state, location, size);
}

light_button light_buttons::indent(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::indent(), check_state, location, size, name);
}

light_button light_buttons::indent(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::indent(), three_state, check_state);
}

light_button light_buttons::indent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::indent(), three_state, check_state, location);
}

light_button light_buttons::indent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::indent(), three_state, check_state, location, size);
}

light_button light_buttons::indent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::indent(), three_state, check_state, location, size, name);
}

light_button light_buttons::indent(const control& parent) {
  return light_button::create(parent, system_texts::indent());
}

light_button light_buttons::indent(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::indent(), location);
}

light_button light_buttons::indent(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::indent(), location, size);
}

light_button light_buttons::indent(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::indent(), location, size, name);
}

light_button light_buttons::indent(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::indent(), check_state);
}

light_button light_buttons::indent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::indent(), check_state, location);
}

light_button light_buttons::indent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::indent(), check_state, location, size);
}

light_button light_buttons::indent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::indent(), check_state, location, size, name);
}

light_button light_buttons::indent(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::indent(), three_state, check_state);
}

light_button light_buttons::indent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::indent(), three_state, check_state, location);
}

light_button light_buttons::indent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::indent(), three_state, check_state, location, size);
}

light_button light_buttons::indent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::indent(), three_state, check_state, location, size, name);
}

light_button light_buttons::index() {
  return light_button::create(system_texts::index());
}

light_button light_buttons::index(const drawing::point& location) {
  return light_button::create(system_texts::index(), location);
}

light_button light_buttons::index(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::index(), location, size);
}

light_button light_buttons::index(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::index(), location, size, name);
}

light_button light_buttons::index(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::index(), check_state);
}

light_button light_buttons::index(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::index(), check_state, location);
}

light_button light_buttons::index(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::index(), check_state, location, size);
}

light_button light_buttons::index(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::index(), check_state, location, size, name);
}

light_button light_buttons::index(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::index(), three_state, check_state);
}

light_button light_buttons::index(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::index(), three_state, check_state, location);
}

light_button light_buttons::index(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::index(), three_state, check_state, location, size);
}

light_button light_buttons::index(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::index(), three_state, check_state, location, size, name);
}

light_button light_buttons::index(const control& parent) {
  return light_button::create(parent, system_texts::index());
}

light_button light_buttons::index(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::index(), location);
}

light_button light_buttons::index(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::index(), location, size);
}

light_button light_buttons::index(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::index(), location, size, name);
}

light_button light_buttons::index(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::index(), check_state);
}

light_button light_buttons::index(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::index(), check_state, location);
}

light_button light_buttons::index(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::index(), check_state, location, size);
}

light_button light_buttons::index(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::index(), check_state, location, size, name);
}

light_button light_buttons::index(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::index(), three_state, check_state);
}

light_button light_buttons::index(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::index(), three_state, check_state, location);
}

light_button light_buttons::index(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::index(), three_state, check_state, location, size);
}

light_button light_buttons::index(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::index(), three_state, check_state, location, size, name);
}

light_button light_buttons::info() {
  return light_button::create(system_texts::info());
}

light_button light_buttons::info(const drawing::point& location) {
  return light_button::create(system_texts::info(), location);
}

light_button light_buttons::info(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::info(), location, size);
}

light_button light_buttons::info(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::info(), location, size, name);
}

light_button light_buttons::info(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::info(), check_state);
}

light_button light_buttons::info(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::info(), check_state, location);
}

light_button light_buttons::info(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::info(), check_state, location, size);
}

light_button light_buttons::info(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::info(), check_state, location, size, name);
}

light_button light_buttons::info(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::info(), three_state, check_state);
}

light_button light_buttons::info(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::info(), three_state, check_state, location);
}

light_button light_buttons::info(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::info(), three_state, check_state, location, size);
}

light_button light_buttons::info(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::info(), three_state, check_state, location, size, name);
}

light_button light_buttons::info(const control& parent) {
  return light_button::create(parent, system_texts::info());
}

light_button light_buttons::info(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::info(), location);
}

light_button light_buttons::info(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::info(), location, size);
}

light_button light_buttons::info(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::info(), location, size, name);
}

light_button light_buttons::info(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::info(), check_state);
}

light_button light_buttons::info(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::info(), check_state, location);
}

light_button light_buttons::info(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::info(), check_state, location, size);
}

light_button light_buttons::info(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::info(), check_state, location, size, name);
}

light_button light_buttons::info(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::info(), three_state, check_state);
}

light_button light_buttons::info(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::info(), three_state, check_state, location);
}

light_button light_buttons::info(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::info(), three_state, check_state, location, size);
}

light_button light_buttons::info(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::info(), three_state, check_state, location, size, name);
}

light_button light_buttons::italic() {
  return light_button::create(system_texts::italic());
}

light_button light_buttons::italic(const drawing::point& location) {
  return light_button::create(system_texts::italic(), location);
}

light_button light_buttons::italic(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::italic(), location, size);
}

light_button light_buttons::italic(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::italic(), location, size, name);
}

light_button light_buttons::italic(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::italic(), check_state);
}

light_button light_buttons::italic(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::italic(), check_state, location);
}

light_button light_buttons::italic(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::italic(), check_state, location, size);
}

light_button light_buttons::italic(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::italic(), check_state, location, size, name);
}

light_button light_buttons::italic(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::italic(), three_state, check_state);
}

light_button light_buttons::italic(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::italic(), three_state, check_state, location);
}

light_button light_buttons::italic(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::italic(), three_state, check_state, location, size);
}

light_button light_buttons::italic(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::italic(), three_state, check_state, location, size, name);
}

light_button light_buttons::italic(const control& parent) {
  return light_button::create(parent, system_texts::italic());
}

light_button light_buttons::italic(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::italic(), location);
}

light_button light_buttons::italic(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::italic(), location, size);
}

light_button light_buttons::italic(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::italic(), location, size, name);
}

light_button light_buttons::italic(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::italic(), check_state);
}

light_button light_buttons::italic(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::italic(), check_state, location);
}

light_button light_buttons::italic(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::italic(), check_state, location, size);
}

light_button light_buttons::italic(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::italic(), check_state, location, size, name);
}

light_button light_buttons::italic(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::italic(), three_state, check_state);
}

light_button light_buttons::italic(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::italic(), three_state, check_state, location);
}

light_button light_buttons::italic(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::italic(), three_state, check_state, location, size);
}

light_button light_buttons::italic(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::italic(), three_state, check_state, location, size, name);
}

light_button light_buttons::justified() {
  return light_button::create(system_texts::justified());
}

light_button light_buttons::justified(const drawing::point& location) {
  return light_button::create(system_texts::justified(), location);
}

light_button light_buttons::justified(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::justified(), location, size);
}

light_button light_buttons::justified(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::justified(), location, size, name);
}

light_button light_buttons::justified(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::justified(), check_state);
}

light_button light_buttons::justified(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::justified(), check_state, location);
}

light_button light_buttons::justified(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::justified(), check_state, location, size);
}

light_button light_buttons::justified(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::justified(), check_state, location, size, name);
}

light_button light_buttons::justified(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::justified(), three_state, check_state);
}

light_button light_buttons::justified(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::justified(), three_state, check_state, location);
}

light_button light_buttons::justified(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::justified(), three_state, check_state, location, size);
}

light_button light_buttons::justified(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::justified(), three_state, check_state, location, size, name);
}

light_button light_buttons::justified(const control& parent) {
  return light_button::create(parent, system_texts::justified());
}

light_button light_buttons::justified(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::justified(), location);
}

light_button light_buttons::justified(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::justified(), location, size);
}

light_button light_buttons::justified(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::justified(), location, size, name);
}

light_button light_buttons::justified(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::justified(), check_state);
}

light_button light_buttons::justified(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::justified(), check_state, location);
}

light_button light_buttons::justified(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::justified(), check_state, location, size);
}

light_button light_buttons::justified(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::justified(), check_state, location, size, name);
}

light_button light_buttons::justified(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::justified(), three_state, check_state);
}

light_button light_buttons::justified(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::justified(), three_state, check_state, location);
}

light_button light_buttons::justified(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::justified(), three_state, check_state, location, size);
}

light_button light_buttons::justified(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::justified(), three_state, check_state, location, size, name);
}

light_button light_buttons::jump_to() {
  return light_button::create(system_texts::jump_to());
}

light_button light_buttons::jump_to(const drawing::point& location) {
  return light_button::create(system_texts::jump_to(), location);
}

light_button light_buttons::jump_to(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::jump_to(), location, size);
}

light_button light_buttons::jump_to(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::jump_to(), location, size, name);
}

light_button light_buttons::jump_to(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::jump_to(), check_state);
}

light_button light_buttons::jump_to(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::jump_to(), check_state, location);
}

light_button light_buttons::jump_to(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::jump_to(), check_state, location, size);
}

light_button light_buttons::jump_to(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::jump_to(), check_state, location, size, name);
}

light_button light_buttons::jump_to(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::jump_to(), three_state, check_state);
}

light_button light_buttons::jump_to(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::jump_to(), three_state, check_state, location);
}

light_button light_buttons::jump_to(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::jump_to(), three_state, check_state, location, size);
}

light_button light_buttons::jump_to(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::jump_to(), three_state, check_state, location, size, name);
}

light_button light_buttons::jump_to(const control& parent) {
  return light_button::create(parent, system_texts::jump_to());
}

light_button light_buttons::jump_to(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::jump_to(), location);
}

light_button light_buttons::jump_to(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::jump_to(), location, size);
}

light_button light_buttons::jump_to(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::jump_to(), location, size, name);
}

light_button light_buttons::jump_to(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::jump_to(), check_state);
}

light_button light_buttons::jump_to(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::jump_to(), check_state, location);
}

light_button light_buttons::jump_to(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::jump_to(), check_state, location, size);
}

light_button light_buttons::jump_to(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::jump_to(), check_state, location, size, name);
}

light_button light_buttons::jump_to(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::jump_to(), three_state, check_state);
}

light_button light_buttons::jump_to(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::jump_to(), three_state, check_state, location);
}

light_button light_buttons::jump_to(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::jump_to(), three_state, check_state, location, size);
}

light_button light_buttons::jump_to(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::jump_to(), three_state, check_state, location, size, name);
}

light_button light_buttons::last() {
  return light_button::create(system_texts::last());
}

light_button light_buttons::last(const drawing::point& location) {
  return light_button::create(system_texts::last(), location);
}

light_button light_buttons::last(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::last(), location, size);
}

light_button light_buttons::last(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::last(), location, size, name);
}

light_button light_buttons::last(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::last(), check_state);
}

light_button light_buttons::last(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::last(), check_state, location);
}

light_button light_buttons::last(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::last(), check_state, location, size);
}

light_button light_buttons::last(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::last(), check_state, location, size, name);
}

light_button light_buttons::last(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::last(), three_state, check_state);
}

light_button light_buttons::last(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::last(), three_state, check_state, location);
}

light_button light_buttons::last(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::last(), three_state, check_state, location, size);
}

light_button light_buttons::last(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::last(), three_state, check_state, location, size, name);
}

light_button light_buttons::last(const control& parent) {
  return light_button::create(parent, system_texts::last());
}

light_button light_buttons::last(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::last(), location);
}

light_button light_buttons::last(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::last(), location, size);
}

light_button light_buttons::last(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::last(), location, size, name);
}

light_button light_buttons::last(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::last(), check_state);
}

light_button light_buttons::last(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::last(), check_state, location);
}

light_button light_buttons::last(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::last(), check_state, location, size);
}

light_button light_buttons::last(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::last(), check_state, location, size, name);
}

light_button light_buttons::last(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::last(), three_state, check_state);
}

light_button light_buttons::last(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::last(), three_state, check_state, location);
}

light_button light_buttons::last(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::last(), three_state, check_state, location, size);
}

light_button light_buttons::last(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::last(), three_state, check_state, location, size, name);
}

light_button light_buttons::network() {
  return light_button::create(system_texts::network());
}

light_button light_buttons::network(const drawing::point& location) {
  return light_button::create(system_texts::network(), location);
}

light_button light_buttons::network(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::network(), location, size);
}

light_button light_buttons::network(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::network(), location, size, name);
}

light_button light_buttons::network(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::network(), check_state);
}

light_button light_buttons::network(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::network(), check_state, location);
}

light_button light_buttons::network(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::network(), check_state, location, size);
}

light_button light_buttons::network(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::network(), check_state, location, size, name);
}

light_button light_buttons::network(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::network(), three_state, check_state);
}

light_button light_buttons::network(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::network(), three_state, check_state, location);
}

light_button light_buttons::network(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::network(), three_state, check_state, location, size);
}

light_button light_buttons::network(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::network(), three_state, check_state, location, size, name);
}

light_button light_buttons::network(const control& parent) {
  return light_button::create(parent, system_texts::network());
}

light_button light_buttons::network(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::network(), location);
}

light_button light_buttons::network(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::network(), location, size);
}

light_button light_buttons::network(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::network(), location, size, name);
}

light_button light_buttons::network(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::network(), check_state);
}

light_button light_buttons::network(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::network(), check_state, location);
}

light_button light_buttons::network(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::network(), check_state, location, size);
}

light_button light_buttons::network(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::network(), check_state, location, size, name);
}

light_button light_buttons::network(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::network(), three_state, check_state);
}

light_button light_buttons::network(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::network(), three_state, check_state, location);
}

light_button light_buttons::network(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::network(), three_state, check_state, location, size);
}

light_button light_buttons::network(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::network(), three_state, check_state, location, size, name);
}

light_button light_buttons::new_() {
  return light_button::create(system_texts::new_());
}

light_button light_buttons::new_(const drawing::point& location) {
  return light_button::create(system_texts::new_(), location);
}

light_button light_buttons::new_(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::new_(), location, size);
}

light_button light_buttons::new_(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::new_(), location, size, name);
}

light_button light_buttons::new_(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::new_(), check_state);
}

light_button light_buttons::new_(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::new_(), check_state, location);
}

light_button light_buttons::new_(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::new_(), check_state, location, size);
}

light_button light_buttons::new_(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::new_(), check_state, location, size, name);
}

light_button light_buttons::new_(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::new_(), three_state, check_state);
}

light_button light_buttons::new_(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::new_(), three_state, check_state, location);
}

light_button light_buttons::new_(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::new_(), three_state, check_state, location, size);
}

light_button light_buttons::new_(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::new_(), three_state, check_state, location, size, name);
}

light_button light_buttons::new_(const control& parent) {
  return light_button::create(parent, system_texts::new_());
}

light_button light_buttons::new_(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::new_(), location);
}

light_button light_buttons::new_(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::new_(), location, size);
}

light_button light_buttons::new_(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::new_(), location, size, name);
}

light_button light_buttons::new_(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::new_(), check_state);
}

light_button light_buttons::new_(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::new_(), check_state, location);
}

light_button light_buttons::new_(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::new_(), check_state, location, size);
}

light_button light_buttons::new_(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::new_(), check_state, location, size, name);
}

light_button light_buttons::new_(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::new_(), three_state, check_state);
}

light_button light_buttons::new_(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::new_(), three_state, check_state, location);
}

light_button light_buttons::new_(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::new_(), three_state, check_state, location, size);
}

light_button light_buttons::new_(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::new_(), three_state, check_state, location, size, name);
}

light_button light_buttons::next() {
  return light_button::create(system_texts::next());
}

light_button light_buttons::next(const drawing::point& location) {
  return light_button::create(system_texts::next(), location);
}

light_button light_buttons::next(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::next(), location, size);
}

light_button light_buttons::next(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::next(), location, size, name);
}

light_button light_buttons::next(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::next(), check_state);
}

light_button light_buttons::next(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::next(), check_state, location);
}

light_button light_buttons::next(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::next(), check_state, location, size);
}

light_button light_buttons::next(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::next(), check_state, location, size, name);
}

light_button light_buttons::next(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::next(), three_state, check_state);
}

light_button light_buttons::next(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::next(), three_state, check_state, location);
}

light_button light_buttons::next(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::next(), three_state, check_state, location, size);
}

light_button light_buttons::next(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::next(), three_state, check_state, location, size, name);
}

light_button light_buttons::next(const control& parent) {
  return light_button::create(parent, system_texts::next());
}

light_button light_buttons::next(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::next(), location);
}

light_button light_buttons::next(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::next(), location, size);
}

light_button light_buttons::next(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::next(), location, size, name);
}

light_button light_buttons::next(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::next(), check_state);
}

light_button light_buttons::next(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::next(), check_state, location);
}

light_button light_buttons::next(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::next(), check_state, location, size);
}

light_button light_buttons::next(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::next(), check_state, location, size, name);
}

light_button light_buttons::next(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::next(), three_state, check_state);
}

light_button light_buttons::next(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::next(), three_state, check_state, location);
}

light_button light_buttons::next(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::next(), three_state, check_state, location, size);
}

light_button light_buttons::next(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::next(), three_state, check_state, location, size, name);
}

light_button light_buttons::no() {
  return light_button::create(system_texts::no());
}

light_button light_buttons::no(const drawing::point& location) {
  return light_button::create(system_texts::no(), location);
}

light_button light_buttons::no(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::no(), location, size);
}

light_button light_buttons::no(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::no(), location, size, name);
}

light_button light_buttons::no(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::no(), check_state);
}

light_button light_buttons::no(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::no(), check_state, location);
}

light_button light_buttons::no(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::no(), check_state, location, size);
}

light_button light_buttons::no(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::no(), check_state, location, size, name);
}

light_button light_buttons::no(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::no(), three_state, check_state);
}

light_button light_buttons::no(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::no(), three_state, check_state, location);
}

light_button light_buttons::no(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::no(), three_state, check_state, location, size);
}

light_button light_buttons::no(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::no(), three_state, check_state, location, size, name);
}

light_button light_buttons::no(const control& parent) {
  return light_button::create(parent, system_texts::no());
}

light_button light_buttons::no(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::no(), location);
}

light_button light_buttons::no(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::no(), location, size);
}

light_button light_buttons::no(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::no(), location, size, name);
}

light_button light_buttons::no(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::no(), check_state);
}

light_button light_buttons::no(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::no(), check_state, location);
}

light_button light_buttons::no(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::no(), check_state, location, size);
}

light_button light_buttons::no(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::no(), check_state, location, size, name);
}

light_button light_buttons::no(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::no(), three_state, check_state);
}

light_button light_buttons::no(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::no(), three_state, check_state, location);
}

light_button light_buttons::no(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::no(), three_state, check_state, location, size);
}

light_button light_buttons::no(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::no(), three_state, check_state, location, size, name);
}

light_button light_buttons::ok() {
  return light_button::create(system_texts::ok());
}

light_button light_buttons::ok(const drawing::point& location) {
  return light_button::create(system_texts::ok(), location);
}

light_button light_buttons::ok(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::ok(), location, size);
}

light_button light_buttons::ok(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::ok(), location, size, name);
}

light_button light_buttons::ok(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::ok(), check_state);
}

light_button light_buttons::ok(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::ok(), check_state, location);
}

light_button light_buttons::ok(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::ok(), check_state, location, size);
}

light_button light_buttons::ok(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::ok(), check_state, location, size, name);
}

light_button light_buttons::ok(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::ok(), three_state, check_state);
}

light_button light_buttons::ok(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::ok(), three_state, check_state, location);
}

light_button light_buttons::ok(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::ok(), three_state, check_state, location, size);
}

light_button light_buttons::ok(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::ok(), three_state, check_state, location, size, name);
}

light_button light_buttons::ok(const control& parent) {
  return light_button::create(parent, system_texts::ok());
}

light_button light_buttons::ok(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::ok(), location);
}

light_button light_buttons::ok(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::ok(), location, size);
}

light_button light_buttons::ok(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::ok(), location, size, name);
}

light_button light_buttons::ok(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::ok(), check_state);
}

light_button light_buttons::ok(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::ok(), check_state, location);
}

light_button light_buttons::ok(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::ok(), check_state, location, size);
}

light_button light_buttons::ok(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::ok(), check_state, location, size, name);
}

light_button light_buttons::ok(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::ok(), three_state, check_state);
}

light_button light_buttons::ok(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::ok(), three_state, check_state, location);
}

light_button light_buttons::ok(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::ok(), three_state, check_state, location, size);
}

light_button light_buttons::ok(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::ok(), three_state, check_state, location, size, name);
}

light_button light_buttons::open() {
  return light_button::create(system_texts::open());
}

light_button light_buttons::open(const drawing::point& location) {
  return light_button::create(system_texts::open(), location);
}

light_button light_buttons::open(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::open(), location, size);
}

light_button light_buttons::open(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::open(), location, size, name);
}

light_button light_buttons::open(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::open(), check_state);
}

light_button light_buttons::open(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::open(), check_state, location);
}

light_button light_buttons::open(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::open(), check_state, location, size);
}

light_button light_buttons::open(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::open(), check_state, location, size, name);
}

light_button light_buttons::open(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::open(), three_state, check_state);
}

light_button light_buttons::open(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::open(), three_state, check_state, location);
}

light_button light_buttons::open(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::open(), three_state, check_state, location, size);
}

light_button light_buttons::open(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::open(), three_state, check_state, location, size, name);
}

light_button light_buttons::open(const control& parent) {
  return light_button::create(parent, system_texts::open());
}

light_button light_buttons::open(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::open(), location);
}

light_button light_buttons::open(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::open(), location, size);
}

light_button light_buttons::open(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::open(), location, size, name);
}

light_button light_buttons::open(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::open(), check_state);
}

light_button light_buttons::open(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::open(), check_state, location);
}

light_button light_buttons::open(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::open(), check_state, location, size);
}

light_button light_buttons::open(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::open(), check_state, location, size, name);
}

light_button light_buttons::open(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::open(), three_state, check_state);
}

light_button light_buttons::open(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::open(), three_state, check_state, location);
}

light_button light_buttons::open(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::open(), three_state, check_state, location, size);
}

light_button light_buttons::open(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::open(), three_state, check_state, location, size, name);
}

light_button light_buttons::options() {
  return light_button::create(system_texts::options());
}

light_button light_buttons::options(const drawing::point& location) {
  return light_button::create(system_texts::options(), location);
}

light_button light_buttons::options(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::options(), location, size);
}

light_button light_buttons::options(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::options(), location, size, name);
}

light_button light_buttons::options(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::options(), check_state);
}

light_button light_buttons::options(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::options(), check_state, location);
}

light_button light_buttons::options(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::options(), check_state, location, size);
}

light_button light_buttons::options(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::options(), check_state, location, size, name);
}

light_button light_buttons::options(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::options(), three_state, check_state);
}

light_button light_buttons::options(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::options(), three_state, check_state, location);
}

light_button light_buttons::options(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::options(), three_state, check_state, location, size);
}

light_button light_buttons::options(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::options(), three_state, check_state, location, size, name);
}

light_button light_buttons::options(const control& parent) {
  return light_button::create(parent, system_texts::options());
}

light_button light_buttons::options(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::options(), location);
}

light_button light_buttons::options(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::options(), location, size);
}

light_button light_buttons::options(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::options(), location, size, name);
}

light_button light_buttons::options(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::options(), check_state);
}

light_button light_buttons::options(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::options(), check_state, location);
}

light_button light_buttons::options(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::options(), check_state, location, size);
}

light_button light_buttons::options(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::options(), check_state, location, size, name);
}

light_button light_buttons::options(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::options(), three_state, check_state);
}

light_button light_buttons::options(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::options(), three_state, check_state, location);
}

light_button light_buttons::options(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::options(), three_state, check_state, location, size);
}

light_button light_buttons::options(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::options(), three_state, check_state, location, size, name);
}

light_button light_buttons::paste() {
  return light_button::create(system_texts::paste());
}

light_button light_buttons::paste(const drawing::point& location) {
  return light_button::create(system_texts::paste(), location);
}

light_button light_buttons::paste(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::paste(), location, size);
}

light_button light_buttons::paste(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::paste(), location, size, name);
}

light_button light_buttons::paste(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::paste(), check_state);
}

light_button light_buttons::paste(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::paste(), check_state, location);
}

light_button light_buttons::paste(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::paste(), check_state, location, size);
}

light_button light_buttons::paste(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::paste(), check_state, location, size, name);
}

light_button light_buttons::paste(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::paste(), three_state, check_state);
}

light_button light_buttons::paste(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::paste(), three_state, check_state, location);
}

light_button light_buttons::paste(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::paste(), three_state, check_state, location, size);
}

light_button light_buttons::paste(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::paste(), three_state, check_state, location, size, name);
}

light_button light_buttons::paste(const control& parent) {
  return light_button::create(parent, system_texts::paste());
}

light_button light_buttons::paste(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::paste(), location);
}

light_button light_buttons::paste(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::paste(), location, size);
}

light_button light_buttons::paste(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::paste(), location, size, name);
}

light_button light_buttons::paste(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::paste(), check_state);
}

light_button light_buttons::paste(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::paste(), check_state, location);
}

light_button light_buttons::paste(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::paste(), check_state, location, size);
}

light_button light_buttons::paste(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::paste(), check_state, location, size, name);
}

light_button light_buttons::paste(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::paste(), three_state, check_state);
}

light_button light_buttons::paste(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::paste(), three_state, check_state, location);
}

light_button light_buttons::paste(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::paste(), three_state, check_state, location, size);
}

light_button light_buttons::paste(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::paste(), three_state, check_state, location, size, name);
}

light_button light_buttons::preferences() {
  return light_button::create(system_texts::preferences());
}

light_button light_buttons::preferences(const drawing::point& location) {
  return light_button::create(system_texts::preferences(), location);
}

light_button light_buttons::preferences(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::preferences(), location, size);
}

light_button light_buttons::preferences(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::preferences(), location, size, name);
}

light_button light_buttons::preferences(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::preferences(), check_state);
}

light_button light_buttons::preferences(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::preferences(), check_state, location);
}

light_button light_buttons::preferences(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::preferences(), check_state, location, size);
}

light_button light_buttons::preferences(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::preferences(), check_state, location, size, name);
}

light_button light_buttons::preferences(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::preferences(), three_state, check_state);
}

light_button light_buttons::preferences(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::preferences(), three_state, check_state, location);
}

light_button light_buttons::preferences(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::preferences(), three_state, check_state, location, size);
}

light_button light_buttons::preferences(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::preferences(), three_state, check_state, location, size, name);
}

light_button light_buttons::preferences(const control& parent) {
  return light_button::create(parent, system_texts::preferences());
}

light_button light_buttons::preferences(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::preferences(), location);
}

light_button light_buttons::preferences(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::preferences(), location, size);
}

light_button light_buttons::preferences(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::preferences(), location, size, name);
}

light_button light_buttons::preferences(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::preferences(), check_state);
}

light_button light_buttons::preferences(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::preferences(), check_state, location);
}

light_button light_buttons::preferences(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::preferences(), check_state, location, size);
}

light_button light_buttons::preferences(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::preferences(), check_state, location, size, name);
}

light_button light_buttons::preferences(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::preferences(), three_state, check_state);
}

light_button light_buttons::preferences(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::preferences(), three_state, check_state, location);
}

light_button light_buttons::preferences(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::preferences(), three_state, check_state, location, size);
}

light_button light_buttons::preferences(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::preferences(), three_state, check_state, location, size, name);
}

light_button light_buttons::previous() {
  return light_button::create(system_texts::previous());
}

light_button light_buttons::previous(const drawing::point& location) {
  return light_button::create(system_texts::previous(), location);
}

light_button light_buttons::previous(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::previous(), location, size);
}

light_button light_buttons::previous(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::previous(), location, size, name);
}

light_button light_buttons::previous(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::previous(), check_state);
}

light_button light_buttons::previous(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::previous(), check_state, location);
}

light_button light_buttons::previous(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::previous(), check_state, location, size);
}

light_button light_buttons::previous(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::previous(), check_state, location, size, name);
}

light_button light_buttons::previous(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::previous(), three_state, check_state);
}

light_button light_buttons::previous(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::previous(), three_state, check_state, location);
}

light_button light_buttons::previous(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::previous(), three_state, check_state, location, size);
}

light_button light_buttons::previous(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::previous(), three_state, check_state, location, size, name);
}

light_button light_buttons::previous(const control& parent) {
  return light_button::create(parent, system_texts::previous());
}

light_button light_buttons::previous(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::previous(), location);
}

light_button light_buttons::previous(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::previous(), location, size);
}

light_button light_buttons::previous(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::previous(), location, size, name);
}

light_button light_buttons::previous(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::previous(), check_state);
}

light_button light_buttons::previous(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::previous(), check_state, location);
}

light_button light_buttons::previous(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::previous(), check_state, location, size);
}

light_button light_buttons::previous(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::previous(), check_state, location, size, name);
}

light_button light_buttons::previous(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::previous(), three_state, check_state);
}

light_button light_buttons::previous(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::previous(), three_state, check_state, location);
}

light_button light_buttons::previous(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::previous(), three_state, check_state, location, size);
}

light_button light_buttons::previous(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::previous(), three_state, check_state, location, size, name);
}

light_button light_buttons::print() {
  return light_button::create(system_texts::print());
}

light_button light_buttons::print(const drawing::point& location) {
  return light_button::create(system_texts::print(), location);
}

light_button light_buttons::print(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::print(), location, size);
}

light_button light_buttons::print(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::print(), location, size, name);
}

light_button light_buttons::print(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::print(), check_state);
}

light_button light_buttons::print(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::print(), check_state, location);
}

light_button light_buttons::print(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::print(), check_state, location, size);
}

light_button light_buttons::print(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::print(), check_state, location, size, name);
}

light_button light_buttons::print(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::print(), three_state, check_state);
}

light_button light_buttons::print(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::print(), three_state, check_state, location);
}

light_button light_buttons::print(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::print(), three_state, check_state, location, size);
}

light_button light_buttons::print(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::print(), three_state, check_state, location, size, name);
}

light_button light_buttons::print(const control& parent) {
  return light_button::create(parent, system_texts::print());
}

light_button light_buttons::print(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::print(), location);
}

light_button light_buttons::print(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::print(), location, size);
}

light_button light_buttons::print(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::print(), location, size, name);
}

light_button light_buttons::print(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::print(), check_state);
}

light_button light_buttons::print(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::print(), check_state, location);
}

light_button light_buttons::print(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::print(), check_state, location, size);
}

light_button light_buttons::print(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::print(), check_state, location, size, name);
}

light_button light_buttons::print(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::print(), three_state, check_state);
}

light_button light_buttons::print(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::print(), three_state, check_state, location);
}

light_button light_buttons::print(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::print(), three_state, check_state, location, size);
}

light_button light_buttons::print(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::print(), three_state, check_state, location, size, name);
}

light_button light_buttons::print_preview() {
  return light_button::create(system_texts::print_preview());
}

light_button light_buttons::print_preview(const drawing::point& location) {
  return light_button::create(system_texts::print_preview(), location);
}

light_button light_buttons::print_preview(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::print_preview(), location, size);
}

light_button light_buttons::print_preview(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::print_preview(), location, size, name);
}

light_button light_buttons::print_preview(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::print_preview(), check_state);
}

light_button light_buttons::print_preview(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::print_preview(), check_state, location);
}

light_button light_buttons::print_preview(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::print_preview(), check_state, location, size);
}

light_button light_buttons::print_preview(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::print_preview(), check_state, location, size, name);
}

light_button light_buttons::print_preview(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::print_preview(), three_state, check_state);
}

light_button light_buttons::print_preview(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::print_preview(), three_state, check_state, location);
}

light_button light_buttons::print_preview(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::print_preview(), three_state, check_state, location, size);
}

light_button light_buttons::print_preview(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::print_preview(), three_state, check_state, location, size, name);
}

light_button light_buttons::print_preview(const control& parent) {
  return light_button::create(parent, system_texts::print_preview());
}

light_button light_buttons::print_preview(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::print_preview(), location);
}

light_button light_buttons::print_preview(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::print_preview(), location, size);
}

light_button light_buttons::print_preview(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::print_preview(), location, size, name);
}

light_button light_buttons::print_preview(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::print_preview(), check_state);
}

light_button light_buttons::print_preview(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::print_preview(), check_state, location);
}

light_button light_buttons::print_preview(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::print_preview(), check_state, location, size);
}

light_button light_buttons::print_preview(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::print_preview(), check_state, location, size, name);
}

light_button light_buttons::print_preview(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::print_preview(), three_state, check_state);
}

light_button light_buttons::print_preview(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::print_preview(), three_state, check_state, location);
}

light_button light_buttons::print_preview(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::print_preview(), three_state, check_state, location, size);
}

light_button light_buttons::print_preview(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::print_preview(), three_state, check_state, location, size, name);
}

light_button light_buttons::properties() {
  return light_button::create(system_texts::properties());
}

light_button light_buttons::properties(const drawing::point& location) {
  return light_button::create(system_texts::properties(), location);
}

light_button light_buttons::properties(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::properties(), location, size);
}

light_button light_buttons::properties(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::properties(), location, size, name);
}

light_button light_buttons::properties(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::properties(), check_state);
}

light_button light_buttons::properties(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::properties(), check_state, location);
}

light_button light_buttons::properties(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::properties(), check_state, location, size);
}

light_button light_buttons::properties(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::properties(), check_state, location, size, name);
}

light_button light_buttons::properties(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::properties(), three_state, check_state);
}

light_button light_buttons::properties(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::properties(), three_state, check_state, location);
}

light_button light_buttons::properties(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::properties(), three_state, check_state, location, size);
}

light_button light_buttons::properties(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::properties(), three_state, check_state, location, size, name);
}

light_button light_buttons::properties(const control& parent) {
  return light_button::create(parent, system_texts::properties());
}

light_button light_buttons::properties(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::properties(), location);
}

light_button light_buttons::properties(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::properties(), location, size);
}

light_button light_buttons::properties(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::properties(), location, size, name);
}

light_button light_buttons::properties(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::properties(), check_state);
}

light_button light_buttons::properties(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::properties(), check_state, location);
}

light_button light_buttons::properties(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::properties(), check_state, location, size);
}

light_button light_buttons::properties(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::properties(), check_state, location, size, name);
}

light_button light_buttons::properties(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::properties(), three_state, check_state);
}

light_button light_buttons::properties(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::properties(), three_state, check_state, location);
}

light_button light_buttons::properties(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::properties(), three_state, check_state, location, size);
}

light_button light_buttons::properties(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::properties(), three_state, check_state, location, size, name);
}

light_button light_buttons::quit() {
  return light_button::create(system_texts::quit());
}

light_button light_buttons::quit(const drawing::point& location) {
  return light_button::create(system_texts::quit(), location);
}

light_button light_buttons::quit(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::quit(), location, size);
}

light_button light_buttons::quit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::quit(), location, size, name);
}

light_button light_buttons::quit(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::quit(), check_state);
}

light_button light_buttons::quit(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::quit(), check_state, location);
}

light_button light_buttons::quit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::quit(), check_state, location, size);
}

light_button light_buttons::quit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::quit(), check_state, location, size, name);
}

light_button light_buttons::quit(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::quit(), three_state, check_state);
}

light_button light_buttons::quit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::quit(), three_state, check_state, location);
}

light_button light_buttons::quit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::quit(), three_state, check_state, location, size);
}

light_button light_buttons::quit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::quit(), three_state, check_state, location, size, name);
}

light_button light_buttons::quit(const control& parent) {
  return light_button::create(parent, system_texts::quit());
}

light_button light_buttons::quit(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::quit(), location);
}

light_button light_buttons::quit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::quit(), location, size);
}

light_button light_buttons::quit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::quit(), location, size, name);
}

light_button light_buttons::quit(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::quit(), check_state);
}

light_button light_buttons::quit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::quit(), check_state, location);
}

light_button light_buttons::quit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::quit(), check_state, location, size);
}

light_button light_buttons::quit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::quit(), check_state, location, size, name);
}

light_button light_buttons::quit(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::quit(), three_state, check_state);
}

light_button light_buttons::quit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::quit(), three_state, check_state, location);
}

light_button light_buttons::quit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::quit(), three_state, check_state, location, size);
}

light_button light_buttons::quit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::quit(), three_state, check_state, location, size, name);
}

light_button light_buttons::redo() {
  return light_button::create(system_texts::redo());
}

light_button light_buttons::redo(const drawing::point& location) {
  return light_button::create(system_texts::redo(), location);
}

light_button light_buttons::redo(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::redo(), location, size);
}

light_button light_buttons::redo(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::redo(), location, size, name);
}

light_button light_buttons::redo(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::redo(), check_state);
}

light_button light_buttons::redo(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::redo(), check_state, location);
}

light_button light_buttons::redo(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::redo(), check_state, location, size);
}

light_button light_buttons::redo(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::redo(), check_state, location, size, name);
}

light_button light_buttons::redo(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::redo(), three_state, check_state);
}

light_button light_buttons::redo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::redo(), three_state, check_state, location);
}

light_button light_buttons::redo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::redo(), three_state, check_state, location, size);
}

light_button light_buttons::redo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::redo(), three_state, check_state, location, size, name);
}

light_button light_buttons::redo(const control& parent) {
  return light_button::create(parent, system_texts::redo());
}

light_button light_buttons::redo(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::redo(), location);
}

light_button light_buttons::redo(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::redo(), location, size);
}

light_button light_buttons::redo(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::redo(), location, size, name);
}

light_button light_buttons::redo(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::redo(), check_state);
}

light_button light_buttons::redo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::redo(), check_state, location);
}

light_button light_buttons::redo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::redo(), check_state, location, size);
}

light_button light_buttons::redo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::redo(), check_state, location, size, name);
}

light_button light_buttons::redo(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::redo(), three_state, check_state);
}

light_button light_buttons::redo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::redo(), three_state, check_state, location);
}

light_button light_buttons::redo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::redo(), three_state, check_state, location, size);
}

light_button light_buttons::redo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::redo(), three_state, check_state, location, size, name);
}

light_button light_buttons::refresh() {
  return light_button::create(system_texts::refresh());
}

light_button light_buttons::refresh(const drawing::point& location) {
  return light_button::create(system_texts::refresh(), location);
}

light_button light_buttons::refresh(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::refresh(), location, size);
}

light_button light_buttons::refresh(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::refresh(), location, size, name);
}

light_button light_buttons::refresh(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::refresh(), check_state);
}

light_button light_buttons::refresh(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::refresh(), check_state, location);
}

light_button light_buttons::refresh(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::refresh(), check_state, location, size);
}

light_button light_buttons::refresh(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::refresh(), check_state, location, size, name);
}

light_button light_buttons::refresh(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::refresh(), three_state, check_state);
}

light_button light_buttons::refresh(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::refresh(), three_state, check_state, location);
}

light_button light_buttons::refresh(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::refresh(), three_state, check_state, location, size);
}

light_button light_buttons::refresh(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::refresh(), three_state, check_state, location, size, name);
}

light_button light_buttons::refresh(const control& parent) {
  return light_button::create(parent, system_texts::refresh());
}

light_button light_buttons::refresh(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::refresh(), location);
}

light_button light_buttons::refresh(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::refresh(), location, size);
}

light_button light_buttons::refresh(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::refresh(), location, size, name);
}

light_button light_buttons::refresh(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::refresh(), check_state);
}

light_button light_buttons::refresh(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::refresh(), check_state, location);
}

light_button light_buttons::refresh(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::refresh(), check_state, location, size);
}

light_button light_buttons::refresh(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::refresh(), check_state, location, size, name);
}

light_button light_buttons::refresh(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::refresh(), three_state, check_state);
}

light_button light_buttons::refresh(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::refresh(), three_state, check_state, location);
}

light_button light_buttons::refresh(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::refresh(), three_state, check_state, location, size);
}

light_button light_buttons::refresh(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::refresh(), three_state, check_state, location, size, name);
}

light_button light_buttons::remove() {
  return light_button::create(system_texts::remove());
}

light_button light_buttons::remove(const drawing::point& location) {
  return light_button::create(system_texts::remove(), location);
}

light_button light_buttons::remove(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::remove(), location, size);
}

light_button light_buttons::remove(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::remove(), location, size, name);
}

light_button light_buttons::remove(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::remove(), check_state);
}

light_button light_buttons::remove(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::remove(), check_state, location);
}

light_button light_buttons::remove(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::remove(), check_state, location, size);
}

light_button light_buttons::remove(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::remove(), check_state, location, size, name);
}

light_button light_buttons::remove(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::remove(), three_state, check_state);
}

light_button light_buttons::remove(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::remove(), three_state, check_state, location);
}

light_button light_buttons::remove(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::remove(), three_state, check_state, location, size);
}

light_button light_buttons::remove(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::remove(), three_state, check_state, location, size, name);
}

light_button light_buttons::remove(const control& parent) {
  return light_button::create(parent, system_texts::remove());
}

light_button light_buttons::remove(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::remove(), location);
}

light_button light_buttons::remove(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::remove(), location, size);
}

light_button light_buttons::remove(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::remove(), location, size, name);
}

light_button light_buttons::remove(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::remove(), check_state);
}

light_button light_buttons::remove(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::remove(), check_state, location);
}

light_button light_buttons::remove(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::remove(), check_state, location, size);
}

light_button light_buttons::remove(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::remove(), check_state, location, size, name);
}

light_button light_buttons::remove(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::remove(), three_state, check_state);
}

light_button light_buttons::remove(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::remove(), three_state, check_state, location);
}

light_button light_buttons::remove(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::remove(), three_state, check_state, location, size);
}

light_button light_buttons::remove(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::remove(), three_state, check_state, location, size, name);
}

light_button light_buttons::replace() {
  return light_button::create(system_texts::replace());
}

light_button light_buttons::replace(const drawing::point& location) {
  return light_button::create(system_texts::replace(), location);
}

light_button light_buttons::replace(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::replace(), location, size);
}

light_button light_buttons::replace(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::replace(), location, size, name);
}

light_button light_buttons::replace(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::replace(), check_state);
}

light_button light_buttons::replace(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::replace(), check_state, location);
}

light_button light_buttons::replace(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::replace(), check_state, location, size);
}

light_button light_buttons::replace(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::replace(), check_state, location, size, name);
}

light_button light_buttons::replace(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::replace(), three_state, check_state);
}

light_button light_buttons::replace(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::replace(), three_state, check_state, location);
}

light_button light_buttons::replace(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::replace(), three_state, check_state, location, size);
}

light_button light_buttons::replace(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::replace(), three_state, check_state, location, size, name);
}

light_button light_buttons::replace(const control& parent) {
  return light_button::create(parent, system_texts::replace());
}

light_button light_buttons::replace(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::replace(), location);
}

light_button light_buttons::replace(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::replace(), location, size);
}

light_button light_buttons::replace(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::replace(), location, size, name);
}

light_button light_buttons::replace(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::replace(), check_state);
}

light_button light_buttons::replace(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::replace(), check_state, location);
}

light_button light_buttons::replace(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::replace(), check_state, location, size);
}

light_button light_buttons::replace(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::replace(), check_state, location, size, name);
}

light_button light_buttons::replace(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::replace(), three_state, check_state);
}

light_button light_buttons::replace(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::replace(), three_state, check_state, location);
}

light_button light_buttons::replace(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::replace(), three_state, check_state, location, size);
}

light_button light_buttons::replace(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::replace(), three_state, check_state, location, size, name);
}

light_button light_buttons::retry() {
  return light_button::create(system_texts::retry());
}

light_button light_buttons::retry(const drawing::point& location) {
  return light_button::create(system_texts::retry(), location);
}

light_button light_buttons::retry(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::retry(), location, size);
}

light_button light_buttons::retry(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::retry(), location, size, name);
}

light_button light_buttons::retry(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::retry(), check_state);
}

light_button light_buttons::retry(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::retry(), check_state, location);
}

light_button light_buttons::retry(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::retry(), check_state, location, size);
}

light_button light_buttons::retry(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::retry(), check_state, location, size, name);
}

light_button light_buttons::retry(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::retry(), three_state, check_state);
}

light_button light_buttons::retry(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::retry(), three_state, check_state, location);
}

light_button light_buttons::retry(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::retry(), three_state, check_state, location, size);
}

light_button light_buttons::retry(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::retry(), three_state, check_state, location, size, name);
}

light_button light_buttons::retry(const control& parent) {
  return light_button::create(parent, system_texts::retry());
}

light_button light_buttons::retry(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::retry(), location);
}

light_button light_buttons::retry(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::retry(), location, size);
}

light_button light_buttons::retry(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::retry(), location, size, name);
}

light_button light_buttons::retry(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::retry(), check_state);
}

light_button light_buttons::retry(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::retry(), check_state, location);
}

light_button light_buttons::retry(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::retry(), check_state, location, size);
}

light_button light_buttons::retry(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::retry(), check_state, location, size, name);
}

light_button light_buttons::retry(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::retry(), three_state, check_state);
}

light_button light_buttons::retry(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::retry(), three_state, check_state, location);
}

light_button light_buttons::retry(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::retry(), three_state, check_state, location, size);
}

light_button light_buttons::retry(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::retry(), three_state, check_state, location, size, name);
}

light_button light_buttons::revert_to_saved() {
  return light_button::create(system_texts::revert_to_saved());
}

light_button light_buttons::revert_to_saved(const drawing::point& location) {
  return light_button::create(system_texts::revert_to_saved(), location);
}

light_button light_buttons::revert_to_saved(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::revert_to_saved(), location, size);
}

light_button light_buttons::revert_to_saved(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::revert_to_saved(), location, size, name);
}

light_button light_buttons::revert_to_saved(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::revert_to_saved(), check_state);
}

light_button light_buttons::revert_to_saved(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::revert_to_saved(), check_state, location);
}

light_button light_buttons::revert_to_saved(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::revert_to_saved(), check_state, location, size);
}

light_button light_buttons::revert_to_saved(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::revert_to_saved(), check_state, location, size, name);
}

light_button light_buttons::revert_to_saved(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::revert_to_saved(), three_state, check_state);
}

light_button light_buttons::revert_to_saved(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::revert_to_saved(), three_state, check_state, location);
}

light_button light_buttons::revert_to_saved(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::revert_to_saved(), three_state, check_state, location, size);
}

light_button light_buttons::revert_to_saved(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::revert_to_saved(), three_state, check_state, location, size, name);
}

light_button light_buttons::revert_to_saved(const control& parent) {
  return light_button::create(parent, system_texts::revert_to_saved());
}

light_button light_buttons::revert_to_saved(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::revert_to_saved(), location);
}

light_button light_buttons::revert_to_saved(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::revert_to_saved(), location, size);
}

light_button light_buttons::revert_to_saved(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::revert_to_saved(), location, size, name);
}

light_button light_buttons::revert_to_saved(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::revert_to_saved(), check_state);
}

light_button light_buttons::revert_to_saved(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::revert_to_saved(), check_state, location);
}

light_button light_buttons::revert_to_saved(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::revert_to_saved(), check_state, location, size);
}

light_button light_buttons::revert_to_saved(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::revert_to_saved(), check_state, location, size, name);
}

light_button light_buttons::revert_to_saved(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::revert_to_saved(), three_state, check_state);
}

light_button light_buttons::revert_to_saved(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::revert_to_saved(), three_state, check_state, location);
}

light_button light_buttons::revert_to_saved(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::revert_to_saved(), three_state, check_state, location, size);
}

light_button light_buttons::revert_to_saved(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::revert_to_saved(), three_state, check_state, location, size, name);
}

light_button light_buttons::save() {
  return light_button::create(system_texts::save());
}

light_button light_buttons::save(const drawing::point& location) {
  return light_button::create(system_texts::save(), location);
}

light_button light_buttons::save(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::save(), location, size);
}

light_button light_buttons::save(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::save(), location, size, name);
}

light_button light_buttons::save(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::save(), check_state);
}

light_button light_buttons::save(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::save(), check_state, location);
}

light_button light_buttons::save(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::save(), check_state, location, size);
}

light_button light_buttons::save(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::save(), check_state, location, size, name);
}

light_button light_buttons::save(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::save(), three_state, check_state);
}

light_button light_buttons::save(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::save(), three_state, check_state, location);
}

light_button light_buttons::save(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::save(), three_state, check_state, location, size);
}

light_button light_buttons::save(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::save(), three_state, check_state, location, size, name);
}

light_button light_buttons::save(const control& parent) {
  return light_button::create(parent, system_texts::save());
}

light_button light_buttons::save(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::save(), location);
}

light_button light_buttons::save(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::save(), location, size);
}

light_button light_buttons::save(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::save(), location, size, name);
}

light_button light_buttons::save(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::save(), check_state);
}

light_button light_buttons::save(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::save(), check_state, location);
}

light_button light_buttons::save(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::save(), check_state, location, size);
}

light_button light_buttons::save(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::save(), check_state, location, size, name);
}

light_button light_buttons::save(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::save(), three_state, check_state);
}

light_button light_buttons::save(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::save(), three_state, check_state, location);
}

light_button light_buttons::save(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::save(), three_state, check_state, location, size);
}

light_button light_buttons::save(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::save(), three_state, check_state, location, size, name);
}

light_button light_buttons::save_as() {
  return light_button::create(system_texts::save_as());
}

light_button light_buttons::save_as(const drawing::point& location) {
  return light_button::create(system_texts::save_as(), location);
}

light_button light_buttons::save_as(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::save_as(), location, size);
}

light_button light_buttons::save_as(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::save_as(), location, size, name);
}

light_button light_buttons::save_as(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::save_as(), check_state);
}

light_button light_buttons::save_as(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::save_as(), check_state, location);
}

light_button light_buttons::save_as(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::save_as(), check_state, location, size);
}

light_button light_buttons::save_as(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::save_as(), check_state, location, size, name);
}

light_button light_buttons::save_as(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::save_as(), three_state, check_state);
}

light_button light_buttons::save_as(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::save_as(), three_state, check_state, location);
}

light_button light_buttons::save_as(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::save_as(), three_state, check_state, location, size);
}

light_button light_buttons::save_as(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::save_as(), three_state, check_state, location, size, name);
}

light_button light_buttons::save_as(const control& parent) {
  return light_button::create(parent, system_texts::save_as());
}

light_button light_buttons::save_as(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::save_as(), location);
}

light_button light_buttons::save_as(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::save_as(), location, size);
}

light_button light_buttons::save_as(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::save_as(), location, size, name);
}

light_button light_buttons::save_as(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::save_as(), check_state);
}

light_button light_buttons::save_as(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::save_as(), check_state, location);
}

light_button light_buttons::save_as(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::save_as(), check_state, location, size);
}

light_button light_buttons::save_as(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::save_as(), check_state, location, size, name);
}

light_button light_buttons::save_as(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::save_as(), three_state, check_state);
}

light_button light_buttons::save_as(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::save_as(), three_state, check_state, location);
}

light_button light_buttons::save_as(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::save_as(), three_state, check_state, location, size);
}

light_button light_buttons::save_as(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::save_as(), three_state, check_state, location, size, name);
}

light_button light_buttons::search() {
  return light_button::create(system_texts::search());
}

light_button light_buttons::search(const drawing::point& location) {
  return light_button::create(system_texts::search(), location);
}

light_button light_buttons::search(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::search(), location, size);
}

light_button light_buttons::search(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::search(), location, size, name);
}

light_button light_buttons::search(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::search(), check_state);
}

light_button light_buttons::search(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::search(), check_state, location);
}

light_button light_buttons::search(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::search(), check_state, location, size);
}

light_button light_buttons::search(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::search(), check_state, location, size, name);
}

light_button light_buttons::search(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::search(), three_state, check_state);
}

light_button light_buttons::search(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::search(), three_state, check_state, location);
}

light_button light_buttons::search(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::search(), three_state, check_state, location, size);
}

light_button light_buttons::search(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::search(), three_state, check_state, location, size, name);
}

light_button light_buttons::search(const control& parent) {
  return light_button::create(parent, system_texts::search());
}

light_button light_buttons::search(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::search(), location);
}

light_button light_buttons::search(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::search(), location, size);
}

light_button light_buttons::search(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::search(), location, size, name);
}

light_button light_buttons::search(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::search(), check_state);
}

light_button light_buttons::search(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::search(), check_state, location);
}

light_button light_buttons::search(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::search(), check_state, location, size);
}

light_button light_buttons::search(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::search(), check_state, location, size, name);
}

light_button light_buttons::search(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::search(), three_state, check_state);
}

light_button light_buttons::search(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::search(), three_state, check_state, location);
}

light_button light_buttons::search(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::search(), three_state, check_state, location, size);
}

light_button light_buttons::search(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::search(), three_state, check_state, location, size, name);
}

light_button light_buttons::select_all() {
  return light_button::create(system_texts::select_all());
}

light_button light_buttons::select_all(const drawing::point& location) {
  return light_button::create(system_texts::select_all(), location);
}

light_button light_buttons::select_all(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::select_all(), location, size);
}

light_button light_buttons::select_all(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::select_all(), location, size, name);
}

light_button light_buttons::select_all(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::select_all(), check_state);
}

light_button light_buttons::select_all(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::select_all(), check_state, location);
}

light_button light_buttons::select_all(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::select_all(), check_state, location, size);
}

light_button light_buttons::select_all(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::select_all(), check_state, location, size, name);
}

light_button light_buttons::select_all(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::select_all(), three_state, check_state);
}

light_button light_buttons::select_all(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::select_all(), three_state, check_state, location);
}

light_button light_buttons::select_all(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::select_all(), three_state, check_state, location, size);
}

light_button light_buttons::select_all(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::select_all(), three_state, check_state, location, size, name);
}

light_button light_buttons::select_all(const control& parent) {
  return light_button::create(parent, system_texts::select_all());
}

light_button light_buttons::select_all(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::select_all(), location);
}

light_button light_buttons::select_all(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::select_all(), location, size);
}

light_button light_buttons::select_all(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::select_all(), location, size, name);
}

light_button light_buttons::select_all(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::select_all(), check_state);
}

light_button light_buttons::select_all(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::select_all(), check_state, location);
}

light_button light_buttons::select_all(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::select_all(), check_state, location, size);
}

light_button light_buttons::select_all(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::select_all(), check_state, location, size, name);
}

light_button light_buttons::select_all(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::select_all(), three_state, check_state);
}

light_button light_buttons::select_all(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::select_all(), three_state, check_state, location);
}

light_button light_buttons::select_all(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::select_all(), three_state, check_state, location, size);
}

light_button light_buttons::select_all(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::select_all(), three_state, check_state, location, size, name);
}

light_button light_buttons::settings() {
  return light_button::create(system_texts::settings());
}

light_button light_buttons::settings(const drawing::point& location) {
  return light_button::create(system_texts::settings(), location);
}

light_button light_buttons::settings(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::settings(), location, size);
}

light_button light_buttons::settings(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::settings(), location, size, name);
}

light_button light_buttons::settings(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::settings(), check_state);
}

light_button light_buttons::settings(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::settings(), check_state, location);
}

light_button light_buttons::settings(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::settings(), check_state, location, size);
}

light_button light_buttons::settings(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::settings(), check_state, location, size, name);
}

light_button light_buttons::settings(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::settings(), three_state, check_state);
}

light_button light_buttons::settings(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::settings(), three_state, check_state, location);
}

light_button light_buttons::settings(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::settings(), three_state, check_state, location, size);
}

light_button light_buttons::settings(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::settings(), three_state, check_state, location, size, name);
}

light_button light_buttons::settings(const control& parent) {
  return light_button::create(parent, system_texts::settings());
}

light_button light_buttons::settings(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::settings(), location);
}

light_button light_buttons::settings(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::settings(), location, size);
}

light_button light_buttons::settings(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::settings(), location, size, name);
}

light_button light_buttons::settings(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::settings(), check_state);
}

light_button light_buttons::settings(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::settings(), check_state, location);
}

light_button light_buttons::settings(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::settings(), check_state, location, size);
}

light_button light_buttons::settings(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::settings(), check_state, location, size, name);
}

light_button light_buttons::settings(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::settings(), three_state, check_state);
}

light_button light_buttons::settings(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::settings(), three_state, check_state, location);
}

light_button light_buttons::settings(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::settings(), three_state, check_state, location, size);
}

light_button light_buttons::settings(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::settings(), three_state, check_state, location, size, name);
}

light_button light_buttons::spell_check() {
  return light_button::create(system_texts::spell_check());
}

light_button light_buttons::spell_check(const drawing::point& location) {
  return light_button::create(system_texts::spell_check(), location);
}

light_button light_buttons::spell_check(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::spell_check(), location, size);
}

light_button light_buttons::spell_check(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::spell_check(), location, size, name);
}

light_button light_buttons::spell_check(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::spell_check(), check_state);
}

light_button light_buttons::spell_check(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::spell_check(), check_state, location);
}

light_button light_buttons::spell_check(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::spell_check(), check_state, location, size);
}

light_button light_buttons::spell_check(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::spell_check(), check_state, location, size, name);
}

light_button light_buttons::spell_check(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::spell_check(), three_state, check_state);
}

light_button light_buttons::spell_check(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::spell_check(), three_state, check_state, location);
}

light_button light_buttons::spell_check(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::spell_check(), three_state, check_state, location, size);
}

light_button light_buttons::spell_check(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::spell_check(), three_state, check_state, location, size, name);
}

light_button light_buttons::spell_check(const control& parent) {
  return light_button::create(parent, system_texts::spell_check());
}

light_button light_buttons::spell_check(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::spell_check(), location);
}

light_button light_buttons::spell_check(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::spell_check(), location, size);
}

light_button light_buttons::spell_check(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::spell_check(), location, size, name);
}

light_button light_buttons::spell_check(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::spell_check(), check_state);
}

light_button light_buttons::spell_check(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::spell_check(), check_state, location);
}

light_button light_buttons::spell_check(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::spell_check(), check_state, location, size);
}

light_button light_buttons::spell_check(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::spell_check(), check_state, location, size, name);
}

light_button light_buttons::spell_check(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::spell_check(), three_state, check_state);
}

light_button light_buttons::spell_check(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::spell_check(), three_state, check_state, location);
}

light_button light_buttons::spell_check(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::spell_check(), three_state, check_state, location, size);
}

light_button light_buttons::spell_check(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::spell_check(), three_state, check_state, location, size, name);
}

light_button light_buttons::stop() {
  return light_button::create(system_texts::stop());
}

light_button light_buttons::stop(const drawing::point& location) {
  return light_button::create(system_texts::stop(), location);
}

light_button light_buttons::stop(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::stop(), location, size);
}

light_button light_buttons::stop(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::stop(), location, size, name);
}

light_button light_buttons::stop(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::stop(), check_state);
}

light_button light_buttons::stop(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::stop(), check_state, location);
}

light_button light_buttons::stop(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::stop(), check_state, location, size);
}

light_button light_buttons::stop(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::stop(), check_state, location, size, name);
}

light_button light_buttons::stop(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::stop(), three_state, check_state);
}

light_button light_buttons::stop(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::stop(), three_state, check_state, location);
}

light_button light_buttons::stop(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::stop(), three_state, check_state, location, size);
}

light_button light_buttons::stop(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::stop(), three_state, check_state, location, size, name);
}

light_button light_buttons::stop(const control& parent) {
  return light_button::create(parent, system_texts::stop());
}

light_button light_buttons::stop(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::stop(), location);
}

light_button light_buttons::stop(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::stop(), location, size);
}

light_button light_buttons::stop(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::stop(), location, size, name);
}

light_button light_buttons::stop(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::stop(), check_state);
}

light_button light_buttons::stop(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::stop(), check_state, location);
}

light_button light_buttons::stop(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::stop(), check_state, location, size);
}

light_button light_buttons::stop(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::stop(), check_state, location, size, name);
}

light_button light_buttons::stop(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::stop(), three_state, check_state);
}

light_button light_buttons::stop(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::stop(), three_state, check_state, location);
}

light_button light_buttons::stop(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::stop(), three_state, check_state, location, size);
}

light_button light_buttons::stop(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::stop(), three_state, check_state, location, size, name);
}

light_button light_buttons::strikeout() {
  return light_button::create(system_texts::strikeout());
}

light_button light_buttons::strikeout(const drawing::point& location) {
  return light_button::create(system_texts::strikeout(), location);
}

light_button light_buttons::strikeout(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::strikeout(), location, size);
}

light_button light_buttons::strikeout(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::strikeout(), location, size, name);
}

light_button light_buttons::strikeout(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::strikeout(), check_state);
}

light_button light_buttons::strikeout(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::strikeout(), check_state, location);
}

light_button light_buttons::strikeout(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::strikeout(), check_state, location, size);
}

light_button light_buttons::strikeout(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::strikeout(), check_state, location, size, name);
}

light_button light_buttons::strikeout(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::strikeout(), three_state, check_state);
}

light_button light_buttons::strikeout(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::strikeout(), three_state, check_state, location);
}

light_button light_buttons::strikeout(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::strikeout(), three_state, check_state, location, size);
}

light_button light_buttons::strikeout(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::strikeout(), three_state, check_state, location, size, name);
}

light_button light_buttons::strikeout(const control& parent) {
  return light_button::create(parent, system_texts::strikeout());
}

light_button light_buttons::strikeout(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::strikeout(), location);
}

light_button light_buttons::strikeout(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::strikeout(), location, size);
}

light_button light_buttons::strikeout(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::strikeout(), location, size, name);
}

light_button light_buttons::strikeout(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::strikeout(), check_state);
}

light_button light_buttons::strikeout(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::strikeout(), check_state, location);
}

light_button light_buttons::strikeout(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::strikeout(), check_state, location, size);
}

light_button light_buttons::strikeout(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::strikeout(), check_state, location, size, name);
}

light_button light_buttons::strikeout(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::strikeout(), three_state, check_state);
}

light_button light_buttons::strikeout(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::strikeout(), three_state, check_state, location);
}

light_button light_buttons::strikeout(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::strikeout(), three_state, check_state, location, size);
}

light_button light_buttons::strikeout(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::strikeout(), three_state, check_state, location, size, name);
}

light_button light_buttons::tools() {
  return light_button::create(system_texts::tools());
}

light_button light_buttons::tools(const drawing::point& location) {
  return light_button::create(system_texts::tools(), location);
}

light_button light_buttons::tools(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::tools(), location, size);
}

light_button light_buttons::tools(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::tools(), location, size, name);
}

light_button light_buttons::tools(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::tools(), check_state);
}

light_button light_buttons::tools(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::tools(), check_state, location);
}

light_button light_buttons::tools(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::tools(), check_state, location, size);
}

light_button light_buttons::tools(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::tools(), check_state, location, size, name);
}

light_button light_buttons::tools(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::tools(), three_state, check_state);
}

light_button light_buttons::tools(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::tools(), three_state, check_state, location);
}

light_button light_buttons::tools(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::tools(), three_state, check_state, location, size);
}

light_button light_buttons::tools(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::tools(), three_state, check_state, location, size, name);
}

light_button light_buttons::tools(const control& parent) {
  return light_button::create(parent, system_texts::tools());
}

light_button light_buttons::tools(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::tools(), location);
}

light_button light_buttons::tools(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::tools(), location, size);
}

light_button light_buttons::tools(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::tools(), location, size, name);
}

light_button light_buttons::tools(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::tools(), check_state);
}

light_button light_buttons::tools(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::tools(), check_state, location);
}

light_button light_buttons::tools(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::tools(), check_state, location, size);
}

light_button light_buttons::tools(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::tools(), check_state, location, size, name);
}

light_button light_buttons::tools(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::tools(), three_state, check_state);
}

light_button light_buttons::tools(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::tools(), three_state, check_state, location);
}

light_button light_buttons::tools(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::tools(), three_state, check_state, location, size);
}

light_button light_buttons::tools(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::tools(), three_state, check_state, location, size, name);
}

light_button light_buttons::top() {
  return light_button::create(system_texts::top());
}

light_button light_buttons::top(const drawing::point& location) {
  return light_button::create(system_texts::top(), location);
}

light_button light_buttons::top(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::top(), location, size);
}

light_button light_buttons::top(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::top(), location, size, name);
}

light_button light_buttons::top(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::top(), check_state);
}

light_button light_buttons::top(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::top(), check_state, location);
}

light_button light_buttons::top(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::top(), check_state, location, size);
}

light_button light_buttons::top(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::top(), check_state, location, size, name);
}

light_button light_buttons::top(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::top(), three_state, check_state);
}

light_button light_buttons::top(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::top(), three_state, check_state, location);
}

light_button light_buttons::top(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::top(), three_state, check_state, location, size);
}

light_button light_buttons::top(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::top(), three_state, check_state, location, size, name);
}

light_button light_buttons::top(const control& parent) {
  return light_button::create(parent, system_texts::top());
}

light_button light_buttons::top(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::top(), location);
}

light_button light_buttons::top(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::top(), location, size);
}

light_button light_buttons::top(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::top(), location, size, name);
}

light_button light_buttons::top(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::top(), check_state);
}

light_button light_buttons::top(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::top(), check_state, location);
}

light_button light_buttons::top(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::top(), check_state, location, size);
}

light_button light_buttons::top(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::top(), check_state, location, size, name);
}

light_button light_buttons::top(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::top(), three_state, check_state);
}

light_button light_buttons::top(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::top(), three_state, check_state, location);
}

light_button light_buttons::top(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::top(), three_state, check_state, location, size);
}

light_button light_buttons::top(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::top(), three_state, check_state, location, size, name);
}

light_button light_buttons::undelete() {
  return light_button::create(system_texts::undelete());
}

light_button light_buttons::undelete(const drawing::point& location) {
  return light_button::create(system_texts::undelete(), location);
}

light_button light_buttons::undelete(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::undelete(), location, size);
}

light_button light_buttons::undelete(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::undelete(), location, size, name);
}

light_button light_buttons::undelete(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::undelete(), check_state);
}

light_button light_buttons::undelete(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::undelete(), check_state, location);
}

light_button light_buttons::undelete(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::undelete(), check_state, location, size);
}

light_button light_buttons::undelete(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::undelete(), check_state, location, size, name);
}

light_button light_buttons::undelete(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::undelete(), three_state, check_state);
}

light_button light_buttons::undelete(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::undelete(), three_state, check_state, location);
}

light_button light_buttons::undelete(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::undelete(), three_state, check_state, location, size);
}

light_button light_buttons::undelete(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::undelete(), three_state, check_state, location, size, name);
}

light_button light_buttons::undelete(const control& parent) {
  return light_button::create(parent, system_texts::undelete());
}

light_button light_buttons::undelete(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::undelete(), location);
}

light_button light_buttons::undelete(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::undelete(), location, size);
}

light_button light_buttons::undelete(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::undelete(), location, size, name);
}

light_button light_buttons::undelete(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::undelete(), check_state);
}

light_button light_buttons::undelete(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::undelete(), check_state, location);
}

light_button light_buttons::undelete(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::undelete(), check_state, location, size);
}

light_button light_buttons::undelete(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::undelete(), check_state, location, size, name);
}

light_button light_buttons::undelete(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::undelete(), three_state, check_state);
}

light_button light_buttons::undelete(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::undelete(), three_state, check_state, location);
}

light_button light_buttons::undelete(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::undelete(), three_state, check_state, location, size);
}

light_button light_buttons::undelete(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::undelete(), three_state, check_state, location, size, name);
}

light_button light_buttons::underline() {
  return light_button::create(system_texts::underline());
}

light_button light_buttons::underline(const drawing::point& location) {
  return light_button::create(system_texts::underline(), location);
}

light_button light_buttons::underline(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::underline(), location, size);
}

light_button light_buttons::underline(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::underline(), location, size, name);
}

light_button light_buttons::underline(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::underline(), check_state);
}

light_button light_buttons::underline(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::underline(), check_state, location);
}

light_button light_buttons::underline(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::underline(), check_state, location, size);
}

light_button light_buttons::underline(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::underline(), check_state, location, size, name);
}

light_button light_buttons::underline(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::underline(), three_state, check_state);
}

light_button light_buttons::underline(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::underline(), three_state, check_state, location);
}

light_button light_buttons::underline(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::underline(), three_state, check_state, location, size);
}

light_button light_buttons::underline(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::underline(), three_state, check_state, location, size, name);
}

light_button light_buttons::underline(const control& parent) {
  return light_button::create(parent, system_texts::underline());
}

light_button light_buttons::underline(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::underline(), location);
}

light_button light_buttons::underline(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::underline(), location, size);
}

light_button light_buttons::underline(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::underline(), location, size, name);
}

light_button light_buttons::underline(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::underline(), check_state);
}

light_button light_buttons::underline(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::underline(), check_state, location);
}

light_button light_buttons::underline(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::underline(), check_state, location, size);
}

light_button light_buttons::underline(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::underline(), check_state, location, size, name);
}

light_button light_buttons::underline(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::underline(), three_state, check_state);
}

light_button light_buttons::underline(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::underline(), three_state, check_state, location);
}

light_button light_buttons::underline(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::underline(), three_state, check_state, location, size);
}

light_button light_buttons::underline(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::underline(), three_state, check_state, location, size, name);
}

light_button light_buttons::undo() {
  return light_button::create(system_texts::undo());
}

light_button light_buttons::undo(const drawing::point& location) {
  return light_button::create(system_texts::undo(), location);
}

light_button light_buttons::undo(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::undo(), location, size);
}

light_button light_buttons::undo(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::undo(), location, size, name);
}

light_button light_buttons::undo(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::undo(), check_state);
}

light_button light_buttons::undo(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::undo(), check_state, location);
}

light_button light_buttons::undo(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::undo(), check_state, location, size);
}

light_button light_buttons::undo(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::undo(), check_state, location, size, name);
}

light_button light_buttons::undo(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::undo(), three_state, check_state);
}

light_button light_buttons::undo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::undo(), three_state, check_state, location);
}

light_button light_buttons::undo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::undo(), three_state, check_state, location, size);
}

light_button light_buttons::undo(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::undo(), three_state, check_state, location, size, name);
}

light_button light_buttons::undo(const control& parent) {
  return light_button::create(parent, system_texts::undo());
}

light_button light_buttons::undo(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::undo(), location);
}

light_button light_buttons::undo(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::undo(), location, size);
}

light_button light_buttons::undo(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::undo(), location, size, name);
}

light_button light_buttons::undo(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::undo(), check_state);
}

light_button light_buttons::undo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::undo(), check_state, location);
}

light_button light_buttons::undo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::undo(), check_state, location, size);
}

light_button light_buttons::undo(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::undo(), check_state, location, size, name);
}

light_button light_buttons::undo(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::undo(), three_state, check_state);
}

light_button light_buttons::undo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::undo(), three_state, check_state, location);
}

light_button light_buttons::undo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::undo(), three_state, check_state, location, size);
}

light_button light_buttons::undo(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::undo(), three_state, check_state, location, size, name);
}

light_button light_buttons::unindent() {
  return light_button::create(system_texts::unindent());
}

light_button light_buttons::unindent(const drawing::point& location) {
  return light_button::create(system_texts::unindent(), location);
}

light_button light_buttons::unindent(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::unindent(), location, size);
}

light_button light_buttons::unindent(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::unindent(), location, size, name);
}

light_button light_buttons::unindent(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::unindent(), check_state);
}

light_button light_buttons::unindent(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::unindent(), check_state, location);
}

light_button light_buttons::unindent(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::unindent(), check_state, location, size);
}

light_button light_buttons::unindent(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::unindent(), check_state, location, size, name);
}

light_button light_buttons::unindent(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::unindent(), three_state, check_state);
}

light_button light_buttons::unindent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::unindent(), three_state, check_state, location);
}

light_button light_buttons::unindent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::unindent(), three_state, check_state, location, size);
}

light_button light_buttons::unindent(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::unindent(), three_state, check_state, location, size, name);
}

light_button light_buttons::unindent(const control& parent) {
  return light_button::create(parent, system_texts::unindent());
}

light_button light_buttons::unindent(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::unindent(), location);
}

light_button light_buttons::unindent(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::unindent(), location, size);
}

light_button light_buttons::unindent(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::unindent(), location, size, name);
}

light_button light_buttons::unindent(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::unindent(), check_state);
}

light_button light_buttons::unindent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::unindent(), check_state, location);
}

light_button light_buttons::unindent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::unindent(), check_state, location, size);
}

light_button light_buttons::unindent(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::unindent(), check_state, location, size, name);
}

light_button light_buttons::unindent(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::unindent(), three_state, check_state);
}

light_button light_buttons::unindent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::unindent(), three_state, check_state, location);
}

light_button light_buttons::unindent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::unindent(), three_state, check_state, location, size);
}

light_button light_buttons::unindent(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::unindent(), three_state, check_state, location, size, name);
}

light_button light_buttons::up() {
  return light_button::create(system_texts::up());
}

light_button light_buttons::up(const drawing::point& location) {
  return light_button::create(system_texts::up(), location);
}

light_button light_buttons::up(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::up(), location, size);
}

light_button light_buttons::up(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::up(), location, size, name);
}

light_button light_buttons::up(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::up(), check_state);
}

light_button light_buttons::up(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::up(), check_state, location);
}

light_button light_buttons::up(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::up(), check_state, location, size);
}

light_button light_buttons::up(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::up(), check_state, location, size, name);
}

light_button light_buttons::up(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::up(), three_state, check_state);
}

light_button light_buttons::up(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::up(), three_state, check_state, location);
}

light_button light_buttons::up(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::up(), three_state, check_state, location, size);
}

light_button light_buttons::up(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::up(), three_state, check_state, location, size, name);
}

light_button light_buttons::up(const control& parent) {
  return light_button::create(parent, system_texts::up());
}

light_button light_buttons::up(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::up(), location);
}

light_button light_buttons::up(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::up(), location, size);
}

light_button light_buttons::up(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::up(), location, size, name);
}

light_button light_buttons::up(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::up(), check_state);
}

light_button light_buttons::up(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::up(), check_state, location);
}

light_button light_buttons::up(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::up(), check_state, location, size);
}

light_button light_buttons::up(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::up(), check_state, location, size, name);
}

light_button light_buttons::up(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::up(), three_state, check_state);
}

light_button light_buttons::up(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::up(), three_state, check_state, location);
}

light_button light_buttons::up(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::up(), three_state, check_state, location, size);
}

light_button light_buttons::up(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::up(), three_state, check_state, location, size, name);
}

light_button light_buttons::view() {
  return light_button::create(system_texts::view());
}

light_button light_buttons::view(const drawing::point& location) {
  return light_button::create(system_texts::view(), location);
}

light_button light_buttons::view(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::view(), location, size);
}

light_button light_buttons::view(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::view(), location, size, name);
}

light_button light_buttons::view(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::view(), check_state);
}

light_button light_buttons::view(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::view(), check_state, location);
}

light_button light_buttons::view(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::view(), check_state, location, size);
}

light_button light_buttons::view(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::view(), check_state, location, size, name);
}

light_button light_buttons::view(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::view(), three_state, check_state);
}

light_button light_buttons::view(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::view(), three_state, check_state, location);
}

light_button light_buttons::view(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::view(), three_state, check_state, location, size);
}

light_button light_buttons::view(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::view(), three_state, check_state, location, size, name);
}

light_button light_buttons::view(const control& parent) {
  return light_button::create(parent, system_texts::view());
}

light_button light_buttons::view(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::view(), location);
}

light_button light_buttons::view(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::view(), location, size);
}

light_button light_buttons::view(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::view(), location, size, name);
}

light_button light_buttons::view(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::view(), check_state);
}

light_button light_buttons::view(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::view(), check_state, location);
}

light_button light_buttons::view(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::view(), check_state, location, size);
}

light_button light_buttons::view(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::view(), check_state, location, size, name);
}

light_button light_buttons::view(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::view(), three_state, check_state);
}

light_button light_buttons::view(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::view(), three_state, check_state, location);
}

light_button light_buttons::view(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::view(), three_state, check_state, location, size);
}

light_button light_buttons::view(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::view(), three_state, check_state, location, size, name);
}

light_button light_buttons::window() {
  return light_button::create(system_texts::window());
}

light_button light_buttons::window(const drawing::point& location) {
  return light_button::create(system_texts::window(), location);
}

light_button light_buttons::window(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::window(), location, size);
}

light_button light_buttons::window(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::window(), location, size, name);
}

light_button light_buttons::window(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::window(), check_state);
}

light_button light_buttons::window(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::window(), check_state, location);
}

light_button light_buttons::window(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::window(), check_state, location, size);
}

light_button light_buttons::window(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::window(), check_state, location, size, name);
}

light_button light_buttons::window(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::window(), three_state, check_state);
}

light_button light_buttons::window(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::window(), three_state, check_state, location);
}

light_button light_buttons::window(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::window(), three_state, check_state, location, size);
}

light_button light_buttons::window(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::window(), three_state, check_state, location, size, name);
}

light_button light_buttons::window(const control& parent) {
  return light_button::create(parent, system_texts::window());
}

light_button light_buttons::window(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::window(), location);
}

light_button light_buttons::window(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::window(), location, size);
}

light_button light_buttons::window(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::window(), location, size, name);
}

light_button light_buttons::window(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::window(), check_state);
}

light_button light_buttons::window(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::window(), check_state, location);
}

light_button light_buttons::window(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::window(), check_state, location, size);
}

light_button light_buttons::window(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::window(), check_state, location, size, name);
}

light_button light_buttons::window(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::window(), three_state, check_state);
}

light_button light_buttons::window(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::window(), three_state, check_state, location);
}

light_button light_buttons::window(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::window(), three_state, check_state, location, size);
}

light_button light_buttons::window(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::window(), three_state, check_state, location, size, name);
}

light_button light_buttons::yes() {
  return light_button::create(system_texts::yes());
}

light_button light_buttons::yes(const drawing::point& location) {
  return light_button::create(system_texts::yes(), location);
}

light_button light_buttons::yes(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::yes(), location, size);
}

light_button light_buttons::yes(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::yes(), location, size, name);
}

light_button light_buttons::yes(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::yes(), check_state);
}

light_button light_buttons::yes(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::yes(), check_state, location);
}

light_button light_buttons::yes(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::yes(), check_state, location, size);
}

light_button light_buttons::yes(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::yes(), check_state, location, size, name);
}

light_button light_buttons::yes(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::yes(), three_state, check_state);
}

light_button light_buttons::yes(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::yes(), three_state, check_state, location);
}

light_button light_buttons::yes(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::yes(), three_state, check_state, location, size);
}

light_button light_buttons::yes(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::yes(), three_state, check_state, location, size, name);
}

light_button light_buttons::yes(const control& parent) {
  return light_button::create(parent, system_texts::yes());
}

light_button light_buttons::yes(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::yes(), location);
}

light_button light_buttons::yes(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::yes(), location, size);
}

light_button light_buttons::yes(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::yes(), location, size, name);
}

light_button light_buttons::yes(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::yes(), check_state);
}

light_button light_buttons::yes(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::yes(), check_state, location);
}

light_button light_buttons::yes(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::yes(), check_state, location, size);
}

light_button light_buttons::yes(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::yes(), check_state, location, size, name);
}

light_button light_buttons::yes(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::yes(), three_state, check_state);
}

light_button light_buttons::yes(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::yes(), three_state, check_state, location);
}

light_button light_buttons::yes(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::yes(), three_state, check_state, location, size);
}

light_button light_buttons::yes(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::yes(), three_state, check_state, location, size, name);
}

light_button light_buttons::zoom_in() {
  return light_button::create(system_texts::zoom_in());
}

light_button light_buttons::zoom_in(const drawing::point& location) {
  return light_button::create(system_texts::zoom_in(), location);
}

light_button light_buttons::zoom_in(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::zoom_in(), location, size);
}

light_button light_buttons::zoom_in(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::zoom_in(), location, size, name);
}

light_button light_buttons::zoom_in(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::zoom_in(), check_state);
}

light_button light_buttons::zoom_in(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::zoom_in(), check_state, location);
}

light_button light_buttons::zoom_in(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::zoom_in(), check_state, location, size);
}

light_button light_buttons::zoom_in(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::zoom_in(), check_state, location, size, name);
}

light_button light_buttons::zoom_in(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::zoom_in(), three_state, check_state);
}

light_button light_buttons::zoom_in(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::zoom_in(), three_state, check_state, location);
}

light_button light_buttons::zoom_in(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::zoom_in(), three_state, check_state, location, size);
}

light_button light_buttons::zoom_in(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::zoom_in(), three_state, check_state, location, size, name);
}

light_button light_buttons::zoom_in(const control& parent) {
  return light_button::create(parent, system_texts::zoom_in());
}

light_button light_buttons::zoom_in(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::zoom_in(), location);
}

light_button light_buttons::zoom_in(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::zoom_in(), location, size);
}

light_button light_buttons::zoom_in(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::zoom_in(), location, size, name);
}

light_button light_buttons::zoom_in(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::zoom_in(), check_state);
}

light_button light_buttons::zoom_in(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::zoom_in(), check_state, location);
}

light_button light_buttons::zoom_in(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::zoom_in(), check_state, location, size);
}

light_button light_buttons::zoom_in(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::zoom_in(), check_state, location, size, name);
}

light_button light_buttons::zoom_in(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::zoom_in(), three_state, check_state);
}

light_button light_buttons::zoom_in(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::zoom_in(), three_state, check_state, location);
}

light_button light_buttons::zoom_in(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::zoom_in(), three_state, check_state, location, size);
}

light_button light_buttons::zoom_in(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::zoom_in(), three_state, check_state, location, size, name);
}

light_button light_buttons::zoom_out() {
  return light_button::create(system_texts::zoom_out());
}

light_button light_buttons::zoom_out(const drawing::point& location) {
  return light_button::create(system_texts::zoom_out(), location);
}

light_button light_buttons::zoom_out(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::zoom_out(), location, size);
}

light_button light_buttons::zoom_out(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::zoom_out(), location, size, name);
}

light_button light_buttons::zoom_out(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::zoom_out(), check_state);
}

light_button light_buttons::zoom_out(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::zoom_out(), check_state, location);
}

light_button light_buttons::zoom_out(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::zoom_out(), check_state, location, size);
}

light_button light_buttons::zoom_out(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::zoom_out(), check_state, location, size, name);
}

light_button light_buttons::zoom_out(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::zoom_out(), three_state, check_state);
}

light_button light_buttons::zoom_out(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::zoom_out(), three_state, check_state, location);
}

light_button light_buttons::zoom_out(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::zoom_out(), three_state, check_state, location, size);
}

light_button light_buttons::zoom_out(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::zoom_out(), three_state, check_state, location, size, name);
}

light_button light_buttons::zoom_out(const control& parent) {
  return light_button::create(parent, system_texts::zoom_out());
}

light_button light_buttons::zoom_out(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::zoom_out(), location);
}

light_button light_buttons::zoom_out(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::zoom_out(), location, size);
}

light_button light_buttons::zoom_out(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::zoom_out(), location, size, name);
}

light_button light_buttons::zoom_out(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::zoom_out(), check_state);
}

light_button light_buttons::zoom_out(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::zoom_out(), check_state, location);
}

light_button light_buttons::zoom_out(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::zoom_out(), check_state, location, size);
}

light_button light_buttons::zoom_out(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::zoom_out(), check_state, location, size, name);
}

light_button light_buttons::zoom_out(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::zoom_out(), three_state, check_state);
}

light_button light_buttons::zoom_out(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::zoom_out(), three_state, check_state, location);
}

light_button light_buttons::zoom_out(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::zoom_out(), three_state, check_state, location, size);
}

light_button light_buttons::zoom_out(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::zoom_out(), three_state, check_state, location, size, name);
}

light_button light_buttons::zoom_to_fit() {
  return light_button::create(system_texts::zoom_to_fit());
}

light_button light_buttons::zoom_to_fit(const drawing::point& location) {
  return light_button::create(system_texts::zoom_to_fit(), location);
}

light_button light_buttons::zoom_to_fit(const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::zoom_to_fit(), location, size);
}

light_button light_buttons::zoom_to_fit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::zoom_to_fit(), location, size, name);
}

light_button light_buttons::zoom_to_fit(xtd::forms::check_state check_state) {
  return light_button::create(system_texts::zoom_to_fit(), check_state);
}

light_button light_buttons::zoom_to_fit(xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::zoom_to_fit(), check_state, location);
}

light_button light_buttons::zoom_to_fit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::zoom_to_fit(), check_state, location, size);
}

light_button light_buttons::zoom_to_fit(xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::zoom_to_fit(), check_state, location, size, name);
}

light_button light_buttons::zoom_to_fit(bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(system_texts::zoom_to_fit(), three_state, check_state);
}

light_button light_buttons::zoom_to_fit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(system_texts::zoom_to_fit(), three_state, check_state, location);
}

light_button light_buttons::zoom_to_fit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(system_texts::zoom_to_fit(), three_state, check_state, location, size);
}

light_button light_buttons::zoom_to_fit(bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(system_texts::zoom_to_fit(), three_state, check_state, location, size, name);
}

light_button light_buttons::zoom_to_fit(const control& parent) {
  return light_button::create(parent, system_texts::zoom_to_fit());
}

light_button light_buttons::zoom_to_fit(const control& parent, const drawing::point& location) {
  return light_button::create(parent, system_texts::zoom_to_fit(), location);
}

light_button light_buttons::zoom_to_fit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::zoom_to_fit(), location, size);
}

light_button light_buttons::zoom_to_fit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::zoom_to_fit(), location, size, name);
}

light_button light_buttons::zoom_to_fit(const control& parent, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::zoom_to_fit(), check_state);
}

light_button light_buttons::zoom_to_fit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::zoom_to_fit(), check_state, location);
}

light_button light_buttons::zoom_to_fit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::zoom_to_fit(), check_state, location, size);
}

light_button light_buttons::zoom_to_fit(const control& parent, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::zoom_to_fit(), check_state, location, size, name);
}

light_button light_buttons::zoom_to_fit(const control& parent, bool three_state, xtd::forms::check_state check_state) {
  return light_button::create(parent, system_texts::zoom_to_fit(), three_state, check_state);
}

light_button light_buttons::zoom_to_fit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location) {
  return light_button::create(parent, system_texts::zoom_to_fit(), three_state, check_state, location);
}

light_button light_buttons::zoom_to_fit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size) {
  return light_button::create(parent, system_texts::zoom_to_fit(), three_state, check_state, location, size);
}

light_button light_buttons::zoom_to_fit(const control& parent, bool three_state, xtd::forms::check_state check_state, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return light_button::create(parent, system_texts::zoom_to_fit(), three_state, check_state, location, size, name);
}
