#include "../../../include/xtd/forms/radio_buttons.h"
#include <xtd/drawing/texts>

using namespace xtd;
using namespace xtd::drawing;
using namespace xtd::forms;

radio_button radio_buttons::abort() {
  return radio_button::create(texts::abort());
}

radio_button radio_buttons::abort(const drawing::point& location) {
  return radio_button::create(texts::abort(), location);
}

radio_button radio_buttons::abort(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::abort(), location, size);
}

radio_button radio_buttons::abort(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::abort(), location, size, name);
}

radio_button radio_buttons::abort(bool checked) {
  return radio_button::create(texts::abort(), checked);
}

radio_button radio_buttons::abort(bool checked, const drawing::point& location) {
  return radio_button::create(texts::abort(), checked, location);
}

radio_button radio_buttons::abort(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::abort(), checked, location, size);
}

radio_button radio_buttons::abort(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::abort(), checked, location, size, name);
}

radio_button radio_buttons::abort(const control& parent) {
  return radio_button::create(parent, texts::abort());
}

radio_button radio_buttons::abort(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::abort(), location);
}

radio_button radio_buttons::abort(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::abort(), location, size);
}

radio_button radio_buttons::abort(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::abort(), location, size, name);
}

radio_button radio_buttons::abort(const control& parent, bool checked) {
  return radio_button::create(parent, texts::abort(), checked);
}

radio_button radio_buttons::abort(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::abort(), checked, location);
}

radio_button radio_buttons::abort(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::abort(), checked, location, size);
}

radio_button radio_buttons::abort(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::abort(), checked, location, size, name);
}

radio_button radio_buttons::about() {
  return radio_button::create(texts::about());
}

radio_button radio_buttons::about(const drawing::point& location) {
  return radio_button::create(texts::about(), location);
}

radio_button radio_buttons::about(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::about(), location, size);
}

radio_button radio_buttons::about(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::about(), location, size, name);
}

radio_button radio_buttons::about(bool checked) {
  return radio_button::create(texts::about(), checked);
}

radio_button radio_buttons::about(bool checked, const drawing::point& location) {
  return radio_button::create(texts::about(), checked, location);
}

radio_button radio_buttons::about(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::about(), checked, location, size);
}

radio_button radio_buttons::about(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::about(), checked, location, size, name);
}

radio_button radio_buttons::about(const control& parent) {
  return radio_button::create(parent, texts::about());
}

radio_button radio_buttons::about(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::about(), location);
}

radio_button radio_buttons::about(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::about(), location, size);
}

radio_button radio_buttons::about(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::about(), location, size, name);
}

radio_button radio_buttons::about(const control& parent, bool checked) {
  return radio_button::create(parent, texts::about(), checked);
}

radio_button radio_buttons::about(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::about(), checked, location);
}

radio_button radio_buttons::about(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::about(), checked, location, size);
}

radio_button radio_buttons::about(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::about(), checked, location, size, name);
}

radio_button radio_buttons::actual_size() {
  return radio_button::create(texts::actual_size());
}

radio_button radio_buttons::actual_size(const drawing::point& location) {
  return radio_button::create(texts::actual_size(), location);
}

radio_button radio_buttons::actual_size(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::actual_size(), location, size);
}

radio_button radio_buttons::actual_size(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::actual_size(), location, size, name);
}

radio_button radio_buttons::actual_size(bool checked) {
  return radio_button::create(texts::actual_size(), checked);
}

radio_button radio_buttons::actual_size(bool checked, const drawing::point& location) {
  return radio_button::create(texts::actual_size(), checked, location);
}

radio_button radio_buttons::actual_size(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::actual_size(), checked, location, size);
}

radio_button radio_buttons::actual_size(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::actual_size(), checked, location, size, name);
}

radio_button radio_buttons::actual_size(const control& parent) {
  return radio_button::create(parent, texts::actual_size());
}

radio_button radio_buttons::actual_size(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::actual_size(), location);
}

radio_button radio_buttons::actual_size(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::actual_size(), location, size);
}

radio_button radio_buttons::actual_size(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::actual_size(), location, size, name);
}

radio_button radio_buttons::actual_size(const control& parent, bool checked) {
  return radio_button::create(parent, texts::actual_size(), checked);
}

radio_button radio_buttons::actual_size(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::actual_size(), checked, location);
}

radio_button radio_buttons::actual_size(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::actual_size(), checked, location, size);
}

radio_button radio_buttons::actual_size(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::actual_size(), checked, location, size, name);
}

radio_button radio_buttons::add() {
  return radio_button::create(texts::add());
}

radio_button radio_buttons::add(const drawing::point& location) {
  return radio_button::create(texts::add(), location);
}

radio_button radio_buttons::add(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::add(), location, size);
}

radio_button radio_buttons::add(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::add(), location, size, name);
}

radio_button radio_buttons::add(bool checked) {
  return radio_button::create(texts::add(), checked);
}

radio_button radio_buttons::add(bool checked, const drawing::point& location) {
  return radio_button::create(texts::add(), checked, location);
}

radio_button radio_buttons::add(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::add(), checked, location, size);
}

radio_button radio_buttons::add(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::add(), checked, location, size, name);
}

radio_button radio_buttons::add(const control& parent) {
  return radio_button::create(parent, texts::add());
}

radio_button radio_buttons::add(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::add(), location);
}

radio_button radio_buttons::add(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::add(), location, size);
}

radio_button radio_buttons::add(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::add(), location, size, name);
}

radio_button radio_buttons::add(const control& parent, bool checked) {
  return radio_button::create(parent, texts::add(), checked);
}

radio_button radio_buttons::add(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::add(), checked, location);
}

radio_button radio_buttons::add(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::add(), checked, location, size);
}

radio_button radio_buttons::add(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::add(), checked, location, size, name);
}

radio_button radio_buttons::alignment() {
  return radio_button::create(texts::alignment());
}

radio_button radio_buttons::alignment(const drawing::point& location) {
  return radio_button::create(texts::alignment(), location);
}

radio_button radio_buttons::alignment(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::alignment(), location, size);
}

radio_button radio_buttons::alignment(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::alignment(), location, size, name);
}

radio_button radio_buttons::alignment(bool checked) {
  return radio_button::create(texts::alignment(), checked);
}

radio_button radio_buttons::alignment(bool checked, const drawing::point& location) {
  return radio_button::create(texts::alignment(), checked, location);
}

radio_button radio_buttons::alignment(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::alignment(), checked, location, size);
}

radio_button radio_buttons::alignment(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::alignment(), checked, location, size, name);
}

radio_button radio_buttons::alignment(const control& parent) {
  return radio_button::create(parent, texts::alignment());
}

radio_button radio_buttons::alignment(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::alignment(), location);
}

radio_button radio_buttons::alignment(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::alignment(), location, size);
}

radio_button radio_buttons::alignment(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::alignment(), location, size, name);
}

radio_button radio_buttons::alignment(const control& parent, bool checked) {
  return radio_button::create(parent, texts::alignment(), checked);
}

radio_button radio_buttons::alignment(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::alignment(), checked, location);
}

radio_button radio_buttons::alignment(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::alignment(), checked, location, size);
}

radio_button radio_buttons::alignment(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::alignment(), checked, location, size, name);
}

radio_button radio_buttons::align_left() {
  return radio_button::create(texts::align_left());
}

radio_button radio_buttons::align_left(const drawing::point& location) {
  return radio_button::create(texts::align_left(), location);
}

radio_button radio_buttons::align_left(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::align_left(), location, size);
}

radio_button radio_buttons::align_left(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::align_left(), location, size, name);
}

radio_button radio_buttons::align_left(bool checked) {
  return radio_button::create(texts::align_left(), checked);
}

radio_button radio_buttons::align_left(bool checked, const drawing::point& location) {
  return radio_button::create(texts::align_left(), checked, location);
}

radio_button radio_buttons::align_left(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::align_left(), checked, location, size);
}

radio_button radio_buttons::align_left(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::align_left(), checked, location, size, name);
}

radio_button radio_buttons::align_left(const control& parent) {
  return radio_button::create(parent, texts::align_left());
}

radio_button radio_buttons::align_left(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::align_left(), location);
}

radio_button radio_buttons::align_left(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::align_left(), location, size);
}

radio_button radio_buttons::align_left(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::align_left(), location, size, name);
}

radio_button radio_buttons::align_left(const control& parent, bool checked) {
  return radio_button::create(parent, texts::align_left(), checked);
}

radio_button radio_buttons::align_left(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::align_left(), checked, location);
}

radio_button radio_buttons::align_left(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::align_left(), checked, location, size);
}

radio_button radio_buttons::align_left(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::align_left(), checked, location, size, name);
}

radio_button radio_buttons::align_right() {
  return radio_button::create(texts::align_right());
}

radio_button radio_buttons::align_right(const drawing::point& location) {
  return radio_button::create(texts::align_right(), location);
}

radio_button radio_buttons::align_right(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::align_right(), location, size);
}

radio_button radio_buttons::align_right(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::align_right(), location, size, name);
}

radio_button radio_buttons::align_right(bool checked) {
  return radio_button::create(texts::align_right(), checked);
}

radio_button radio_buttons::align_right(bool checked, const drawing::point& location) {
  return radio_button::create(texts::align_right(), checked, location);
}

radio_button radio_buttons::align_right(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::align_right(), checked, location, size);
}

radio_button radio_buttons::align_right(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::align_right(), checked, location, size, name);
}

radio_button radio_buttons::align_right(const control& parent) {
  return radio_button::create(parent, texts::align_right());
}

radio_button radio_buttons::align_right(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::align_right(), location);
}

radio_button radio_buttons::align_right(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::align_right(), location, size);
}

radio_button radio_buttons::align_right(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::align_right(), location, size, name);
}

radio_button radio_buttons::align_right(const control& parent, bool checked) {
  return radio_button::create(parent, texts::align_right(), checked);
}

radio_button radio_buttons::align_right(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::align_right(), checked, location);
}

radio_button radio_buttons::align_right(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::align_right(), checked, location, size);
}

radio_button radio_buttons::align_right(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::align_right(), checked, location, size, name);
}

radio_button radio_buttons::apply() {
  return radio_button::create(texts::apply());
}

radio_button radio_buttons::apply(const drawing::point& location) {
  return radio_button::create(texts::apply(), location);
}

radio_button radio_buttons::apply(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::apply(), location, size);
}

radio_button radio_buttons::apply(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::apply(), location, size, name);
}

radio_button radio_buttons::apply(bool checked) {
  return radio_button::create(texts::apply(), checked);
}

radio_button radio_buttons::apply(bool checked, const drawing::point& location) {
  return radio_button::create(texts::apply(), checked, location);
}

radio_button radio_buttons::apply(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::apply(), checked, location, size);
}

radio_button radio_buttons::apply(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::apply(), checked, location, size, name);
}

radio_button radio_buttons::apply(const control& parent) {
  return radio_button::create(parent, texts::apply());
}

radio_button radio_buttons::apply(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::apply(), location);
}

radio_button radio_buttons::apply(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::apply(), location, size);
}

radio_button radio_buttons::apply(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::apply(), location, size, name);
}

radio_button radio_buttons::apply(const control& parent, bool checked) {
  return radio_button::create(parent, texts::apply(), checked);
}

radio_button radio_buttons::apply(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::apply(), checked, location);
}

radio_button radio_buttons::apply(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::apply(), checked, location, size);
}

radio_button radio_buttons::apply(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::apply(), checked, location, size, name);
}

radio_button radio_buttons::ascending() {
  return radio_button::create(texts::ascending());
}

radio_button radio_buttons::ascending(const drawing::point& location) {
  return radio_button::create(texts::ascending(), location);
}

radio_button radio_buttons::ascending(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::ascending(), location, size);
}

radio_button radio_buttons::ascending(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::ascending(), location, size, name);
}

radio_button radio_buttons::ascending(bool checked) {
  return radio_button::create(texts::ascending(), checked);
}

radio_button radio_buttons::ascending(bool checked, const drawing::point& location) {
  return radio_button::create(texts::ascending(), checked, location);
}

radio_button radio_buttons::ascending(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::ascending(), checked, location, size);
}

radio_button radio_buttons::ascending(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::ascending(), checked, location, size, name);
}

radio_button radio_buttons::ascending(const control& parent) {
  return radio_button::create(parent, texts::ascending());
}

radio_button radio_buttons::ascending(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::ascending(), location);
}

radio_button radio_buttons::ascending(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::ascending(), location, size);
}

radio_button radio_buttons::ascending(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::ascending(), location, size, name);
}

radio_button radio_buttons::ascending(const control& parent, bool checked) {
  return radio_button::create(parent, texts::ascending(), checked);
}

radio_button radio_buttons::ascending(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::ascending(), checked, location);
}

radio_button radio_buttons::ascending(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::ascending(), checked, location, size);
}

radio_button radio_buttons::ascending(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::ascending(), checked, location, size, name);
}

radio_button radio_buttons::back() {
  return radio_button::create(texts::back());
}

radio_button radio_buttons::back(const drawing::point& location) {
  return radio_button::create(texts::back(), location);
}

radio_button radio_buttons::back(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::back(), location, size);
}

radio_button radio_buttons::back(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::back(), location, size, name);
}

radio_button radio_buttons::back(bool checked) {
  return radio_button::create(texts::back(), checked);
}

radio_button radio_buttons::back(bool checked, const drawing::point& location) {
  return radio_button::create(texts::back(), checked, location);
}

radio_button radio_buttons::back(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::back(), checked, location, size);
}

radio_button radio_buttons::back(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::back(), checked, location, size, name);
}

radio_button radio_buttons::back(const control& parent) {
  return radio_button::create(parent, texts::back());
}

radio_button radio_buttons::back(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::back(), location);
}

radio_button radio_buttons::back(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::back(), location, size);
}

radio_button radio_buttons::back(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::back(), location, size, name);
}

radio_button radio_buttons::back(const control& parent, bool checked) {
  return radio_button::create(parent, texts::back(), checked);
}

radio_button radio_buttons::back(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::back(), checked, location);
}

radio_button radio_buttons::back(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::back(), checked, location, size);
}

radio_button radio_buttons::back(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::back(), checked, location, size, name);
}

radio_button radio_buttons::bold() {
  return radio_button::create(texts::bold());
}

radio_button radio_buttons::bold(const drawing::point& location) {
  return radio_button::create(texts::bold(), location);
}

radio_button radio_buttons::bold(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::bold(), location, size);
}

radio_button radio_buttons::bold(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::bold(), location, size, name);
}

radio_button radio_buttons::bold(bool checked) {
  return radio_button::create(texts::bold(), checked);
}

radio_button radio_buttons::bold(bool checked, const drawing::point& location) {
  return radio_button::create(texts::bold(), checked, location);
}

radio_button radio_buttons::bold(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::bold(), checked, location, size);
}

radio_button radio_buttons::bold(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::bold(), checked, location, size, name);
}

radio_button radio_buttons::bold(const control& parent) {
  return radio_button::create(parent, texts::bold());
}

radio_button radio_buttons::bold(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::bold(), location);
}

radio_button radio_buttons::bold(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::bold(), location, size);
}

radio_button radio_buttons::bold(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::bold(), location, size, name);
}

radio_button radio_buttons::bold(const control& parent, bool checked) {
  return radio_button::create(parent, texts::bold(), checked);
}

radio_button radio_buttons::bold(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::bold(), checked, location);
}

radio_button radio_buttons::bold(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::bold(), checked, location, size);
}

radio_button radio_buttons::bold(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::bold(), checked, location, size, name);
}

radio_button radio_buttons::bottom() {
  return radio_button::create(texts::bottom());
}

radio_button radio_buttons::bottom(const drawing::point& location) {
  return radio_button::create(texts::bottom(), location);
}

radio_button radio_buttons::bottom(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::bottom(), location, size);
}

radio_button radio_buttons::bottom(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::bottom(), location, size, name);
}

radio_button radio_buttons::bottom(bool checked) {
  return radio_button::create(texts::bottom(), checked);
}

radio_button radio_buttons::bottom(bool checked, const drawing::point& location) {
  return radio_button::create(texts::bottom(), checked, location);
}

radio_button radio_buttons::bottom(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::bottom(), checked, location, size);
}

radio_button radio_buttons::bottom(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::bottom(), checked, location, size, name);
}

radio_button radio_buttons::bottom(const control& parent) {
  return radio_button::create(parent, texts::bottom());
}

radio_button radio_buttons::bottom(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::bottom(), location);
}

radio_button radio_buttons::bottom(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::bottom(), location, size);
}

radio_button radio_buttons::bottom(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::bottom(), location, size, name);
}

radio_button radio_buttons::bottom(const control& parent, bool checked) {
  return radio_button::create(parent, texts::bottom(), checked);
}

radio_button radio_buttons::bottom(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::bottom(), checked, location);
}

radio_button radio_buttons::bottom(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::bottom(), checked, location, size);
}

radio_button radio_buttons::bottom(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::bottom(), checked, location, size, name);
}

radio_button radio_buttons::cancel() {
  return radio_button::create(texts::cancel());
}

radio_button radio_buttons::cancel(const drawing::point& location) {
  return radio_button::create(texts::cancel(), location);
}

radio_button radio_buttons::cancel(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::cancel(), location, size);
}

radio_button radio_buttons::cancel(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::cancel(), location, size, name);
}

radio_button radio_buttons::cancel(bool checked) {
  return radio_button::create(texts::cancel(), checked);
}

radio_button radio_buttons::cancel(bool checked, const drawing::point& location) {
  return radio_button::create(texts::cancel(), checked, location);
}

radio_button radio_buttons::cancel(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::cancel(), checked, location, size);
}

radio_button radio_buttons::cancel(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::cancel(), checked, location, size, name);
}

radio_button radio_buttons::cancel(const control& parent) {
  return radio_button::create(parent, texts::cancel());
}

radio_button radio_buttons::cancel(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::cancel(), location);
}

radio_button radio_buttons::cancel(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::cancel(), location, size);
}

radio_button radio_buttons::cancel(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::cancel(), location, size, name);
}

radio_button radio_buttons::cancel(const control& parent, bool checked) {
  return radio_button::create(parent, texts::cancel(), checked);
}

radio_button radio_buttons::cancel(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::cancel(), checked, location);
}

radio_button radio_buttons::cancel(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::cancel(), checked, location, size);
}

radio_button radio_buttons::cancel(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::cancel(), checked, location, size, name);
}

radio_button radio_buttons::cdrom() {
  return radio_button::create(texts::cdrom());
}

radio_button radio_buttons::cdrom(const drawing::point& location) {
  return radio_button::create(texts::cdrom(), location);
}

radio_button radio_buttons::cdrom(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::cdrom(), location, size);
}

radio_button radio_buttons::cdrom(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::cdrom(), location, size, name);
}

radio_button radio_buttons::cdrom(bool checked) {
  return radio_button::create(texts::cdrom(), checked);
}

radio_button radio_buttons::cdrom(bool checked, const drawing::point& location) {
  return radio_button::create(texts::cdrom(), checked, location);
}

radio_button radio_buttons::cdrom(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::cdrom(), checked, location, size);
}

radio_button radio_buttons::cdrom(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::cdrom(), checked, location, size, name);
}

radio_button radio_buttons::cdrom(const control& parent) {
  return radio_button::create(parent, texts::cdrom());
}

radio_button radio_buttons::cdrom(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::cdrom(), location);
}

radio_button radio_buttons::cdrom(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::cdrom(), location, size);
}

radio_button radio_buttons::cdrom(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::cdrom(), location, size, name);
}

radio_button radio_buttons::cdrom(const control& parent, bool checked) {
  return radio_button::create(parent, texts::cdrom(), checked);
}

radio_button radio_buttons::cdrom(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::cdrom(), checked, location);
}

radio_button radio_buttons::cdrom(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::cdrom(), checked, location, size);
}

radio_button radio_buttons::cdrom(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::cdrom(), checked, location, size, name);
}

radio_button radio_buttons::centered() {
  return radio_button::create(texts::centered());
}

radio_button radio_buttons::centered(const drawing::point& location) {
  return radio_button::create(texts::centered(), location);
}

radio_button radio_buttons::centered(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::centered(), location, size);
}

radio_button radio_buttons::centered(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::centered(), location, size, name);
}

radio_button radio_buttons::centered(bool checked) {
  return radio_button::create(texts::centered(), checked);
}

radio_button radio_buttons::centered(bool checked, const drawing::point& location) {
  return radio_button::create(texts::centered(), checked, location);
}

radio_button radio_buttons::centered(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::centered(), checked, location, size);
}

radio_button radio_buttons::centered(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::centered(), checked, location, size, name);
}

radio_button radio_buttons::centered(const control& parent) {
  return radio_button::create(parent, texts::centered());
}

radio_button radio_buttons::centered(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::centered(), location);
}

radio_button radio_buttons::centered(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::centered(), location, size);
}

radio_button radio_buttons::centered(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::centered(), location, size, name);
}

radio_button radio_buttons::centered(const control& parent, bool checked) {
  return radio_button::create(parent, texts::centered(), checked);
}

radio_button radio_buttons::centered(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::centered(), checked, location);
}

radio_button radio_buttons::centered(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::centered(), checked, location, size);
}

radio_button radio_buttons::centered(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::centered(), checked, location, size, name);
}

radio_button radio_buttons::clear() {
  return radio_button::create(texts::clear());
}

radio_button radio_buttons::clear(const drawing::point& location) {
  return radio_button::create(texts::clear(), location);
}

radio_button radio_buttons::clear(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::clear(), location, size);
}

radio_button radio_buttons::clear(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::clear(), location, size, name);
}

radio_button radio_buttons::clear(bool checked) {
  return radio_button::create(texts::clear(), checked);
}

radio_button radio_buttons::clear(bool checked, const drawing::point& location) {
  return radio_button::create(texts::clear(), checked, location);
}

radio_button radio_buttons::clear(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::clear(), checked, location, size);
}

radio_button radio_buttons::clear(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::clear(), checked, location, size, name);
}

radio_button radio_buttons::clear(const control& parent) {
  return radio_button::create(parent, texts::clear());
}

radio_button radio_buttons::clear(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::clear(), location);
}

radio_button radio_buttons::clear(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::clear(), location, size);
}

radio_button radio_buttons::clear(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::clear(), location, size, name);
}

radio_button radio_buttons::clear(const control& parent, bool checked) {
  return radio_button::create(parent, texts::clear(), checked);
}

radio_button radio_buttons::clear(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::clear(), checked, location);
}

radio_button radio_buttons::clear(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::clear(), checked, location, size);
}

radio_button radio_buttons::clear(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::clear(), checked, location, size, name);
}

radio_button radio_buttons::close() {
  return radio_button::create(texts::close());
}

radio_button radio_buttons::close(const drawing::point& location) {
  return radio_button::create(texts::close(), location);
}

radio_button radio_buttons::close(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::close(), location, size);
}

radio_button radio_buttons::close(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::close(), location, size, name);
}

radio_button radio_buttons::close(bool checked) {
  return radio_button::create(texts::close(), checked);
}

radio_button radio_buttons::close(bool checked, const drawing::point& location) {
  return radio_button::create(texts::close(), checked, location);
}

radio_button radio_buttons::close(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::close(), checked, location, size);
}

radio_button radio_buttons::close(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::close(), checked, location, size, name);
}

radio_button radio_buttons::close(const control& parent) {
  return radio_button::create(parent, texts::close());
}

radio_button radio_buttons::close(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::close(), location);
}

radio_button radio_buttons::close(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::close(), location, size);
}

radio_button radio_buttons::close(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::close(), location, size, name);
}

radio_button radio_buttons::close(const control& parent, bool checked) {
  return radio_button::create(parent, texts::close(), checked);
}

radio_button radio_buttons::close(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::close(), checked, location);
}

radio_button radio_buttons::close(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::close(), checked, location, size);
}

radio_button radio_buttons::close(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::close(), checked, location, size, name);
}

radio_button radio_buttons::color() {
  return radio_button::create(texts::color());
}

radio_button radio_buttons::color(const drawing::point& location) {
  return radio_button::create(texts::color(), location);
}

radio_button radio_buttons::color(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::color(), location, size);
}

radio_button radio_buttons::color(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::color(), location, size, name);
}

radio_button radio_buttons::color(bool checked) {
  return radio_button::create(texts::color(), checked);
}

radio_button radio_buttons::color(bool checked, const drawing::point& location) {
  return radio_button::create(texts::color(), checked, location);
}

radio_button radio_buttons::color(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::color(), checked, location, size);
}

radio_button radio_buttons::color(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::color(), checked, location, size, name);
}

radio_button radio_buttons::color(const control& parent) {
  return radio_button::create(parent, texts::color());
}

radio_button radio_buttons::color(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::color(), location);
}

radio_button radio_buttons::color(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::color(), location, size);
}

radio_button radio_buttons::color(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::color(), location, size, name);
}

radio_button radio_buttons::color(const control& parent, bool checked) {
  return radio_button::create(parent, texts::color(), checked);
}

radio_button radio_buttons::color(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::color(), checked, location);
}

radio_button radio_buttons::color(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::color(), checked, location, size);
}

radio_button radio_buttons::color(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::color(), checked, location, size, name);
}

radio_button radio_buttons::contents() {
  return radio_button::create(texts::contents());
}

radio_button radio_buttons::contents(const drawing::point& location) {
  return radio_button::create(texts::contents(), location);
}

radio_button radio_buttons::contents(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::contents(), location, size);
}

radio_button radio_buttons::contents(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::contents(), location, size, name);
}

radio_button radio_buttons::contents(bool checked) {
  return radio_button::create(texts::contents(), checked);
}

radio_button radio_buttons::contents(bool checked, const drawing::point& location) {
  return radio_button::create(texts::contents(), checked, location);
}

radio_button radio_buttons::contents(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::contents(), checked, location, size);
}

radio_button radio_buttons::contents(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::contents(), checked, location, size, name);
}

radio_button radio_buttons::contents(const control& parent) {
  return radio_button::create(parent, texts::contents());
}

radio_button radio_buttons::contents(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::contents(), location);
}

radio_button radio_buttons::contents(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::contents(), location, size);
}

radio_button radio_buttons::contents(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::contents(), location, size, name);
}

radio_button radio_buttons::contents(const control& parent, bool checked) {
  return radio_button::create(parent, texts::contents(), checked);
}

radio_button radio_buttons::contents(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::contents(), checked, location);
}

radio_button radio_buttons::contents(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::contents(), checked, location, size);
}

radio_button radio_buttons::contents(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::contents(), checked, location, size, name);
}

radio_button radio_buttons::convert() {
  return radio_button::create(texts::convert());
}

radio_button radio_buttons::convert(const drawing::point& location) {
  return radio_button::create(texts::convert(), location);
}

radio_button radio_buttons::convert(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::convert(), location, size);
}

radio_button radio_buttons::convert(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::convert(), location, size, name);
}

radio_button radio_buttons::convert(bool checked) {
  return radio_button::create(texts::convert(), checked);
}

radio_button radio_buttons::convert(bool checked, const drawing::point& location) {
  return radio_button::create(texts::convert(), checked, location);
}

radio_button radio_buttons::convert(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::convert(), checked, location, size);
}

radio_button radio_buttons::convert(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::convert(), checked, location, size, name);
}

radio_button radio_buttons::convert(const control& parent) {
  return radio_button::create(parent, texts::convert());
}

radio_button radio_buttons::convert(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::convert(), location);
}

radio_button radio_buttons::convert(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::convert(), location, size);
}

radio_button radio_buttons::convert(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::convert(), location, size, name);
}

radio_button radio_buttons::convert(const control& parent, bool checked) {
  return radio_button::create(parent, texts::convert(), checked);
}

radio_button radio_buttons::convert(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::convert(), checked, location);
}

radio_button radio_buttons::convert(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::convert(), checked, location, size);
}

radio_button radio_buttons::convert(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::convert(), checked, location, size, name);
}

radio_button radio_buttons::copy() {
  return radio_button::create(texts::copy());
}

radio_button radio_buttons::copy(const drawing::point& location) {
  return radio_button::create(texts::copy(), location);
}

radio_button radio_buttons::copy(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::copy(), location, size);
}

radio_button radio_buttons::copy(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::copy(), location, size, name);
}

radio_button radio_buttons::copy(bool checked) {
  return radio_button::create(texts::copy(), checked);
}

radio_button radio_buttons::copy(bool checked, const drawing::point& location) {
  return radio_button::create(texts::copy(), checked, location);
}

radio_button radio_buttons::copy(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::copy(), checked, location, size);
}

radio_button radio_buttons::copy(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::copy(), checked, location, size, name);
}

radio_button radio_buttons::copy(const control& parent) {
  return radio_button::create(parent, texts::copy());
}

radio_button radio_buttons::copy(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::copy(), location);
}

radio_button radio_buttons::copy(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::copy(), location, size);
}

radio_button radio_buttons::copy(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::copy(), location, size, name);
}

radio_button radio_buttons::copy(const control& parent, bool checked) {
  return radio_button::create(parent, texts::copy(), checked);
}

radio_button radio_buttons::copy(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::copy(), checked, location);
}

radio_button radio_buttons::copy(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::copy(), checked, location, size);
}

radio_button radio_buttons::copy(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::copy(), checked, location, size, name);
}

radio_button radio_buttons::customize() {
  return radio_button::create(texts::customize());
}

radio_button radio_buttons::customize(const drawing::point& location) {
  return radio_button::create(texts::customize(), location);
}

radio_button radio_buttons::customize(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::customize(), location, size);
}

radio_button radio_buttons::customize(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::customize(), location, size, name);
}

radio_button radio_buttons::customize(bool checked) {
  return radio_button::create(texts::customize(), checked);
}

radio_button radio_buttons::customize(bool checked, const drawing::point& location) {
  return radio_button::create(texts::customize(), checked, location);
}

radio_button radio_buttons::customize(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::customize(), checked, location, size);
}

radio_button radio_buttons::customize(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::customize(), checked, location, size, name);
}

radio_button radio_buttons::customize(const control& parent) {
  return radio_button::create(parent, texts::customize());
}

radio_button radio_buttons::customize(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::customize(), location);
}

radio_button radio_buttons::customize(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::customize(), location, size);
}

radio_button radio_buttons::customize(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::customize(), location, size, name);
}

radio_button radio_buttons::customize(const control& parent, bool checked) {
  return radio_button::create(parent, texts::customize(), checked);
}

radio_button radio_buttons::customize(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::customize(), checked, location);
}

radio_button radio_buttons::customize(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::customize(), checked, location, size);
}

radio_button radio_buttons::customize(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::customize(), checked, location, size, name);
}

radio_button radio_buttons::cut() {
  return radio_button::create(texts::cut());
}

radio_button radio_buttons::cut(const drawing::point& location) {
  return radio_button::create(texts::cut(), location);
}

radio_button radio_buttons::cut(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::cut(), location, size);
}

radio_button radio_buttons::cut(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::cut(), location, size, name);
}

radio_button radio_buttons::cut(bool checked) {
  return radio_button::create(texts::cut(), checked);
}

radio_button radio_buttons::cut(bool checked, const drawing::point& location) {
  return radio_button::create(texts::cut(), checked, location);
}

radio_button radio_buttons::cut(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::cut(), checked, location, size);
}

radio_button radio_buttons::cut(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::cut(), checked, location, size, name);
}

radio_button radio_buttons::cut(const control& parent) {
  return radio_button::create(parent, texts::cut());
}

radio_button radio_buttons::cut(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::cut(), location);
}

radio_button radio_buttons::cut(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::cut(), location, size);
}

radio_button radio_buttons::cut(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::cut(), location, size, name);
}

radio_button radio_buttons::cut(const control& parent, bool checked) {
  return radio_button::create(parent, texts::cut(), checked);
}

radio_button radio_buttons::cut(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::cut(), checked, location);
}

radio_button radio_buttons::cut(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::cut(), checked, location, size);
}

radio_button radio_buttons::cut(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::cut(), checked, location, size, name);
}

radio_button radio_buttons::del() {
  return radio_button::create(texts::del());
}

radio_button radio_buttons::del(const drawing::point& location) {
  return radio_button::create(texts::del(), location);
}

radio_button radio_buttons::del(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::del(), location, size);
}

radio_button radio_buttons::del(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::del(), location, size, name);
}

radio_button radio_buttons::del(bool checked) {
  return radio_button::create(texts::del(), checked);
}

radio_button radio_buttons::del(bool checked, const drawing::point& location) {
  return radio_button::create(texts::del(), checked, location);
}

radio_button radio_buttons::del(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::del(), checked, location, size);
}

radio_button radio_buttons::del(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::del(), checked, location, size, name);
}

radio_button radio_buttons::del(const control& parent) {
  return radio_button::create(parent, texts::del());
}

radio_button radio_buttons::del(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::del(), location);
}

radio_button radio_buttons::del(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::del(), location, size);
}

radio_button radio_buttons::del(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::del(), location, size, name);
}

radio_button radio_buttons::del(const control& parent, bool checked) {
  return radio_button::create(parent, texts::del(), checked);
}

radio_button radio_buttons::del(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::del(), checked, location);
}

radio_button radio_buttons::del(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::del(), checked, location, size);
}

radio_button radio_buttons::del(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::del(), checked, location, size, name);
}

radio_button radio_buttons::descending() {
  return radio_button::create(texts::descending());
}

radio_button radio_buttons::descending(const drawing::point& location) {
  return radio_button::create(texts::descending(), location);
}

radio_button radio_buttons::descending(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::descending(), location, size);
}

radio_button radio_buttons::descending(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::descending(), location, size, name);
}

radio_button radio_buttons::descending(bool checked) {
  return radio_button::create(texts::descending(), checked);
}

radio_button radio_buttons::descending(bool checked, const drawing::point& location) {
  return radio_button::create(texts::descending(), checked, location);
}

radio_button radio_buttons::descending(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::descending(), checked, location, size);
}

radio_button radio_buttons::descending(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::descending(), checked, location, size, name);
}

radio_button radio_buttons::descending(const control& parent) {
  return radio_button::create(parent, texts::descending());
}

radio_button radio_buttons::descending(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::descending(), location);
}

radio_button radio_buttons::descending(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::descending(), location, size);
}

radio_button radio_buttons::descending(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::descending(), location, size, name);
}

radio_button radio_buttons::descending(const control& parent, bool checked) {
  return radio_button::create(parent, texts::descending(), checked);
}

radio_button radio_buttons::descending(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::descending(), checked, location);
}

radio_button radio_buttons::descending(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::descending(), checked, location, size);
}

radio_button radio_buttons::descending(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::descending(), checked, location, size, name);
}

radio_button radio_buttons::down() {
  return radio_button::create(texts::down());
}

radio_button radio_buttons::down(const drawing::point& location) {
  return radio_button::create(texts::down(), location);
}

radio_button radio_buttons::down(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::down(), location, size);
}

radio_button radio_buttons::down(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::down(), location, size, name);
}

radio_button radio_buttons::down(bool checked) {
  return radio_button::create(texts::down(), checked);
}

radio_button radio_buttons::down(bool checked, const drawing::point& location) {
  return radio_button::create(texts::down(), checked, location);
}

radio_button radio_buttons::down(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::down(), checked, location, size);
}

radio_button radio_buttons::down(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::down(), checked, location, size, name);
}

radio_button radio_buttons::down(const control& parent) {
  return radio_button::create(parent, texts::down());
}

radio_button radio_buttons::down(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::down(), location);
}

radio_button radio_buttons::down(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::down(), location, size);
}

radio_button radio_buttons::down(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::down(), location, size, name);
}

radio_button radio_buttons::down(const control& parent, bool checked) {
  return radio_button::create(parent, texts::down(), checked);
}

radio_button radio_buttons::down(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::down(), checked, location);
}

radio_button radio_buttons::down(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::down(), checked, location, size);
}

radio_button radio_buttons::down(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::down(), checked, location, size, name);
}

radio_button radio_buttons::edit() {
  return radio_button::create(texts::edit());
}

radio_button radio_buttons::edit(const drawing::point& location) {
  return radio_button::create(texts::edit(), location);
}

radio_button radio_buttons::edit(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::edit(), location, size);
}

radio_button radio_buttons::edit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::edit(), location, size, name);
}

radio_button radio_buttons::edit(bool checked) {
  return radio_button::create(texts::edit(), checked);
}

radio_button radio_buttons::edit(bool checked, const drawing::point& location) {
  return radio_button::create(texts::edit(), checked, location);
}

radio_button radio_buttons::edit(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::edit(), checked, location, size);
}

radio_button radio_buttons::edit(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::edit(), checked, location, size, name);
}

radio_button radio_buttons::edit(const control& parent) {
  return radio_button::create(parent, texts::edit());
}

radio_button radio_buttons::edit(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::edit(), location);
}

radio_button radio_buttons::edit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::edit(), location, size);
}

radio_button radio_buttons::edit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::edit(), location, size, name);
}

radio_button radio_buttons::edit(const control& parent, bool checked) {
  return radio_button::create(parent, texts::edit(), checked);
}

radio_button radio_buttons::edit(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::edit(), checked, location);
}

radio_button radio_buttons::edit(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::edit(), checked, location, size);
}

radio_button radio_buttons::edit(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::edit(), checked, location, size, name);
}

radio_button radio_buttons::execute() {
  return radio_button::create(texts::execute());
}

radio_button radio_buttons::execute(const drawing::point& location) {
  return radio_button::create(texts::execute(), location);
}

radio_button radio_buttons::execute(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::execute(), location, size);
}

radio_button radio_buttons::execute(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::execute(), location, size, name);
}

radio_button radio_buttons::execute(bool checked) {
  return radio_button::create(texts::execute(), checked);
}

radio_button radio_buttons::execute(bool checked, const drawing::point& location) {
  return radio_button::create(texts::execute(), checked, location);
}

radio_button radio_buttons::execute(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::execute(), checked, location, size);
}

radio_button radio_buttons::execute(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::execute(), checked, location, size, name);
}

radio_button radio_buttons::execute(const control& parent) {
  return radio_button::create(parent, texts::execute());
}

radio_button radio_buttons::execute(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::execute(), location);
}

radio_button radio_buttons::execute(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::execute(), location, size);
}

radio_button radio_buttons::execute(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::execute(), location, size, name);
}

radio_button radio_buttons::execute(const control& parent, bool checked) {
  return radio_button::create(parent, texts::execute(), checked);
}

radio_button radio_buttons::execute(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::execute(), checked, location);
}

radio_button radio_buttons::execute(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::execute(), checked, location, size);
}

radio_button radio_buttons::execute(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::execute(), checked, location, size, name);
}

radio_button radio_buttons::exit() {
  return radio_button::create(texts::exit());
}

radio_button radio_buttons::exit(const drawing::point& location) {
  return radio_button::create(texts::exit(), location);
}

radio_button radio_buttons::exit(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::exit(), location, size);
}

radio_button radio_buttons::exit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::exit(), location, size, name);
}

radio_button radio_buttons::exit(bool checked) {
  return radio_button::create(texts::exit(), checked);
}

radio_button radio_buttons::exit(bool checked, const drawing::point& location) {
  return radio_button::create(texts::exit(), checked, location);
}

radio_button radio_buttons::exit(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::exit(), checked, location, size);
}

radio_button radio_buttons::exit(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::exit(), checked, location, size, name);
}

radio_button radio_buttons::exit(const control& parent) {
  return radio_button::create(parent, texts::exit());
}

radio_button radio_buttons::exit(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::exit(), location);
}

radio_button radio_buttons::exit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::exit(), location, size);
}

radio_button radio_buttons::exit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::exit(), location, size, name);
}

radio_button radio_buttons::exit(const control& parent, bool checked) {
  return radio_button::create(parent, texts::exit(), checked);
}

radio_button radio_buttons::exit(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::exit(), checked, location);
}

radio_button radio_buttons::exit(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::exit(), checked, location, size);
}

radio_button radio_buttons::exit(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::exit(), checked, location, size, name);
}

radio_button radio_buttons::file() {
  return radio_button::create(texts::file());
}

radio_button radio_buttons::file(const drawing::point& location) {
  return radio_button::create(texts::file(), location);
}

radio_button radio_buttons::file(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::file(), location, size);
}

radio_button radio_buttons::file(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::file(), location, size, name);
}

radio_button radio_buttons::file(bool checked) {
  return radio_button::create(texts::file(), checked);
}

radio_button radio_buttons::file(bool checked, const drawing::point& location) {
  return radio_button::create(texts::file(), checked, location);
}

radio_button radio_buttons::file(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::file(), checked, location, size);
}

radio_button radio_buttons::file(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::file(), checked, location, size, name);
}

radio_button radio_buttons::file(const control& parent) {
  return radio_button::create(parent, texts::file());
}

radio_button radio_buttons::file(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::file(), location);
}

radio_button radio_buttons::file(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::file(), location, size);
}

radio_button radio_buttons::file(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::file(), location, size, name);
}

radio_button radio_buttons::file(const control& parent, bool checked) {
  return radio_button::create(parent, texts::file(), checked);
}

radio_button radio_buttons::file(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::file(), checked, location);
}

radio_button radio_buttons::file(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::file(), checked, location, size);
}

radio_button radio_buttons::file(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::file(), checked, location, size, name);
}

radio_button radio_buttons::find() {
  return radio_button::create(texts::find());
}

radio_button radio_buttons::find(const drawing::point& location) {
  return radio_button::create(texts::find(), location);
}

radio_button radio_buttons::find(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::find(), location, size);
}

radio_button radio_buttons::find(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::find(), location, size, name);
}

radio_button radio_buttons::find(bool checked) {
  return radio_button::create(texts::find(), checked);
}

radio_button radio_buttons::find(bool checked, const drawing::point& location) {
  return radio_button::create(texts::find(), checked, location);
}

radio_button radio_buttons::find(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::find(), checked, location, size);
}

radio_button radio_buttons::find(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::find(), checked, location, size, name);
}

radio_button radio_buttons::find(const control& parent) {
  return radio_button::create(parent, texts::find());
}

radio_button radio_buttons::find(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::find(), location);
}

radio_button radio_buttons::find(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::find(), location, size);
}

radio_button radio_buttons::find(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::find(), location, size, name);
}

radio_button radio_buttons::find(const control& parent, bool checked) {
  return radio_button::create(parent, texts::find(), checked);
}

radio_button radio_buttons::find(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::find(), checked, location);
}

radio_button radio_buttons::find(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::find(), checked, location, size);
}

radio_button radio_buttons::find(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::find(), checked, location, size, name);
}

radio_button radio_buttons::first() {
  return radio_button::create(texts::first());
}

radio_button radio_buttons::first(const drawing::point& location) {
  return radio_button::create(texts::first(), location);
}

radio_button radio_buttons::first(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::first(), location, size);
}

radio_button radio_buttons::first(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::first(), location, size, name);
}

radio_button radio_buttons::first(bool checked) {
  return radio_button::create(texts::first(), checked);
}

radio_button radio_buttons::first(bool checked, const drawing::point& location) {
  return radio_button::create(texts::first(), checked, location);
}

radio_button radio_buttons::first(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::first(), checked, location, size);
}

radio_button radio_buttons::first(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::first(), checked, location, size, name);
}

radio_button radio_buttons::first(const control& parent) {
  return radio_button::create(parent, texts::first());
}

radio_button radio_buttons::first(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::first(), location);
}

radio_button radio_buttons::first(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::first(), location, size);
}

radio_button radio_buttons::first(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::first(), location, size, name);
}

radio_button radio_buttons::first(const control& parent, bool checked) {
  return radio_button::create(parent, texts::first(), checked);
}

radio_button radio_buttons::first(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::first(), checked, location);
}

radio_button radio_buttons::first(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::first(), checked, location, size);
}

radio_button radio_buttons::first(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::first(), checked, location, size, name);
}

radio_button radio_buttons::floppy() {
  return radio_button::create(texts::floppy());
}

radio_button radio_buttons::floppy(const drawing::point& location) {
  return radio_button::create(texts::floppy(), location);
}

radio_button radio_buttons::floppy(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::floppy(), location, size);
}

radio_button radio_buttons::floppy(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::floppy(), location, size, name);
}

radio_button radio_buttons::floppy(bool checked) {
  return radio_button::create(texts::floppy(), checked);
}

radio_button radio_buttons::floppy(bool checked, const drawing::point& location) {
  return radio_button::create(texts::floppy(), checked, location);
}

radio_button radio_buttons::floppy(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::floppy(), checked, location, size);
}

radio_button radio_buttons::floppy(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::floppy(), checked, location, size, name);
}

radio_button radio_buttons::floppy(const control& parent) {
  return radio_button::create(parent, texts::floppy());
}

radio_button radio_buttons::floppy(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::floppy(), location);
}

radio_button radio_buttons::floppy(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::floppy(), location, size);
}

radio_button radio_buttons::floppy(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::floppy(), location, size, name);
}

radio_button radio_buttons::floppy(const control& parent, bool checked) {
  return radio_button::create(parent, texts::floppy(), checked);
}

radio_button radio_buttons::floppy(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::floppy(), checked, location);
}

radio_button radio_buttons::floppy(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::floppy(), checked, location, size);
}

radio_button radio_buttons::floppy(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::floppy(), checked, location, size, name);
}

radio_button radio_buttons::font() {
  return radio_button::create(texts::font());
}

radio_button radio_buttons::font(const drawing::point& location) {
  return radio_button::create(texts::font(), location);
}

radio_button radio_buttons::font(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::font(), location, size);
}

radio_button radio_buttons::font(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::font(), location, size, name);
}

radio_button radio_buttons::font(bool checked) {
  return radio_button::create(texts::font(), checked);
}

radio_button radio_buttons::font(bool checked, const drawing::point& location) {
  return radio_button::create(texts::font(), checked, location);
}

radio_button radio_buttons::font(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::font(), checked, location, size);
}

radio_button radio_buttons::font(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::font(), checked, location, size, name);
}

radio_button radio_buttons::font(const control& parent) {
  return radio_button::create(parent, texts::font());
}

radio_button radio_buttons::font(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::font(), location);
}

radio_button radio_buttons::font(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::font(), location, size);
}

radio_button radio_buttons::font(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::font(), location, size, name);
}

radio_button radio_buttons::font(const control& parent, bool checked) {
  return radio_button::create(parent, texts::font(), checked);
}

radio_button radio_buttons::font(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::font(), checked, location);
}

radio_button radio_buttons::font(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::font(), checked, location, size);
}

radio_button radio_buttons::font(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::font(), checked, location, size, name);
}

radio_button radio_buttons::forward() {
  return radio_button::create(texts::forward());
}

radio_button radio_buttons::forward(const drawing::point& location) {
  return radio_button::create(texts::forward(), location);
}

radio_button radio_buttons::forward(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::forward(), location, size);
}

radio_button radio_buttons::forward(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::forward(), location, size, name);
}

radio_button radio_buttons::forward(bool checked) {
  return radio_button::create(texts::forward(), checked);
}

radio_button radio_buttons::forward(bool checked, const drawing::point& location) {
  return radio_button::create(texts::forward(), checked, location);
}

radio_button radio_buttons::forward(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::forward(), checked, location, size);
}

radio_button radio_buttons::forward(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::forward(), checked, location, size, name);
}

radio_button radio_buttons::forward(const control& parent) {
  return radio_button::create(parent, texts::forward());
}

radio_button radio_buttons::forward(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::forward(), location);
}

radio_button radio_buttons::forward(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::forward(), location, size);
}

radio_button radio_buttons::forward(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::forward(), location, size, name);
}

radio_button radio_buttons::forward(const control& parent, bool checked) {
  return radio_button::create(parent, texts::forward(), checked);
}

radio_button radio_buttons::forward(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::forward(), checked, location);
}

radio_button radio_buttons::forward(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::forward(), checked, location, size);
}

radio_button radio_buttons::forward(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::forward(), checked, location, size, name);
}

radio_button radio_buttons::harddisk() {
  return radio_button::create(texts::harddisk());
}

radio_button radio_buttons::harddisk(const drawing::point& location) {
  return radio_button::create(texts::harddisk(), location);
}

radio_button radio_buttons::harddisk(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::harddisk(), location, size);
}

radio_button radio_buttons::harddisk(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::harddisk(), location, size, name);
}

radio_button radio_buttons::harddisk(bool checked) {
  return radio_button::create(texts::harddisk(), checked);
}

radio_button radio_buttons::harddisk(bool checked, const drawing::point& location) {
  return radio_button::create(texts::harddisk(), checked, location);
}

radio_button radio_buttons::harddisk(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::harddisk(), checked, location, size);
}

radio_button radio_buttons::harddisk(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::harddisk(), checked, location, size, name);
}

radio_button radio_buttons::harddisk(const control& parent) {
  return radio_button::create(parent, texts::harddisk());
}

radio_button radio_buttons::harddisk(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::harddisk(), location);
}

radio_button radio_buttons::harddisk(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::harddisk(), location, size);
}

radio_button radio_buttons::harddisk(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::harddisk(), location, size, name);
}

radio_button radio_buttons::harddisk(const control& parent, bool checked) {
  return radio_button::create(parent, texts::harddisk(), checked);
}

radio_button radio_buttons::harddisk(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::harddisk(), checked, location);
}

radio_button radio_buttons::harddisk(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::harddisk(), checked, location, size);
}

radio_button radio_buttons::harddisk(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::harddisk(), checked, location, size, name);
}

radio_button radio_buttons::help() {
  return radio_button::create(texts::help());
}

radio_button radio_buttons::help(const drawing::point& location) {
  return radio_button::create(texts::help(), location);
}

radio_button radio_buttons::help(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::help(), location, size);
}

radio_button radio_buttons::help(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::help(), location, size, name);
}

radio_button radio_buttons::help(bool checked) {
  return radio_button::create(texts::help(), checked);
}

radio_button radio_buttons::help(bool checked, const drawing::point& location) {
  return radio_button::create(texts::help(), checked, location);
}

radio_button radio_buttons::help(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::help(), checked, location, size);
}

radio_button radio_buttons::help(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::help(), checked, location, size, name);
}

radio_button radio_buttons::help(const control& parent) {
  return radio_button::create(parent, texts::help());
}

radio_button radio_buttons::help(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::help(), location);
}

radio_button radio_buttons::help(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::help(), location, size);
}

radio_button radio_buttons::help(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::help(), location, size, name);
}

radio_button radio_buttons::help(const control& parent, bool checked) {
  return radio_button::create(parent, texts::help(), checked);
}

radio_button radio_buttons::help(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::help(), checked, location);
}

radio_button radio_buttons::help(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::help(), checked, location, size);
}

radio_button radio_buttons::help(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::help(), checked, location, size, name);
}

radio_button radio_buttons::home() {
  return radio_button::create(texts::home());
}

radio_button radio_buttons::home(const drawing::point& location) {
  return radio_button::create(texts::home(), location);
}

radio_button radio_buttons::home(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::home(), location, size);
}

radio_button radio_buttons::home(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::home(), location, size, name);
}

radio_button radio_buttons::home(bool checked) {
  return radio_button::create(texts::home(), checked);
}

radio_button radio_buttons::home(bool checked, const drawing::point& location) {
  return radio_button::create(texts::home(), checked, location);
}

radio_button radio_buttons::home(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::home(), checked, location, size);
}

radio_button radio_buttons::home(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::home(), checked, location, size, name);
}

radio_button radio_buttons::home(const control& parent) {
  return radio_button::create(parent, texts::home());
}

radio_button radio_buttons::home(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::home(), location);
}

radio_button radio_buttons::home(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::home(), location, size);
}

radio_button radio_buttons::home(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::home(), location, size, name);
}

radio_button radio_buttons::home(const control& parent, bool checked) {
  return radio_button::create(parent, texts::home(), checked);
}

radio_button radio_buttons::home(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::home(), checked, location);
}

radio_button radio_buttons::home(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::home(), checked, location, size);
}

radio_button radio_buttons::home(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::home(), checked, location, size, name);
}

radio_button radio_buttons::ignore() {
  return radio_button::create(texts::ignore());
}

radio_button radio_buttons::ignore(const drawing::point& location) {
  return radio_button::create(texts::ignore(), location);
}

radio_button radio_buttons::ignore(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::ignore(), location, size);
}

radio_button radio_buttons::ignore(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::ignore(), location, size, name);
}

radio_button radio_buttons::ignore(bool checked) {
  return radio_button::create(texts::ignore(), checked);
}

radio_button radio_buttons::ignore(bool checked, const drawing::point& location) {
  return radio_button::create(texts::ignore(), checked, location);
}

radio_button radio_buttons::ignore(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::ignore(), checked, location, size);
}

radio_button radio_buttons::ignore(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::ignore(), checked, location, size, name);
}

radio_button radio_buttons::ignore(const control& parent) {
  return radio_button::create(parent, texts::ignore());
}

radio_button radio_buttons::ignore(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::ignore(), location);
}

radio_button radio_buttons::ignore(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::ignore(), location, size);
}

radio_button radio_buttons::ignore(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::ignore(), location, size, name);
}

radio_button radio_buttons::ignore(const control& parent, bool checked) {
  return radio_button::create(parent, texts::ignore(), checked);
}

radio_button radio_buttons::ignore(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::ignore(), checked, location);
}

radio_button radio_buttons::ignore(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::ignore(), checked, location, size);
}

radio_button radio_buttons::ignore(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::ignore(), checked, location, size, name);
}

radio_button radio_buttons::indent() {
  return radio_button::create(texts::indent());
}

radio_button radio_buttons::indent(const drawing::point& location) {
  return radio_button::create(texts::indent(), location);
}

radio_button radio_buttons::indent(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::indent(), location, size);
}

radio_button radio_buttons::indent(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::indent(), location, size, name);
}

radio_button radio_buttons::indent(bool checked) {
  return radio_button::create(texts::indent(), checked);
}

radio_button radio_buttons::indent(bool checked, const drawing::point& location) {
  return radio_button::create(texts::indent(), checked, location);
}

radio_button radio_buttons::indent(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::indent(), checked, location, size);
}

radio_button radio_buttons::indent(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::indent(), checked, location, size, name);
}

radio_button radio_buttons::indent(const control& parent) {
  return radio_button::create(parent, texts::indent());
}

radio_button radio_buttons::indent(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::indent(), location);
}

radio_button radio_buttons::indent(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::indent(), location, size);
}

radio_button radio_buttons::indent(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::indent(), location, size, name);
}

radio_button radio_buttons::indent(const control& parent, bool checked) {
  return radio_button::create(parent, texts::indent(), checked);
}

radio_button radio_buttons::indent(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::indent(), checked, location);
}

radio_button radio_buttons::indent(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::indent(), checked, location, size);
}

radio_button radio_buttons::indent(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::indent(), checked, location, size, name);
}

radio_button radio_buttons::index() {
  return radio_button::create(texts::index());
}

radio_button radio_buttons::index(const drawing::point& location) {
  return radio_button::create(texts::index(), location);
}

radio_button radio_buttons::index(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::index(), location, size);
}

radio_button radio_buttons::index(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::index(), location, size, name);
}

radio_button radio_buttons::index(bool checked) {
  return radio_button::create(texts::index(), checked);
}

radio_button radio_buttons::index(bool checked, const drawing::point& location) {
  return radio_button::create(texts::index(), checked, location);
}

radio_button radio_buttons::index(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::index(), checked, location, size);
}

radio_button radio_buttons::index(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::index(), checked, location, size, name);
}

radio_button radio_buttons::index(const control& parent) {
  return radio_button::create(parent, texts::index());
}

radio_button radio_buttons::index(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::index(), location);
}

radio_button radio_buttons::index(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::index(), location, size);
}

radio_button radio_buttons::index(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::index(), location, size, name);
}

radio_button radio_buttons::index(const control& parent, bool checked) {
  return radio_button::create(parent, texts::index(), checked);
}

radio_button radio_buttons::index(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::index(), checked, location);
}

radio_button radio_buttons::index(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::index(), checked, location, size);
}

radio_button radio_buttons::index(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::index(), checked, location, size, name);
}

radio_button radio_buttons::info() {
  return radio_button::create(texts::info());
}

radio_button radio_buttons::info(const drawing::point& location) {
  return radio_button::create(texts::info(), location);
}

radio_button radio_buttons::info(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::info(), location, size);
}

radio_button radio_buttons::info(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::info(), location, size, name);
}

radio_button radio_buttons::info(bool checked) {
  return radio_button::create(texts::info(), checked);
}

radio_button radio_buttons::info(bool checked, const drawing::point& location) {
  return radio_button::create(texts::info(), checked, location);
}

radio_button radio_buttons::info(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::info(), checked, location, size);
}

radio_button radio_buttons::info(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::info(), checked, location, size, name);
}

radio_button radio_buttons::info(const control& parent) {
  return radio_button::create(parent, texts::info());
}

radio_button radio_buttons::info(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::info(), location);
}

radio_button radio_buttons::info(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::info(), location, size);
}

radio_button radio_buttons::info(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::info(), location, size, name);
}

radio_button radio_buttons::info(const control& parent, bool checked) {
  return radio_button::create(parent, texts::info(), checked);
}

radio_button radio_buttons::info(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::info(), checked, location);
}

radio_button radio_buttons::info(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::info(), checked, location, size);
}

radio_button radio_buttons::info(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::info(), checked, location, size, name);
}

radio_button radio_buttons::italic() {
  return radio_button::create(texts::italic());
}

radio_button radio_buttons::italic(const drawing::point& location) {
  return radio_button::create(texts::italic(), location);
}

radio_button radio_buttons::italic(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::italic(), location, size);
}

radio_button radio_buttons::italic(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::italic(), location, size, name);
}

radio_button radio_buttons::italic(bool checked) {
  return radio_button::create(texts::italic(), checked);
}

radio_button radio_buttons::italic(bool checked, const drawing::point& location) {
  return radio_button::create(texts::italic(), checked, location);
}

radio_button radio_buttons::italic(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::italic(), checked, location, size);
}

radio_button radio_buttons::italic(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::italic(), checked, location, size, name);
}

radio_button radio_buttons::italic(const control& parent) {
  return radio_button::create(parent, texts::italic());
}

radio_button radio_buttons::italic(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::italic(), location);
}

radio_button radio_buttons::italic(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::italic(), location, size);
}

radio_button radio_buttons::italic(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::italic(), location, size, name);
}

radio_button radio_buttons::italic(const control& parent, bool checked) {
  return radio_button::create(parent, texts::italic(), checked);
}

radio_button radio_buttons::italic(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::italic(), checked, location);
}

radio_button radio_buttons::italic(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::italic(), checked, location, size);
}

radio_button radio_buttons::italic(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::italic(), checked, location, size, name);
}

radio_button radio_buttons::justified() {
  return radio_button::create(texts::justified());
}

radio_button radio_buttons::justified(const drawing::point& location) {
  return radio_button::create(texts::justified(), location);
}

radio_button radio_buttons::justified(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::justified(), location, size);
}

radio_button radio_buttons::justified(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::justified(), location, size, name);
}

radio_button radio_buttons::justified(bool checked) {
  return radio_button::create(texts::justified(), checked);
}

radio_button radio_buttons::justified(bool checked, const drawing::point& location) {
  return radio_button::create(texts::justified(), checked, location);
}

radio_button radio_buttons::justified(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::justified(), checked, location, size);
}

radio_button radio_buttons::justified(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::justified(), checked, location, size, name);
}

radio_button radio_buttons::justified(const control& parent) {
  return radio_button::create(parent, texts::justified());
}

radio_button radio_buttons::justified(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::justified(), location);
}

radio_button radio_buttons::justified(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::justified(), location, size);
}

radio_button radio_buttons::justified(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::justified(), location, size, name);
}

radio_button radio_buttons::justified(const control& parent, bool checked) {
  return radio_button::create(parent, texts::justified(), checked);
}

radio_button radio_buttons::justified(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::justified(), checked, location);
}

radio_button radio_buttons::justified(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::justified(), checked, location, size);
}

radio_button radio_buttons::justified(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::justified(), checked, location, size, name);
}

radio_button radio_buttons::jump_to() {
  return radio_button::create(texts::jump_to());
}

radio_button radio_buttons::jump_to(const drawing::point& location) {
  return radio_button::create(texts::jump_to(), location);
}

radio_button radio_buttons::jump_to(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::jump_to(), location, size);
}

radio_button radio_buttons::jump_to(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::jump_to(), location, size, name);
}

radio_button radio_buttons::jump_to(bool checked) {
  return radio_button::create(texts::jump_to(), checked);
}

radio_button radio_buttons::jump_to(bool checked, const drawing::point& location) {
  return radio_button::create(texts::jump_to(), checked, location);
}

radio_button radio_buttons::jump_to(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::jump_to(), checked, location, size);
}

radio_button radio_buttons::jump_to(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::jump_to(), checked, location, size, name);
}

radio_button radio_buttons::jump_to(const control& parent) {
  return radio_button::create(parent, texts::jump_to());
}

radio_button radio_buttons::jump_to(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::jump_to(), location);
}

radio_button radio_buttons::jump_to(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::jump_to(), location, size);
}

radio_button radio_buttons::jump_to(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::jump_to(), location, size, name);
}

radio_button radio_buttons::jump_to(const control& parent, bool checked) {
  return radio_button::create(parent, texts::jump_to(), checked);
}

radio_button radio_buttons::jump_to(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::jump_to(), checked, location);
}

radio_button radio_buttons::jump_to(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::jump_to(), checked, location, size);
}

radio_button radio_buttons::jump_to(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::jump_to(), checked, location, size, name);
}

radio_button radio_buttons::last() {
  return radio_button::create(texts::last());
}

radio_button radio_buttons::last(const drawing::point& location) {
  return radio_button::create(texts::last(), location);
}

radio_button radio_buttons::last(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::last(), location, size);
}

radio_button radio_buttons::last(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::last(), location, size, name);
}

radio_button radio_buttons::last(bool checked) {
  return radio_button::create(texts::last(), checked);
}

radio_button radio_buttons::last(bool checked, const drawing::point& location) {
  return radio_button::create(texts::last(), checked, location);
}

radio_button radio_buttons::last(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::last(), checked, location, size);
}

radio_button radio_buttons::last(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::last(), checked, location, size, name);
}

radio_button radio_buttons::last(const control& parent) {
  return radio_button::create(parent, texts::last());
}

radio_button radio_buttons::last(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::last(), location);
}

radio_button radio_buttons::last(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::last(), location, size);
}

radio_button radio_buttons::last(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::last(), location, size, name);
}

radio_button radio_buttons::last(const control& parent, bool checked) {
  return radio_button::create(parent, texts::last(), checked);
}

radio_button radio_buttons::last(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::last(), checked, location);
}

radio_button radio_buttons::last(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::last(), checked, location, size);
}

radio_button radio_buttons::last(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::last(), checked, location, size, name);
}

radio_button radio_buttons::network() {
  return radio_button::create(texts::network());
}

radio_button radio_buttons::network(const drawing::point& location) {
  return radio_button::create(texts::network(), location);
}

radio_button radio_buttons::network(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::network(), location, size);
}

radio_button radio_buttons::network(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::network(), location, size, name);
}

radio_button radio_buttons::network(bool checked) {
  return radio_button::create(texts::network(), checked);
}

radio_button radio_buttons::network(bool checked, const drawing::point& location) {
  return radio_button::create(texts::network(), checked, location);
}

radio_button radio_buttons::network(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::network(), checked, location, size);
}

radio_button radio_buttons::network(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::network(), checked, location, size, name);
}

radio_button radio_buttons::network(const control& parent) {
  return radio_button::create(parent, texts::network());
}

radio_button radio_buttons::network(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::network(), location);
}

radio_button radio_buttons::network(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::network(), location, size);
}

radio_button radio_buttons::network(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::network(), location, size, name);
}

radio_button radio_buttons::network(const control& parent, bool checked) {
  return radio_button::create(parent, texts::network(), checked);
}

radio_button radio_buttons::network(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::network(), checked, location);
}

radio_button radio_buttons::network(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::network(), checked, location, size);
}

radio_button radio_buttons::network(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::network(), checked, location, size, name);
}

radio_button radio_buttons::new_() {
  return radio_button::create(texts::new_());
}

radio_button radio_buttons::new_(const drawing::point& location) {
  return radio_button::create(texts::new_(), location);
}

radio_button radio_buttons::new_(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::new_(), location, size);
}

radio_button radio_buttons::new_(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::new_(), location, size, name);
}

radio_button radio_buttons::new_(bool checked) {
  return radio_button::create(texts::new_(), checked);
}

radio_button radio_buttons::new_(bool checked, const drawing::point& location) {
  return radio_button::create(texts::new_(), checked, location);
}

radio_button radio_buttons::new_(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::new_(), checked, location, size);
}

radio_button radio_buttons::new_(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::new_(), checked, location, size, name);
}

radio_button radio_buttons::new_(const control& parent) {
  return radio_button::create(parent, texts::new_());
}

radio_button radio_buttons::new_(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::new_(), location);
}

radio_button radio_buttons::new_(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::new_(), location, size);
}

radio_button radio_buttons::new_(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::new_(), location, size, name);
}

radio_button radio_buttons::new_(const control& parent, bool checked) {
  return radio_button::create(parent, texts::new_(), checked);
}

radio_button radio_buttons::new_(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::new_(), checked, location);
}

radio_button radio_buttons::new_(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::new_(), checked, location, size);
}

radio_button radio_buttons::new_(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::new_(), checked, location, size, name);
}

radio_button radio_buttons::next() {
  return radio_button::create(texts::next());
}

radio_button radio_buttons::next(const drawing::point& location) {
  return radio_button::create(texts::next(), location);
}

radio_button radio_buttons::next(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::next(), location, size);
}

radio_button radio_buttons::next(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::next(), location, size, name);
}

radio_button radio_buttons::next(bool checked) {
  return radio_button::create(texts::next(), checked);
}

radio_button radio_buttons::next(bool checked, const drawing::point& location) {
  return radio_button::create(texts::next(), checked, location);
}

radio_button radio_buttons::next(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::next(), checked, location, size);
}

radio_button radio_buttons::next(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::next(), checked, location, size, name);
}

radio_button radio_buttons::next(const control& parent) {
  return radio_button::create(parent, texts::next());
}

radio_button radio_buttons::next(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::next(), location);
}

radio_button radio_buttons::next(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::next(), location, size);
}

radio_button radio_buttons::next(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::next(), location, size, name);
}

radio_button radio_buttons::next(const control& parent, bool checked) {
  return radio_button::create(parent, texts::next(), checked);
}

radio_button radio_buttons::next(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::next(), checked, location);
}

radio_button radio_buttons::next(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::next(), checked, location, size);
}

radio_button radio_buttons::next(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::next(), checked, location, size, name);
}

radio_button radio_buttons::no() {
  return radio_button::create(texts::no());
}

radio_button radio_buttons::no(const drawing::point& location) {
  return radio_button::create(texts::no(), location);
}

radio_button radio_buttons::no(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::no(), location, size);
}

radio_button radio_buttons::no(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::no(), location, size, name);
}

radio_button radio_buttons::no(bool checked) {
  return radio_button::create(texts::no(), checked);
}

radio_button radio_buttons::no(bool checked, const drawing::point& location) {
  return radio_button::create(texts::no(), checked, location);
}

radio_button radio_buttons::no(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::no(), checked, location, size);
}

radio_button radio_buttons::no(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::no(), checked, location, size, name);
}

radio_button radio_buttons::no(const control& parent) {
  return radio_button::create(parent, texts::no());
}

radio_button radio_buttons::no(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::no(), location);
}

radio_button radio_buttons::no(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::no(), location, size);
}

radio_button radio_buttons::no(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::no(), location, size, name);
}

radio_button radio_buttons::no(const control& parent, bool checked) {
  return radio_button::create(parent, texts::no(), checked);
}

radio_button radio_buttons::no(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::no(), checked, location);
}

radio_button radio_buttons::no(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::no(), checked, location, size);
}

radio_button radio_buttons::no(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::no(), checked, location, size, name);
}

radio_button radio_buttons::ok() {
  return radio_button::create(texts::ok());
}

radio_button radio_buttons::ok(const drawing::point& location) {
  return radio_button::create(texts::ok(), location);
}

radio_button radio_buttons::ok(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::ok(), location, size);
}

radio_button radio_buttons::ok(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::ok(), location, size, name);
}

radio_button radio_buttons::ok(bool checked) {
  return radio_button::create(texts::ok(), checked);
}

radio_button radio_buttons::ok(bool checked, const drawing::point& location) {
  return radio_button::create(texts::ok(), checked, location);
}

radio_button radio_buttons::ok(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::ok(), checked, location, size);
}

radio_button radio_buttons::ok(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::ok(), checked, location, size, name);
}

radio_button radio_buttons::ok(const control& parent) {
  return radio_button::create(parent, texts::ok());
}

radio_button radio_buttons::ok(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::ok(), location);
}

radio_button radio_buttons::ok(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::ok(), location, size);
}

radio_button radio_buttons::ok(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::ok(), location, size, name);
}

radio_button radio_buttons::ok(const control& parent, bool checked) {
  return radio_button::create(parent, texts::ok(), checked);
}

radio_button radio_buttons::ok(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::ok(), checked, location);
}

radio_button radio_buttons::ok(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::ok(), checked, location, size);
}

radio_button radio_buttons::ok(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::ok(), checked, location, size, name);
}

radio_button radio_buttons::open() {
  return radio_button::create(texts::open());
}

radio_button radio_buttons::open(const drawing::point& location) {
  return radio_button::create(texts::open(), location);
}

radio_button radio_buttons::open(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::open(), location, size);
}

radio_button radio_buttons::open(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::open(), location, size, name);
}

radio_button radio_buttons::open(bool checked) {
  return radio_button::create(texts::open(), checked);
}

radio_button radio_buttons::open(bool checked, const drawing::point& location) {
  return radio_button::create(texts::open(), checked, location);
}

radio_button radio_buttons::open(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::open(), checked, location, size);
}

radio_button radio_buttons::open(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::open(), checked, location, size, name);
}

radio_button radio_buttons::open(const control& parent) {
  return radio_button::create(parent, texts::open());
}

radio_button radio_buttons::open(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::open(), location);
}

radio_button radio_buttons::open(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::open(), location, size);
}

radio_button radio_buttons::open(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::open(), location, size, name);
}

radio_button radio_buttons::open(const control& parent, bool checked) {
  return radio_button::create(parent, texts::open(), checked);
}

radio_button radio_buttons::open(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::open(), checked, location);
}

radio_button radio_buttons::open(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::open(), checked, location, size);
}

radio_button radio_buttons::open(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::open(), checked, location, size, name);
}

radio_button radio_buttons::options() {
  return radio_button::create(texts::options());
}

radio_button radio_buttons::options(const drawing::point& location) {
  return radio_button::create(texts::options(), location);
}

radio_button radio_buttons::options(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::options(), location, size);
}

radio_button radio_buttons::options(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::options(), location, size, name);
}

radio_button radio_buttons::options(bool checked) {
  return radio_button::create(texts::options(), checked);
}

radio_button radio_buttons::options(bool checked, const drawing::point& location) {
  return radio_button::create(texts::options(), checked, location);
}

radio_button radio_buttons::options(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::options(), checked, location, size);
}

radio_button radio_buttons::options(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::options(), checked, location, size, name);
}

radio_button radio_buttons::options(const control& parent) {
  return radio_button::create(parent, texts::options());
}

radio_button radio_buttons::options(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::options(), location);
}

radio_button radio_buttons::options(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::options(), location, size);
}

radio_button radio_buttons::options(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::options(), location, size, name);
}

radio_button radio_buttons::options(const control& parent, bool checked) {
  return radio_button::create(parent, texts::options(), checked);
}

radio_button radio_buttons::options(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::options(), checked, location);
}

radio_button radio_buttons::options(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::options(), checked, location, size);
}

radio_button radio_buttons::options(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::options(), checked, location, size, name);
}

radio_button radio_buttons::paste() {
  return radio_button::create(texts::paste());
}

radio_button radio_buttons::paste(const drawing::point& location) {
  return radio_button::create(texts::paste(), location);
}

radio_button radio_buttons::paste(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::paste(), location, size);
}

radio_button radio_buttons::paste(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::paste(), location, size, name);
}

radio_button radio_buttons::paste(bool checked) {
  return radio_button::create(texts::paste(), checked);
}

radio_button radio_buttons::paste(bool checked, const drawing::point& location) {
  return radio_button::create(texts::paste(), checked, location);
}

radio_button radio_buttons::paste(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::paste(), checked, location, size);
}

radio_button radio_buttons::paste(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::paste(), checked, location, size, name);
}

radio_button radio_buttons::paste(const control& parent) {
  return radio_button::create(parent, texts::paste());
}

radio_button radio_buttons::paste(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::paste(), location);
}

radio_button radio_buttons::paste(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::paste(), location, size);
}

radio_button radio_buttons::paste(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::paste(), location, size, name);
}

radio_button radio_buttons::paste(const control& parent, bool checked) {
  return radio_button::create(parent, texts::paste(), checked);
}

radio_button radio_buttons::paste(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::paste(), checked, location);
}

radio_button radio_buttons::paste(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::paste(), checked, location, size);
}

radio_button radio_buttons::paste(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::paste(), checked, location, size, name);
}

radio_button radio_buttons::preferences() {
  return radio_button::create(texts::preferences());
}

radio_button radio_buttons::preferences(const drawing::point& location) {
  return radio_button::create(texts::preferences(), location);
}

radio_button radio_buttons::preferences(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::preferences(), location, size);
}

radio_button radio_buttons::preferences(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::preferences(), location, size, name);
}

radio_button radio_buttons::preferences(bool checked) {
  return radio_button::create(texts::preferences(), checked);
}

radio_button radio_buttons::preferences(bool checked, const drawing::point& location) {
  return radio_button::create(texts::preferences(), checked, location);
}

radio_button radio_buttons::preferences(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::preferences(), checked, location, size);
}

radio_button radio_buttons::preferences(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::preferences(), checked, location, size, name);
}

radio_button radio_buttons::preferences(const control& parent) {
  return radio_button::create(parent, texts::preferences());
}

radio_button radio_buttons::preferences(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::preferences(), location);
}

radio_button radio_buttons::preferences(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::preferences(), location, size);
}

radio_button radio_buttons::preferences(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::preferences(), location, size, name);
}

radio_button radio_buttons::preferences(const control& parent, bool checked) {
  return radio_button::create(parent, texts::preferences(), checked);
}

radio_button radio_buttons::preferences(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::preferences(), checked, location);
}

radio_button radio_buttons::preferences(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::preferences(), checked, location, size);
}

radio_button radio_buttons::preferences(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::preferences(), checked, location, size, name);
}

radio_button radio_buttons::previous() {
  return radio_button::create(texts::previous());
}

radio_button radio_buttons::previous(const drawing::point& location) {
  return radio_button::create(texts::previous(), location);
}

radio_button radio_buttons::previous(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::previous(), location, size);
}

radio_button radio_buttons::previous(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::previous(), location, size, name);
}

radio_button radio_buttons::previous(bool checked) {
  return radio_button::create(texts::previous(), checked);
}

radio_button radio_buttons::previous(bool checked, const drawing::point& location) {
  return radio_button::create(texts::previous(), checked, location);
}

radio_button radio_buttons::previous(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::previous(), checked, location, size);
}

radio_button radio_buttons::previous(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::previous(), checked, location, size, name);
}

radio_button radio_buttons::previous(const control& parent) {
  return radio_button::create(parent, texts::previous());
}

radio_button radio_buttons::previous(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::previous(), location);
}

radio_button radio_buttons::previous(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::previous(), location, size);
}

radio_button radio_buttons::previous(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::previous(), location, size, name);
}

radio_button radio_buttons::previous(const control& parent, bool checked) {
  return radio_button::create(parent, texts::previous(), checked);
}

radio_button radio_buttons::previous(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::previous(), checked, location);
}

radio_button radio_buttons::previous(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::previous(), checked, location, size);
}

radio_button radio_buttons::previous(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::previous(), checked, location, size, name);
}

radio_button radio_buttons::print() {
  return radio_button::create(texts::print());
}

radio_button radio_buttons::print(const drawing::point& location) {
  return radio_button::create(texts::print(), location);
}

radio_button radio_buttons::print(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::print(), location, size);
}

radio_button radio_buttons::print(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::print(), location, size, name);
}

radio_button radio_buttons::print(bool checked) {
  return radio_button::create(texts::print(), checked);
}

radio_button radio_buttons::print(bool checked, const drawing::point& location) {
  return radio_button::create(texts::print(), checked, location);
}

radio_button radio_buttons::print(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::print(), checked, location, size);
}

radio_button radio_buttons::print(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::print(), checked, location, size, name);
}

radio_button radio_buttons::print(const control& parent) {
  return radio_button::create(parent, texts::print());
}

radio_button radio_buttons::print(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::print(), location);
}

radio_button radio_buttons::print(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::print(), location, size);
}

radio_button radio_buttons::print(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::print(), location, size, name);
}

radio_button radio_buttons::print(const control& parent, bool checked) {
  return radio_button::create(parent, texts::print(), checked);
}

radio_button radio_buttons::print(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::print(), checked, location);
}

radio_button radio_buttons::print(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::print(), checked, location, size);
}

radio_button radio_buttons::print(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::print(), checked, location, size, name);
}

radio_button radio_buttons::print_preview() {
  return radio_button::create(texts::print_preview());
}

radio_button radio_buttons::print_preview(const drawing::point& location) {
  return radio_button::create(texts::print_preview(), location);
}

radio_button radio_buttons::print_preview(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::print_preview(), location, size);
}

radio_button radio_buttons::print_preview(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::print_preview(), location, size, name);
}

radio_button radio_buttons::print_preview(bool checked) {
  return radio_button::create(texts::print_preview(), checked);
}

radio_button radio_buttons::print_preview(bool checked, const drawing::point& location) {
  return radio_button::create(texts::print_preview(), checked, location);
}

radio_button radio_buttons::print_preview(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::print_preview(), checked, location, size);
}

radio_button radio_buttons::print_preview(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::print_preview(), checked, location, size, name);
}

radio_button radio_buttons::print_preview(const control& parent) {
  return radio_button::create(parent, texts::print_preview());
}

radio_button radio_buttons::print_preview(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::print_preview(), location);
}

radio_button radio_buttons::print_preview(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::print_preview(), location, size);
}

radio_button radio_buttons::print_preview(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::print_preview(), location, size, name);
}

radio_button radio_buttons::print_preview(const control& parent, bool checked) {
  return radio_button::create(parent, texts::print_preview(), checked);
}

radio_button radio_buttons::print_preview(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::print_preview(), checked, location);
}

radio_button radio_buttons::print_preview(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::print_preview(), checked, location, size);
}

radio_button radio_buttons::print_preview(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::print_preview(), checked, location, size, name);
}

radio_button radio_buttons::properties() {
  return radio_button::create(texts::properties());
}

radio_button radio_buttons::properties(const drawing::point& location) {
  return radio_button::create(texts::properties(), location);
}

radio_button radio_buttons::properties(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::properties(), location, size);
}

radio_button radio_buttons::properties(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::properties(), location, size, name);
}

radio_button radio_buttons::properties(bool checked) {
  return radio_button::create(texts::properties(), checked);
}

radio_button radio_buttons::properties(bool checked, const drawing::point& location) {
  return radio_button::create(texts::properties(), checked, location);
}

radio_button radio_buttons::properties(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::properties(), checked, location, size);
}

radio_button radio_buttons::properties(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::properties(), checked, location, size, name);
}

radio_button radio_buttons::properties(const control& parent) {
  return radio_button::create(parent, texts::properties());
}

radio_button radio_buttons::properties(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::properties(), location);
}

radio_button radio_buttons::properties(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::properties(), location, size);
}

radio_button radio_buttons::properties(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::properties(), location, size, name);
}

radio_button radio_buttons::properties(const control& parent, bool checked) {
  return radio_button::create(parent, texts::properties(), checked);
}

radio_button radio_buttons::properties(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::properties(), checked, location);
}

radio_button radio_buttons::properties(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::properties(), checked, location, size);
}

radio_button radio_buttons::properties(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::properties(), checked, location, size, name);
}

radio_button radio_buttons::quit() {
  return radio_button::create(texts::quit());
}

radio_button radio_buttons::quit(const drawing::point& location) {
  return radio_button::create(texts::quit(), location);
}

radio_button radio_buttons::quit(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::quit(), location, size);
}

radio_button radio_buttons::quit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::quit(), location, size, name);
}

radio_button radio_buttons::quit(bool checked) {
  return radio_button::create(texts::quit(), checked);
}

radio_button radio_buttons::quit(bool checked, const drawing::point& location) {
  return radio_button::create(texts::quit(), checked, location);
}

radio_button radio_buttons::quit(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::quit(), checked, location, size);
}

radio_button radio_buttons::quit(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::quit(), checked, location, size, name);
}

radio_button radio_buttons::quit(const control& parent) {
  return radio_button::create(parent, texts::quit());
}

radio_button radio_buttons::quit(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::quit(), location);
}

radio_button radio_buttons::quit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::quit(), location, size);
}

radio_button radio_buttons::quit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::quit(), location, size, name);
}

radio_button radio_buttons::quit(const control& parent, bool checked) {
  return radio_button::create(parent, texts::quit(), checked);
}

radio_button radio_buttons::quit(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::quit(), checked, location);
}

radio_button radio_buttons::quit(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::quit(), checked, location, size);
}

radio_button radio_buttons::quit(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::quit(), checked, location, size, name);
}

radio_button radio_buttons::redo() {
  return radio_button::create(texts::redo());
}

radio_button radio_buttons::redo(const drawing::point& location) {
  return radio_button::create(texts::redo(), location);
}

radio_button radio_buttons::redo(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::redo(), location, size);
}

radio_button radio_buttons::redo(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::redo(), location, size, name);
}

radio_button radio_buttons::redo(bool checked) {
  return radio_button::create(texts::redo(), checked);
}

radio_button radio_buttons::redo(bool checked, const drawing::point& location) {
  return radio_button::create(texts::redo(), checked, location);
}

radio_button radio_buttons::redo(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::redo(), checked, location, size);
}

radio_button radio_buttons::redo(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::redo(), checked, location, size, name);
}

radio_button radio_buttons::redo(const control& parent) {
  return radio_button::create(parent, texts::redo());
}

radio_button radio_buttons::redo(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::redo(), location);
}

radio_button radio_buttons::redo(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::redo(), location, size);
}

radio_button radio_buttons::redo(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::redo(), location, size, name);
}

radio_button radio_buttons::redo(const control& parent, bool checked) {
  return radio_button::create(parent, texts::redo(), checked);
}

radio_button radio_buttons::redo(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::redo(), checked, location);
}

radio_button radio_buttons::redo(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::redo(), checked, location, size);
}

radio_button radio_buttons::redo(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::redo(), checked, location, size, name);
}

radio_button radio_buttons::refresh() {
  return radio_button::create(texts::refresh());
}

radio_button radio_buttons::refresh(const drawing::point& location) {
  return radio_button::create(texts::refresh(), location);
}

radio_button radio_buttons::refresh(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::refresh(), location, size);
}

radio_button radio_buttons::refresh(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::refresh(), location, size, name);
}

radio_button radio_buttons::refresh(bool checked) {
  return radio_button::create(texts::refresh(), checked);
}

radio_button radio_buttons::refresh(bool checked, const drawing::point& location) {
  return radio_button::create(texts::refresh(), checked, location);
}

radio_button radio_buttons::refresh(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::refresh(), checked, location, size);
}

radio_button radio_buttons::refresh(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::refresh(), checked, location, size, name);
}

radio_button radio_buttons::refresh(const control& parent) {
  return radio_button::create(parent, texts::refresh());
}

radio_button radio_buttons::refresh(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::refresh(), location);
}

radio_button radio_buttons::refresh(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::refresh(), location, size);
}

radio_button radio_buttons::refresh(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::refresh(), location, size, name);
}

radio_button radio_buttons::refresh(const control& parent, bool checked) {
  return radio_button::create(parent, texts::refresh(), checked);
}

radio_button radio_buttons::refresh(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::refresh(), checked, location);
}

radio_button radio_buttons::refresh(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::refresh(), checked, location, size);
}

radio_button radio_buttons::refresh(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::refresh(), checked, location, size, name);
}

radio_button radio_buttons::remove() {
  return radio_button::create(texts::remove());
}

radio_button radio_buttons::remove(const drawing::point& location) {
  return radio_button::create(texts::remove(), location);
}

radio_button radio_buttons::remove(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::remove(), location, size);
}

radio_button radio_buttons::remove(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::remove(), location, size, name);
}

radio_button radio_buttons::remove(bool checked) {
  return radio_button::create(texts::remove(), checked);
}

radio_button radio_buttons::remove(bool checked, const drawing::point& location) {
  return radio_button::create(texts::remove(), checked, location);
}

radio_button radio_buttons::remove(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::remove(), checked, location, size);
}

radio_button radio_buttons::remove(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::remove(), checked, location, size, name);
}

radio_button radio_buttons::remove(const control& parent) {
  return radio_button::create(parent, texts::remove());
}

radio_button radio_buttons::remove(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::remove(), location);
}

radio_button radio_buttons::remove(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::remove(), location, size);
}

radio_button radio_buttons::remove(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::remove(), location, size, name);
}

radio_button radio_buttons::remove(const control& parent, bool checked) {
  return radio_button::create(parent, texts::remove(), checked);
}

radio_button radio_buttons::remove(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::remove(), checked, location);
}

radio_button radio_buttons::remove(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::remove(), checked, location, size);
}

radio_button radio_buttons::remove(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::remove(), checked, location, size, name);
}

radio_button radio_buttons::replace() {
  return radio_button::create(texts::replace());
}

radio_button radio_buttons::replace(const drawing::point& location) {
  return radio_button::create(texts::replace(), location);
}

radio_button radio_buttons::replace(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::replace(), location, size);
}

radio_button radio_buttons::replace(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::replace(), location, size, name);
}

radio_button radio_buttons::replace(bool checked) {
  return radio_button::create(texts::replace(), checked);
}

radio_button radio_buttons::replace(bool checked, const drawing::point& location) {
  return radio_button::create(texts::replace(), checked, location);
}

radio_button radio_buttons::replace(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::replace(), checked, location, size);
}

radio_button radio_buttons::replace(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::replace(), checked, location, size, name);
}

radio_button radio_buttons::replace(const control& parent) {
  return radio_button::create(parent, texts::replace());
}

radio_button radio_buttons::replace(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::replace(), location);
}

radio_button radio_buttons::replace(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::replace(), location, size);
}

radio_button radio_buttons::replace(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::replace(), location, size, name);
}

radio_button radio_buttons::replace(const control& parent, bool checked) {
  return radio_button::create(parent, texts::replace(), checked);
}

radio_button radio_buttons::replace(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::replace(), checked, location);
}

radio_button radio_buttons::replace(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::replace(), checked, location, size);
}

radio_button radio_buttons::replace(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::replace(), checked, location, size, name);
}

radio_button radio_buttons::retry() {
  return radio_button::create(texts::retry());
}

radio_button radio_buttons::retry(const drawing::point& location) {
  return radio_button::create(texts::retry(), location);
}

radio_button radio_buttons::retry(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::retry(), location, size);
}

radio_button radio_buttons::retry(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::retry(), location, size, name);
}

radio_button radio_buttons::retry(bool checked) {
  return radio_button::create(texts::retry(), checked);
}

radio_button radio_buttons::retry(bool checked, const drawing::point& location) {
  return radio_button::create(texts::retry(), checked, location);
}

radio_button radio_buttons::retry(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::retry(), checked, location, size);
}

radio_button radio_buttons::retry(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::retry(), checked, location, size, name);
}

radio_button radio_buttons::retry(const control& parent) {
  return radio_button::create(parent, texts::retry());
}

radio_button radio_buttons::retry(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::retry(), location);
}

radio_button radio_buttons::retry(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::retry(), location, size);
}

radio_button radio_buttons::retry(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::retry(), location, size, name);
}

radio_button radio_buttons::retry(const control& parent, bool checked) {
  return radio_button::create(parent, texts::retry(), checked);
}

radio_button radio_buttons::retry(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::retry(), checked, location);
}

radio_button radio_buttons::retry(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::retry(), checked, location, size);
}

radio_button radio_buttons::retry(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::retry(), checked, location, size, name);
}

radio_button radio_buttons::revert_to_saved() {
  return radio_button::create(texts::revert_to_saved());
}

radio_button radio_buttons::revert_to_saved(const drawing::point& location) {
  return radio_button::create(texts::revert_to_saved(), location);
}

radio_button radio_buttons::revert_to_saved(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::revert_to_saved(), location, size);
}

radio_button radio_buttons::revert_to_saved(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::revert_to_saved(), location, size, name);
}

radio_button radio_buttons::revert_to_saved(bool checked) {
  return radio_button::create(texts::revert_to_saved(), checked);
}

radio_button radio_buttons::revert_to_saved(bool checked, const drawing::point& location) {
  return radio_button::create(texts::revert_to_saved(), checked, location);
}

radio_button radio_buttons::revert_to_saved(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::revert_to_saved(), checked, location, size);
}

radio_button radio_buttons::revert_to_saved(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::revert_to_saved(), checked, location, size, name);
}

radio_button radio_buttons::revert_to_saved(const control& parent) {
  return radio_button::create(parent, texts::revert_to_saved());
}

radio_button radio_buttons::revert_to_saved(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::revert_to_saved(), location);
}

radio_button radio_buttons::revert_to_saved(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::revert_to_saved(), location, size);
}

radio_button radio_buttons::revert_to_saved(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::revert_to_saved(), location, size, name);
}

radio_button radio_buttons::revert_to_saved(const control& parent, bool checked) {
  return radio_button::create(parent, texts::revert_to_saved(), checked);
}

radio_button radio_buttons::revert_to_saved(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::revert_to_saved(), checked, location);
}

radio_button radio_buttons::revert_to_saved(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::revert_to_saved(), checked, location, size);
}

radio_button radio_buttons::revert_to_saved(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::revert_to_saved(), checked, location, size, name);
}

radio_button radio_buttons::save() {
  return radio_button::create(texts::save());
}

radio_button radio_buttons::save(const drawing::point& location) {
  return radio_button::create(texts::save(), location);
}

radio_button radio_buttons::save(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::save(), location, size);
}

radio_button radio_buttons::save(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::save(), location, size, name);
}

radio_button radio_buttons::save(bool checked) {
  return radio_button::create(texts::save(), checked);
}

radio_button radio_buttons::save(bool checked, const drawing::point& location) {
  return radio_button::create(texts::save(), checked, location);
}

radio_button radio_buttons::save(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::save(), checked, location, size);
}

radio_button radio_buttons::save(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::save(), checked, location, size, name);
}

radio_button radio_buttons::save(const control& parent) {
  return radio_button::create(parent, texts::save());
}

radio_button radio_buttons::save(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::save(), location);
}

radio_button radio_buttons::save(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::save(), location, size);
}

radio_button radio_buttons::save(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::save(), location, size, name);
}

radio_button radio_buttons::save(const control& parent, bool checked) {
  return radio_button::create(parent, texts::save(), checked);
}

radio_button radio_buttons::save(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::save(), checked, location);
}

radio_button radio_buttons::save(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::save(), checked, location, size);
}

radio_button radio_buttons::save(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::save(), checked, location, size, name);
}

radio_button radio_buttons::save_as() {
  return radio_button::create(texts::save_as());
}

radio_button radio_buttons::save_as(const drawing::point& location) {
  return radio_button::create(texts::save_as(), location);
}

radio_button radio_buttons::save_as(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::save_as(), location, size);
}

radio_button radio_buttons::save_as(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::save_as(), location, size, name);
}

radio_button radio_buttons::save_as(bool checked) {
  return radio_button::create(texts::save_as(), checked);
}

radio_button radio_buttons::save_as(bool checked, const drawing::point& location) {
  return radio_button::create(texts::save_as(), checked, location);
}

radio_button radio_buttons::save_as(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::save_as(), checked, location, size);
}

radio_button radio_buttons::save_as(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::save_as(), checked, location, size, name);
}

radio_button radio_buttons::save_as(const control& parent) {
  return radio_button::create(parent, texts::save_as());
}

radio_button radio_buttons::save_as(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::save_as(), location);
}

radio_button radio_buttons::save_as(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::save_as(), location, size);
}

radio_button radio_buttons::save_as(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::save_as(), location, size, name);
}

radio_button radio_buttons::save_as(const control& parent, bool checked) {
  return radio_button::create(parent, texts::save_as(), checked);
}

radio_button radio_buttons::save_as(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::save_as(), checked, location);
}

radio_button radio_buttons::save_as(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::save_as(), checked, location, size);
}

radio_button radio_buttons::save_as(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::save_as(), checked, location, size, name);
}

radio_button radio_buttons::search() {
  return radio_button::create(texts::search());
}

radio_button radio_buttons::search(const drawing::point& location) {
  return radio_button::create(texts::search(), location);
}

radio_button radio_buttons::search(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::search(), location, size);
}

radio_button radio_buttons::search(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::search(), location, size, name);
}

radio_button radio_buttons::search(bool checked) {
  return radio_button::create(texts::search(), checked);
}

radio_button radio_buttons::search(bool checked, const drawing::point& location) {
  return radio_button::create(texts::search(), checked, location);
}

radio_button radio_buttons::search(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::search(), checked, location, size);
}

radio_button radio_buttons::search(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::search(), checked, location, size, name);
}

radio_button radio_buttons::search(const control& parent) {
  return radio_button::create(parent, texts::search());
}

radio_button radio_buttons::search(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::search(), location);
}

radio_button radio_buttons::search(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::search(), location, size);
}

radio_button radio_buttons::search(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::search(), location, size, name);
}

radio_button radio_buttons::search(const control& parent, bool checked) {
  return radio_button::create(parent, texts::search(), checked);
}

radio_button radio_buttons::search(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::search(), checked, location);
}

radio_button radio_buttons::search(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::search(), checked, location, size);
}

radio_button radio_buttons::search(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::search(), checked, location, size, name);
}

radio_button radio_buttons::select_all() {
  return radio_button::create(texts::select_all());
}

radio_button radio_buttons::select_all(const drawing::point& location) {
  return radio_button::create(texts::select_all(), location);
}

radio_button radio_buttons::select_all(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::select_all(), location, size);
}

radio_button radio_buttons::select_all(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::select_all(), location, size, name);
}

radio_button radio_buttons::select_all(bool checked) {
  return radio_button::create(texts::select_all(), checked);
}

radio_button radio_buttons::select_all(bool checked, const drawing::point& location) {
  return radio_button::create(texts::select_all(), checked, location);
}

radio_button radio_buttons::select_all(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::select_all(), checked, location, size);
}

radio_button radio_buttons::select_all(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::select_all(), checked, location, size, name);
}

radio_button radio_buttons::select_all(const control& parent) {
  return radio_button::create(parent, texts::select_all());
}

radio_button radio_buttons::select_all(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::select_all(), location);
}

radio_button radio_buttons::select_all(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::select_all(), location, size);
}

radio_button radio_buttons::select_all(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::select_all(), location, size, name);
}

radio_button radio_buttons::select_all(const control& parent, bool checked) {
  return radio_button::create(parent, texts::select_all(), checked);
}

radio_button radio_buttons::select_all(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::select_all(), checked, location);
}

radio_button radio_buttons::select_all(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::select_all(), checked, location, size);
}

radio_button radio_buttons::select_all(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::select_all(), checked, location, size, name);
}

radio_button radio_buttons::settings() {
  return radio_button::create(texts::settings());
}

radio_button radio_buttons::settings(const drawing::point& location) {
  return radio_button::create(texts::settings(), location);
}

radio_button radio_buttons::settings(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::settings(), location, size);
}

radio_button radio_buttons::settings(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::settings(), location, size, name);
}

radio_button radio_buttons::settings(bool checked) {
  return radio_button::create(texts::settings(), checked);
}

radio_button radio_buttons::settings(bool checked, const drawing::point& location) {
  return radio_button::create(texts::settings(), checked, location);
}

radio_button radio_buttons::settings(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::settings(), checked, location, size);
}

radio_button radio_buttons::settings(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::settings(), checked, location, size, name);
}

radio_button radio_buttons::settings(const control& parent) {
  return radio_button::create(parent, texts::settings());
}

radio_button radio_buttons::settings(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::settings(), location);
}

radio_button radio_buttons::settings(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::settings(), location, size);
}

radio_button radio_buttons::settings(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::settings(), location, size, name);
}

radio_button radio_buttons::settings(const control& parent, bool checked) {
  return radio_button::create(parent, texts::settings(), checked);
}

radio_button radio_buttons::settings(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::settings(), checked, location);
}

radio_button radio_buttons::settings(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::settings(), checked, location, size);
}

radio_button radio_buttons::settings(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::settings(), checked, location, size, name);
}

radio_button radio_buttons::spell_check() {
  return radio_button::create(texts::spell_check());
}

radio_button radio_buttons::spell_check(const drawing::point& location) {
  return radio_button::create(texts::spell_check(), location);
}

radio_button radio_buttons::spell_check(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::spell_check(), location, size);
}

radio_button radio_buttons::spell_check(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::spell_check(), location, size, name);
}

radio_button radio_buttons::spell_check(bool checked) {
  return radio_button::create(texts::spell_check(), checked);
}

radio_button radio_buttons::spell_check(bool checked, const drawing::point& location) {
  return radio_button::create(texts::spell_check(), checked, location);
}

radio_button radio_buttons::spell_check(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::spell_check(), checked, location, size);
}

radio_button radio_buttons::spell_check(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::spell_check(), checked, location, size, name);
}

radio_button radio_buttons::spell_check(const control& parent) {
  return radio_button::create(parent, texts::spell_check());
}

radio_button radio_buttons::spell_check(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::spell_check(), location);
}

radio_button radio_buttons::spell_check(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::spell_check(), location, size);
}

radio_button radio_buttons::spell_check(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::spell_check(), location, size, name);
}

radio_button radio_buttons::spell_check(const control& parent, bool checked) {
  return radio_button::create(parent, texts::spell_check(), checked);
}

radio_button radio_buttons::spell_check(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::spell_check(), checked, location);
}

radio_button radio_buttons::spell_check(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::spell_check(), checked, location, size);
}

radio_button radio_buttons::spell_check(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::spell_check(), checked, location, size, name);
}

radio_button radio_buttons::stop() {
  return radio_button::create(texts::stop());
}

radio_button radio_buttons::stop(const drawing::point& location) {
  return radio_button::create(texts::stop(), location);
}

radio_button radio_buttons::stop(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::stop(), location, size);
}

radio_button radio_buttons::stop(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::stop(), location, size, name);
}

radio_button radio_buttons::stop(bool checked) {
  return radio_button::create(texts::stop(), checked);
}

radio_button radio_buttons::stop(bool checked, const drawing::point& location) {
  return radio_button::create(texts::stop(), checked, location);
}

radio_button radio_buttons::stop(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::stop(), checked, location, size);
}

radio_button radio_buttons::stop(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::stop(), checked, location, size, name);
}

radio_button radio_buttons::stop(const control& parent) {
  return radio_button::create(parent, texts::stop());
}

radio_button radio_buttons::stop(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::stop(), location);
}

radio_button radio_buttons::stop(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::stop(), location, size);
}

radio_button radio_buttons::stop(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::stop(), location, size, name);
}

radio_button radio_buttons::stop(const control& parent, bool checked) {
  return radio_button::create(parent, texts::stop(), checked);
}

radio_button radio_buttons::stop(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::stop(), checked, location);
}

radio_button radio_buttons::stop(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::stop(), checked, location, size);
}

radio_button radio_buttons::stop(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::stop(), checked, location, size, name);
}

radio_button radio_buttons::strikeout() {
  return radio_button::create(texts::strikeout());
}

radio_button radio_buttons::strikeout(const drawing::point& location) {
  return radio_button::create(texts::strikeout(), location);
}

radio_button radio_buttons::strikeout(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::strikeout(), location, size);
}

radio_button radio_buttons::strikeout(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::strikeout(), location, size, name);
}

radio_button radio_buttons::strikeout(bool checked) {
  return radio_button::create(texts::strikeout(), checked);
}

radio_button radio_buttons::strikeout(bool checked, const drawing::point& location) {
  return radio_button::create(texts::strikeout(), checked, location);
}

radio_button radio_buttons::strikeout(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::strikeout(), checked, location, size);
}

radio_button radio_buttons::strikeout(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::strikeout(), checked, location, size, name);
}

radio_button radio_buttons::strikeout(const control& parent) {
  return radio_button::create(parent, texts::strikeout());
}

radio_button radio_buttons::strikeout(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::strikeout(), location);
}

radio_button radio_buttons::strikeout(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::strikeout(), location, size);
}

radio_button radio_buttons::strikeout(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::strikeout(), location, size, name);
}

radio_button radio_buttons::strikeout(const control& parent, bool checked) {
  return radio_button::create(parent, texts::strikeout(), checked);
}

radio_button radio_buttons::strikeout(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::strikeout(), checked, location);
}

radio_button radio_buttons::strikeout(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::strikeout(), checked, location, size);
}

radio_button radio_buttons::strikeout(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::strikeout(), checked, location, size, name);
}

radio_button radio_buttons::tools() {
  return radio_button::create(texts::tools());
}

radio_button radio_buttons::tools(const drawing::point& location) {
  return radio_button::create(texts::tools(), location);
}

radio_button radio_buttons::tools(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::tools(), location, size);
}

radio_button radio_buttons::tools(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::tools(), location, size, name);
}

radio_button radio_buttons::tools(bool checked) {
  return radio_button::create(texts::tools(), checked);
}

radio_button radio_buttons::tools(bool checked, const drawing::point& location) {
  return radio_button::create(texts::tools(), checked, location);
}

radio_button radio_buttons::tools(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::tools(), checked, location, size);
}

radio_button radio_buttons::tools(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::tools(), checked, location, size, name);
}

radio_button radio_buttons::tools(const control& parent) {
  return radio_button::create(parent, texts::tools());
}

radio_button radio_buttons::tools(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::tools(), location);
}

radio_button radio_buttons::tools(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::tools(), location, size);
}

radio_button radio_buttons::tools(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::tools(), location, size, name);
}

radio_button radio_buttons::tools(const control& parent, bool checked) {
  return radio_button::create(parent, texts::tools(), checked);
}

radio_button radio_buttons::tools(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::tools(), checked, location);
}

radio_button radio_buttons::tools(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::tools(), checked, location, size);
}

radio_button radio_buttons::tools(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::tools(), checked, location, size, name);
}

radio_button radio_buttons::top() {
  return radio_button::create(texts::top());
}

radio_button radio_buttons::top(const drawing::point& location) {
  return radio_button::create(texts::top(), location);
}

radio_button radio_buttons::top(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::top(), location, size);
}

radio_button radio_buttons::top(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::top(), location, size, name);
}

radio_button radio_buttons::top(bool checked) {
  return radio_button::create(texts::top(), checked);
}

radio_button radio_buttons::top(bool checked, const drawing::point& location) {
  return radio_button::create(texts::top(), checked, location);
}

radio_button radio_buttons::top(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::top(), checked, location, size);
}

radio_button radio_buttons::top(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::top(), checked, location, size, name);
}

radio_button radio_buttons::top(const control& parent) {
  return radio_button::create(parent, texts::top());
}

radio_button radio_buttons::top(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::top(), location);
}

radio_button radio_buttons::top(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::top(), location, size);
}

radio_button radio_buttons::top(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::top(), location, size, name);
}

radio_button radio_buttons::top(const control& parent, bool checked) {
  return radio_button::create(parent, texts::top(), checked);
}

radio_button radio_buttons::top(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::top(), checked, location);
}

radio_button radio_buttons::top(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::top(), checked, location, size);
}

radio_button radio_buttons::top(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::top(), checked, location, size, name);
}

radio_button radio_buttons::undelete() {
  return radio_button::create(texts::undelete());
}

radio_button radio_buttons::undelete(const drawing::point& location) {
  return radio_button::create(texts::undelete(), location);
}

radio_button radio_buttons::undelete(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::undelete(), location, size);
}

radio_button radio_buttons::undelete(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::undelete(), location, size, name);
}

radio_button radio_buttons::undelete(bool checked) {
  return radio_button::create(texts::undelete(), checked);
}

radio_button radio_buttons::undelete(bool checked, const drawing::point& location) {
  return radio_button::create(texts::undelete(), checked, location);
}

radio_button radio_buttons::undelete(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::undelete(), checked, location, size);
}

radio_button radio_buttons::undelete(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::undelete(), checked, location, size, name);
}

radio_button radio_buttons::undelete(const control& parent) {
  return radio_button::create(parent, texts::undelete());
}

radio_button radio_buttons::undelete(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::undelete(), location);
}

radio_button radio_buttons::undelete(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::undelete(), location, size);
}

radio_button radio_buttons::undelete(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::undelete(), location, size, name);
}

radio_button radio_buttons::undelete(const control& parent, bool checked) {
  return radio_button::create(parent, texts::undelete(), checked);
}

radio_button radio_buttons::undelete(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::undelete(), checked, location);
}

radio_button radio_buttons::undelete(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::undelete(), checked, location, size);
}

radio_button radio_buttons::undelete(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::undelete(), checked, location, size, name);
}

radio_button radio_buttons::underline() {
  return radio_button::create(texts::underline());
}

radio_button radio_buttons::underline(const drawing::point& location) {
  return radio_button::create(texts::underline(), location);
}

radio_button radio_buttons::underline(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::underline(), location, size);
}

radio_button radio_buttons::underline(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::underline(), location, size, name);
}

radio_button radio_buttons::underline(bool checked) {
  return radio_button::create(texts::underline(), checked);
}

radio_button radio_buttons::underline(bool checked, const drawing::point& location) {
  return radio_button::create(texts::underline(), checked, location);
}

radio_button radio_buttons::underline(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::underline(), checked, location, size);
}

radio_button radio_buttons::underline(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::underline(), checked, location, size, name);
}

radio_button radio_buttons::underline(const control& parent) {
  return radio_button::create(parent, texts::underline());
}

radio_button radio_buttons::underline(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::underline(), location);
}

radio_button radio_buttons::underline(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::underline(), location, size);
}

radio_button radio_buttons::underline(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::underline(), location, size, name);
}

radio_button radio_buttons::underline(const control& parent, bool checked) {
  return radio_button::create(parent, texts::underline(), checked);
}

radio_button radio_buttons::underline(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::underline(), checked, location);
}

radio_button radio_buttons::underline(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::underline(), checked, location, size);
}

radio_button radio_buttons::underline(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::underline(), checked, location, size, name);
}

radio_button radio_buttons::undo() {
  return radio_button::create(texts::undo());
}

radio_button radio_buttons::undo(const drawing::point& location) {
  return radio_button::create(texts::undo(), location);
}

radio_button radio_buttons::undo(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::undo(), location, size);
}

radio_button radio_buttons::undo(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::undo(), location, size, name);
}

radio_button radio_buttons::undo(bool checked) {
  return radio_button::create(texts::undo(), checked);
}

radio_button radio_buttons::undo(bool checked, const drawing::point& location) {
  return radio_button::create(texts::undo(), checked, location);
}

radio_button radio_buttons::undo(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::undo(), checked, location, size);
}

radio_button radio_buttons::undo(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::undo(), checked, location, size, name);
}

radio_button radio_buttons::undo(const control& parent) {
  return radio_button::create(parent, texts::undo());
}

radio_button radio_buttons::undo(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::undo(), location);
}

radio_button radio_buttons::undo(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::undo(), location, size);
}

radio_button radio_buttons::undo(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::undo(), location, size, name);
}

radio_button radio_buttons::undo(const control& parent, bool checked) {
  return radio_button::create(parent, texts::undo(), checked);
}

radio_button radio_buttons::undo(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::undo(), checked, location);
}

radio_button radio_buttons::undo(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::undo(), checked, location, size);
}

radio_button radio_buttons::undo(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::undo(), checked, location, size, name);
}

radio_button radio_buttons::unindent() {
  return radio_button::create(texts::unindent());
}

radio_button radio_buttons::unindent(const drawing::point& location) {
  return radio_button::create(texts::unindent(), location);
}

radio_button radio_buttons::unindent(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::unindent(), location, size);
}

radio_button radio_buttons::unindent(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::unindent(), location, size, name);
}

radio_button radio_buttons::unindent(bool checked) {
  return radio_button::create(texts::unindent(), checked);
}

radio_button radio_buttons::unindent(bool checked, const drawing::point& location) {
  return radio_button::create(texts::unindent(), checked, location);
}

radio_button radio_buttons::unindent(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::unindent(), checked, location, size);
}

radio_button radio_buttons::unindent(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::unindent(), checked, location, size, name);
}

radio_button radio_buttons::unindent(const control& parent) {
  return radio_button::create(parent, texts::unindent());
}

radio_button radio_buttons::unindent(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::unindent(), location);
}

radio_button radio_buttons::unindent(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::unindent(), location, size);
}

radio_button radio_buttons::unindent(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::unindent(), location, size, name);
}

radio_button radio_buttons::unindent(const control& parent, bool checked) {
  return radio_button::create(parent, texts::unindent(), checked);
}

radio_button radio_buttons::unindent(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::unindent(), checked, location);
}

radio_button radio_buttons::unindent(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::unindent(), checked, location, size);
}

radio_button radio_buttons::unindent(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::unindent(), checked, location, size, name);
}

radio_button radio_buttons::up() {
  return radio_button::create(texts::up());
}

radio_button radio_buttons::up(const drawing::point& location) {
  return radio_button::create(texts::up(), location);
}

radio_button radio_buttons::up(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::up(), location, size);
}

radio_button radio_buttons::up(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::up(), location, size, name);
}

radio_button radio_buttons::up(bool checked) {
  return radio_button::create(texts::up(), checked);
}

radio_button radio_buttons::up(bool checked, const drawing::point& location) {
  return radio_button::create(texts::up(), checked, location);
}

radio_button radio_buttons::up(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::up(), checked, location, size);
}

radio_button radio_buttons::up(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::up(), checked, location, size, name);
}

radio_button radio_buttons::up(const control& parent) {
  return radio_button::create(parent, texts::up());
}

radio_button radio_buttons::up(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::up(), location);
}

radio_button radio_buttons::up(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::up(), location, size);
}

radio_button radio_buttons::up(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::up(), location, size, name);
}

radio_button radio_buttons::up(const control& parent, bool checked) {
  return radio_button::create(parent, texts::up(), checked);
}

radio_button radio_buttons::up(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::up(), checked, location);
}

radio_button radio_buttons::up(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::up(), checked, location, size);
}

radio_button radio_buttons::up(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::up(), checked, location, size, name);
}

radio_button radio_buttons::view() {
  return radio_button::create(texts::view());
}

radio_button radio_buttons::view(const drawing::point& location) {
  return radio_button::create(texts::view(), location);
}

radio_button radio_buttons::view(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::view(), location, size);
}

radio_button radio_buttons::view(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::view(), location, size, name);
}

radio_button radio_buttons::view(bool checked) {
  return radio_button::create(texts::view(), checked);
}

radio_button radio_buttons::view(bool checked, const drawing::point& location) {
  return radio_button::create(texts::view(), checked, location);
}

radio_button radio_buttons::view(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::view(), checked, location, size);
}

radio_button radio_buttons::view(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::view(), checked, location, size, name);
}

radio_button radio_buttons::view(const control& parent) {
  return radio_button::create(parent, texts::view());
}

radio_button radio_buttons::view(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::view(), location);
}

radio_button radio_buttons::view(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::view(), location, size);
}

radio_button radio_buttons::view(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::view(), location, size, name);
}

radio_button radio_buttons::view(const control& parent, bool checked) {
  return radio_button::create(parent, texts::view(), checked);
}

radio_button radio_buttons::view(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::view(), checked, location);
}

radio_button radio_buttons::view(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::view(), checked, location, size);
}

radio_button radio_buttons::view(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::view(), checked, location, size, name);
}

radio_button radio_buttons::window() {
  return radio_button::create(texts::window());
}

radio_button radio_buttons::window(const drawing::point& location) {
  return radio_button::create(texts::window(), location);
}

radio_button radio_buttons::window(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::window(), location, size);
}

radio_button radio_buttons::window(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::window(), location, size, name);
}

radio_button radio_buttons::window(bool checked) {
  return radio_button::create(texts::window(), checked);
}

radio_button radio_buttons::window(bool checked, const drawing::point& location) {
  return radio_button::create(texts::window(), checked, location);
}

radio_button radio_buttons::window(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::window(), checked, location, size);
}

radio_button radio_buttons::window(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::window(), checked, location, size, name);
}

radio_button radio_buttons::window(const control& parent) {
  return radio_button::create(parent, texts::window());
}

radio_button radio_buttons::window(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::window(), location);
}

radio_button radio_buttons::window(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::window(), location, size);
}

radio_button radio_buttons::window(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::window(), location, size, name);
}

radio_button radio_buttons::window(const control& parent, bool checked) {
  return radio_button::create(parent, texts::window(), checked);
}

radio_button radio_buttons::window(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::window(), checked, location);
}

radio_button radio_buttons::window(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::window(), checked, location, size);
}

radio_button radio_buttons::window(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::window(), checked, location, size, name);
}

radio_button radio_buttons::yes() {
  return radio_button::create(texts::yes());
}

radio_button radio_buttons::yes(const drawing::point& location) {
  return radio_button::create(texts::yes(), location);
}

radio_button radio_buttons::yes(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::yes(), location, size);
}

radio_button radio_buttons::yes(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::yes(), location, size, name);
}

radio_button radio_buttons::yes(bool checked) {
  return radio_button::create(texts::yes(), checked);
}

radio_button radio_buttons::yes(bool checked, const drawing::point& location) {
  return radio_button::create(texts::yes(), checked, location);
}

radio_button radio_buttons::yes(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::yes(), checked, location, size);
}

radio_button radio_buttons::yes(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::yes(), checked, location, size, name);
}

radio_button radio_buttons::yes(const control& parent) {
  return radio_button::create(parent, texts::yes());
}

radio_button radio_buttons::yes(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::yes(), location);
}

radio_button radio_buttons::yes(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::yes(), location, size);
}

radio_button radio_buttons::yes(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::yes(), location, size, name);
}

radio_button radio_buttons::yes(const control& parent, bool checked) {
  return radio_button::create(parent, texts::yes(), checked);
}

radio_button radio_buttons::yes(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::yes(), checked, location);
}

radio_button radio_buttons::yes(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::yes(), checked, location, size);
}

radio_button radio_buttons::yes(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::yes(), checked, location, size, name);
}

radio_button radio_buttons::zoom_in() {
  return radio_button::create(texts::zoom_in());
}

radio_button radio_buttons::zoom_in(const drawing::point& location) {
  return radio_button::create(texts::zoom_in(), location);
}

radio_button radio_buttons::zoom_in(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::zoom_in(), location, size);
}

radio_button radio_buttons::zoom_in(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::zoom_in(), location, size, name);
}

radio_button radio_buttons::zoom_in(bool checked) {
  return radio_button::create(texts::zoom_in(), checked);
}

radio_button radio_buttons::zoom_in(bool checked, const drawing::point& location) {
  return radio_button::create(texts::zoom_in(), checked, location);
}

radio_button radio_buttons::zoom_in(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::zoom_in(), checked, location, size);
}

radio_button radio_buttons::zoom_in(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::zoom_in(), checked, location, size, name);
}

radio_button radio_buttons::zoom_in(const control& parent) {
  return radio_button::create(parent, texts::zoom_in());
}

radio_button radio_buttons::zoom_in(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::zoom_in(), location);
}

radio_button radio_buttons::zoom_in(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::zoom_in(), location, size);
}

radio_button radio_buttons::zoom_in(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::zoom_in(), location, size, name);
}

radio_button radio_buttons::zoom_in(const control& parent, bool checked) {
  return radio_button::create(parent, texts::zoom_in(), checked);
}

radio_button radio_buttons::zoom_in(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::zoom_in(), checked, location);
}

radio_button radio_buttons::zoom_in(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::zoom_in(), checked, location, size);
}

radio_button radio_buttons::zoom_in(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::zoom_in(), checked, location, size, name);
}

radio_button radio_buttons::zoom_out() {
  return radio_button::create(texts::zoom_out());
}

radio_button radio_buttons::zoom_out(const drawing::point& location) {
  return radio_button::create(texts::zoom_out(), location);
}

radio_button radio_buttons::zoom_out(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::zoom_out(), location, size);
}

radio_button radio_buttons::zoom_out(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::zoom_out(), location, size, name);
}

radio_button radio_buttons::zoom_out(bool checked) {
  return radio_button::create(texts::zoom_out(), checked);
}

radio_button radio_buttons::zoom_out(bool checked, const drawing::point& location) {
  return radio_button::create(texts::zoom_out(), checked, location);
}

radio_button radio_buttons::zoom_out(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::zoom_out(), checked, location, size);
}

radio_button radio_buttons::zoom_out(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::zoom_out(), checked, location, size, name);
}

radio_button radio_buttons::zoom_out(const control& parent) {
  return radio_button::create(parent, texts::zoom_out());
}

radio_button radio_buttons::zoom_out(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::zoom_out(), location);
}

radio_button radio_buttons::zoom_out(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::zoom_out(), location, size);
}

radio_button radio_buttons::zoom_out(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::zoom_out(), location, size, name);
}

radio_button radio_buttons::zoom_out(const control& parent, bool checked) {
  return radio_button::create(parent, texts::zoom_out(), checked);
}

radio_button radio_buttons::zoom_out(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::zoom_out(), checked, location);
}

radio_button radio_buttons::zoom_out(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::zoom_out(), checked, location, size);
}

radio_button radio_buttons::zoom_out(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::zoom_out(), checked, location, size, name);
}

radio_button radio_buttons::zoom_to_fit() {
  return radio_button::create(texts::zoom_to_fit());
}

radio_button radio_buttons::zoom_to_fit(const drawing::point& location) {
  return radio_button::create(texts::zoom_to_fit(), location);
}

radio_button radio_buttons::zoom_to_fit(const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::zoom_to_fit(), location, size);
}

radio_button radio_buttons::zoom_to_fit(const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::zoom_to_fit(), location, size, name);
}

radio_button radio_buttons::zoom_to_fit(bool checked) {
  return radio_button::create(texts::zoom_to_fit(), checked);
}

radio_button radio_buttons::zoom_to_fit(bool checked, const drawing::point& location) {
  return radio_button::create(texts::zoom_to_fit(), checked, location);
}

radio_button radio_buttons::zoom_to_fit(bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(texts::zoom_to_fit(), checked, location, size);
}

radio_button radio_buttons::zoom_to_fit(bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(texts::zoom_to_fit(), checked, location, size, name);
}

radio_button radio_buttons::zoom_to_fit(const control& parent) {
  return radio_button::create(parent, texts::zoom_to_fit());
}

radio_button radio_buttons::zoom_to_fit(const control& parent, const drawing::point& location) {
  return radio_button::create(parent, texts::zoom_to_fit(), location);
}

radio_button radio_buttons::zoom_to_fit(const control& parent, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::zoom_to_fit(), location, size);
}

radio_button radio_buttons::zoom_to_fit(const control& parent, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::zoom_to_fit(), location, size, name);
}

radio_button radio_buttons::zoom_to_fit(const control& parent, bool checked) {
  return radio_button::create(parent, texts::zoom_to_fit(), checked);
}

radio_button radio_buttons::zoom_to_fit(const control& parent, bool checked, const drawing::point& location) {
  return radio_button::create(parent, texts::zoom_to_fit(), checked, location);
}

radio_button radio_buttons::zoom_to_fit(const control& parent, bool checked, const drawing::point& location, const drawing::size& size) {
  return radio_button::create(parent, texts::zoom_to_fit(), checked, location, size);
}

radio_button radio_buttons::zoom_to_fit(const control& parent, bool checked, const drawing::point& location, const drawing::size& size, const xtd::ustring& name) {
  return radio_button::create(parent, texts::zoom_to_fit(), checked, location, size, name);
}
