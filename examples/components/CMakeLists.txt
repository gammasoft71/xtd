cmake_minimum_required(VERSION 3.3)

project(xtd.forms.components.examples)
find_package(xtd.forms REQUIRED)

add_projects(
  background_worker
  cursor
  image_list
  screen
  settings
  settings2
  timer
)
