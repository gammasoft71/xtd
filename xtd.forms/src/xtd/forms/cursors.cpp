#include <xtd/forms/native/cursors.hpp>
#include "../../../include/xtd/forms/cursors.hpp"

using namespace xtd::forms;

cursor cursors::app_starting() {
  return cursor(native::cursors::app_starting(), true);
}

cursor cursors::arrow() {
  return cursor(native::cursors::arrow(), true);
}

cursor cursors::close_hand() {
  return cursor(native::cursors::close_hand(), true);
}

cursor cursors::contextual_menu() {
  return cursor(native::cursors::contextual_menu(), true);
}

cursor cursors::cross() {
  return cursor(native::cursors::cross(), true);
}

cursor cursors::default_cursor() {
  return cursor(native::cursors::default_cursor(), true);
}

cursor cursors::disappearing_item() {
  return cursor(native::cursors::disappearing_item(), true);
}

cursor cursors::drag_copy() {
  return cursor(native::cursors::drag_copy(), true);
}

cursor cursors::drag_link() {
  return cursor(native::cursors::drag_link(), true);
}

cursor cursors::hand() {
  return cursor(native::cursors::hand(), true);
}

cursor cursors::help() {
  return cursor(native::cursors::help(), true);
}

cursor cursors::hsplit() {
  return cursor(native::cursors::hsplit(), true);
}

cursor cursors::ibeam() {
  return cursor(native::cursors::ibeam(), true);
}

cursor cursors::no() {
  return cursor(native::cursors::no(), true);
}

cursor cursors::no_move_2d() {
  return cursor(native::cursors::no_move_2d(), true);
}

cursor cursors::no_move_horiz() {
  return cursor(native::cursors::no_move_horiz(), true);
}

cursor cursors::no_move_vert() {
  return cursor(native::cursors::no_move_vert(), true);
}

cursor cursors::open_hand() {
  return cursor(native::cursors::open_hand(), true);
}

cursor cursors::pan_east() {
  return cursor(native::cursors::pan_east(), true);
}

cursor cursors::pan_ne() {
  return cursor(native::cursors::pan_ne(), true);
}

cursor cursors::pan_north() {
  return cursor(native::cursors::pan_north(), true);
}

cursor cursors::pan_nw() {
  return cursor(native::cursors::pan_nw(), true);
}

cursor cursors::pan_se() {
  return cursor(native::cursors::pan_se(), true);
}

cursor cursors::pan_south() {
  return cursor(native::cursors::pan_south(), true);
}

cursor cursors::pan_sw() {
  return cursor(native::cursors::pan_sw(), true);
}

cursor cursors::pan_west() {
  return cursor(native::cursors::pan_west(), true);
}

cursor cursors::size_all() {
  return cursor(native::cursors::size_all(), true);
}

cursor cursors::size_nesw() {
  return cursor(native::cursors::size_nesw(), true);
}

cursor cursors::size_ns() {
  return cursor(native::cursors::size_ns(), true);
}

cursor cursors::size_nwse() {
  return cursor(native::cursors::size_nwse(), true);
}

cursor cursors::size_we() {
  return cursor(native::cursors::size_we(), true);
}

cursor cursors::up_arrow() {
  return cursor(native::cursors::up_arrow(), true);
}

cursor cursors::vibeam() {
  return cursor(native::cursors::vibeam(), true);
}

cursor cursors::vsplit() {
  return cursor(native::cursors::vsplit(), true);
}

cursor cursors::wait_cursor() {
  return cursor(native::cursors::wait_cursor(), true);
}
